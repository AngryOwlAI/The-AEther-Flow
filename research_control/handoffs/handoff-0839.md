<!-- authority: control -->

# Handoff 0839 — Warning contract repaired; checkpoint blocked

Handoff ID: `handoff-0839`.

`RT-20260723-006` reconciled the stale PASS-only Director-context assertion
with the registered ordinary-route contract. At the threshold-next boundary,
`WARN` remains visible and nonblocking; only `FAIL` blocks continuation. The
focused regression, all eight ordinary-route guard tests, and the required
155-test P12 shard pass.

The single normal checkpoint then stopped before commit. Final staged
`git diff --cached --check` found one trailing blank line at EOF in each of
four preserved P12-T05 files. The checkpoint restored the entry index, staged
nothing, committed nothing, and did not use legacy validation. This consumed
worker invocation does not retry the checkpoint or repair those files.

The registered policy, evaluator, continuation runtime, route history,
threshold-three hard guard, all-ready-science exception, P12-T05 metric
semantics, scientific status, ontology, scientific ledgers, Distance-to-GR,
proof, publication, and P12-T06 execution are unchanged.

## Checkpoint boundary

One fresh bounded project-system recovery may remove exactly one final blank
line from each named blocker path, prove every other byte and all metric
semantics unchanged, rerun the required gates, and invoke at most one future
normal checkpoint. Legacy validation and a checkpoint retry in generation 72
are not authorized.

## Next route

Only after that future checkpoint commits may one separately claimed P12-T06
project-control-maintainer packet may aggregate append-only attempt, rework,
pass, saturation, repair, and abandonment events. It must bind source hashes
and freshness, distinguish process failure classes, suppress private prompt
and credential text, and avoid treating system metrics as physics evidence.

The ordinary-route exception is exact: dependency-ready science route P4-T05
requires explicit human Gate Chair authority under the registered v21 backlog.
This relay generation does not possess that authority.

No scientific claim, source law, ontology adoption, effective metric, matter
coupling, Einstein-equation derivation, benchmark promotion, proof authority,
publication authority, completed derivation, or global no-go follows from this
project-control recovery.
