# Handoff 0868 — cubic-amplitude source dynamics constructed

`handoff-0868` records the bounded P5-T03 result. On the exact P5-T01
torus-amplitude witness, the task-local generator

`X_gamma(q_a) = -gamma a^3 sin(theta_1)`, with `gamma>0`,

has the unique global forward solution

`a(lambda) = a0/sqrt(1+2 gamma a0^2 lambda)`.

The integrated maps form a jointly smooth P5-T02 forward action. For positive
parameter they are injective but nonsurjective, with exact range
`(-1/sqrt(2 gamma lambda), 1/sqrt(2 gamma lambda))`. Nonzero backward orbits
blow up at finite negative parameter, so there is no global real-parameter
action on the entire amplitude family.

The source control functional `L=a^2/2` decreases along nonzero orbits. It is
not physical energy, an action, a Hamiltonian, entropy, or a thermodynamic
potential. The forward-only result is not information loss, physical
irreversibility, causal order, a physical beginning, or a spacetime
singularity. The parameter remains mathematical source order, and `gamma`
fixes no physical clock or dimensionful scale.

This construction is `draft/control`, `proposal-only`, `source-extension
data`, and `blocked_adoption_open_continuation`. It is not canonical ontology,
an adopted or rejected source law, physical time, physical energy, physical
gauge, source-manifold locality, reconstruction, `M_src`, `g_eff`, matter
coupling, Einstein-equation recovery, benchmark promotion, proof, publication,
push, or completed derivation.

After the governed checkpoint commits, the next bounded route is P5-T04 under
`continue-research` with `ontology-formalizer@0.2.0`: analyze this exact
unchanged candidate for source-manifold locality or its absence, composition,
constraints, and conserved structure. P5-T04 is not executed by this handoff.
