# Handoff 0872 — cubic-amplitude background and mode analysis complete

`handoff-0872` records the bounded P5-T05 result for the exact unchanged
P5-T03 cubic-amplitude source dynamics and preserves every P5-T04 structural
finding.

For `gamma>0`, `q_0` is the unique stationary background. Positive and
negative amplitudes form transient forward-invariant sign sectors exchanged
by reflection; neither is dynamically preferred, and both converge to the
reflection-symmetric zero state.

Along an exact trajectory background, the admitted perturbation satisfies

`eta'=-3 gamma a_b(lambda)^2 eta`

and

`eta(lambda)=eta_0/(1+2 gamma a_b0^2 lambda)^(3/2)`.

There is one decaying admitted source-tangent mode along a nonzero trajectory
and one neutral linear mode at `q_0`. The nonlinear flow nevertheless makes
`q_0` globally Lyapunov stable and globally asymptotically attractive, with
polynomial rather than exponential decay.

The candidate has one admitted source-tangent dimension and zero gauge modes.
A physical mode count is not available, Hamiltonian constrained-mode counts
are not applicable, and off-family modes are undefined because no off-family
dynamics is supplied. The sign sectors are not persistent ordered phases, and
forward source order is not physical time orientation.

`OBST-P5T05-QAMP-NO-RECONSTRUCTION-PHASE-001` records the decisive scoped
result. The exact family has no stationary nonzero phase; its sole stationary
background has zero linearized generator; and its only nonzero-background
perturbation is a single global decaying amplitude mode. Together with the
P5-T04 patch-amplitude obstruction, this prevents a nontrivial local
reconstruction-suitable phase inside `Q_amp`.

This does not rule out a conservative multi-mode, locally patchable, coupled,
stochastic, or other source extension and is not a global theory rejection.
The result remains `draft/control`, `proposal-only`, and `source-extension
data`.

After the governed checkpoint commits, the next bounded route is P5-T06 under
`continue-research` with `ontology-formalizer@0.2.0`: define a typed
source-only coarse-graining or reconstruction map with declared domain,
codomain, scale, approximation, information loss, and error, or return a
distinct precise map obstruction without target fitting. P5-T06 is not
executed by this handoff.
