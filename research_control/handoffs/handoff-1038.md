---
object_id: MD-RESEARCH-HANDOFF-1038
title: "Research handoff 1038: source-derived occurrence/admissibility descent-kernel theorem"
authority: control
status: active
handoff_id: handoff-1038
task_id: RT-20260815-002
---

# Research handoff 1038

RT-20260815-002 compares exactly four materially distinct continuations after
the scoped projective-conormal robustness, natural-selection, coorientation,
and conformal-lift obstruction. Both independent physics children and the
parent select Route A:
`A_SOURCE_DERIVED_OCCURRENCE_ADMISSIBILITY_THEOREM`.

The future packet is
`PKT-V22-P4T02-B2-SOURCE-DERIVED-OCCURRENCE-ADMISSIBILITY-DESCENT-KERNEL-THEOREM-V1`.
It is a `bounded_theoretical_calculation` on the
`ontology-law-research-packet` route for `ontology-formalizer@0.2.0`, with
status `selected_not_executed` and adoption status
`blocked_adoption_open_continuation`.

The dependency ordering is exact. A representation-valued bridge does not say
which proposal is admissible or occurs. The future packet must separately type
and test:

1. deterministic natural sections;
2. proper nonempty invariant admissible subfunctors; and
3. normalized equivariant occurrence kernels.

It must also prove restriction, overlap, source-arrow transport, and gluing
descent, then establish that any generator factors through typed current-source
data fixed before bridge outputs or P4 diagnostics. The two-point swap has no
fixed token and no proper nonempty invariant subset but has one uniform kernel,
which selects no token. A translation torsor need not admit an invariant
probability at all. Compatible local kernels descend; opposite Dirac kernels do
not. These controls prevent a probability distribution from being overread as
a realized occurrence.

Route B is deferred because nonselection alone is not P4 irrelevance. Route C
remains constructively available—the exact rational symmetric-hyperbolic pencil
control is positive—but another representation would retain the occurrence
debt. Route D is premature because the bounded Route A packet remains lawful.

All eight active freezes remain unchanged and every expanded Distance-to-GR row
remains `no_delta`. D7 is not reevaluated, B2 remains inactive, and P4-T03
remains locked.

After the single checkpoint for `AJ-RT-20260815-002-001`, execute exactly one
separately admitted `ontology-formalizer@0.2.0` AgentJob for the selected packet.
It must return a theorem with hypotheses, a proposal-only law pending a separate
Smuggling Audit, or the first precise typing, provenance, descent, or
invariant-kernel obstruction.

The future packet is not executed here. No occurrence or source law is proved,
constructed, adopted, or physically interpreted. No frozen route replay,
physical probability, realized token, causal cone, time orientation, response
semantics, conformal geometry, effective metric, coupling, Einstein equations,
benchmark, Gate result, proof promotion, publication, push, external action,
global no-go, future-extension impossibility, or completed derivation is
authorized.
