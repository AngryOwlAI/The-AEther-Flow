<!-- authority: control -->

# Research handoff 0787

Handoff ID: handoff-0787

Task RT-20260720-017 completed only v21 work item P2-T04 under
candidate-constructor@0.2.0.

The registered draft/control artifact `EQSRC-FINITE-COUNTERMODEL-ATLAS-V1`
places four historical finite families into the unchanged P2-T03 theorem
coordinates: declared structural group, eligible-choice space, orbit
decomposition, fixed set, and relation map where present.

The twelve fixtures contain five direct empty-fixed theorem instances: the
`X_empty` sign and token swaps, the `GL(2,2)` action on three orientation
lines, and the two- and four-cycle root actions. They contain three
fixed-point-positive controls: an explicitly tagged response singleton, a
supplied-root singleton, and the proposal-only four-chain minimum. The trivial
group on two choices is the multiple-fixed control; injective `K` preserves
two relation images while constant `K` collapses them to one.

The periodic, multiple-minimum, and non-graded graded-orbit examples are
guarded noninstances. Their proposal admission fails before the theorem's
eligible selector domain is formed. This preserves their original fail-closed
meaning and prevents them from being overread as theorem countermodels.

All original task, candidate, countermodel, obstruction, and freeze IDs remain
intact. All five source hashes match. The task-local validator independently
closes each finite permutation group and recomputes every orbit, fixed set,
`K` image, and relation stabilizer. It also reproduces the orientation counts,
the rooted four-token graph census, and the graded unary-action census. All 92
checks pass, and the TeX source compiles twice to a four-page local scratch
PDF.

The result remains proposal-only and source-extension data; adoption is
`blocked_adoption_open_continuation`. Structural automorphisms are not physical
gauge transformations. Supplied tags or roots are not current-ontology
derivations. File and process metadata are not source symmetry. No exact family
is reopened, no scientific ledger changes, and general `EqSrc` remains open.

The exact next action is one bounded P2-T05 Ontology Formalizer formal-proof
packet to mechanize the selector theorem core and produce a reproducible proof
receipt. P2-T06 remains dependent on P2-T04 and P2-T05 and retains independent
Smuggling Auditor ownership. P2-T07 retains independent Refuter ownership. Do
not adopt the proposal domain or selector, infer physical gauge, reopen a
frozen family, change scientific ledgers, promote, publish, or execute another
v21 work item in the same task.
