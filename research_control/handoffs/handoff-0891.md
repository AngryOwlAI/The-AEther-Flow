<!-- authority: control -->

# Handoff handoff-0891: P7-T02 finite source transition kernel

## Completed scope

`RT-20260728-001` constructs
`SourceMatterFiniteTransitionKernelCandidate_v1` as one task-local
`draft/control`, `proposal-only` source-transition law. Every finite nonempty
enabled row contains exactly one typed identity transition and receives
uniform nonnegative rational bookkeeping weights. The associated update is an
explicit finite equation on nonnegative rational bookkeeping weights.

The packet proves exact row normalization, finite update closure, preservation
of total bookkeeping mass, inherited `(Q,[z])` sector support, and
source-presentation equivariance. It includes the exact positive two-state
solution `mu_n(x_u)=2^-n`, `mu_n(x_v)=1-2^-n`, a Cartesian factorization
control, a synchronized disjoint-component nonfactorization control, and
separate null, degenerate, malformed, and underdetermined branches.

## Status boundary

The mathematical source-transition-rule sub-burden narrows, so the
`matter_coupling` Distance-to-GR row now records the explicit proposal-only
kernel and its exact finite constraint structure. Current ontology does not
derive the enabled rows, typed identity convention, uniform coefficients, or
update rule.

The coefficients are not physical probabilities, rates, frequencies, or
propensities. The composition index is not physical time. Bookkeeping-mass
preservation and inherited sector support are not physical conservation
theorems. Factorization is not empirical independence, and the synchronized
row is not a physical force, interaction, or coupling.

The result is not canonical ontology, an adopted source law, detector
semantics, effective geometry, a matter action, stress energy, universal
coupling, Einstein equations, benchmark recovery, proof authority,
publication, or a completed derivation. P6 Gate B remains zero of eight.
Adoption remains `blocked_adoption_open_continuation`.

## Next bounded route

First run one governed checkpoint for `AJ-RT-20260728-001-001`. Only after it
commits may one fresh bounded `candidate-constructor@0.2.0` packet execute
v21 P7-T03 by constructing source-level operational clocks, rods, signals,
detectors, and free-fall protocols.

That packet must define explicit source objects, readout maps, interventions,
success conditions, and failure branches. It must not infer physical
probability, time, detector meaning, geometry, or coupling from the P7-T02
kernel, and it must not execute P7-T04.
