<!-- authority: control -->

# Handoff 0876 — P6-T01 causal and characteristic target

Status: `ready_after_checkpoint`.

`handoff-0876` records the bounded P6-T01 target-formalization result.

## Result

P6-T01 defines formal source influence as the nonzero-block support of a
source-local linear response. It conditionally defines characteristic sets,
principal-polynomial hyperbolicity data, and the additional obligations needed
before the word “causal” is physically justified. None of these definitions
uses a target metric or GR null cone.

The packet proves two theorem-level statements:

- response support under a linear cocycle is contained in relational
  composition; and
- for the proposal-only two-site cubic family, every coupling `c` preserves
  the exact P5 diagonal flow and symmetric tangent response, while `c=0` has
  no cross-site response and `c>0` has positive cross-site response.

The unchanged P5 data therefore do not select a unique region-resolved
influence relation. This is a precise scoped obstruction, not a global no-go
theorem. The exact inference from unchanged P5 diagonal data to unique local
influence is locally frozen. Conservative source extensions remain open.

Gate B is `NOT_READY_PRECISE_SCOPED_OBSTRUCTION`: source localization,
restriction and gluing, local evolution, characteristic extraction,
orientation, hyperbolicity, naturality, robustness, operational signal
meaning, and common-signal universality are missing or untested. Metric scale
remains a later burden. The historical scoped `g_eff` status is unchanged and
is not a physical Lorentzian metric premise.

## Next action

Run the one governed checkpoint for `AJ-RT-20260726-002-001`. Only after that
commit may one fresh bounded P6-T02 `candidate-constructor@0.2.0` packet add
materially new source-local law content and construct or precisely obstruct
one propagation relation, principal polynomial, or causal order.

P6-T02 is not executed by this handoff. No canonical ontology or source-law
adoption, physical causality or geometry, `M_src`, unscoped `g_eff`, matter
coupling, Einstein equations, benchmark promotion, global rejection, future-
extension impossibility, proof, publication, push, or completed derivation is
authorized.
