---
authority: control
status: ready_after_checkpoint
handoff_id: handoff-0881
task_id: RT-20260726-007
plan_task_id: P6-T05
---

# Handoff 0881: P6-T04 scale and calibration nonselection

## Completed packet

Generation 127 executed exactly one `continue-research` worker and one outer
Candidate Constructor AgentJob for v21 P6-T04. The packet is complete and
ready for one governed checkpoint.

The task-local manuscript proves a conditional positive theorem: given an
independently supplied four-dimensional Lorentzian conformal class and a
positive density, the unique representative with that volume density is
selected by

`Omega=(mu/|vol_g|)^(1/4)`.

The theorem does not construct the missing conformal class and does not make
the density physical.

## Precise obstruction

In flow-box coordinates with `V=partial_s`, the family

`g_a=diag(-1,exp(2a),exp(-2a),1)`

has determinant `-1` for every `a`. Every member carries the same oriented
`V` ray, annihilator, forward flow preorder, source density, and scalar phase
carriers. Distinct members are nonconformal and generally have different null
cones and transverse rod lengths.

Positive flow reparameterization `V -> f V` separately rescales every
absolute phase rate by `f`; only ratios of two phase rates remain invariant.
A bounded relative-device protocol can compare cycle ratios, but it supplies
neither an absolute time unit nor rod calibration.

The exact disposition is
`OBST-P6T04-VOLUME-TICK-CALIBRATION-NONSELECTION-001`. The reconstruction
route from unchanged one-ray data plus an unnormalized positive density and
dimensionless phase carriers is locally frozen.

## Scope and authority

Current ontology does not derive a Lorentzian conformal-shape law, physically
normalized volume law, absolute clock normalization, transverse rod or
coframe law, or cross-device compatibility and robustness law. Adoption
remains `blocked_adoption_open_continuation`; materially richer source-side
laws may reopen the burden.

This result is `draft/control`, `proposal-only` source-extension analysis. It
does not establish physical volume, proper time, rods, conformal geometry, or
metric scale. It does not expand historical scoped `g_eff`, import target
geometry or tune `G` or `c`, couple matter, derive Einstein equations, promote
the exact-GR benchmark, prove a global no-go, authorize proof or publication,
push, or claim completed derivation.

## Validation

The task-local validator passes 61 of 61 checks, including exact fixed-source
hashes, four-dimensional volume scaling, determinant-one Lorentzian
witnesses, nonconformality, cone and rod separation, phase-rate invariance,
dimensional controls, TeX markers, and all authority flags. The registered
TeX compiles to a four-page scratch PDF.

Repository-wide memory, documentation, claim-language, research-control,
graph, test, whitespace, and checkpoint gates remain mandatory.

## Next lawful action

Run one governed checkpoint for `AJ-RT-20260726-007-001`. Only after it
commits may one fresh bounded P6-T05
`ontology-formalizer@0.2.0` packet test Lorentzian signature,
nondegeneracy, and source-induced transformation-law obligations under the
P6-T03 and P6-T04 nonselection results. It must not assume that a physical
metric has been reconstructed.
