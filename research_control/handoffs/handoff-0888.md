<!-- authority: control -->

# Handoff handoff-0888: P15-T04 report to P7-T01 source matter

P15-T04 is complete pending one governed checkpoint.

The unchanged registered P5-T01 through P6-T08 package is assembled in a
durable task-local technical report, runnable archive, exact source-hash
manifest, assumption ledger, and status-complete review packet.
`P15T04-THM-FIXED-PACKAGE-COMPOSED-STATUS-BARRIER` records two independent
necessary-condition failures: the explicitly hypothesized P5
factor-through-one-amplitude reconstruction has rank at most one, and the P6
structural and same-premise controls do not entail the required geometry
outputs. This is not a literal composition of P5 and P6 maps and is not a
global no-go theorem.

The coherent-reporting burden is discharged. The effective-metric derivation
burden is unchanged. Gate B remains zero of eight,
`ScopedMetricStructureRecord_src` and its exact legacy alias retain their
historical scoped status, bare `g_eff` remains unresolved, and the unchanged
P6 package remains locally frozen as an unscoped geometry or geometry input.
The two child perspectives are same-context internal AI reviews, not external
human review or independent replication.

After checkpoint, route one bounded P7-T01 `ontology-law-research-packet`
through `ontology-formalizer@0.2.0`. It may define a proposal-only source
matter ontology candidate, sector and charge taxonomy, nontrivial
configuration, malformed branch, and exact assumption table. It must not use
the unchanged P6 package as effective geometry or assume a target metric,
stress tensor, coupling, or GR behavior.

No canonical ontology, source law, adopted matter ontology, physical metric,
matter coupling, Einstein equation, benchmark, proof, publication, push,
future-impossibility claim, or completed derivation is authorized.
