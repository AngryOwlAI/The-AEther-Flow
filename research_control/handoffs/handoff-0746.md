<!-- authority: control -->

# Research Handoff handoff-0746: EqSrc Closure Candidate v3 Ready for Refuter Stress

RT-20260718-021 completed the fresh Smuggling Auditor review required by
handoff-0745. Proposal-only `EqSrcClosureLaw_src^cand v3` passes the bounded
statement-level audit:

- endpoint-indexed identity, inverse, and composition terms are typed;
- every v2 proxy-constructor, congruence, and negative-control-domain gap is
  closed;
- the rank-0 through rank-4 dependency graph excludes acceptance recursion;
- component congruence and saturated outcome sets make `Accept_src`
  invariant;
- accepted identity, inverse, and composition imply the conditional
  equivalence theorem; and
- transitive provenance plus semantic source factorization exclude target and
  workflow aliases at the proposal-schema level.

The exact result is
`statement_level_smuggling_audit_pass_with_instantiation_provenance_limit`.
The audit adds:

- a certificate-acceptance congruence lemma;
- a source-factorization propagation lemma;
- an abstract one-object consistency witness; and
- a countermodel showing that coarse object equivalence does not imply the
  full coherence law.

These results are bounded. `Accept_src` is total on its typed certificate
domain; malformed outside-domain data cause model rejection unless a later
formalization adds an ambient error carrier. The source-factorization guard is
an audited admissibility condition, not a concrete current-ontology
interpretation. No definitional translation, unique expansion, conservativity
theorem, nontrivial physical instance, naturalness, uniqueness, empirical
adequacy, exact-GR recovery, or robustness result is supplied.

The candidate remains `proposal-only`, `draft/control` source-extension data
with `blocked_adoption_open_continuation`. Current ontology does not derive or
instantiate it. No canonical ontology edit, source-law adoption, general
`EqSrc`, RetainH, GenH, `M_src`, `g_eff`, matter coupling, Einstein equations,
Distance-to-GR ledger change, benchmark promotion, Gate Chair verdict,
completed derivation, program-level no-go, future source-extension
impossibility, or unscoped theory-wide rejection follows.

The repeated EqSrc route is not frozen because the exact v2 statement defect
does not recur and the audit adds new mathematical payload. The next lawful
packet is exactly one bounded `refuter@0.2.0` stress test of exact v3,
emphasizing nontrivial source-only model existence, transitive provenance,
malformed-definition rejection, congruence and accepted-totality stability,
coherence independence, degenerate models, and finite-variation robustness.
No human gate is required for stress. Any adoption remains a separate protected
human-gated decision.
