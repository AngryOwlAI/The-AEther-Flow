---
authority: control
handoff_id: handoff-0767
task_id: RT-20260718-042
status: completed
---

# Handoff 0767: Ordered-Motion Partition Provenance to Formalization

## Completed packet

`RT-20260718-042` executed one bounded
`theoretical-continuation-selector@0.1.0`
`ontology-law-research-packet`.

It selected the proposal-only
`EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1` target for one bounded
`ontology-formalizer@0.2.0` packet.

The target moves upstream of the frozen orientation-torsor line candidate. It
asks whether a finite directed source record with explicit
\(\Phi_\lambda\)-compatible transition certificates, an independently
declared event-to-record projection, and a unique source-intrinsic root
certificate can naturally define an ordered parity partition before any line
or relation is evaluated.

On the admitted branch, every directed path from the certified root to a
record token must have the same length parity. The even and odd tokens define
\(P_U\) and \(Q_U\), and only then may the candidate form
\(B_U=\operatorname{span}_{\mathbb F_2}\{\mathbf 1_{Q_U}\}\) and its
kernel-pair relation.

## Required controls

The formalizer must supply:

1. exact typed definitions for the source record, transition certificates,
   projection, root certificate, parity, partition, pointing, relation,
   morphisms, variations, and bottom tags;
2. a two-event directed path with an independently certified root as the
   minimal positive witness;
3. an unrooted directed two-cycle with exchange symmetry as a no-natural-root
   negative control;
4. rejection of roots created only by observer sampling truncation;
5. path-parity, odd-cycle, disconnected-component, inverse, cocycle, and
   finite-variation branches; and
6. per-datum source-extension classification and no-target-import audit
   obligations.

The canonical foundations source provides a schematic ordered-motion map but
does not presently derive the finite projection, root certificate, partition,
morphism category, or variation category. The selected target therefore
remains a `proposal-only` `new_ontology_primitive_candidate`, with
`blocked_adoption_open_continuation`.

## Status and freeze

Exact `EqSrcOrientationTorsorDescentLaw_src^cand,v1` construction, audit, and
stress repetition remains `locally_frozen`. This is not a general `EqSrc`
no-go, future source-extension impossibility, or global theory rejection.

No Distance-to-GR or metric-use ledger row changes. No source law, canonical
ontology, physical admissibility, general `EqSrc`, downstream GR object,
benchmark status, Gate Chair verdict, or completed derivation is promoted.

## Required next packet

Run one bounded `ontology-formalizer@0.2.0`
`ontology-law-research-packet` for
`EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1`.

The packet must formalize the exact target or return a precise obstruction. It
may not repair or adopt exact orientation-torsor candidate v1, treat observer
sampling or repository process as source provenance, edit canonical ontology,
establish physical admissibility, discharge general `EqSrc`, change either
ledger, promote downstream GR objects, issue a Gate Chair verdict, claim a
completed derivation, or make a program-wide rejection claim.
