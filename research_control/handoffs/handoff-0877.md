<!-- authority: control -->

# Handoff 0877 — P6-T02 source-local transport characteristic

Status: `ready_after_checkpoint`.

`handoff-0877` records the bounded P6-T02 Candidate Constructor result.

## Result

P6-T02 adds one materially new, proposal-only source-local equation on the
canonical smooth four-dimensional source arena:

`L_V a + gamma a^3 = 0`.

Along every positive integral curve of `V`, this exactly recovers the P5 cubic
flow and tangent response. Linearization gives the principal polynomial
`P_x(xi)=xi(V_x)`, characteristic covectors annihilating `V_x`, a cotangent
hyperbolicity half-space, one nonnegative dual propagation ray, and a formal
forward reachability preorder. These objects follow from the actual equation;
they are not copied from the GR cone.

The packet also records:

- a nontrivial smooth `R^4` flow-box solution;
- a first-order `O(epsilon)` source-derivative approximation bound;
- principal degeneracy when `V` vanishes;
- failure of antisymmetric order on closed `V` orbits;
- nonselection between competing generators with the same restricted P5
  behavior; and
- multiple propagation rays in a noncollinear two-sector control.

The decisive result is `constructed_candidate`. It remains `draft/control`,
`proposal-only`, and `source-extension data`.

## Physical boundary

Physical Gate B remains not ready. Current ontology does not derive or select
the scalar field, `V`, its positive orientation, normalization, global
nonvanishing, chronology, operational signal meaning, or cross-sector
universality. Mathematical hyperbolicity is not physical causality, positive
source order is not physical time, and a single ray is not a four-dimensional
Lorentzian conformal cone.

## Next action

Run the one governed checkpoint for `AJ-RT-20260726-003-001`. Only after that
commit may one fresh bounded P6-T03 `ontology-formalizer@0.2.0` packet
determine whether the one-ray characteristic data fix any effective conformal
class or prove a precise insufficiency.

P6-T03 is not executed by this handoff. No canonical ontology or source-law
adoption, physical causality or metric, `M_src`, unscoped `g_eff`, matter
coupling, Einstein equations, benchmark promotion, global rejection,
future-extension impossibility, proof, publication, push, or completed
derivation is authorized.
