<!-- authority: control -->

# Handoff handoff-0890: P7-T02 source-matter dynamics selector

## Completed selector scope

`RT-20260727-008` compares the source-action,
source-evolution-equation, and source-transition-law branches against the
fixed proposal-only `SourceMatterIncidenceCandidate_v1`. Both internal child
reviews select the transition-law branch. The parent synthesis selects one
future `SourceMatterFiniteTransitionKernelCandidate_v1` construction under
`ontology-law-research-packet` and `candidate-constructor@0.2.0`.

The kernel's support relation is primary. Its normalized weights are
proposal-only mathematical coefficients, not physical probabilities, rates,
frequencies, propensities, or physical-time evolution. A relation-only rule
remains the fallback if nontrivial equivariant weights cannot be constructed
without undeclared source data.

## Status boundary

The selector passes 60 of 60 task-local checks and executes no dynamics. It
constructs no action, evolution equation, transition kernel, solution,
trajectory, physical time, probability, or conservation law. It creates no
Distance-to-GR delta.

Current ontology does not derive the needed transition selector or weighting
law. This is not an impossibility theorem. Adoption remains
`blocked_adoption_open_continuation`. Detector semantics, effective geometry,
stress energy, universal coupling, Einstein equations, benchmark promotion,
proof, publication, push, and completed derivation remain blocked.

## Next bounded route

First run one governed checkpoint for
`AJ-RT-20260727-008-001`. Only after it commits may one fresh bounded
`candidate-constructor@0.2.0` packet continue the same P7-T02 plan item.

That packet must construct or precisely obstruct
`SourceMatterFiniteTransitionKernelCandidate_v1`, derive one reproducible
finite source update plus normalization and closure, test
source-presentation equivariance, provide positive, null, interacting,
malformed, and degenerate controls, and separate inherited P7-T01
admissibility from genuinely new kernel consequences. It may import no target
geometry, detector, stress-energy, coupling, GR, or benchmark premise.
