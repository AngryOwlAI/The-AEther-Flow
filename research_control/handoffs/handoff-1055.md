<!-- authority: control -->

# handoff-1055 — Idempotent equation-decoration obstruction to theoretical selection

RT-20260816-012 completed one bounded Refuter stress. The parent and both
independent specialist children return exactly `scoped_obstruction` for the
unchanged proposal-only idempotent equation-decoration candidate.

The exact conditional algebra remains intact, as do RT009’s construction and
RT011’s narrow `source_pure_as_written` verdict. The obstruction is sharper:
full `GL(6)` naturality admits both `P=0` and `P=I`; complement symmetry swaps
them; semantic relabeling changes no algebra; and every endpoint-distinguishing
image, kernel, quotient, rank, or projector descendant fails factorization
through the unchanged source reduct. If old laws ignore `P`, the extension is
inert and has no bridge force. If a law reads `P`, that reading is new
source-law debt. Exact endpoint isolation and rank distance remain algebraic
controls, not a derived perturbative or physical robustness law.

The result is candidate-local, not a global no-go theorem. Diagnostic use and
a materially distinct independently source-provenanced law remain open. All
nine inherited freezes remain active, and one local freeze is added:

`NDCL-V22-P4T02-B2-IDEMPOTENT-EQUATION-DECORATION-NULL-SEMANTICS-RANK-TOPOLOGY-BRIDGE-FORCE`

All fourteen Distance-to-GR rows remain `no_delta`. D7 is not reevaluated, B2
remains inactive, and P4-T03 remains locked.

After the single governed checkpoint for `AJ-RT-20260816-012-001`, the next
research action may be exactly one separately admitted
`theoretical-continuation-selector@0.1.0` packet:

`PKT-V22-P4T02-B2-POST-IDEMPOTENT-EQUATION-DECORATION-REFUTER-THEORETICAL-CONTINUATION-SELECTION-V1`

That selector is selected but not created or executed. It must choose a
materially distinct continuation without replaying any active freeze. This
handoff does not authorize candidate repair, source-law or ontology adoption,
physical occurrence or probability, projector or quotient bridge adoption,
D7, B2, P4-T03, `g_eff`, coupling, Einstein equations, Gate action, proof or
publication promotion, push, external action, future impossibility, or global
no-go claims.
