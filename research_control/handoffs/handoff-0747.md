<!-- authority: control -->

# Research Handoff handoff-0747: EqSrc v3 Selector and Stability Obstruction

RT-20260718-022 completed the bounded Refuter stress required by
handoff-0746. Exact proposal-only `EqSrcClosureLaw_src^cand v3` has a
nonidentity abstract source-only realization at finite congruence-quotient
level under its intended provenance cancellation equations.

That positive result does not settle selection or robustness. The Refuter held
the two-object model's carriers, constructors, congruences, provenance,
negative controls, and proxy decisions fixed, then changed only the two
off-diagonal singleton ledger decisions:

- all endpoint decisions Pass, inducing universal equivalence; and
- only diagonal decisions Pass, inducing equality.

Both interpretations satisfy the full candidate law. The exact one-of-five
Refuter classification is `scoped_obstruction`, recorded as
`OBST-EQSRC-CLOSURE-V3-VARIATION-SELECTOR-001`. V3 supplies no
current-ontology-derived rule selecting its primitive decision maps and no
explicit admissible finite-variation class or stability certificate.

The result is local. It does not refute every v3 interpretation, close the
EqSrc milestone globally, reject the ontology, or establish future
source-extension impossibility. The candidate remains `proposal-only` with
`blocked_adoption_open_continuation`. General EqSrc remains undischarged and
the Distance-to-GR ledger is unchanged.

`Accept_src` remains total on its typed certificate domain. Malformed external
values cause model-level rejection unless an ambient error carrier is added.
The constructed witness has complete abstract source provenance but does not
establish current-ontology grounding, physical necessity, naturalness,
uniqueness, empirical adequacy, or exact-GR recovery.

Freeze criteria are evaluated as `not_frozen`: the stress adds a new
nonidentity witness and a discriminating countermodel. However, another
generic closure-law signature repair or repeat audit is barred.

The next lawful packet is exactly one bounded
`candidate-constructor@0.2.0` ontology-law-research packet. It must construct
one proposal-only, nondegenerate, provenance-complete source interpretation
with:

- a source-rooted cross-fiber acceptance selector;
- an explicit admissible finite-variation class;
- a stability or naturality certificate for that class, or its minimal
  countermodel; and
- complete separation from target, workflow, registry, validator, adoption,
  and benchmark authority.

No human gate is required for that construction. Any ontology or source-law
adoption remains a separate protected human decision. No canonical ontology
edit, RetainH, GenH, or M_src adoption, effective metric result, matter
coupling, Einstein equations, benchmark promotion, Gate Chair verdict,
completed derivation, global no-go, or future source-extension impossibility
follows.
