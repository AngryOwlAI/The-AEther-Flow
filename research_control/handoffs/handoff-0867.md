# Handoff 0867 — proposal-only Phi_src semantics fixed

`handoff-0867` records the bounded P5-T02 result. `Phi_src` is now tested as
one precise source-side candidate: a jointly continuous unital forward action
of `(R_ge_0,+,0)` on the P5-T01 admissible source-configuration space.

The task-local result includes:

- identity, composition, admissibility, declared-redundancy compatibility,
  and joint continuity;
- a redundancy-quotient descent theorem with the exact additional condition
  needed for quotient-action continuity;
- classification of continuous additive order-preserving
  reparameterizations as nonnegative scalings;
- a genuinely noninvertible continuous witness and a discrete `N_0`
  specialization; and
- explicit periodic, bi-infinite, dense, branching, stochastic, continuous,
  and discrete dispositions.

This is `draft/control`, `proposal-only`, `source-extension data`, and
`blocked_adoption_open_continuation`. It is not canonical-ontology
modification, an adopted or rejected source law, physical time, a clock,
gauge, physical dynamics, a generator, reconstruction, `M_src`, `g_eff`,
matter coupling, Einstein-equation recovery, benchmark promotion, proof,
publication, push, or completed derivation.

After the governed checkpoint commits, the next bounded route is P5-T03 under
`continue-research` with `candidate-constructor@0.2.0`: construct one
nontrivial source-dynamics candidate compatible with the proposal-only
semiflow or return a precise obstruction. P5-T03 is not executed by this
handoff.
