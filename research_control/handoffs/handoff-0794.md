<!-- authority: control -->

# Handoff 0794

Handoff ID: handoff-0794

P3-T03 completed a conditional source-readout theorem and a precise bounded
obstruction. For a readout family fixed before representative evaluation, the
joint readout factors uniquely through the structural orbit quotient exactly
when it is constant on each structural orbit. Equality across every admissible
choice is stronger: on a nonempty domain the joint image must be a singleton.
Equivariance transports values and does not imply raw equality unless every
image value is fixed.

The six finite controls exhaust their declared groups, orbits, and readout
tables. Every invariant subfamily factors. Five transitive invariant controls
are all-choice constant; the two-orbit invariant control is not. Every full raw
interface contains at least one representative-sensitive readout, giving six
scoped countermodels to full raw-interface irrelevance.

The finite invariant real-readout algebra is exactly the function algebra on
the orbit quotient. Reynolds averaging is restricted to finite real-valued
readouts, is generally nonmultiplicative, and constructs a new invariant
readout rather than proving the original readout irrelevant. Objectwise
factorization is not a natural family theorem without congruence and transport
compatibility. No invariant family is shown complete, operational, empirical,
or physically privileged.

The focused validator passes 14 checks, and the registered TeX source compiles
to five pages. The result is draft/control, proposal-only, and source-extension
data. Structural automorphisms are not physical gauge. No ontology, general
EqSrc, scientific ledger, downstream GR object, promotion, publication,
global no-go, or completed-derivation status changes.

Exactly P3-T04 is selected for the next relay generation through
`ontology-formalizer@0.2.0`. It must define exact source-side probability
measures and dynamical sector-selection structures, prove what they do and do
not select, and test finite uniform-distribution controls. P3-T05 remains
dependency-blocked until P3-T04 completes.
