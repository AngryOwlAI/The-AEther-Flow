<!-- authority: control -->

# Handoff handoff-0893: P7-T04 common source propagation

## Completed scope

`RT-20260728-003` defines task-local, `draft/control`, `proposal-only`
Boolean source support and finite reachability profiles for the explicit
P7-T03 protocol controls. Compatible source-address bijections intertwine
every Boolean reachability power. With the proposal-only map \(b_{RS}\)
stipulated, `Rod_src`, `Signal_src`, and `Detector_src` therefore share one
exact three-address-chain relation.

The full suite does not satisfy the named \(U_{\mathrm{bij}}\) predicate. The
equal-cardinality `Clock_src` and baseline `FreeFall_src` relations have
incompatible strongly connected component counts and nontrivial-cycle
content, so no bijection can intertwine them. This is
`OBST-P7T04-FULL-SUITE-COMMON-PROPAGATION-001`, a precise obstruction for the
current explicit candidate data.

## Status boundary

The result is a finite source-relation theorem and countermodel only. Current
ontology does not derive or adopt the support profiles or \(b_{RS}\).
Adoption remains `blocked_adoption_open_continuation`.

No energy or scale variable, differential operator, covector domain,
principal symbol, characteristic cone, metric, physical detector semantics,
matter-sector interpretation, matter action, universal coupling, stress
energy, or GR result was supplied. The obstruction does not establish
physical nonuniversality, birefringence, equivalence-principle failure, a
global no-go theorem, global theory rejection, or future source-extension
impossibility.

## Next bounded route

First run one governed checkpoint for `AJ-RT-20260728-003-001`. Only after it
commits may one fresh bounded `candidate-constructor@0.2.0` packet execute
v21 P7-T05 by constructing the actual universal matter-coupling candidate or
returning a distinct precise obstruction.

That packet must not repeat the frozen P7-T04 \(U_{\mathrm{bij}}\) profile or
assume a common physical cone, metric, detector semantics, stress energy,
coupling, or GR behavior. This handoff does not execute P7-T05.
