<!-- authority: control -->

# Handoff handoff-0894: P7-T05 source support comparison

## Completed scope

`RT-20260728-004` constructs a task-local, `draft/control`,
`proposal-only` comparison for finite source controls that are explicitly
equipped with a bijective row-, transition-, support-, and
kernel-preserving map to the stipulated three-address carrier \(B_3\). The
induced bookkeeping-state reindexing exactly intertwines finite updates and
composes under the declared equipment-coherence equation.

The exact P7-T02 sector \(\sigma_0=(1,[0])\) also admits two valid controls on
the same two configurations \(x_u,x_v\). The identity-only control has two
full support edges. Enabling \(T_e:x_u\to x_v\) gives three. Their supports
are not permutation-equivalent. Since `Sec_src=(Q,D)` does not retain enabled
rows or equipment, current sector data do not derive one sector-indexed
control and exact comparison family. This is
`OBST-P7T05-SECTOR-COUPLING-FUNCTOR-NONSELECTION-001`.

## Status boundary

The result is a finite support comparison and current-data nonselection
theorem only. Current ontology does not derive or adopt the selector,
equipment family, or comparison. Adoption remains
`blocked_adoption_open_continuation`.

\(B_3\) is not a physical carrier, cone, metric, or common geometry.
Rod_src, Signal_src, and Detector_src are three protocol-role examples, not
all admitted matter sectors or physical matter species. No physical
coupling, covariant derivative, action, stress energy, conservation law,
detector semantics, equivalence-principle behavior, Einstein equations, or
exact-GR result follows.

The obstruction does not establish physical nonuniversality, a global no-go
theorem, a theory-wide impossibility conclusion, or future source-extension impossibility.
New source selectors, different carriers, nonbijective comparisons,
information-controlled quotients, and conservative source extensions remain
open.

## Next bounded route

First run one governed checkpoint for `AJ-RT-20260728-004-001`. Only after it
commits may one fresh bounded `ontology-formalizer@0.2.0`
`ontology-law-research-packet` execute v21 P7-T06.

That packet must construct a proposal-only source-equivalent variational
object, stress-energy-equivalent object, and conservation conditions, or
return a precise missing-source-law obstruction. It inherits the P7-T05
nonselection result and may not assume a target metric, physical coupling,
action, stress energy, or Einstein equations. Any source-law adoption remains
human-gated. This handoff does not execute P7-T06.
