---
authority: control
handoff_id: "handoff-1008"
task_id: "RT-20260810-011"
job_id: "AJ-RT-20260810-011-001"
status: "ready_for_v22_p4_t02_b2_common_character_independent_variation_refuter_stress_after_checkpoint"
validation_status: "PASS_PRECHECKPOINT_STAGING_REQUIRED"
---

# Handoff 1008 — common-character candidate is locally source-pure

## Answer first

RT-20260810-011 returns `source_pure_as_written` for the exact RT010 candidate,
strictly as a pointed, framed, anchored, finite, proposal-only existence witness.
No target geometry, empirical answer, physical interpretation, benchmark result,
generated derivative, registry status, role identity, validator result,
checkpoint, or protected authority is used as a scientific premise.

This is a provenance pass, not a derivation or adoption. The common-character
equation transparently loads the desired formal common-descent property. The
source cycle, point, frames, anchors, cochains, balance matrix, gauge, covector,
and sector scope remain new, underived proposal data. An acyclic dependency graph
establishes definitional noncircularity but not epistemically independent
selection.

## Exact new stress burden

The audit supplies four sharp, target-free controls:

- descent to some common holonomy is codimension two in the three-sector class
  space, while descent to the fixed RT010 `chi` is codimension three in the
  four-class space;
- an arbitrarily small single-edge `H^1` perturbation changes one sector loop
  holonomy and defeats every intertwiner to the fixed `chi`;
- the bare oriented cycle selects no point and an oriented line selects no
  positive frame; anchors choose representatives but cannot repair unequal
  holonomy;
- at the admitted flow value `t=-1/2`, `mu=0` and `chi=a_s=rho_s=1`, so formal
  descent survives while every nontrivial transition collapses.

The alternative covector `m_0=(1,-1,1,-1)` has the same source type and acyclic
DAG shape but is zero at the base state and along the quotient flow. This proves
that dependency order alone cannot select the RT010 covector.

## Freeze and claim boundary

Both inherited freezes remain in force:

- `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR`
- `NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS`

The common-character branch is not frozen before its first independent Refuter
stress. D7 is not reevaluated, B2 remains inactive and undisposed, and P4-T03
remains locked. No all-P7 universality, empirical response, physical causality,
calibration, conformal geometry, scale, `g_eff`, coupling, Einstein equation,
source-law or ontology adoption, Gate verdict, benchmark, proof promotion,
publication, push, external action, global no-go, future-extension impossibility,
or completed derivation follows. Distance-to-GR is unchanged.

## Next governed action

Run the single checkpoint for `AJ-RT-20260810-011-001`. In a fresh invocation,
admit exactly one `refuter@0.2.0` packet for
`PKT-V22-P4T02-B2-COMMON-CHARACTER-INDEPENDENT-VARIATION-REFUTER-STRESS-V1`.
Stress the unchanged candidate against the exact holonomy perturbation,
`t=-1/2` collapse, reanchoring and presentation enlargement, point and frame
nonselection, alternative covectors, rank-preserving balance variations, inverse
branches, and frozen-route controls. This handoff selects that stress but does
not execute it or prejudge its result.
