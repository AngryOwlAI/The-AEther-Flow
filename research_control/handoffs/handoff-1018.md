---
handoff_id: "handoff-1018"
task_id: "RT-20260811-004"
job_id: "AJ-RT-20260811-004-001"
status: "ready_after_checkpoint"
---

# Handoff 1018 — signed cubic viability-selector Refuter stress

RT-20260811-004 completed the focused source-extension Smuggling Auditor
packet. The fixed `SignedCubicViabilitySelector_v1` is
`source_pure_as_written` only in the narrow source-syntax and provenance sense.
Its dependency graph contains no literal target geometry, empirical answer,
frozen selector, generated derivative, registry, role, validator, or checkpoint
premise.

The audit does not derive or adopt `gamma`, `Delta`, `a_star`, or `sigma`.
Sigma remains the sole supplied component-identity token, and reflection
equivariance selects neither token. Component viability selects no amplitude
point or probability law. Predeclaration establishes syntax-level
noncircularity, not epistemic justification or physical meaning.

The fused audit adds exact conditional mathematics. Positive regraduation
orbits are completely classified by
`H=gamma*Delta*a_star^2`, with normal form `(H,1,1)`. The finite transitive
closure from a nonzero amplitude is its same-sign ray up to the initial
magnitude. The sharp `n`-step factor is
`Lambda_n=(1+2*n*H)^(-1/2)`, which tends to zero. No invariant Borel
probability is supported on `U_sigma`; on the full line the invariant
probability is `delta_0`, outside either selected component.

After the RT004 checkpoint, execute exactly one separately admitted
`refuter@0.2.0` packet:
`PKT-V22-P4T02-B2-SIGNED-CUBIC-VIABILITY-SELECTOR-SOURCE-EXTENSION-REFUTER-STRESS-V1`.
Stress only perturbative and off-family robustness, token nonselection,
finite-versus-asymptotic viability, and invariant-measure limitations. Do not
repair or adopt the candidate in that packet.

The stress is not executed here. All four freezes remain active, all fourteen
Distance-to-GR rows remain `no_delta`, D7 is not reevaluated, B2 remains
inactive, P4-T03 stays locked, and no physical or empirical semantics,
adoption, Gate, benchmark, proof, publication, push, external action, global
no-go, or completed derivation is authorized.
