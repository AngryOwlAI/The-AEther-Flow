<!-- authority: control -->

# handoff-0854 — P4-T05 payload complete; checkpoint blocked

Generation 99 completed the protected P4-T05 Gate Chair packet under
`approval-20260724-001`.

The exact decision is
`SELECT_CONTINUUM_FIRST_WITH_NARROW_PREMETRIC_BOUNDARY`. Continuum-first is
selected as the canonical mainline research architecture. Its source arena is
a four-dimensional smooth manifold carried as explicit primitive debt, not as
a derived or already physical spacetime. `Phi` remains only an abstract
source-order or evolution slot; no parameter domain, algebraic structure,
invertibility, stochastic status, gauge status, or physical-time meaning is
adopted.

Emergence-first remains proposal-only, noncanonical, and historically
preserved. It may be reopened only by a P5-T08 scoped obstruction tied to the
continuum primitive, an unrepaired P6 no-target-import failure, or materially
stronger evidence.

P4-T05 does not edit canonical ontology sources. It selects and delimits the
mainline. P4-T06 owns the separate canonical integration transaction.

The task-local payload passes, but required repository validation fails on
`job_plan_task_id_not_selected_by_handoff`. The prospective ordinary-route
guard requires a new job to repeat the predecessor handoff's selected plan
task and has no fail-closed branch for this generation's later exact human
authorization, tracked approval, Director decision, and immutable recursive
route. Rewriting `handoff-0853`, substituting an unrelated handoff, or
mislabeling this job as P13-T02 would misstate the authority chain. No
checkpoint ran.

## Required next action

Run one governed `improve-project-system` recovery for the prospective
ordinary-route protected-human-override admission contract. The recovery must
validate the exact approval, Director decision, goal identity, generation,
route hash, plan-task identity, and one-time consumption while preserving the
ordinary equality rule for jobs without that protected contract.

Preserve the complete P4-T05 payload and consumed approval, rerun every
required gate, and checkpoint the combined transaction once. Only after that
commit may a separately claimed P4-T06 `continue-research` packet integrate
the selected narrow continuum-first boundary into canonical ontology sources,
public claim vocabulary, burden definitions, and routing, and retire hybrid
mainline language.

## Authority boundary

This handoff does not establish ontology truth, physical spacetime, `Phi`
semantics, source dynamics, Lorentzian metric, causal structure, clocks,
scale, matter semantics, matter coupling, stress-energy, Einstein equations,
exact-GR derivation, benchmark promotion, publication authority, push
authority, or completed derivation. It does not reject or delete the
emergence-first proposal. It does not authorize approval reuse, Gate Chair
re-execution, shared-validator edits under the blocked P4-T05 AgentJob, or
checkpointing with failed required validation.
