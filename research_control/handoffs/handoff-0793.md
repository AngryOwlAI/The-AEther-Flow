<!-- authority: control -->

# Handoff 0793

Handoff ID: handoff-0793

P3-T02 completed one proposal-only source-extension comparison packet. The
fused result separates eight typed declaration atoms and proves that their
syntactic Boolean envelope has 256 nodes and 1024 directed covers. A separate
well-typed reduct predicate prevents the envelope from being misread as the
space of physically realizable theories. Ten burden axes remain componentwise;
there is no scalar scientific ranking.

For a realized finite extension \(e_E\), the exact deterministic-selector set
is the compatible fixed locus
\(\Sigma(E,e_E)=C_E(e_E)\cap\operatorname{Fix}(H_E)\). It is unique exactly
when this set is a singleton. A smaller stabilizer can enlarge the raw fixed
locus, so symmetry reduction alone does not prove uniqueness. Eight finite
controls preserve the point, probability, dynamics, provenance, and
discriminator distinctions. Five historical families are placed at
finite-witness-local candidate nodes with every freeze and unmet obligation
preserved.

The focused validator passes 19 checks, and the registered TeX source compiles
to six pages. The result is draft/control, proposal-only, and source-extension
data. No extension is derived or adopted. Structural automorphisms are not
physical gauge; formal uniqueness is not physical admissibility or empirical
support. General EqSrc, both scientific ledgers, downstream GR objects,
promotion, publication, and completed-derivation status remain unchanged.

P3-T03 and P3-T04 are now dependency-ready. Exactly P3-T03 is selected for the
next relay generation through ontology-formalizer@0.2.0. It must define an
explicit source-observable target and prove factorization or observable
independence across all admissible representatives, or return a precise scoped
obstruction. It must not identify structural symmetry with physical gauge or
execute P3-T04.
