<!-- authority: control -->

# Handoff 0837 — P12-T05 implementation complete; checkpoint recovery required

P12-T05 implemented the durable scientific-quality metric surface. Eight
advisory metric families now require explicit eligible sets and immutable
identity bindings, preserve absent authoritative denominators as
`not_measured`, reject aliasing and artifact-splitting defects, and emit no
aggregate truth score. Fourteen calibration cases pass 44 checks. The live
report has three measured, five `not_measured`, and zero invalid metrics.

The physics-progress report and AI methodology dashboard now present these
durable metrics as the primary quality surface. Raw packet and artifact counts
remain visible only as operational context. A metric value, warning, dashboard
state, validator PASS, route, or checkpoint is not theorem truth, scientific
evidence, physics progress, or Distance-to-GR progress.

No ontology, source law, candidate status, scientific ledger, Distance-to-GR
row, benchmark, proof, publication, or completed-derivation authority changed.
P4-T05 remains dependency-ready but human-gated. The ordinary-route guard
observes one consecutive project-system implementation, below its threshold of
three; the transaction itself remains checkpoint-blocked.

The one normal planner-compare checkpoint stopped before staging because four
generated reporting outputs and two governed metric-reporting scripts remain
classified as `unknown_governed_path`. Nothing was staged or committed, the
entry index was preserved, and legacy validation was not used. Generation 70
does not authorize classifier policy changes or a second checkpoint attempt.

The recursive relay must next run one fresh bounded `improve-project-system`
recovery. It may classify only the six exact blocker paths using existing
validation-family vocabulary, add focused fail-closed regressions, preserve
unrelated unknown-path fallback, and invoke one future normal checkpoint
without legacy validation. P12-T06 remains selected only after that checkpoint
commits and is not execution-ready now.

The authoritative machine-readable handoff is
`research_control/handoffs/handoff-0837.yaml`.
