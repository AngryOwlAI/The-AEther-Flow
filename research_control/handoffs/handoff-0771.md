---
authority: control
status: completed
handoff_id: handoff-0771
task_id: RT-20260718-046
job_id: AJ-RT-20260718-046-001
---

# Handoff 0771: Flow-generated graded-orbit root formalization

RT-20260718-046 completed the one bounded theoretical-selection packet
required by `handoff-0770`.

The selected proposal-only target is
`EqSrcFlowGeneratedGradedOrbitRootLaw_src^cand,v1`, routed to one future
`ontology-formalizer@0.2.0` packet. It is materially distinct from exact
rooted-partition candidate v1: the next packet may not load the transition
graph \(D_\Phi\), an event-to-record projection, or a root certificate as
independent inputs. It must derive or reject the transition graph, root, and
rank from one explicitly proposed source-side ordered-monoid action.

On a full source orbit component, reachability is proposed by
\(x\preceq_\Phi y\) when an allowed nonnegative source-order element carries
\(x\) to \(y\). An admitted branch must prove that reachability is a locally
finite partial order, derive its directed cover graph, prove one unique global
minimum, and prove that every saturated chain from that minimum to an event has
the same finite length. Only then may rank parity define two nonempty ordered
blocks and a proposal-only associated-state line and kernel-pair relation.

The required positive control is a finite four-event action chain. Periodic
orbits must fail antisymmetry; bi-infinite orbits must fail the global-minimum
guard; dense orbits without locally finite covers must fail the rank
interface; multiple minima and non-graded reachability must fail closed. A
finite observed segment of a rootless orbit is a mandatory provenance control:
its first sample is not a source-global minimum.

Current ontology does not derive an ordered monoid action, discrete orbit
structure, cover relation, unique minimum, graded rank, or physical morphism
and variation categories. The target is therefore a `proposal-only`
`new_ontology_primitive_candidate`, with
`blocked_adoption_open_continuation`.

The exact `EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1` construction,
audit, and stress cycle remains locally frozen. Repeating, repairing, adopting,
or relabeling its loaded graph, projection, and root is barred.

No Distance-to-GR or metric-use ledger row changes. No source law, canonical
ontology, physical admissibility, general `EqSrc`, downstream GR object,
benchmark status, Gate Chair verdict, completed derivation, or
future-extension impossibility is promoted. No program-wide conclusion is
authorized beyond this scoped packet.

## Next action

Run exactly one bounded Ontology Formalizer `ontology-law-research-packet`.
Define the ordered action, full orbit domain, reachability, cover relation,
unique-minimum guard, graded rank, parity partition, morphisms, variations,
positive chain, and every fail-closed countermodel. Return a precise
obstruction if any prerequisite cannot be stated source-only. Do not adopt the
law or edit canonical ontology.
