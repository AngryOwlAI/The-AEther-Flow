<!-- authority: control -->

# Handoff 0879 — P6-T03 one-ray conformal nonselection

Status: `ready_after_checkpoint`.

`handoff-0879` records the bounded generation-124 Ontology Formalizer result.

## Result

P6-T03 proves two linked statements about the proposal-only P6-T02 input:

- the characteristic hyperplane `Ann(V_x)` cannot equal the quadratic null
  cone of a nondegenerate Lorentzian form; and
- the family `g_H = -ds^2 + H_ij dy^i dy^j` shares the same oriented `V` ray,
  annihilator, and flow preorder for every positive-definite `H`, while
  distinct choices need not be conformally related.

The explicit pair `diag(-1,1,1,1)` and `diag(-1,2,1,1)` has different null
cones. The complete P6-T02 one-ray datum therefore does not select a
Lorentzian conformal class.

## Scientific boundary

The disposition
`OBST-P6T03-ONE-RAY-CONFORMAL-NONSELECTION-001` is scoped to reconstruction
from only the P6-T02 ray, annihilator, and flow preorder. It is not a global
no-go theorem. Current ontology does not derive the missing full cone,
quadratic form, transverse conformal shape, compatibility, covariance, or
universality law.

Adoption remains `blocked_adoption_open_continuation`. A materially richer
source-side law or theorem may reopen the milestone. Overall metric scale,
clock, rod, volume, and operational calibration remain open.

## Next action

Run the one governed checkpoint for `AJ-RT-20260726-005-001`. Only after that
commit may one fresh bounded P6-T04 `candidate-constructor@0.2.0` packet test
source-defined scale and calibration structures while carrying the P6-T03
absence of a selected conformal class as a hard input, or return its own typed
obstruction.

No canonical ontology or source-law adoption, physical causality, conformal
metric, scale, historical scoped `g_eff` expansion, matter coupling, Einstein
equations, benchmark promotion, global rejection, future-extension
impossibility, proof, publication, push, or completed derivation is
authorized.
