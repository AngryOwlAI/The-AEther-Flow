<!-- authority: control -->

# Handoff 0969 — V22 P0-T03 registration requires route-namespace repair

## Result

`RT-20260808-003` registered the exact reviewed V22 draft/control plan and
materialized its compact project-control traceability backlog. The task-local
deterministic result is:

- 19 review recommendations;
- 12 recommended sequence steps;
- 10 cross-cutting directives;
- 11 phases;
- 40 unique work packages;
- 61 resolved dependency edges;
- zero cycles, dangling dependencies, unreachable packages, or coverage
  orphans.

The broad integration gate then activated P0-T03's exact `ID collision` stop
condition. The ordinary-route guard still resolves `plan_task_id` only against
the V21 backlog, so V22 `P1-T01` is misread as the unrelated V21 science task
with the same local ID. The registration and traceability outputs remain
useful, but P0-T03 is not qualifying-complete and the V22 runtime backlog is
not admitted.

This is a project-system contract defect, not a scientific result. It changes
no Gate, benchmark, external-review, replication, publication, or
Distance-to-GR status.

## Next bounded route

After the single governed checkpoint for `AJ-RT-20260808-003-001`, execute one
separately admitted `P12-T04` `improve-project-system` repair. Its scope is
plan-version-aware backlog resolution, plan-qualified completed-work-item
identity, V22 runtime routing metadata, regression fixtures, and fresh P0-T03
integration validation. The exact blocker is
`research_control/tasks/RT-20260808-003/artifacts/v22_p0_t03_runtime_route_collision_blocker.yaml`.

Do not execute V22 `P1-T01` until that repair and a fresh qualifying P0-T03
closure.

No tag, public release, publication, submission, push, reviewer contact, or
other external action is authorized.
