---
authority: control
handoff_id: handoff-0986
task_id: RT-20260809-016
job_id: AJ-RT-20260809-016-001
status: ready_for_v22_p4_t01_after_checkpoint
---

# Handoff 0986 — P3-T04 background and linear response

P3-T04 is complete pending its single governed checkpoint. The bounded packet
selects the exact nonconstant source background
`qbar=(s1-s0,s2-s0,0,0,0,3)` by a rational lexicographic rule frozen before
target access. It solves all six fixed P3-T02 equations, has a positive `q6`
margin, and lies on the rank-two regular response patch. It is not a physical
vacuum, Minkowski background, or adopted source state.

For the sealed fixed coefficient datum, the Fréchet operator is exactly
`L_Du=(X_1u1,...,X_6u6)`. The candidate has no algebraic field constraints and
no internal field-gauge generators; passive source-chart covariance is kept as
representation parity rather than physical gauge. The two logistic response
channels have rank two and preserve the precise P2 verdict
`necessary_condition_met_not_sufficient`. No physical geometry probe receives
complete typing.

On the declared uniform periodic source grids, constant-coefficient upwind
evolution commutes with backward differences. The P3-T03 estimate therefore
gives a bounded source-coordinate `C1` comparison for `C3` perturbations.
Value and derivative errors both decrease over levels 24, 32, 48, and 64. A
separate implementation reproduces all six tangent channels below
`5.8e-11`. All 92 focused checks pass, all five parent conflicts are resolved,
and visual review of all five compiled TeX pages finds no clipping, overlap,
broken table, or unreadable glyph.

The source-only coupled counterfamily preserves the selected background and
changes principal zero sets, but it also changes the sealed P3-T02 law. It
therefore records source-law-selection debt and freezes only cross-law
uniqueness overread; it is not a within-candidate counterexample or a physical
inequivalence theorem. The fixed law is ready for a source-principal
calculation, while a physical cone, Lorentzian signature, scale, and `g_eff`
remain absent. The Distance-to-GR ledger is unchanged.

After checkpoint, the next bounded packet is P4-T01: an Ontology
Formalizer/Candidate Constructor job with hyperbolic-PDE and Refuter pressure.
It must derive the gauge-reduced source principal symbol and polynomial,
including multiplicities, density weight, covariance, factorization, and
branch conditions, without inserting a quadratic metric form. P4-T01 is
selected but unexecuted. P2-T02 still requires real reviewer-contact authority
and is not satisfied by internal work.

No canonical adoption, physical-cone or signature claim, effective metric,
coupling, Einstein equation, Gate verdict, benchmark promotion, external
review, publication, release, push, external action, or completed-derivation
authority is created.
