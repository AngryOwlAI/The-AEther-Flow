<!-- authority: control -->

# Handoff 0949 — P16-T02 classifier recovery checkpoint

`handoff-0949` records the bounded generation-235 project-system recovery.

Generation 235 repaired exactly three checkpoint-planner path classifications:
`accepted_status_calibration_v1.yaml`, `accepted_status_calibration_v2.yaml`,
and `distance_to_gr_status_aliases.yaml`. Each now receives the stable
`claim_language` and `role_or_schema_contract` path-family tags. Their bytes
and meanings are unchanged, and unrelated design YAML remains fail-closed.

The focused 22-test shard, cumulative checkpoint planner, control spine,
29-gate full profile, and 1,293-test repository-wide suite pass with zero hard
precheckpoint errors. The sole governed checkpoint attempt then fails staged
documentation-impact acceptance: its memory synchronization changes
`FOLDER_MAP.md`, while the cumulative RT-005 and RT-006 receipts omit that live
generated derivative. The index is restored and no commit occurs. This
project-system result is not scientific progress,
a Gate verdict, Distance-to-GR movement, benchmark promotion, proof,
publication, push, or completed derivation.

The next packet is one materially distinct project-system recovery. It may add
only the generation-235 blocker artifact to `changed_paths` and
`FOLDER_MAP.md` to `generated_derivatives` in the exact RT-005 and RT-006
documentation-impact receipts, regenerate and revalidate the cumulative
transaction, and invoke at most one fresh governed checkpoint. It must not
execute P16-T03 payload work. P16-T02 must still be freshly re-audited before
P16-T03 payload or P16-T04.
