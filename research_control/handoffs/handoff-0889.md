<!-- authority: control -->

# Handoff handoff-0889: P7-T01 source-matter ontology target

## Completed scope

`RT-20260727-007` constructs
`SourceMatterIncidenceCandidate_v1` as a task-local `draft/control`,
`proposal-only` source-matter ontology target. Its primitive fields are source
occupation multiplicities and a closed defect chain. Occupation, charge
chains, total charge, and representation grades are derived; the
transition-invariant sector is `(Q,[z])`.

The packet includes an exact finite nontrivial witness, valid null and
nontrivial-neutral configurations, a malformed transition with a nonzero
continuity residual, and a typed underdetermined request. The exact
source-presentation naturality theorem is conditional on the declared
candidate data and compatible isomorphisms.

## Status boundary

The mathematical ontology-target sub-burden is narrowed, so the
`matter_coupling` Distance-to-GR row now records that a proposal-only draft
object exists. This is not canonical ontology, physical matter, a physical
charge or conservation law, detector semantics, a matter action, stress
energy, universal coupling, effective geometry, Einstein equations, benchmark
recovery, proof authority, publication, or a completed derivation.

All legacy matter-profile and certificate objects retain their inherited
statuses. P6 Gate B remains zero of eight, and the unchanged P6 package is not
a lawful geometry input. Adoption remains
`blocked_adoption_open_continuation`.

## Next bounded route

First run one governed checkpoint for
`AJ-RT-20260727-007-001`. Only after it commits may one fresh bounded
`theoretical-continuation-selector@0.1.0` packet compare the P7-T02 action,
evolution-equation, and transition-law branches and select exactly one
source-only constructive packet.

The selector must not execute its selected dynamics packet, import target
geometry or detector semantics, or create ontology, coupling, stress-energy,
GR, proof, or promotion authority.
