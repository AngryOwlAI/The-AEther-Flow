<!-- authority: control -->

# Research Handoff handoff-0744: EqSrc Closure Candidate v2 Signature Repair Required

RT-20260718-019 completed the fresh Smuggling Auditor review required by
handoff-0743. The audit confirms two points:

- no explicit target atlas, target metric, detector, benchmark, registry,
  role, validator, generated derivative, handoff, checkpoint, approval, or
  downstream GR premise is imported; and
- the v2 accepted identity, inverse, and composition clauses repair
  `OBST-EQSRC-CLOSURE-TOTALITY-001` at statement level, so the conditional
  equivalence implication is valid under a well-formed interpretation.

The advertised closed source signature is not complete as written. `Sigma_src`
does not declare the proxy witness symbols used by identity, inverse, and
composition certificates. It declares only ledger congruence although later
clauses invoke additional component congruences. It also does not type and
stratify `NegPass_n` so that acceptance is provably independent of
`Accept_src`.

This is `OBST-EQSRC-CLOSURE-SIGNATURE-001`, a precise repairable
source-specification obstruction. It is not detected target smuggling, a
global no-go theorem, or a theory rejection.

The candidate remains `proposal-only` source-extension data with
`blocked_adoption_open_continuation`. No canonical ontology edit, source-law
adoption, general `EqSrc`, RetainH, GenH, `M_src`, `g_eff`, matter coupling,
Einstein equations, finite-variation robustness, Distance-to-GR ledger change,
benchmark promotion, Gate Chair verdict, completed derivation, global no-go,
or future source-extension impossibility follows.

The next lawful packet is exactly one bounded
`ontology-formalizer@0.2.0` proposal-only v3 repair. It must declare or
explicitly quantify every proxy witness constructor, declare all component
congruences, type negative-control predicates on displayed domains, and impose
an acyclic source-only dependency order before `Accept_src`. It must preserve
the v2 accepted-totality clauses. A fresh Smuggling Auditor review precedes
Refuter stress. Any adoption remains a separate protected human-gated
decision.
