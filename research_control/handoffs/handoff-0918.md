<!-- authority: control -->

# Handoff handoff-0918 — P9-T04 causal, local-inertial, and free-fall case

Status: `ready_after_checkpoint`.

`handoff-0918` records the bounded generation-179 Candidate Constructor
result.

## Result

P9-T04 constructs exact formal propagation and P7 operational-device traces
in two physically interpreted source sectors, `(1,[0])` and `(2,[0])`.

The movable finite kernel gives endpoint response `1-2^(-n)`, while the
identity-only kernel gives zero. A common task-local equipment record gives
the same propagation, free-fall, clock, rod, and signal response tuple in both
sectors. A split record uses a movable control in one sector and a valid
identity-only control in the other. Their exact residual vectors are
`(0,0,0,0,0)` and `(1,1/2,1/4,1/4,1/4)`.

The current source package therefore does not select the sector/role
equipment, physical calibration, and model-to-world law required for a unique
physical causal or free-fall prediction. The decisive result is
`OBST-P9T04-SECTOR-CAUSAL-FREEFALL-SELECTOR-ABSENT-001`.

## Benchmark disposition

The case is `INCONCLUSIVE`, with secondary label `FORMAL_ANALOGY`, not
`PASS`. The formal slope is address-hops per update index, not physical speed.
The P7 operational values are source-token responses, not physical clock
rates, rod lengths, signals, trajectories, or accelerations.

No physical characteristic equation, time/length calibration, local
transformation law, free-fall map, correction budget, uncertainty model,
target equivalence relation, tolerance, or independent reproduction exists.
The target comparison oracle was not opened, no source record was selected
from target expectations, and no target-informed rerun occurred.

## Checkpoint boundary

The P9-T04 transaction is ready for one governed checkpoint under
`AJ-RT-20260730-011-001`. P9-T05 remains unexecuted until that checkpoint
commits.

After the commit, one fresh bounded Candidate Constructor packet may execute
the distinct P9-T05 source-derived benchmark case. It must carry the P9-T02
through P9-T04 `INCONCLUSIVE` dispositions and zero passes, and it may not
reinterpret the formal P9-T04 residuals as physical causal or equivalence
principle results.

## Scientific boundary

Across P9-T02 through P9-T04, three source cases have been executed and all
are `INCONCLUSIVE`; there are zero benchmark passes. Gate D remains
`NOT_READY_EINSTEIN_SECTOR_DERIVATION_REQUIREMENTS_UNMET`. No Gate E verdict
was issued. The Distance-to-GR row gains obstruction evidence but does not
change physical or promotion status.

The common record demonstrates constructible formal equality, not a derived
equivalence principle. The split record demonstrates current-source
nonselection, not an empirical equivalence-principle violation. The movable
kernel's direction asymmetry is not a preferred-frame or birefringence
measurement.

The packet adds no canonical ontology or source law, physical speed, null
cone, local Lorentz or local-inertial recovery, universal physical free fall,
equivalence-principle verdict, effective metric, Einstein equation, exact-GR
recovery, benchmark pass, promotion, proof, publication, push, global no-go,
future-impossibility, or completed-derivation authority.

## Next action

Run the complete governed checkpoint once. If it commits, continue with one
fresh P9-T05 packet; do not rerun P9-T04 unchanged or select the common
equipment record from target universality expectations.
