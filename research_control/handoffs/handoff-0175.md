# Handoff 0175

## Status

Task `RT-20260614-140` is complete.

## Summary

The post-MetricLaw non-bottom metric-data selector is complete.

The selector chooses one bounded `candidate-constructor@0.2.0` packet:

```text
metric_law_src_gsc_nonbottom_metric_data_witness_or_obstruction
```

The selected packet is a bounded theoretical calculation. It may attempt to
construct source-side `MetricData(E)` prerequisite data under the adopted
fail-closed `MetricLaw_src^{GSC}` interface, or emit one primary `OB-GEFF-*`
obstruction.

This is not a construction of `g_eff`. It does not prove non-bottom metric
data exists, does not prove selector existence or uniqueness, and does not
unlock matter coupling, Einstein equations, benchmark promotion, or
completed-derivation claims.

## Claim Boundary

`MetricLaw_src^{GSC}` remains adopted only as a fail-closed source-law
interface over scoped `M_src^{GSC}(E)`.

The next Candidate Constructor packet must keep `MetricData(E)` separate from
`g_eff`. Its maximum positive output is draft/control source-side prerequisite
data:

```text
MetricData_src^{GSC-cand}(E)
```

If any required component is missing, nonunique, target-contaminated, or
process-contaminated, the packet must return one primary `OB-GEFF-*`
obstruction.

## Required Next-Packet Components

- `Sigma_src`: orientation or signature selector.
- `Lambda_src`: scale or measure selector.
- `C_src`: causal preorder or cone analogue.
- `Cov_tau^met`: source-transition covariance.
- `N_src`: nondegeneracy or controlled degeneracy.
- `VarML_src`: finite-variation metric-law compatibility.
- `B_src`: bottom or obstruction behavior.

## Distance-to-GR Delta

`M_src`: unchanged, accepted only within the scoped source-only
`M_src^{GSC}(E)` boundary from `RT-20260614-134`.

`MetricLaw_src^{GSC}`: accepted only as a fail-closed source-law interface.

`g_eff`: still not constructed. No non-bottom effective metric is supplied.

Matter coupling: blocked/not started.

Einstein equations: blocked/not started.

Benchmark: blocked.

## Next Action

Run one bounded Candidate Constructor packet for:

```text
metric_law_src_gsc_nonbottom_metric_data_witness_or_obstruction
```

Do not define or adopt `g_eff` in that packet.
