# Handoff 0581

RT-20260705-008 completed v16 P6-T01.

Selected target:

```text
certificate record type and validity predicate
```

Selected toolchain:

```text
Python typed algebraic spec plus unittest
```

The next bounded packet is P6-T02:

```text
support_only_executable_certificate_spec_v16
```

Recommended role:

```text
validator-engineer@0.2.0
```

The next packet must implement only a support-only finite/local executable
certificate spec. It must cover at least one positive and three negative
cases, map those cases to P4/P5 material, and state that the pilot proves no
project physics claim.

Hard blocks remain unchanged: no canonical ontology edit, source-law adoption,
`RR_ETransportCompletenessOrInvarianceLaw_v1` adoption, unrestricted `RR_E`
theorem, matter-semantics adoption, detector-semantics adoption, coupling-law
adoption, matter-coupling derivation or adoption, stress-energy semantics,
matter action, Einstein equations, benchmark promotion, proof authority, or
completed derivation.
