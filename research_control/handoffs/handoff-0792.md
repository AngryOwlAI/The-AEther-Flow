<!-- authority: control -->

# Handoff 0792

Handoff ID: handoff-0792

P3-T01 completed one proposal-only selector-resolution taxonomy. A complete,
hash-bound burden record now receives exactly one primary class under the
ordered normal form `RI -> PS -> DSB -> AS -> SO`, or fails closed as
unclassified. The five classes are added source structure, representative
irrelevance, dynamical symmetry breaking, probabilistic selection, and scoped
obstruction.

Two independent child perspectives agreed on the class set and primary order.
The parent resolved five differences in one round: target-type granularity,
assumption-cost representation, nonvacuous representative irrelevance,
dynamics-label strength, and overlap precedence. Five worked controls classify
one-to-one; two additional child controls preserve overlap cases. The focused
validator passes 15 checks, and the registered TeX source compiles to five
pages.

The taxonomy is `draft/control` and `proposal-only`. It does not derive or
adopt added structures, dynamics, measures, probability laws, quotients, or a
selector law. Structural automorphisms are not physical gauge by label. A
probability law is not a deterministic point. A scoped obstruction is not a
global no-go. General EqSrc, both scientific ledgers, downstream GR objects,
promotion, publication, and completed-derivation status remain unchanged.

The next bounded route is P3-T02 through
`candidate-constructor@0.2.0`: construct a mechanically checked partial order
of roots, orientations, gradings, measures, states, boundary data, and
dynamics by assumption inclusion and symmetry reduction. Preserve
incomparability; do not force unlike costs into a false total order or adopt an
extension in that packet. P3-T03 and P3-T04 remain blocked on P3-T02.
