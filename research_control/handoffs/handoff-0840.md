<!-- authority: control -->

# Handoff 0840 — Integrity repaired; checkpoint blocked

Handoff ID: `handoff-0840`.

`RT-20260723-007` repaired the two immutable generation-74 blocker classes.
The paired prior handoff Markdown now identifies `handoff-0839`, and exactly
one final newline byte was removed from each of the four hash-sealed P12-T05
targets. The task-local validator checks their expected hashes, sizes,
terminal-newline form, and fixture parse. Runtime, validator, test, route,
metric, and scientific semantics are unchanged.

The one authorized governed checkpoint then stopped without commit. Its
1,182-test repository shard found one P10-T05 event-store architecture
validation failure and one cascading migration-readiness setup error. The
entry index was restored. This worker does not retry the checkpoint, use
legacy validation, or repair the newly exposed dependency.

## Next route

One fresh bounded P12-T05 `improve-project-system` recovery may inspect the
exact P10-T05 architecture validator findings, repair only uniquely determined
content-address or receipt drift under its own allowlist, preserve every
completed P12-T05 repair, rerun the required gates, and invoke at most one
future normal checkpoint. P12-T06 remains blocked until that checkpoint
commits.

The ordinary-route exception remains exact: dependency-ready science route
P4-T05 requires explicit human Gate Chair authority under the registered v21
backlog. This relay generation does not possess that authority.

No scientific claim, source law, ontology adoption, effective metric, matter
coupling, Einstein-equation derivation, benchmark promotion, proof authority,
publication authority, completed derivation, or global no-go follows from
this project-control recovery.
