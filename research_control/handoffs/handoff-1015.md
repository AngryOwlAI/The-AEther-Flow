---
authority: control
handoff_id: handoff-1015
task_id: RT-20260811-001
status: ready_for_post_oriented_matroid_refuter_theoretical_selector_after_checkpoint
---

# Handoff 1015 — oriented-matroid bridge Refuter stress complete

## Result

RT018 returns `scoped_obstruction` for one precise claim: the unchanged arbitrary-finite-rank `Bridge_OM` classifier is not by itself a selected, finite-variation-stable source-to-physical P4-T02 bridge.

The fixed RT016 mathematics survives, and RT017's `source_pure_as_written` verdict remains valid in its narrow proposal-only provenance scope. Exact source-linear controls show:

- a rational rank-two nonzero-column path crosses from strict feasibility to obstruction at arbitrarily small distance;
- one source reorientation flips the total branch at every finite rank;
- one rank-preserving finite refinement destroys strict feasibility;
- named sign, circuit, total, chamber, and trivial refinement/path component values can agree while a labeled projective cross-ratio differs; and
- pointwise totality does not select its input, admissible variations, stable component, or physical interpretation.

Both independent specialists agree on the scientific result. One simultaneous metadata-only revision round aligned the freeze, obstruction, and successor identifiers. No blocking conflict remains.

## Freeze and preserved limits

The new local freeze is:

- `NDCL-V22-P4T02-B2-ORIENTED-MATROID-BRIDGE-SELECTION-ROBUSTNESS`

It blocks only replay of the unchanged classifier as a standalone P4-T02 source-to-physical solution without a separately derived selector, stability law, and physical interpretation. Diagnostic use of covectors, circuits, refinements, and wall records remains open.

The three inherited freezes remain active:

- `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR`
- `NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-COMMON-CHARACTER-HOLONOMY-PROTECTION`

All fourteen Distance-to-GR burdens remain unchanged. D7 remains unevaluated, B2 inactive and undisposed, and P4-T03 locked. No ontology adoption, physical cone, conformal class, scale, `g_eff`, coupling, Einstein equation, Gate verdict, benchmark or proof promotion, publication, push, external action, global no-go, future-extension impossibility, or completed derivation follows.

## Next action

Run the single governed checkpoint for `AJ-RT-20260811-001-001`. After that commit, admit exactly one:

`PKT-V22-P4T02-B2-POST-ORIENTED-MATROID-REFUTER-THEORETICAL-CONTINUATION-SELECTION-V1`

under `theoretical-continuation-selector@0.1.0`.

The selector must choose exactly one materially distinct same-milestone source-side selection or admissibility theorem, realization-sensitive bridge, source-side irrelevance theorem, or protected human-gated ontology stop. It must preserve all four freezes and every downstream block. It must not replay the unchanged bridge, reevaluate D7, act on B2, unlock P4-T03, adopt, promote, publish, push, or take external action.

The theoretical selector is selected but not executed in RT018.
