<!-- authority: control -->

# Research Handoff handoff-0745: EqSrc Closure Candidate v3 Ready for Fresh Audit

RT-20260718-020 completed the exact Ontology Formalizer repair required by
handoff-0744. Proposal-only `EqSrcClosureLaw_src^cand v3` now declares:

- endpoint-indexed ledger, proxy, provenance, outcome, and raw-certificate
  fibers;
- typed identity, inverse, and composition maps for every certificate
  component;
- ledger, proxy, provenance, negative-outcome, and certificate congruences;
- total negative-control evaluators on displayed raw-certificate domains; and
- a finite rank-0 through rank-4 dependency order in which all component
  decisions precede and are independent of `Accept_src`.

The candidate adds a statement-level declaration-closure proposition and an
acyclic certificate-admissibility totality proposition. It preserves the v2
accepted identity, inverse, and composition clauses, so the conditional
equivalence proof remains valid in the repaired typed language.

This is
`statement_level_signature_and_noncircularity_repair_supplied`. It repairs
`OBST-EQSRC-CLOSURE-SIGNATURE-001` at proposal statement level, not at
independent-audit, ontology-instantiation, or adoption level.

The no-target/no-process guard now covers every lower-rank definition tree,
transitive provenance, and semantic factorization: holding declared source
leaves fixed while varying target or workflow state must leave all source
interpretations, certificate decisions, `Accept_src` values, and
candidate-law truth values unchanged. A fresh auditor must still test that
condition. “Acceptance” means internal certificate admissibility only, not
audit PASS, approval, adoption, or benchmark acceptance.

The candidate remains `proposal-only`, `draft/control` source-extension data
with `blocked_adoption_open_continuation`. Current ontology does not derive or
instantiate it. No canonical ontology edit, source-law adoption, general
`EqSrc`, RetainH, GenH, `M_src`, `g_eff`, matter coupling, Einstein equations,
finite-variation robustness, Distance-to-GR ledger change, benchmark
promotion, Gate Chair verdict, completed derivation, program-level no-go,
future source-extension impossibility, or any unscoped rejection conclusion follows.

The repeated EqSrc route is not frozen because v3 supplies materially new
typed declarations and theorem-level payload. The next lawful packet is
exactly one fresh bounded `smuggling-auditor@0.2.0` review of the complete v3
signature, dependency graph, semantic factorization, component congruence,
accepted-totality clauses, and proofs. Refuter stress follows only if that
audit passes. Any adoption remains a separate protected human-gated decision.
