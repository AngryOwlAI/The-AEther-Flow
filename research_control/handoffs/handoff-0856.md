<!-- authority: control -->

# handoff-0856 — P4-T05 exact-path recovery checkpoint blocked

Generation 101 completed one bounded Process Integrity Auditor recovery under
`AJ-RT-20260724-006-001`.

Six literal `.gitattributes` rules now classify only the immutable P4-T05
paths named by the checkpoint blocker. All six payload hashes remain exact,
as do the consumed approval, protected completion and handoff, parent recovery
completion and handoff, and blocker receipt. There is no wildcard or
directory-wide exemption.

The task-local proof confirms that the six intended files pass isolated
blank-at-EOF checking. An unrelated control remains unclassified and fails
with the expected blank-at-EOF finding. The inherited protected-route recovery
also remains valid.

The one authorized normal checkpoint passed classification, signal,
documentation, memory, research-control, and staged Git-diff acceptance. Its
repository-wide test shard then ran 1,231 tests and reported three failures
plus one cascading setup error. The exact blocker records a stale P10-T05
event-store source binding, a protected-gate taxonomy enum mismatch, a
support-only route-orbit diagnostic expectation mismatch, and the P10
migration-readiness cascade. The checkpoint restored the entry index, left
nothing staged, and created no commit.

## Required next action

Run one fresh bounded `improve-project-system` recovery using a Validator
Engineer. It may repair only the four recorded test-contract drifts, must
preserve every exact-path rule and protected P4-T05 hash, and may invoke one
future normal checkpoint without legacy validation.

Do not rerun generation 101 or its consumed checkpoint, weaken the repository
test shard, fabricate route history, reuse the approval, rewrite protected
P4-T05 evidence, or execute `P4-T06` first.

## Authority boundary

This recovery changes Git path classification and project-control evidence
only. It does not reuse the consumed approval, re-run the Gate Chair decision,
change a protected P4-T05 payload byte, execute P4-T06, change Distance-to-GR,
or create scientific, ontology-adoption, source-law, physics-promotion,
publication, push, proof, or completed-derivation authority.
