<!-- authority: control -->

# Handoff handoff-0892: P7-T03 source operational-device suite

## Completed scope

`RT-20260728-002` constructs
`SourceOperationalDeviceSuiteCandidate_v1` as one task-local
`draft/control`, `proposal-only` finite source protocol suite.  The protocol
tuple contains finite source preparations, intervention labels, formal token
alphabets, total source-history readouts, success subsets, and fail-closed
classifiers.

Five guarded roles—`Clock_src`, `Rod_src`, `Signal_src`, `Detector_src`, and
`FreeFall_src`—have explicit source objects and execution rules.  The packet
proves exact finite token-response closure, token-mass partition, zero-sum
equal-mass intervention differences, and naturality under transport of the
entire protocol tuple.  It includes exact positive controls, a disjoint
clock–signal product cell `1/16=(1/4)(1/4)`, a signal–detector response
agreement, a valid no-event branch, and distinct malformed,
underdetermined, and inconsistent branches.

## Status boundary

The familiar role names and tokens have formal source meaning only.  A tick
is not proper time; an endpoint or hop count is not physical length; an
arrival token is not causal propagation; a trigger is not empirical
detection; and a baseline/intervention contrast is not gravitational free
fall.

Current ontology does not derive or select the preparations, interventions,
alphabets, readout maps, success subsets, failure classifiers, or
cross-device identifications.  The result is not canonical ontology, an
adopted source law, adopted operational semantics, physical probability,
effective geometry, matter coupling, stress energy, Einstein equations,
benchmark recovery, proof authority, publication, or a completed
derivation.  Adoption remains `blocked_adoption_open_continuation`.

## Next bounded route

First run one governed checkpoint for `AJ-RT-20260728-002-001`.  Only after
it commits may one fresh bounded `candidate-constructor@0.2.0` packet execute
v21 P7-T04 by deriving or precisely obstructing one common formal source
propagation structure across multiple P7-T03 protocols.

That packet must keep every P7 candidate proposal-only, define its common
structure entirely from source protocol data, and include compatibility and
failure controls.  It must not infer physical causality, a common cone,
effective geometry, detector semantics, universal coupling, or GR behavior,
and it must not execute P7-T05.
