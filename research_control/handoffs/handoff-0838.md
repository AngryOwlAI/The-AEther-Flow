<!-- authority: control -->

# Handoff 0838 — P12-T05 classifier repair complete, validation recovery required

The exact P12-T05 checkpoint-classifier recovery is complete. Four named report
outputs are generated derivatives, and the two named metric-reporting scripts
use the existing `validator_code` family. The exact 61-path predecessor
manifest now produces a safe legacy-authority, comparison-required shadow plan.
An unrelated governed path still falls back to `unknown_governed_path` and the
full validation profile.

The repair does not change the eight P12-T05 metric families, their eligible
sets, identities, measured or `not_measured` values, calibration warnings, or
nonclaims. The preserved task-local metric validator passes 44 checks across 14
fixtures. Classifier, validation-plan, and checkpoint-planner coverage passes
105 tests. The required 155-test P12-T05 shard then passed 154 tests and failed
one continuation-context assertion. The ordinary-route guard truthfully emitted
its designed threshold-next `WARN` after two consecutive project-system tasks,
while the integration contract required `PASS`.

No metric, validator, route, dashboard, or checkpoint is theorem truth,
scientific evidence, physics progress, or Distance-to-GR progress. No ontology,
source law, scientific ledger, benchmark, proof, publication, or
completed-derivation authority changed. P4-T05 remains human-gated.

The checkpoint was not invoked, and nothing was staged or committed. One fresh
bounded project-system recovery must reconcile the threshold-next warning with
the continuation validation contract without falsifying route history,
weakening the threshold-three hard guard, or broadly skipping route-control
failures. Only after that recovery passes and one future normal checkpoint
commits may a fresh relay generation execute P12-T06.

The exact blocker is preserved at
`research_control/tasks/RT-20260723-005/artifacts/validation_blocker_ordinary_route_guard_threshold_warning_contract_v1.yaml`.

The authoritative machine-readable handoff is
`research_control/handoffs/handoff-0838.yaml`.
