<!-- authority: control -->

# Handoff handoff-0913 — P9-T01 task-taxonomy slug recovery

Status: `ready_after_checkpoint`.

`handoff-0913` records the bounded generation-172 Process Integrity Auditor
recovery.

## Result

Exactly one task-control scalar changes. The
`task_taxonomy.candidate_family` in `RT-20260729-012` now reads
`source_derived_benchmark_protocol_v1`, satisfying the executable lowercase
slug contract. Reversing that substitution reproduces the exact sealed
preimage.

The repository taxonomy now reports zero required errors, zero historical
source mutations, and zero stronger-science inferences. Eight protected
generation-171 hashes remain exact. The P9-T01 protocol retains 84 passing
checks, all six benchmark cases remain `NOT_RUN`, and no benchmark case was
executed.

## Checkpoint boundary

The exact recovery and ordinary control records are ready for one governed
cumulative checkpoint under `AJ-RT-20260730-005-001`. That checkpoint has not
yet been invoked in this handoff.

P9-T02 remains unexecuted until the checkpoint commits. After that commit, a
fresh bounded Candidate Constructor packet may construct and run the
source-only vacuum/Minkowski case under the existing target-import firewall.

## Scientific boundary

This is project-system taxonomy normalization only. Gate D remains
`NOT_READY`, Gate E remains `NOT_READY_NO_BENCHMARK_CASE_EXECUTED`, and the
Distance-to-GR ledger does not move.

The repair adds no scientific evidence, ontology, source law, effective
geometry, Einstein equation, exact-GR recovery, benchmark result, promotion,
proof, publication, push, or completed-derivation authority.

## Next action

Run the complete precheckpoint validation stack and invoke the one governed
cumulative checkpoint only if every gate passes. Then continue with a fresh
P9-T02 packet; do not treat this recovery or its checkpoint as benchmark
evidence.
