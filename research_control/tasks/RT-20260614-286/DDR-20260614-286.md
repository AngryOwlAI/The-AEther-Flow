---
decision_id: "DDR-20260614-286"
task_id: "RT-20260614-286"
agent_job_id: "AJ-RT-20260614-286-001"
director_version: "director-of-research@0.3.0"
decision_type: "task_overlay"
selected_role_id: "validator-engineer"
selected_role_version: "0.2.0"
status: "completed"
requires_human_gate: false
created_at: "2026-06-28T23:34:09Z"
---

# Director Decision Record: DDR-20260614-286

## Decision

Select `validator-engineer@0.2.0` for one bounded P1-T03
active-state drift validator guard packet.

## Basis

`program_state.yaml` names `RT-20260614-285` and `handoff-0318`, and
`handoff-0318` requires one bounded P1-T03 packet to add a deterministic check
for contradictions between `research_control/current_frontier.md` and live
active-state authority. The v11 plan identifies this as project-system
validator work with no physics milestone.

## Scope

Allowed:

- Add a deterministic guard to `scripts/research_control/validate_research_control.py`.
- Add focused regression tests in `tests/test_research_control.py`.
- Compare `current_frontier.md` against `program_state.yaml`, the latest
  handoff, the active task folder, and `registries/DISTANCE_TO_GR_LEDGER.csv`
  when an active Distance-to-GR burden exists.
- Resynchronize `current_frontier.md` to the post-P1-T03 tracked state so the
  newly active guard validates the transaction.
- Update control receipts, registries, generated memory derivatives, generated
  dependency graph artifacts, and `program_state.yaml`.

Blocked:

- adding `render_current_frontier.py` in this packet;
- changing `registries/DISTANCE_TO_GR_LEDGER.csv`;
- canonical ontology edit;
- source-law adoption;
- `MetricData(E)` adoption;
- `g_eff` adoption or scope expansion;
- coupling-law adoption;
- matter-coupling derivation or adoption;
- stress-energy semantics;
- stress-energy tensor;
- matter action;
- detector semantics;
- Einstein equations;
- benchmark promotion;
- completed derivation;
- Gate Chair verdict or protected evidence-status change.

## Role-Fit Matrix

| Candidate role | Fit judgment | Reason |
| --- | --- | --- |
| `validator-engineer@0.2.0` | selected | The packet implements deterministic validator behavior and focused regression tests. |
| `process-integrity-auditor@0.1.0` | not selected | P1-T03 is no longer just an audit or snapshot synchronization task; it changes validator code. |
| Physics roles | not selected | This packet has no derivational milestone or scientific payload. |

## Result

The packet adds the active-state drift guard, verifies it against synchronized
and stale fixtures, and routes the next bounded step to P1-T04 generated
current-state report tooling. No physics status changed.

## References

The AEther-Flow Research Project. (2026, June 28). *Handoff 0318* [Internal
control handoff].

The AEther-Flow Research Project. (2026, June 28). *Recommendations
implementation plan continue task v11* [Internal implementation plan].
