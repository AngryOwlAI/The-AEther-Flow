# Parent Fusion Notes: EqSrc Cycle–Boundary Selector Law

## Scope

The parent fused the two internal perspectives into proposal-only
`EqSrcCycleBoundarySelectorLaw_src^cand,v1`. The packet answers the exact
handoff-0759 formalization burden. It does not derive or adopt the oriented
mark, edit canonical ontology, establish physical admissibility, or discharge
general EqSrc.

## Mathematical Result

An admitted source record is a finite set \(X\) with a nonempty proper
odd-cardinality oriented mark \(P\). Its mark-preserving automorphisms define
the fixed space \(A_S\), and mark parity defines the kernel \(B_S\). Exact
calculation gives
\[
A_S=\operatorname{span}(\mathbf1_P,\mathbf1_{X\setminus P}),\qquad
B_S=\operatorname{span}(\mathbf1_{X\setminus P}).
\]
Candidate EqSrc is the kernel pair of \(A_S\to A_S/B_S\).

The variation certificate is ordered to avoid using the answer it must test:
the mark first defines \(u_S=\mathbf1_{X\setminus P}\) and the affine action
\(a\mapsto a+\epsilon u_S\). The packet later proves
\(B_S=\mathbb F_2u_S\). Thus the action is fixed before the quotient relation
is evaluated. This establishes formal non-circularity inside the proposal,
not physical admissibility.

## Theorems and Witness

The fused candidate proves marked-bijection naturality, conditional
quotient-label uniqueness for the same kernel pair, compatibility of chain
presentations with the selected pair, and finite robustness for the affine
action. It does not claim uniqueness of the mark, parity selector, or full
chain presentation.

For \(X=\{0,1,2,3\}\), the marks \(\{0\}\) and \(\{1,2,3\}\) give the same
four-state fixed space but distinct boundary subspaces and equivalence
relations. This is fixed-carrier discrimination relative to explicit marked
data. It is also the central adverse result: if physical gauge exchanges or
forgets the two orientations, extra source-side provenance is required.

## Parent Resolutions and Next Route

The translations are affine automorphisms, not linear endomorphisms.
Naturality is limited to mark-preserving bijections, and automorphism
equivariance is weak because the selected state space is fixed pointwise.
An external chain presentation with the wrong boundary image is
`presentation_incompatible`; it does not alter the canonical selector output.

The exact v3 cycle remains locally frozen. This distinct marked-source route
is not frozen because it supplies new definitions, proofs, explicit failure
branches, and a relation-changing witness. The next lawful step is one fresh
bounded Smuggling Auditor audit of this exact candidate, focused on mark
provenance, hidden choice renaming, target or process import, covariance
overread, and finite-witness generalization. The audit may not repair or adopt
the candidate.
