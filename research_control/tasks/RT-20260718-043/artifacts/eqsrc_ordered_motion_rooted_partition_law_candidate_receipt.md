---
authority: project-control
status: draft/control
task_id: RT-20260718-043
candidate: EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1
---

# Ordered-motion rooted partition candidate receipt

## Result

One proposal-only candidate was formalized. It defines an ordered parity
partition only after a finite directed source record has independently
supplied transition provenance, an event-to-record projection, a unique
source-intrinsic root certificate, and pre-relation morphism and variation
families.

The positive control is a certified rooted two-event path. It yields two
nonempty parity blocks and an eight-pair kernel relation on a four-element
indicator state space.

The unrooted directed two-cycle is the symmetry-negative control. Its exchange
automorphism fixes no singleton root. Observer sampling cannot repair this
failure because a sampling-window start is not source-intrinsic provenance.

## Conditional theorem status

- Path parity, partition, and the kernel relation are well-defined on admitted
  records.
- Admitted source isomorphisms preserve the root, both parity blocks, the
  boundary line, and the relation.
- Strictly increasing reparameterization of the ordering parameter preserves
  the candidate when the source transition endpoints are unchanged.
- Independently declared finite variations preserve the candidate only when
  they preserve provenance, root, reachability, and parity.
- Mixed path parity, reachable odd cycles, unreachable tokens, root-moving
  maps, target imports, and process imports fail closed.

## Source-extension classification

`EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1` is a
`proposal-only new ontology primitive candidate`. The canonical foundations
source contains only a schematic ordered-motion map and does not derive the
finite record package. Current adoption is blocked while one fresh audit
remains authorized: `blocked_adoption_open_continuation`.

## Non-conclusions

This receipt does not establish current-ontology derivation, physical
admissibility, covariance, detector meaning, general `EqSrc`, either ledger
delta, downstream GR objects, benchmark promotion, Gate Chair authority,
completed derivation, future source-extension impossibility, or global theory
rejection. Validator PASS, registries, generated derivatives, role identity,
handoff state, checkpoint state, and file order are not physics evidence.

## Next route

Run one fresh `smuggling-auditor@0.2.0`
`ontology-law-research-packet` against the exact candidate. Audit projection
and root provenance, transition compatibility, purpose-built morphism and
variation categories, sampling truncation, hidden target/process imports, and
physical-covariance overread. Do not repair, stress, adopt, or promote the
candidate inside that audit.
