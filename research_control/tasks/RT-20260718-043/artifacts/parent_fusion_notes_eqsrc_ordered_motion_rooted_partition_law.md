---
authority: science-draft
status: proposal-only
task_id: RT-20260718-043
candidate: EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1
---

# Parent fusion: ordered-motion rooted partition law

## Fused result

The parent accepts the two child outputs without an unresolved conflict and
formalizes one exact proposal-only source-extension target:
`EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1`.

The dependency order is strict. A packet begins with a finite event set, a
finite record-token set, a source-side directed transition-certificate family,
an independently declared event-to-record projection, a singleton
source-intrinsic root certificate, and independently declared morphism and
variation families. Only after these data pass the admission predicate may
the packet define path parity, the ordered partition, the indicator state
space, the one-dimensional boundary, and its kernel-pair relation.

## Mathematical payload

On an admitted record, every root-to-token directed path has the same length
modulo two. This defines the even block (P_U) and odd block (Q_U). Every
admitted source isomorphism preserves the root and directed certificates, so
it transports both blocks and the resulting relation. Strictly increasing
reparameterizations of the ordering parameter preserve the transition order
and therefore preserve the candidate. Finite variations are accepted only
when they independently preserve provenance, the root, reachability, and
parity; otherwise they fail closed.

The minimal positive control is a certified two-event path. The exact relation
on the four-element indicator state space has two equivalence classes and
eight ordered related pairs. The unrooted directed two-cycle has a
vertex-exchange automorphism and therefore no automorphism-invariant singleton
root. A sampling-window start is not a source root. Mixed path parity, odd
cycles, unreachable tokens, root-moving maps, target structures, benchmark
success, validators, registries, roles, handoffs, file order, and task process
are all explicit failure branches.

## Authority separation

The canonical foundations source contributes only the schematic
(Phi_\lambda) ordered-motion anchor. It does not derive the finite record,
projection, transition certificates, root certificate, morphism category, or
variation category. The candidate is therefore classified as a
`proposal-only new ontology primitive candidate`, with
`blocked_adoption_open_continuation`.

The formal theorems are conditional mathematical results. They do not
establish physical admissibility, covariance, detector semantics, or empirical
recovery. Validator success, registry entries, generated derivatives, role
identity, handoff state, and checkpoint status remain non-promotional.

## Freeze and next route

The exact `EqSrcOrientationTorsorDescentLaw_src^cand,v1` construction, audit,
and stress cycle remains locally frozen. This packet neither repairs nor
adopts it. The new rooted-partition candidate is materially upstream and must
receive one fresh `smuggling-auditor@0.2.0` review before any stress or
protected decision.

No Distance-to-GR or metric-use ledger row changes. General `EqSrc` and every
downstream GR burden remain blocked.
