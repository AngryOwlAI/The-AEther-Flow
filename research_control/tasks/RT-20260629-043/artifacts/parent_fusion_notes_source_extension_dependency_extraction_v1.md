<!-- authority: control -->

# Parent Fusion Notes: P4-T01 Dependency Extraction

The parent synthesis accepts both child outputs. The physicist-mathematician
child supplies the dependency partition and essential-use structure. The
physicist-philosopher child supplies the authority and overread guard
discipline.

No conflict required revision. The fused output is
`source_extension_dependency_extraction_v1.md`, with the operative
machine-readable table in `source_extension_dependency_table_v1.yaml`.

The fused output preserves the following conclusion: the current source
stack is dependency-mapped only. No compression result is established; no
datum is adopted, rejected, promoted, or marked impossible.
