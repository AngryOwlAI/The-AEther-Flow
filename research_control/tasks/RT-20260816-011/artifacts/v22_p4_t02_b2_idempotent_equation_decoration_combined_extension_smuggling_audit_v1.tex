\documentclass[11pt]{article}
\usepackage[margin=0.82in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs,array,longtable}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{xcolor}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.55em}
\sloppy

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newcommand{\Weq}{W_{\mathrm{eq}}}
\newcommand{\Focc}{\mathcal F_{\mathrm{occ}}}
\newcommand{\Adm}{\mathrm{Adm}_{\mathrm{occ}}}
\newcommand{\Xocc}{\mathcal X_{\mathrm{OCC}}}

\title{V22 P4--T02 B2: Idempotent Equation-Decoration Combined-Extension Smuggling Audit}
\author{RT-20260816-011 / AJ-RT-20260816-011-001}
\date{2026-08-16}

\begin{document}
\maketitle

\begin{center}
\fbox{\parbox{0.94\linewidth}{
\textbf{Decisive audit verdict.} The fixed RT009 construction is
\texttt{source\_pure\_as\_written}, narrowly at disclosed written-syntax
scope. No target geometry, desired P4 answer, empirical datum, benchmark
answer, or workflow status selects its roots. The algebraic carrier is
available from the registered equation bundle, but interpreting idempotents as
occurrence/admissibility data and designating zero as the sole attachment are
proposal-only new-primitive semantics. They are not current-ontology-derived,
epistemically selected, adopted, physical, empirical, or P4-crediting.
}}
\end{center}

\section{Fixed candidate and audit boundary}

The audit holds fixed the registered rank-six reduced equation bundle $\Weq$
and the RT009 construction
\[
 \Focc(O)=\Gamma(O,\operatorname{End}(\Weq)),\qquad
 \Adm(O)=\{P:P^2=P\}.
\]
An independently admitted source/equation-frame arrow $g$ transports a
decoration by pullback and invertible conjugation. The combined carrier
$\Xocc$ is the category of elements of $\Adm$, and the one declared attachment
is $s_0=(d,O,0_{\Weq})$. The finite control is
\[
 d_{\mathrm{rank},O}(P,Q)=\sup_{x\in O}\operatorname{rank}(P_x-Q_x).
\]

The conditional carrier, transport, idempotency, restriction, gluing, null
section, and rank calculations remain correct. The audit asks a different
question: do those equations derive their occurrence, admissibility, and
attachment meanings, or do target, desired-output, empirical, or workflow
inputs secretly choose them?

\section{Source-purity classification}

No target atlas, Lorentzian metric, cone, tetrad, coframe, detector response,
benchmark label, frozen-candidate output, registry row, role identity,
validator result, or checkpoint result occurs in the positive definitions.
The equation-frame action is the full independently admitted action used by
the candidate; it is not restricted to arrows that preserve a desired token.
The written dependency graph is acyclic and pre-outcome.

This establishes local written-syntax purity, not independent source
provenance. Once $\Weq$ is fixed, $\operatorname{End}(\Weq)$, zero, identity,
composition, idempotency, and rank are algebraically available. The new
content is the semantic use of those objects: calling the idempotent locus an
occurrence/admissibility fibre and calling zero the sole attachment. At that
semantic-law level the extension is a \texttt{new\_ontology\_primitive}, held
only as proposal-only source-extension data under
\texttt{blocked\_adoption\_open\_continuation}.
The exact audit verdict is \texttt{source\_pure\_as\_written}: zero and identity
remain equally natural, and the rank-topology control supplies no selector.

\section{Natural sections do not select the null endpoint}

\begin{theorem}[Universal idempotent-section nonselection]
For a bare real rank-six equation fibre, an endomorphism fixed by conjugation
under every element of $\mathrm{GL}(6,\mathbb R)$ is scalar. If it is also
idempotent, it is either $0$ or $I$. Consequently both $s_0$ and $s_I$ are
restriction- and conjugation-natural idempotent sections, and frame naturality
plus idempotency does not select $s_0$ over $s_I$.
\end{theorem}

\begin{proof}
Commutation with every diagonal scaling eliminates all off-diagonal entries.
Commutation with permutation matrices makes all diagonal entries equal, so the
endomorphism is $\lambda I$. Idempotency gives
$\lambda^2=\lambda$, hence $\lambda\in\{0,1\}$. Zero and identity are both
fixed by every conjugation and every restriction.
\end{proof}

The same conclusion needed by the audit is even weaker: regardless of how
large the admitted arrow class is, both zero and identity are universal
natural sections. The exclusive null designation is therefore transparent
proposal input, not a theorem of naturality.

\begin{lemma}[Conjugacy and splitting]
Every idempotent on a real six-dimensional fibre is conjugate to
$\operatorname{diag}(I_r,0)$ for a unique $r\in\{0,\ldots,6\}$, and
\[
 \Weq=\operatorname{im}P\oplus\ker P.
\]
\end{lemma}

\begin{proof}
The minimal polynomial of an idempotent divides $x(x-1)$, which has distinct
roots. Thus the endomorphism is diagonalizable with zero and one eigenspaces;
their dimensions are $6-r$ and $r$. Rank is conjugation invariant.
\end{proof}

This makes the firewall precise. Images, kernels, quotients, and rank strata
are not algebraically absent. They are present but have no admitted occurrence,
sector, response, or P4 meaning. Any such use would inherit the unselected
endpoint or orbit and requires a distinct packet.

\section{Rank isolation is not perturbative robustness}

\begin{theorem}[Rank-topology counterfamily]
Let $P=\operatorname{diag}(1,0,\ldots,0)$,
$E=E_{21}$, and $S_t=I+tE$ for nonzero real $t$. Then
\[
 P_t=S_tPS_t^{-1}=P+tE
\]
is an idempotent conjugate to $P$. As $t\to0$, $P_t\to P$ in every ordinary
finite-dimensional matrix norm, but
\[
 \operatorname{rank}(P_t-P)=1
\]
for every $t\ne0$.
\end{theorem}

\begin{proof}
Because $E^2=0$, $S_t^{-1}=I-tE$. Moreover $EP=E$ and $PE=0$, giving the
displayed formula. Conjugation preserves idempotency. The difference $tE$ has
rank one for nonzero $t$, while its norm tends to zero with $|t|$.
\end{proof}

Thus $d_{\mathrm{rank}}$ gives every distinct decoration a positive integer
distance and makes every radius-less-than-one ball a singleton. Its isolation
is discrete algebraic coding. It is not a topology of source perturbations, a
continuum stability theorem, physical robustness, or empirical robustness.

\section{Semantic relabeling and conditional conservativity}

If the words ``occurrence'' and ``nonoccurrence'' are exchanged while the
carrier, arrows, idempotent locus, null section, and rank control are left
unchanged, every formal equation and executable check is unchanged. The
identity map on the algebraic structure witnesses this semantic-relabeling
invariance. A source-side observable or law that distinguishes the readings is
absent. Therefore the algebra does not establish either realized occurrence or
realized nonoccurrence.

The old-language model-conservativity theorem also remains conditional. If no
old equation, coefficient, constraint, gauge rule, or lawful reduction reads
$P$, then every old model lifts by $P=0$ and forgetting $P$ returns the old
model. If any old rule reads $P$, that argument fails. Even when it holds,
model conservativity establishes neither independent provenance nor semantic
or definitional conservativity of the occurrence designation.

\section{Twelve-dimension audit summary}

\begin{longtable}{p{0.29\linewidth}p{0.64\linewidth}}
\toprule
Dimension & Disposition \\
\midrule
Current source vs. proposal roots & Algebraic sublayer definable; semantic law
is proposal-only new primitive. \\
Target atlas/metric & No written import. \\
Desired P4 output & No written back edge; epistemic justification still absent. \\
Empirical/benchmark input & No import and no empirical meaning. \\
Equation-frame arrows & Full conditional conjugation action preserved; no
output-relative filter. \\
Idempotency & Transparent target-free goal preload, not source-derived
occurrence law. \\
Null attachment & Natural but not uniquely selected; zero and identity both
universal. \\
Same reduct & Chosen-token nonselection only, not blanket nondefinability. \\
Rank distance & Exact discrete algebraic control only. \\
Projector descendants & Algebraically present but semantically and P4
firewalled. \\
Old-language conservativity & Valid only under the explicit inertness premise. \\
Workflow/adoption & No validation, registry, role, or checkpoint authority;
adoption blocked. \\
\bottomrule
\end{longtable}

The executable audit model passes all twenty exact checks with payload SHA-256:
\begin{center}
\texttt{9972d157f24ea7f29ecf047dcb7c8f50406c055a51dffb1eb589cc56361dd0fa}.
\end{center}

\section{Freeze, Distance to GR, and successor}

All nine inherited freeze labels remain active and unopened. Every one of the
fourteen Distance-to-GR burdens remains literal \texttt{no\_delta}. D7 is not
reevaluated, B2 remains inactive, and P4--T03 remains locked. No new freeze is
created by this first focused audit because the universal-endpoint,
semantic-relabeling, rank-topology, conjugacy, conservativity, and firewall
results support one materially distinct stress packet.

% Exact successor identity: PKT-V22-P4T02-B2-IDEMPOTENT-EQUATION-DECORATION-NULL-SEMANTICS-RANK-TOPOLOGY-REFUTER-STRESS-V1
Exactly one successor is selected but not created or executed:
\[
\begin{split}
&\texttt{PKT-V22-P4T02-B2-IDEMPOTENT-EQUATION-DECORATION-}\\
&\texttt{NULL-SEMANTICS-RANK-TOPOLOGY-REFUTER-STRESS-V1}.
\end{split}
\]
It is a \texttt{source\_extension\_refuter\_stress} packet for
\texttt{refuter@0.2.0}. It may stress null-versus-identity selection, semantic
relabeling, inertness, continuous-topology fragility, and attempted projector
descendant use. It may not repair or adopt the candidate.

\section{Authority boundary}

This audit proves no physical occurrence, nonoccurrence, probability,
empirical frequency, causal mechanism, response, cone, conformal geometry,
scale, $g_{\mathrm{eff}}$, coupling, Einstein equation, benchmark, Gate result,
or completed derivation. It supplies no proof or publication authority and
authorizes no push or external action.

\end{document}
