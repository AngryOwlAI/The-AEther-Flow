<!-- authority: scientific_draft_control -->

# Parent fusion: idempotent equation-decoration combined-extension smuggling audit

## Decisive verdict

The fixed RT009 construction is `source_pure_as_written`, narrowly and only at
the level of its disclosed written syntax. No target atlas, target metric,
desired cone, desired P4 answer, empirical datum, benchmark answer,
frozen-candidate output, registry status, role identity, validator result, or
checkpoint result selects its roots.

That verdict is deliberately weaker than independent source provenance. The
endomorphism carrier, zero, identity, composition, idempotency, conjugation,
and rank are an algebraically definable sublayer once the registered rank-six
equation bundle is fixed. Interpreting idempotents as occurrence/admissibility
data and designating zero as the sole attachment are transparent proposal-only
semantic roots. At that semantic level the extension is a
`new_ontology_primitive`, held only as proposal-only source-extension data. It
is not current-ontology-derived, epistemically selected, adopted, physical,
empirical, or P4-crediting.

In particular, zero and identity remain equally natural endpoints; the
rank-topology control does not turn either endpoint into a selected occurrence
law.

## Fixed conditional mathematics preserved

The audit preserves the correct RT009 construction:

1. Smooth endomorphism sections form the declared carrier.
2. Invertible equation-frame conjugation preserves products and idempotency on
   the full independently admitted arrow class.
3. Pointwise idempotency is stable under restriction and compatible gluing.
4. Zero is a deterministic restriction- and conjugation-natural section.
5. The rank-difference control is bounded, conjugation invariant, restriction
   nonexpansive, and integer-valued.
6. Old-language model conservativity follows under the explicit premise that
   no old equation, coefficient, constraint, gauge rule, or lawful reduction
   reads the decoration.

No output-preserving arrow subclass is used. These statements are conditional
mathematics, not adoption or occurrence semantics.

## New mathematical payload

### Zero and identity are both universal natural idempotents

On a bare real rank-six fibre, an endomorphism invariant under all frame
conjugations is scalar. Diagonal scalings eliminate off-diagonal entries and
permutations equate the diagonal entries. Idempotency then leaves exactly zero
and identity. Both are also natural under restriction. Natural covariance and
idempotency therefore do not select the null section over the identity
section.

This sharpens the RT009 same-reduct control. It proves chosen-decoration
nonselection, not blanket nondefinability of `End(W_eq)`, zero, identity,
idempotency, rank, or the carrier construction.

### Idempotent orbits and the projector firewall

Every real rank-six idempotent is conjugate to `diag(I_r,0)` for one rank
`r=0,...,6`, and splits the fibre as `im(P) direct-sum ker(P)`. Those
descendants are algebraically present; the firewall is therefore semantic and
operational, not a claim of algebraic absence. The two universal endpoints
exchange the extreme descendants: `im(0)=0`, `ker(0)=W_eq`, `im(I)=W_eq`, and
`ker(I)=0`. Any sector, quotient, response, or P4 meaning built from them would
inherit the unselected endpoint and requires a separate packet.

### Rank isolation is not perturbative robustness

For `P=diag(1,0,...,0)`, `E=E_21`, and `S_t=I+tE`, exact conjugation gives
`P_t=S_t P S_t^{-1}=P+tE`. For every nonzero `t`, `P_t` is idempotent and
`rank(P_t-P)=1`, while `P_t` tends to `P` in every ordinary finite-dimensional
matrix norm as `t` tends to zero. Thus the rank control makes every distinct
decoration at least unit-distant even along an arbitrarily small continuous
family. It is discrete algebraic coding, not source-perturbation, physical, or
empirical robustness.

The independent philosophical child supplies a complementary rotation family
of rank-one orthogonal projectors whose ordinary norm distance tends to zero
while the difference has rank two away from the endpoint. The two controls are
consistent and reinforce the same scope boundary.

### Semantic relabeling and inertness

Swapping the external words “occurrence” and “nonoccurrence” while retaining
the carrier, arrows, idempotent locus, zero section, and rank control changes
no equation or exact check. No current source observable distinguishes the two
readings. Similarly, the old-language conservativity proof is valid precisely
when the decoration is inert; any old rule that reads the decoration is its
exact falsifier.

## Parent-child synthesis

The Physicist-Mathematician and Physicist-Philosopher children independently
select the same exclusive verdict, the same two-level source-extension
classification, and the same exact successor. Both preserve the full-arrow
conditional covariance, all nine freezes, all fourteen no-delta rows, and all
authority blocks.

The mathematical child emphasizes conjugacy-orbit classification, the
`im(P) direct-sum ker(P)` splitting, and the precise distinction between an
algebraic sublayer and semantic occurrence law. The philosophical child adds
semantic-relabeling nonidentifiability, the rotation-projector rank-topology
counterfamily, and the distinction between frame covariance and adopted
`EqSrc`. These contributions are compatible. There is no blocking or
unresolved conflict.

## Audit interpretation

`source_pure_as_written` means target-free, desired-output-free,
empirical-free, and workflow-authority-free under disclosed proposal roots. It
does not mean source-derived, definitionally conservative at the semantic-law
level, epistemically selected, physically meaningful, empirically supported,
adopted, robust under source perturbations, or relevant to P4-T02 acceptance.

The null attachment is neither occurrence nor nonoccurrence. Idempotency is a
transparent target-free goal preload, not a derived occurrence law. Rank
isolation is not a physical norm. Images, kernels, quotients, projectors, rank
strata, sectors, responses, and P4 uses remain firewalled.

## Freeze and Distance-to-GR status

All nine inherited freeze labels remain active and unopened. No new freeze is
created by this first focused audit. Every one of the fourteen Distance-to-GR
burdens remains literal `no_delta`. D7 is not reevaluated, B2 remains inactive,
and P4-T03 remains locked.

## Successor selected, not executed

Exactly one later packet is selected but not created or executed:

`PKT-V22-P4T02-B2-IDEMPOTENT-EQUATION-DECORATION-NULL-SEMANTICS-RANK-TOPOLOGY-REFUTER-STRESS-V1`

It is a `source_extension_refuter_stress` packet for `refuter@0.2.0`. Its
bounded purpose is to stress null-versus-identity natural selection, semantic
relabeling, inert-expansion dependence, continuous-topology fragility, and any
attempted projector or quotient descendant use without repairing, adopting,
or promoting the candidate.

## Authority boundary

This audit proves no physical occurrence, nonoccurrence, probability,
empirical frequency, causal mechanism, response, cone, conformal geometry,
scale, `g_eff`, coupling, Einstein equation, benchmark, Gate result, or
completed derivation. It provides no proof or publication authority, and it
authorizes no push or external action.
