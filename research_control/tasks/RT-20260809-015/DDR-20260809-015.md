---
authority: control
decision_id: "DDR-20260809-015"
task_id: "RT-20260809-015"
created_at: "2026-08-09T16:05:03Z"
activated_at: "2026-08-09T16:05:03Z"
director_version: "director-of-research@0.3.0"
decision_type: "task_overlay"
selected_role_id: "candidate-constructor"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260809-015-001"
status: "completed"
validation_status: "PASS_PRECHECKPOINT_STAGING_REQUIRED"
supersedes_decision_id: "DDR-20260809-014"
requires_human_gate: false
notes: "Handoff-0984 selects one bounded P3-T03 controlled-refinement construction with Refuter and Validator Engineer perspectives. The packet may construct proposal-only numerical comparison data but cannot infer a physical cone, signature, target geometry, or effective metric."
role_fit_candidates:
  - "candidate-constructor@0.2.0|selected|The role must construct one explicit source-side refinement family and return a decisive no-fog result."
  - "refuter@0.2.0|supporting_internal_perspective|The plan requires adversarial tests of convergence, CFL, boundary, mesh, finite-size, target-import, and property-preservation overreads within the same outer job."
  - "validator-engineer@0.2.0|validation_internal_perspective|An independent task-local implementation and deterministic evidence audit are required; no global validator semantics are changed."
  - "smuggling-auditor@0.2.0|not_selected|The physicist-philosopher child performs the bounded source-purity audit; independent audit remains later if routed."
  - "gate-chair@0.1.0|not_selected|No protected adoption or Gate verdict is requested."
---

# DDR-20260809-015 — V22 P3-T03 controlled source refinement

## Current objective

Execute only P3-T03 after the committed RT-014 recovery checkpoint. Construct
and stress one finite-to-continuum comparison family for the proposal-only
P3-T02 six-channel transport law. Preserve the distinction between a
source-chart numerical convergence result and any physical geometry claim.

## Authority surfaces read

The Director read SOUL, root and research-control guidance, the named
continue-research skill, Director, Candidate Constructor, Refuter, and
Validator Engineer contracts, the validation-output and obligation policies,
the physics-payload admission policy, the burden map, handoff-0984, the
registered V22 plan and backlog row, the P3-T02 registered manuscript and
machine specification, the Distance-to-GR ledger, registries, and live memory
status. Retrieval results were navigation only; registered sources and tracked
control records govern.

## Role-Fit Matrix

| Role | Fit | Decision |
| --- | --- | --- |
| Candidate Constructor `0.2.0` | Constructs the exact finite source objects, comparison maps, analytic bounds, and decisive no-fog result required by P3-T03. | Selected execution role. |
| Refuter `0.2.0` | Stresses CFL, mesh, boundary, target-import, zero-remainder, property-preservation, and authority overreads inside the same bounded job. | Selected supporting perspective. |
| Validator Engineer `0.2.0` | Supplies a separate task-local implementation and deterministic evidence audit without changing global validator semantics. | Selected validation perspective. |
| Smuggling Auditor `0.2.0` | Independent adoption-facing audit belongs to the later P3-T04 route; the present source-purity child is bounded to P3-T03. | Not selected as the execution role. |
| Gate Chair `0.1.0` | No protected adoption, physical-geometry, or Gate verdict is requested. | Not selected. |

## Routing decision

Use an `ordinary-research-packet` Candidate Constructor overlay with an
internal Refuter child and a task-local Validator Engineer perspective. This
is an ordinary gap under the existing proposal-only B1 source model: it
constructs comparison maps, a stable finite scheme, and error evidence. It is
not an ontology-law packet because no source law is adopted, and it is not a
Gate review because no physical cone, signature, scale, or metric is claimed.

## Constructive design

On the periodic source-chart fixture, define `K_n` as directed product cell
complexes with maximum source-coordinate spacing `h_n`. Sampling `I_n` is
point evaluation and reconstruction `R_n` is the declared cellwise-constant
source-chart map. The finite evolution is a monotone explicit upwind map for
the six fixed P3-T02 source velocities under a source CFL condition. Uniform,
smoothly graded, and source-indicator-adapted meshes share the same fixed
continuum transport semigroup. A separate inflow slab supplies boundary and
finite-size controls.

The analytic burden is an `l_infinity` contraction plus a first-order global
error bound for `C^2` data and a frozen-principal-factor consistency bound.
Nonzero truncation errors, multi-resolution decrease, mesh sensitivity, and an
independent one-dimensional implementation must all be recorded. Coordinate
spacings and reconstruction are computational source-chart data fixed before
any target comparison; they are not physical lengths or target interpolation.

## Claim boundary and next route

The packet may establish controlled convergence to the declared source-side
transport semigroup. It may preserve constants, positivity margins, source
evolution orientation, and fixed low-frequency principal factors. P3-T02 did
not construct a quadratic physical cone or Lorentzian signature, so the
required preservation test must return the precise status
`upstream_object_undefined`; it cannot report physical preservation. If the
source refinement passes, route exactly one P3-T04 background and
pre-principal-readiness packet after checkpoint. No process PASS changes the
Distance-to-GR ledger.
