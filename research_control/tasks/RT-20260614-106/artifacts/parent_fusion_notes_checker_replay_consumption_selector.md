# Parent Fusion Notes: Checker Replay Consumption Selector

The physicist-mathematician child treated the `pass_candidate_local` replay as
finite/local consistency evidence with explicit limitations. The
physicist-philosopher child emphasized that the replay is process-mediated
draft/control data and cannot serve as proof or adoption authority.

Consensus:

- select one next packet: `source_extension_smuggling_audit`;
- target the finite source-cover checker replay and finite/local witness
  linkage, not `g_eff` or adoption;
- keep the branch unfrozen because a new executable replay payload exists and a
  non-promotional audit remains available;
- request no human gate because no exact protected decision is ready.

Unique contribution preserved from the mathematical side: the audit should
inspect checker schema predicates, bottom demonstrations, witness linkage, and
the meaning of `pass_candidate_local`.

Unique contribution preserved from the philosophical side: the audit should
explicitly reject validator/checker PASS, registry rows, handoffs, and role
identity as physics authority.

No unresolved blocking conflict remains.
