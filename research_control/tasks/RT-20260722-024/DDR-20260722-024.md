---
authority: "control"
decision_id: "DDR-20260722-024"
task_id: "RT-20260722-024"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "validator-engineer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260722-024-001"
status: "blocked"
requires_human_gate: false
created_at: "2026-07-23T04:52:51Z"
activated_at: "2026-07-23T04:52:51Z"
completed_at: "2026-07-23T05:44:47Z"
validation_status: "FAIL"
supersedes_decision_id: "DDR-20260722-023"
role_fit_candidates:
  - "validator-engineer@0.2.0|selected|The blocker is one deterministic path-family omission plus focused fail-closed regression coverage."
  - "project-control-maintainer@0.2.0|not_selected|No skill role schema permission or authority contract changes."
  - "process-integrity-auditor@0.1.0|not_selected|The prior byte-preservation policy is complete and the remaining defect is validator code."
  - "documentation-curator@2.0.0|not_selected|The canonical taxonomy already defines ci_orchestration and unknown-path fail-closed behavior."
---

# DDR-20260722-024 — Admit only root `.gitattributes`

## Current Objective

Classify the literal repository-root `.gitattributes` path as the existing
`ci_orchestration` family, add positive classifier and planner regressions,
retain an unrelated unknown-path negative control, and retry the preserved
P15-T01 transaction through one normal governed checkpoint.

## Authority Surfaces Read

The repository control instructions, `improve-project-system`, Validator
Engineer role, goal relay route, `handoff-0832`, its exact blocker, the current
classifier and planner tests, the registered validation taxonomy and change
matrix, project-improvement signal registries, live program state, and the
predecessor task/job/completion were read. Generation 65 was claimed and
consumed exactly once before this worker step.

## Role-Fit Matrix

| Role | Fit | Reason |
| --- | --- | --- |
| `validator-engineer@0.2.0` | selected | Owns deterministic validator behavior and focused regressions. |
| `project-control-maintainer@0.2.0` | not selected | No control contract or authority semantics change. |
| `process-integrity-auditor@0.1.0` | not selected | The four exact Git rules and byte-preservation proof are already complete. |
| `documentation-curator@2.0.0` | not selected | Existing canonical documentation already states the required families and fail-safe. |

## Selected Role

Use one task overlay of `validator-engineer@0.2.0`. Its source change is
limited to the classifier and two focused test modules. Existing P15-T01
artifacts, `.gitattributes`, scientific ledgers, and protected authority
surfaces are staging inputs only and may not be changed.

## Claim Boundary

The recovery may recognize only the exact string `.gitattributes`. It may not
use a suffix, prefix, dotfile, or Git-policy wildcard. An unrelated governed
path must remain `unknown_governed_path` and force the full fail-safe. Passing
validation or checkpointing this transaction is project-system evidence only:
it does not execute P15-T03, alter scientific status, authorize publication,
or promote any ontology, benchmark, proof, or derivation claim.

## Validation

Require syntax checks; exact classifier and planner regressions; explicit
positive and negative classifications; the prior exact-path policy and
P15-T01 package checks; the exact 171-test shard; affected-plan explanation;
project-change, signal, documentation, claim-language, research-control,
memory, and Git gates; and one normal governed checkpoint with no legacy
fallback.

The classifier repair, focused tests, exact-path policy, protected P15-T01
hashes, task-local package, and pre-checkpoint shard all passed. The one normal
checkpoint then reached the full 1,172-test repository shard and stopped
safely with two failures and one cascading setup error. The direct causes are
invalid v21 task-taxonomy values on `RT-20260722-022` through
`RT-20260722-024` and a stale sealed P10-T05 source hash for
`scripts/research_control/report_physics_progress_metrics.py`. Nothing was
staged or committed, the index was restored, and no legacy-validation flag was
used.

Those two repairs are outside this exact classifier overlay. They are
preserved in
`artifacts/validation_blocker_checkpoint_repository_shard_taxonomy_and_event_store_v1.yaml`
for one fresh governed project-system recovery. P15-T03 remains unexecuted.
