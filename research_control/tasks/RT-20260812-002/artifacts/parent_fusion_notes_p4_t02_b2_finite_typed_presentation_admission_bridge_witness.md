<!-- authority: science_draft -->

# Parent fusion: finite typed presentation-admission bridge witness

## Decisive result

RT-20260812-002 returns exactly one Candidate Constructor result:
`precise_obstruction`, with scientific subtype
`source_provenance_obstruction` and canonical obstruction identity
`OB-V22-P4T02-B2-FINITE-TYPED-PRESENTATION-ADMISSION-BRIDGE-SOURCE-PROVENANCE-001`.

The finite mathematical shell succeeds, but its source-provenance gate does
not. The minimum presentation-object, arrow, category, Boolean-admission, and
transport interface is well typed as proposal-only extension syntax. The
two-object pair groupoid satisfies its category and inverse laws. A disjoint
finite product expansion is conservative for every old-language sentence.
The two automorphisms, four Boolean predicates, and every identity and
nonidentity naturality square are exhausted.

Those successes do not yield a uniquely admitted presentation. Under trivial
Boolean transport, the two nonidentity arrows force both admission values to
agree, leaving exactly the constant-false and constant-true natural
predicates. The swap fixes no object, so no automorphism-equivariant root
exists. The current canonical source package supplies no independently
provenanced root, orientation, order, semantic label, admission bit,
source-to-presentation map, nonidentity transport asymmetry, or lawful arrow
restriction. `GATE-04` is therefore the sole decisive failed gate, and the
positive `ConstructionCert` branch is prohibited.

## Exact finite mathematics

Let the presentation-object carrier be (P=\{0,1\}) and let the arrow carrier
be (P\times P), with the unique arrow (a_{ij}:i\to j). Identities are
(a_{ii}), composition is (a_{jk}\circ a_{ij}=a_{ik}), and inversion is
(a_{ij}^{-1}=a_{ji}). These definitions discharge the endpoint, identity,
associativity, and inverse laws for all finite cases.

Every automorphism is induced by a permutation of (P), so the automorphism
group is exactly (S_2). With identity transport on the two-element Boolean
carrier, naturality along either nonidentity arrow says (q(0)=q(1)). Of the
four predicates (00,01,10,11), exactly (00) and (11) satisfy all
naturality squares; neither admits one object uniquely.

There is also no (S_2)-equivariant map from the trivial singleton to the
regular two-object torsor. Such a map would choose a point fixed by the swap,
but the swap fixes neither object. This is a theorem about the exact finite
control, not a global theorem forbidding every richer source extension.

For every model (M\models T_{\rm cur}), adjoining the same independent
finite pair-groupoid diagram and either constant natural predicate gives an
expansion whose old-language reduct is exactly (M). Hence the extension and
the current theory have the same consequences in the old language. This is
logical conservativity, not definability, source derivation, epistemic
selection, or adoption.

## Gate audit

The six mandatory gates resolve as follows:

1. The minimum typed interface and finite category laws pass.
2. Theory-scope old-language conservativity passes.
3. Complete automorphism, predicate, and naturality classification passes.
4. Independent noncircular source provenance for every asymmetric datum
   fails decisively.
5. The target, empirical, benchmark, workflow, and frozen-route import audit
   passes.
6. Proposal-only status, blocked adoption, and semantic-layer separation
   pass.

The rooted fixture remains conditional proof data. Moreover, a root token by
itself does not alter naturality along the full pair-groupoid arrows. A later
positive branch would need independently sourced provenance both for the root
or equivalent asymmetry and for whatever arrow typing, transport law, or
lawful restriction makes unique admission covariant. Stipulating either item
from the desired result would reproduce the exact obstruction.

## Parent-child synthesis

The Physicist-Mathematician and Physicist-Philosopher independently reached
the same result type, subtype, obstruction identity, failed gate, and
successor. No resolution round was needed and no blocking conflict remains.

The fusion preserves the mathematical child's complete finite category proof,
theory-scope conservativity theorem, exhaustive (S_2) and naturality census,
and transport-asymmetry precision. It preserves the philosophical child's
separation of current ontology, proposal syntax, mathematical consistency,
source provenance, physical meaning, empirical meaning, adoption, and
workflow authority, together with the explicit warning that transparent
stipulation is not epistemic justification.

## Scope and falsifier

The obstruction is current-source and candidate local. It says that the
current canonical package does not derive the asymmetric presentation data
needed by this finite bridge. It does not reject the project globally, prove
future conservative extensions impossible, or authorize ontology adoption.

The smallest falsifier is a tracked current-source derivation fixed before
outcome inspection that types the presentation interface and uniquely derives
an automorphism-covariant asymmetry or allowed-arrow/transport rule without
using the desired admission, target geometry, empirical labels, benchmark
answers, workflow status, or a frozen route.

## Freeze and Distance-to-GR status

All six inherited local freezes remain active:

- `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR`
- `NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-COMMON-CHARACTER-HOLONOMY-PROTECTION`
- `NDCL-V22-P4T02-B2-ORIENTED-MATROID-BRIDGE-SELECTION-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-SIGNED-CUBIC-VIABILITY-SELECTOR-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-SOURCE-LAW-SPACE-ROBUST-INVARIANCE-PROTECTION-ROBUSTNESS`

All fourteen Distance-to-GR rows remain `no_delta`. The task adopts no source
primitive or `EqSrc`, constructs no retention, generation, localization,
response, source spacetime, cone, conformal class, scale, `g_eff`, matter
coupling, Einstein equation, robustness theorem, benchmark evidence, or
protected verdict.

## Separately admitted continuation

The next exact packet is
`PKT-V22-P4T02-B2-POST-FINITE-TYPED-PRESENTATION-ADMISSION-BRIDGE-PROVENANCE-OBSTRUCTION-THEORETICAL-CONTINUATION-SELECTION-V1`,
of type `theoretical_continuation_selector`, for
`theoretical-continuation-selector@0.1.0`. It is selected but not executed.
That later packet must choose a materially distinct source-side continuation
or a protected human-gated stop without replaying this finite bridge or any of
the six frozen routes.

## Exact authority boundary

No canonical ontology or source law is edited, proved, adopted, rejected, or
promoted. Finite admission is not physical response, causality, time
orientation, a cone, conformal geometry, or `g_eff`. D7 is not reevaluated,
B2 remains inactive, and P4-T03 remains locked. No global no-go,
future-extension impossibility, Gate Chair verdict, benchmark promotion,
proof authority, publication, push, external action, or completed derivation
follows.
