---
authority: control
decision_id: "DDR-20260715-003"
task_id: "RT-20260715-003"
created_at: "2026-07-15T14:04:30Z"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "validator-engineer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260715-003-001"
status: "completed"
completed_at: "2026-07-15T14:13:52Z"
supersedes_decision_id: ""
requires_human_gate: false
validation_status: "PASS"
---

# DDR-20260715-003: Consolidate runner and checkpoint research-control supersets

## Decision

Execute one bounded Validator Engineer AgentJob for v19 `P2-T02`. Remove only
the plain research-control invocation immediately preceding its same-scope
`--check-diff` superset in the local runner and checkpoint post-sync working
scope. Retain the final staged-only diff gate and final staged memory gate.

The plan names `tests/test_validation_orchestration.py` as its test write
surface, while its required existing runner-plan test in
`tests/test_run_full_research_control_validation.py` hard-codes the legacy
two-command plan. This decision authorizes the minimal expectation update in
that existing test so the bounded behavior change does not leave a known test
regression. No other path-scope expansion is authorized.

Do not change index restoration, staged residue detection, allowlist
selection, claim-language validation, memory synchronization, CI, skills,
roles, planner behavior, caching, compact output, or physics authority.

## Basis

P2-T01 is checkpointed at
`f98bec9155a2c0a156dbaa2eba1514634ce3c1c7` and explicitly unlocks P2-T02.
The P1 equivalence corpus records zero blocking mismatches between the legacy
core-plus-diff sequence and the consolidated diff candidate. The v19 plan
assigns P2-T02 to `validator-engineer@0.2.0` in the
`legacy_consolidated` epoch.

## Role-Fit Matrix

| Role | Decision | Reason |
| --- | --- | --- |
| `validator-engineer@0.2.0` | Selected | P2-T02 changes deterministic validation orchestration and focused regression evidence. |
| `project-system-director@0.2.0` | Routing authority | It bounds the packet and retains the working-versus-staged and rollback gates. |
| Physics roles | Rejected | No derivation, ontology, proof, benchmark, or scientific promotion occurs. |

## Authority boundary

The existing `--check-diff` implementation satisfies both
`research_control_core` and `research_control_diff` only for an unchanged
Python, base reference, configuration, implementation family, path set, and
tree scope. Working-scope evidence never satisfies the independent staged-only
gate. Unknown or different identities remain non-deduplicable.

`handoff-0740` and `EqSrc_family_closure_repair_or_stress` remain the ordinary
research authority. P2-T03 and later tasks require their own bounded packets.

## Outcome

The local runner and checkpoint post-sync working scope each now execute one
research-control `--check-diff` superset. The independent final staged-only
diff and memory gates remain present. Twenty focused orchestration,
equivalence, ordering, rollback, residue, and synthetic no-commit tests pass.
