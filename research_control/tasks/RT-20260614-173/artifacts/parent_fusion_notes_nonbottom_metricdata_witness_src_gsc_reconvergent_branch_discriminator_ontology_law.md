# Parent Fusion Notes: Reconvergent Branch Discriminator Ontology-Law Packet

## Consensus

Both child units agree that the missing object should be expressed as a
proposal-only source-side discriminator candidate. The candidate is named
`BranchDisc_src^{GSC}` and is defined only on marked source packages where the
branch-origin data are part of the source-side presentation.

## Unique Contributions

The Physicist-Mathematician child supplied the formal object, domain, map,
covariance rule, bottom cases, and proof obligations.

The Physicist-Philosopher child supplied the status separation: proposal-only
source-extension data is not ontology adoption, not `MetricData(E)` adoption,
not `g_eff`, and not downstream GR recovery.

## Fusion Decision

The fused artifact defines `BranchDisc_src^{GSC}` as a candidate law that
breaks the diamond automorphism only by extending the source presentation. The
unmarked symmetric diamond remains bottom for this route. This preserves
`blocked_adoption_open_continuation`.

## Next Route

The next bounded packet is a Smuggling Auditor audit of the candidate law for
hidden target import, process-authority laundering, and premature promotion.
