---
authority: control
decision_id: "DDR-20260718-014"
task_id: "RT-20260718-014"
created_at: "2026-07-19T03:27:25Z"
activated_at: "2026-07-19T03:27:25Z"
director_version: "director-of-research@0.3.0"
decision_type: "task_overlay"
selected_role_id: "project-control-maintainer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260718-014-001"
status: "completed"
completed_at: "2026-07-19T03:35:50Z"
validation_status: "PASS"
supersedes_decision_id: ""
requires_human_gate: false
role_fit_candidates:
  - "project-control-maintainer@0.2.0|selected|The direct human policy decision requires synchronized relay skill schema helper and test changes without scientific authority."
  - "validator-engineer@0.2.0|support_only|Focused state-machine and static policy checks support the implementation without a separate authority-bearing job."
  - "director-of-research@0.3.0|director_only|The Director preserves ordinary research and protected scientific authority while the bounded project-control overlay executes."
---

# DDR-20260718-014: Permit main for new v4 production relays

## Objective and evidence

Implement the direct user policy decision to remove the production
`main`-branch prohibition. The live repository is a clean `main` checkout at
`fe9f51d1fdd2c670f8ea343f6be4b370ab3c70fe`, and the focused pre-change suite
passes 63 tests.

The executable prohibition is distributed across the launcher, recursive
worker, schema reference, state helper, and focused tests. Deleting only one
sentence would leave contradictory and still-enforced behavior.

## Decision

Authorize `main` for newly initialized `continue-research-goal.v4` records only
when `repository_binding.execution_profile` is `production_profile`. Retain
the existing `codex/*` production option. Keep `acceptance_test` on its exact
disposable branch and preserve the historical rule that retained v1-v3 records
bound to `main` fail validation.

Branch authorization does not weaken the clean validated checkpoint,
saved-project identity, local-mode, repository/root binding, lease, reasoning,
scope, validation, finite-guard, or one-successor contracts. It does not
authorize push, merge, rebase, publication, external mutation, or a live relay
during this implementation.

## Scope alarm resolution

The work spans five runtime/test surfaces because those surfaces jointly form
the existing executable contract. Updating fewer would produce a false or
internally inconsistent policy. The boundary remains those five files, one
task-local policy/validator/receipt packet, standard control records and
registries, and generated memory derivatives.

## Role-Fit Matrix

| Candidate | Disposition | Evidence |
|---|---|---|
| `project-control-maintainer@0.2.0` | selected | The direct human policy decision requires one synchronized project-control runtime change. |
| `validator-engineer@0.2.0` | support only | Focused and task-local validation can verify the branch matrix without another authority-bearing job. |
| `director-of-research@0.3.0` | director only | The Director preserves ordinary research and all protected scientific authority. |

## Preserved authority

No `program_state`, ordinary handoff, completed RT-20260718-013 artifact,
physics source, ontology, scientific claim, Distance-to-GR row, Gate Chair
authority, publication surface, external system, or live relay changes.

## Outcome

The launcher, recursive worker, schema, helper, and focused tests now implement
one consistent policy: new v4 production records may bind `main`, while
acceptance records and retained v1-v3 records may not. Task-local validation
passes and confirms the protected research-control hashes are unchanged.
