---
authority: science-draft
status: draft/control
task_id: RT-20260718-038
agent_job_id: AJ-RT-20260718-038-001
---

# Parent fusion notes: EqSrc orientation-torsor route selector

Both required perspectives select one distinct constructive and
non-promotional next packet: an `ontology-law-research-packet` executed by
`ontology-formalizer@0.2.0`.

The selected proposal-only target is
`EqSrcOrientationTorsorDescentLaw_src^cand,v1`. On a source record carrying an
independently sourced unordered bipartition, it must retain the two
complementary orientations as an unpointed \(C_2\)-torsor. Each orientation
has its own boundary line and kernel-pair relation. Complement transport
exchanges the fibres, and the packet must define an equivariant relation
bundle or associated state object without selecting a preferred fibre.

The minimal two-point record supplies the decisive control. Its two boundary
lines are \(\langle p\rangle\) and \(\langle q\rangle\), so the corresponding
relations on the common \(\mathbb F_2^2\) state space are unequal. Strict
scalar descent of exact candidate v1 therefore remains blocked. The
complement involution transports the two relations exactly, however, so a
bundle-valued descent theorem is a materially different formal target.

The formalizer must prove ordering independence, the \(C_2\) cocycle,
equivariance, functoriality, nondegeneracy, pullback to either trivialized
fibre, and strict scalar non-descent on the two-point witness. It must also
classify possible scalarizations: the complement-invariant diagonal line is a
different scalar-law candidate, not descent of either frozen fibre.

This route is unpointed. A distinguished torsor element, global section,
preferred boundary line, or variation class generated from the selected line
would reproduce the missing mark and count as disguised repair of exact
candidate v1. If current source data do not derive the unordered bipartition,
the packet must expose the full three-line `GL(2,2)` orbit or fail closed; it
may not assume that the symmetry has already reduced to \(C_2\). Parity and
domain stability must likewise be explicit.

The relation bundle is mathematical source-extension data. It does not
establish physical gauge equivalence, covariance, admissible variations,
detector semantics, or an orientation-independent physical observable. A
later source-purity audit and stress are required if the formalizer produces
a nontrivial candidate.

The exact `EqSrcCycleBoundarySelectorLaw_src^cand,v1` construction, audit, and
stress cycle remains locally frozen. The earlier exact-v3 route remains
locally frozen as well. These freezes are local; they are not a general
`EqSrc` no-go, future source-extension impossibility, or global theory
rejection.

No Distance-to-GR or metric-use ledger row changes. No source law, canonical
ontology, physical admissibility, `RetainH`, `GenH`, `M_src`, `g_eff`, matter
coupling, Einstein equations, benchmark status, Gate Chair verdict, or
completed derivation is promoted.
