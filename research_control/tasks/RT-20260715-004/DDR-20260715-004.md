---
authority: control
decision_id: "DDR-20260715-004"
task_id: "RT-20260715-004"
created_at: "2026-07-15T14:44:40Z"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "validator-engineer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260715-004-001"
status: "completed"
completed_at: "2026-07-15T15:03:10Z"
supersedes_decision_id: ""
requires_human_gate: false
validation_status: "PASS"
---

# DDR-20260715-004: Execute v19 P2-T03 changed-claim supersedence consolidation

## Status

Completed with PASS.

## Decision

Authorize exactly one `validator-engineer@0.2.0` AgentJob for v19 `P2-T03`.
The job may remove a standalone changed-claim execution only when the adjacent
integrated `research_control_diff` execution uses the same implementation,
environment, tree state, base ref, staged flag, selected path set, taxonomy,
reviewed-context policy, severity mapping, and claim configuration.

The local full runner has one working-tree pair. The checkpoint transaction has
one post-sync working-tree pair and one final staged-tree pair. Each pair may be
consolidated independently. A working-tree result may not satisfy the staged
obligation. The orchestration receipts must normalize integrated output so
claim-language finding counts and IDs remain visible.

The standalone validator remains available as an editor-facing command and for
profiles that do not execute `research_control_diff`.

## Role-Fit Matrix

| Role | Decision | Reason |
| --- | --- | --- |
| `validator-engineer@0.2.0` | Selected | P2-T03 changes deterministic claim-language validation orchestration and focused equivalence evidence. |
| `project-system-director@0.2.0` | Routing authority | It bounds the packet and retains same-scope identity plus working-versus-staged gates. |
| Physics roles | Rejected | No derivation, ontology, proof, benchmark, or scientific promotion occurs. |

## Evidence

- `P2-T02` completed with PASS at commit
  `f76c1ddc9dfd72e8f58466ed0299f1a7079ff157`.
- The worktree was clean and local `main` was two commits ahead of
  `origin/main` before this task was opened.
- The v19 plan identifies `P2-T03` as dependency-ready after `P2-T02` and
  authorizes one Validator Engineer AgentJob without a human gate.
- The P1 equivalence corpus passed all public, control, historical, safe,
  configuration-error, and diff-boundary cases without a missing hard finding
  or warning classification mismatch.
- Baseline claim-language, equivalence, runner, and orchestration validation
  passed 54 tests before implementation.

## Write-boundary decision

The plan names the two orchestration scripts and
`tests/test_validation_equivalence.py`. The existing
`tests/test_run_full_research_control_validation.py` assertion directly encodes
the pre-P2-T03 standalone runner invocation, while
`tests/test_research_control.py` directly requires the pre-P2-T03 checkpoint
standalone invocation. This decision adds only those two existing test files to
the task overlay so their contracts can be changed from "standalone command
present" to "claim obligation satisfied by the integrated diff gate." No
additional production path or behavior is authorized.

## Preserved boundaries

- Preserve `handoff-0740` and `EqSrc_family_closure_repair_or_stress` as the
  ordinary research authority.
- Preserve final staged classification, signal, documentation-impact,
  research-control diff, memory, residue, rollback, and commit-acceptance
  behavior.
- Do not change claim taxonomy, reviewed contexts, severity mapping, changed
  path selection, base-ref handling, CI, Make profiles, skills, roles, planner,
  cache, compact-output, rollback, or physics behavior.
- Do not treat validator results, tests, receipts, or checkpoint commits as
  physics proof.

## Outcome rule

PASS requires zero eligible duplicate standalone executions, one independent
integrated diff execution per working or staged scope, preserved normalized
finding sets, negative identity tests, required summary counts and IDs, clean
validation, and no unrelated tracked change. Otherwise stop with
`REPAIR_REQUIRED` or `BLOCKED` and do not checkpoint invalid state.
