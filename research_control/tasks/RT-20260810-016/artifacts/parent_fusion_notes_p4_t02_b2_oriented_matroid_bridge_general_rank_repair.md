---
authority: science_draft
task_id: RT-20260810-016
job_id: AJ-RT-20260810-016-001
status: completed
result: constructed_candidate
---

# Parent fusion — oriented-matroid bridge general-rank repair

## Answer first

The fused Candidate Constructor result is `constructed_candidate`. The fixed
proposal-only `Bridge_OM(A)` construction now applies at every finite intrinsic
rank

`1 <= r = rank(A) <= |S| < infinity`,

and every support-minimal positive circuit obeys the sharp bound
`|C| <= r + 1`. The mathematically unnecessary, target-shaped rank-four cap
is absent from the theorem domain. Every fixed-rank fixture and bounded model
loop is explicitly `nonphysical_conformance_sample` metadata.

This repair changes no other `Bridge_OM` definition, typing repair, supplied
input, status boundary, or inherited freeze. It is proposal-only
source-extension data pending one separate focused Smuggling Auditor packet;
it is not a source-law adoption, physical causal structure, P7 universality,
conformal reconstruction, or Distance-to-GR advance.

## Parent-child synthesis

Both internal specialists independently return `constructed_candidate` with
zero blocking conflicts. Their only bookkeeping difference was whether the
patch count denotes two theorem-formula edits or three semantic entries. The
fused record uses the exact resolution: two theorem edits plus one mandatory
fixture-status label, with zero drift in the fixed bridge definitions.

The mathematical child contributes:

- factorization through the effective span `W_A = span(A)`;
- the nonnegative-kernel boundary proof of the minimal positive-circuit bound;
- sharp feasible and obstructed standard-sum families for every finite rank;
  and
- exact covector and tope counts for the mandatory controls.

The philosophical child contributes:

- a dimension-erasure criterion that requires an intrinsic theorem domain,
  quarantined fixed numerals, and controls crossing the former cap;
- the nonimplication from arbitrary finite algebraic rank to P7 physical-sector
  universality; and
- the separation of conformance evidence from ontology, physical meaning,
  empirical semantics, adoption, and workflow authority.

The final manuscript incorporates all of those contributions. The
philosophical child’s recorded parent hash is correctly retained as a
concurrent pre-fusion snapshot; no child result changed during fusion.

## Exact mathematical result

Let `C` support a strictly positive relation `A_C alpha = 0`. If
`ker(A_C)` had dimension greater than one, perturb `alpha` along an independent
kernel direction until the first coordinate reaches zero while all coordinates
remain nonnegative. The endpoint is a nonzero nonnegative dependence on a
proper subset, contradicting support minimality. Therefore

`dim ker(A_C) = 1`, `rank(A_C) = |C| - 1 <= r`, and `|C| <= r + 1`.

The family

- `A_r^+ = [e_1, ..., e_r, +sum_i e_i]`, and
- `A_r^- = [e_1, ..., e_r, -sum_i e_i]`

exercises the unchanged Feasible and Obstructed branches. The negative-sum
configuration has one positive circuit on all `r+1` columns, so the bound is
sharp for every finite rank.

The complete bridge depends only on `W_A`: restriction from `V*` to `W_A*` is
surjective, while rank, kernel, circuit, refinement, and minor data depend only
on the columns in `W_A`. Unused ambient dimensions therefore add no bridge
information and carry no physical interpretation.

## Exact controls and evidence boundary

Exact rational controls pass for ranks one through eight. The required ranks
five and six cross the former cap and give support-six and support-seven
obstructions. For each standard-sum sign,

`|C*(A_r^+/-)| = 3^(r+1) - 2^(r+2) + 2`

and the tope count is `2^(r+1) - 2`. Each rank-five control has 603 covectors
and 62 topes; each rank-six control has 1933 covectors and 126 topes. Across
the four mandatory controls, the totals are 5072 covectors and 376 topes.

These finite counts check conformance and theorem nonvacuity only. They are not
source occurrence, a spacetime-dimension measurement, a physical sector
census, empirical data, representativeness evidence, continuum robustness, or
P7 coverage.

## Conservative repair and remaining limits

The semantic patch has exactly three entries:

1. replace the general domain `1 <= r <= 4` by arbitrary finite
   `1 <= r <= |S|`;
2. replace `|C| <= r + 1 <= 5` by `|C| <= r + 1`; and
3. classify every bounded fixture or model loop as nonphysical
   conformance-only metadata.

Complete covectors, Gordan totality, presentation-groupoid transport,
certificate-orbit typing, finite refinements, and tame exact-path
stratification remain the fixed RT014 construction. The three inherited
freezes remain active:

- `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR`
- `NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-COMMON-CHARACTER-HOLONOMY-PROTECTION`

No D7 adequacy reevaluation, B2 action, P4-T03 unlock, source-law or ontology
adoption, physical or empirical semantics, common cone, conformal class,
scale, `g_eff`, matter coupling, Einstein equation, Gate verdict, benchmark or
proof promotion, publication, push, external action, global no-go, or future
extension impossibility follows. All fourteen Distance-to-GR burdens remain
`no_delta`.

## Next lawful packet

Select exactly one separately admitted
`PKT-V22-P4T02-B2-ORIENTED-MATROID-BRIDGE-GENERAL-RANK-POST-REPAIR-AUDIT-V1`
packet of type `source_extension_smuggling_audit` under
`smuggling-auditor@0.2.0`. It may audit only the repaired arbitrary-rank domain
and conformance quarantine while holding every previously audited input,
typing repair, freeze, and authority boundary fixed.

The audit is selected but not executed in RT016. Refuter stress remains later
and separately admitted.
