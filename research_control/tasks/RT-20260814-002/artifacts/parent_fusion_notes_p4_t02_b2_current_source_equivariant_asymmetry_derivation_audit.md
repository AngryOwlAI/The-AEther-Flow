<!-- authority: science-draft -->

# Parent fusion — current-source equivariant-asymmetry derivation audit

## Result

RT-20260814-002 returns
`current_source_equivariant_asymmetry_derivation_theorem`. Exactly one of the
six predeclared candidate classes has a positive literal mathematical witness:
`ASYM-ARROW-SUBCLASS`.

Let `Diff_c(M)` be the compactly supported diffeomorphisms of a nonempty smooth
four-manifold `M`, and define

`K_star(M) = Diff_c(M)` when `Diff_c(M)` is proper in `Diff(M)`, and
`K_star(M) = {id_M}` otherwise.

The assignment is defined from the current smooth topology, is transported
exactly by conjugation under every diffeomorphism, is constant on every
current-reduct fibre, is objectwise proper, and contains nonidentity arrows on
the source model class. It therefore defines a wide normal subgroupoid under
the packet's literal mathematical arrow-subclass reading.

This is a bounded source-mathematical theorem, not an adopted source law. It
does not establish that `K_star` is `EqSrc`, that its arrows are physically
allowed, that it selects a presentation, or that it bears on causal response
or an effective metric.

## Exact current signature

The registered source package positively types a smooth four-dimensional
manifold as primitive debt. `Phi_src` remains unresolved vocabulary: it has no
registered sort, arity, domain, codomain, action, composition law, inverse,
generator, gauge status, stochastic status, or physical-time meaning.

For this audit, that unresolved vocabulary is carried only as an inert debt
marker. It is not interpreted as a nonlogical semantic symbol. The maximal
typed semantic groupoid is consequently `SmoothMan_4^iso`, with smooth
four-manifolds as objects and all diffeomorphisms as arrows.

There is an exact alternative reading. If `Phi_src` were required to be an
interpreted semantic symbol in every current model, model and arrow formation
would fail before naturality could be asked. That conditional typing
obstruction is preserved, but it is not the selected result because it would
assign `Phi_src` a logical status not registered by the current source.

## The `K_star` theorem

Compactly supported diffeomorphisms contain the identity. They are closed
under inverse because the support of an inverse is the image of the original
support, and under composition because the support of a composite is contained
in a finite union of compact sets.

For every diffeomorphism `h:M->N`, conjugation maps the support of `f` to the
image of that support. Hence

`h Diff_c(M) h^{-1} = Diff_c(N)`.

Conjugation also preserves whether `Diff_c` equals the full diffeomorphism
group. It therefore transports `K_star(M)` exactly to `K_star(N)` in both
branches.

Objectwise properness holds by construction in the first branch. In the
fallback branch, every nonempty smooth manifold has a coordinate ball on
which a nonzero compactly supported smooth vector field has a nonidentity
small-time flow. Thus `Diff(M)` is nontrivial and `{id_M}` is proper.

The associated wide subgroupoid contains every object and only automorphism
arrows in `K_star(M)`. A wide subgroupoid need not contain cross-object arrows.
The construction is therefore literal, total on the admitted object class,
and natural under every current isomorphism.

## Five negative candidates

`ASYM-POINT` fails natural selection: translations of `R^4` have no common
fixed point and no nonempty finite invariant point set.

`ASYM-ORIENTATION` fails natural selection: a coordinate reflection exchanges
the two orientations of `R^4`.

`ASYM-ORDER` fails natural selection: a smooth point-exchanging involution
would require both `p<q` and `q<p` for an invariant strict total order.

`ASYM-ROOT-LABEL` has no current presentation carrier, root type, label type,
or transport law. `ASYM-ADMISSION` likewise has no presentation-candidate
carrier, admission predicate, or transport law. Their first failures are
formation failures, not global impossibility results.

## Parent-child resolution

Both independent physics reviews converge on the positive theorem after one
precision round. The parent initially retained a possible full-model typing
obstruction, but the children separated an inert unresolved-debt marker from
an interpreted semantic symbol. They also separated the packet's mathematical
arrow-subclass codomain from a normative adopted `EqSrc` or admission law.

The mathematics child supplied the compact-support conjugation theorem and
the objectwise-proper `K_star` refinement. The philosophy child supplied the
semantic-symbol and normative-allowedness alternative readings and the strict
non-reification boundary. The fused theorem adopts the stronger `K_star`
witness and preserves both alternative readings as conditional caveats. No
blocking conflict remains.

## Meaning and authority boundary

The current smooth structure mathematically defines `K_star`, but it does not
select a point, root, label, orientation, total order, or unique admission. A
natural subgroupoid is not automatically a physical gauge group, a causal
arrow, a source evolution law, or empirical response semantics. Registry,
validator, compile, and checkpoint success cannot create that missing meaning.

No source primitive or source law is added. `Phi_src` remains unresolved.
`EqSrc` is not adopted. No current ontology, target geometry, metric, cone,
matter coupling, Einstein equation, benchmark, or protected Gate status is
changed.

## Distance-to-GR and continuation

All six inherited freezes remain active and unchanged. All fourteen
Distance-to-GR burdens remain `no_delta`. D7 was not reevaluated, B2 was not
activated, and P4-T03 remains locked.

One separately admitted theoretical-continuation-selector packet is selected
but not executed:
`PKT-V22-P4T02-B2-POST-CURRENT-SOURCE-EQUIVARIANT-ASYMMETRY-DERIVATION-THEOREM-THEORETICAL-CONTINUATION-SELECTION-V1`.
It must decide whether `K_star` has any materially distinct bridge-facing
P4-T02 role or is source-side classification only. It may not preselect a new
source law, adopt `K_star`, replay this six-candidate audit, or claim physical
progress from mathematical naturality alone.

No global no-go, future-extension impossibility, proof promotion, publication,
push, external action, or completed derivation follows.
