<!-- authority: control -->

# Parent Fusion Notes: General Source-Cover Law

The parent reviewed both child outputs for `RT-20260614-110`.

Shared consensus:

- The packet may define proposal-only `MSRC-GSC-GENERAL-SOURCE-COVER-LAW`.
- The output must remain `draft/control` and `proposal-only`.
- The next bounded packet is Smuggling Auditor audit, not adoption, `g_eff`,
  or Gate Chair review.

The mathematical child supplied the law interface:
`AdmExt_src^GSC`, `NonBottomCover_src^GSC`, `CoverExtend_src^GSC`,
certificate families, and `Bottom_src` guards. The philosophical child
preserved the authority boundary: finite checker replay evidence and prior
general source-cover artifacts are not adoption authority.

The fused artifact defines the law package as an auditable source-extension
candidate. It does not claim general source-cover existence for arbitrary
source data, does not adopt `FVR_src^GSC`, `RegSold_src^GSC`, or `M_src`, and
does not unlock downstream GR work.
