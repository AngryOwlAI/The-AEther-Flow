<!-- authority: science-draft -->

# Parent fusion: source-law-space robust-invariance protection smuggling audit

## Fused result

Both internal child perspectives independently return
`source_pure_as_written`, narrowly. The fixed RT007 candidate contains no
literal target atlas, metric, tetrad, coframe, desired GR cone, physical norm,
detector response, empirical answer, benchmark result, frozen-selector output,
generated-derivative authority, or workflow-status premise. Its mathematical
roots are source-typed and disclosed as proposal-only source-extension data.

The phrase `as_written` is load-bearing. Written-syntax purity supplies no
independent source provenance, current-ontology derivation, epistemic root
selection, physical or empirical meaning, adoption, P4-T02 sufficiency, or
Distance-to-GR credit. `X_law`, `K`, `A`, `Delta`, the inward orientation,
strict margins, and `EqSrc_prot` remain openly supplied constructive preload.
Predeclaration and hashes establish traceability and within-packet
nonadaptivity only.

The candidate remains proposal-only source-extension data classified as a new
ontology primitive candidate. It is neither a conservative definitional
extension of current ontology nor a canonical-ontology candidate, adopted law,
or rejected law. Adoption remains `blocked_adoption_open_continuation`.

## Mathematical synthesis

Let `R_root` be the disjoint union of all regular proposal tuples admitted by
the fixed RT007 typing, modulo declared `EqSrc_prot` arrows and certificate
transport. The robustness map

`C : R_root -> {pass, fail, undefined}`

is a classifier, not a selector or adoption map. The exact tuples

- `D1 = (R, R_ge_0, {F=+1}, {0}, Id)`,
- `D2 = (R^2, R_ge_0^2, {F=(1,1)}, {(0,0)}, Id)`, and
- `D3 = (R, R_ge_0, {F=-1}, {0}, Id)`

give `C(D1)=pass`, `C(D2)=pass`, and `C(D3)=fail`. At every active face of
`D1` and `D2`, each active coordinate velocity is strictly positive. At the
boundary of `D3`, `-1` lies outside even the weak tangent cone. The two passing
tuples cannot lie in one presentation orbit because a local diffeomorphism
preserves ambient manifold dimension. Therefore a pass value cannot determine
a unique root orbit, and target-free syntax alone determines neither roots nor
outcome.

The parent exact model localizes this debt further. With the other roots held
fixed, reversing the admissible-set orientation, changing `F=+1` to `F=-1`, or
enlarging the variation class from `{0}` to `{-2}` flips pass to fail. These
are target-free mathematical alternatives. Removing the failing alternatives
only after seeing their result would be circular, but the fixed RT007 packet
does not perform that move and retains explicit negative controls.

Reflection transports a supplied passing tuple and its inward conormal without
selecting an orientation. Positive barrier regraduation preserves residual
signs while removing raw certificate normalization, but selects no root.
Every typed tuple admits an identity-only coherent groupoid, so groupoid
existence alone has no discriminating force. All seven fixed RT007 conditional
results are preserved under their stated hypotheses.

## Conceptual synthesis

The children agree on a strict separation:

- clean written dependency syntax is not independent source legitimacy;
- mathematical conditional validity is not ontology derivation;
- a protected invariant set is not a physical characteristic cone;
- inward orientation is not physical time orientation;
- presentation naturality is not physical gauge;
- strong invariance is not occurrence, detector response, or empirical
  adequacy; and
- hashes, roles, registries, validators, and checkpoints are control evidence,
  not scientific premises.

Constructive goal preload is present and disclosed. That makes the proposal a
falsifiable candidate architecture, not a hidden target import. It also means
that a passing fixture cannot explain why the source should instantiate this
arena, admissible region, law family, perturbation class, orientation, margin,
or equivalence groupoid.

## Conflict review

There are no blocking conflicts. Both children use the same verdict, the same
scope qualification, the same D1/D2/D3 nonselection theorem, the same seven
preserved conditional results, the same five freezes, and the same successor.
The mathematician child supplies the exact orbit and classifier proof; the
philosopher child supplies the purity-versus-legitimacy separation and the
constructive-preload boundary. The parent combines them without strengthening
either into source derivation, physical interpretation, or adoption.

## Preserved boundary and next packet

All five inherited freezes remain active:

- `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR`
- `NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-COMMON-CHARACTER-HOLONOMY-PROTECTION`
- `NDCL-V22-P4T02-B2-ORIENTED-MATROID-BRIDGE-SELECTION-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-SIGNED-CUBIC-VIABILITY-SELECTOR-ROBUSTNESS`

Every one of the fourteen Distance-to-GR burdens remains `no_delta`. D7 is not
reevaluated, B2 remains inactive and undisposed, P4-T02 remains incomplete for
the plan dependency, and P4-T03 remains locked.

The fused audit selects, but does not execute,
`PKT-V22-P4T02-B2-SOURCE-LAW-SPACE-ROBUST-INVARIANCE-PROTECTION-REFUTER-STRESS-V1`,
type `source_extension_refuter_stress`, under `refuter@0.2.0`. A separately
admitted packet may stress the fixed candidate under independently specified
root and law perturbations, orientation reversal, variation enlargement,
nontrivial presentation demands, margin collapse, and dimensional extension.

No candidate repair, canonical ontology edit, source-law adoption, physical or
empirical interpretation, `g_eff`, coupling, Einstein equation, benchmark
promotion, Gate action, proof promotion, publication, push, external action,
global no-go, or completed derivation is authorized.
