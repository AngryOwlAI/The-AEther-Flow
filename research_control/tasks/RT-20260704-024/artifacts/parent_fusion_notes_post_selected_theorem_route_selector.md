# RT-20260704-024 Parent Fusion Notes

## Consensus

Both child perspectives select `concrete_certificate_instance_library` as the
next route. The formal reason is that the P3 target specification exists,
survived the P3-T03 smuggling audit as source-pure as written, and survived
P3-T04 Refuter stress only as draft/control pending selector.

The philosophical/status reason is that moving to finite/local certificate
instances is the lowest-risk next step: it makes the abstract target concrete
without converting stress survival into adoption, derivation, or benchmark
status.

## Route Exclusions

The selector does not route to P3 repair because no blocking defect was found.
It does not route directly to source model zoo because P4 certificate
instances are the lower-level dependency for P10. It does not route to P6
formalization because executable checks should consume concrete instances. It
does not route to coupling-law candidate construction because no finite/local
certificate-instance library exists yet.

## Fused Decision

Run P4-T01 next: a bounded `ontology-formalizer@0.2.0` packet for
certificate-instance library schema. The packet should define schema fields
for valid transport, invariance, factorization, missing certificate,
malformed certificate, and target-import examples, while preserving all
no-adoption and no-promotion boundaries.

## Limitations

This fusion note is control evidence only. It is not a proof of a coupling law,
matter coupling, stress-energy semantics, matter action, Einstein equations,
benchmark recovery, or completed derivation.
