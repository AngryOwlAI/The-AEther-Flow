<!-- authority: control -->

# Parent Fusion Notes: P3-T02 Operation Laws

The parent synthesis fused the mathematical child recommendation to prove
conditional source-certificate operation laws with the epistemic-boundary child
recommendation to keep those laws as certificate hygiene rather than physical
semantics or downstream GR evidence.

The fused TeX artifact states:

- identity declared-scope preservation;
- compatible composition source-side closure;
- source restriction closure;
- malformed certificate fail-closed behavior;
- missing certificate separation-preservation or a named obstruction;
- target-import invalidity and fail-closed behavior.

The missing-certificate target is deliberately conditional. When declared
`RR_E` separation is present, missing certificates preserve it. When the
record lacks enough source object data to state separation, the artifact names
`OB-P3T02-MISSING-CERT-RRE-SEPARATION-DATA` rather than promoting an
identification or a global no-go result.

The fusion routes next to P3-T03 certificate checklist integration. It does
not adopt source law, `RR_ETransportCompletenessOrInvarianceLaw_v1`,
`PositiveMSProfile_v1`, `SourceMatterSemanticsAdoptionReadinessLaw_v1`,
matter semantics, detector semantics, coupling law, matter coupling,
stress-energy semantics, matter action, Einstein equations, benchmark status,
or completed derivation.
