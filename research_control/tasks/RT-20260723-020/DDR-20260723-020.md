---
authority: "control"
decision_id: "DDR-20260723-020"
task_id: "RT-20260723-020"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "validator-engineer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260723-020-001"
status: "blocked"
supersedes_decision_id: "DDR-20260723-019"
requires_human_gate: false
role_fit_candidates:
  - "validator-engineer@0.2.0|selected|P13-T04 requires deterministic lint typing portability API and CI-contract implementation with focused tests."
  - "project-control-maintainer@0.2.0|not_selected|The packet changes operational validators and tests rather than only control policy."
  - "process-integrity-auditor@0.1.0|not_selected|The packet constructs new quality gates rather than only auditing existing evidence."
created_at: "2026-07-24T05:27:33Z"
activated_at: "2026-07-24T05:27:33Z"
completed_at: "2026-07-24T05:44:42Z"
validation_status: "FAIL"
---

# DDR-20260723-020 — Implement the P13-T04 quality and portability contract

## Current Objective

Execute only dependency-ready v21 `P13-T04`: add incremental Ruff and mypy
gates, a declared Python and operating-system CI matrix, a shared prospective
portable-path linter with case-collision fixtures, and a documented typed
programmatic validator API.

## Authority Surfaces Read

The repository instructions, recursive relay and project-system skills,
Project-System Director, Validator Engineer and schema contracts, generation
92 immutable route, v21 plan and backlog, `program_state.yaml`,
`handoff-0849`, P13-T03 and P10-T07 completions, validation-environment
contract, project metadata and lock wrappers, both CI workflows, validation
planner and executor code, tests, signal registries, and relevant control
registries were inspected. Memory results were used only to locate the
registered v21 plan; the canonical plan and registry row were then inspected.

## Role-Fit Matrix

| Role | Fit | Reason |
| --- | --- | --- |
| `validator-engineer@0.2.0` | selected | Owns deterministic quality gates, validator APIs, portability checks, and focused regressions. |
| `project-control-maintainer@0.2.0` | partial | Could maintain policy prose but is not the narrowest role for typed validator implementation. |
| `process-integrity-auditor@0.1.0` | not selected | This packet creates and tests new operational surfaces rather than only auditing them. |
| Physics roles | not selected | No derivation, ontology, theorem, benchmark, or promotion work occurs. |

## Selected Role

Use one Validator Engineer task overlay. It may add exact quality-tool
dependencies, incremental configuration and Make targets, one CI matrix job,
two shared validation modules, focused tests, task-local contract and receipt
artifacts, the minimum source-document updates, normal control records and
generated derivatives, and one checkpoint.

## Claim Boundary

The packet may claim only that the scoped quality commands, typed API examples,
prospective path rules, local macOS cell, and deterministic CI matrix contract
pass. Hosted Linux or macOS runs are not inferred unless separately observed.
The packet may not infer physics progress, theorem truth, proof authority,
ontology or source-law adoption, Distance-to-GR movement, benchmark promotion,
publication authority, or a completed derivation.

## Validation

PASS requires exact dependency-lock verification, Ruff and mypy success on the
declared incremental surface, API example tests, path-length and
case-sensitivity fixtures, CI matrix contract checks, documentation-impact and
signal parity, affected research-control validation, memory synchronization
and core validation, Git diff checks, and one governed checkpoint.

## Outcome

The bounded implementation and focused quality evidence pass. Final
pre-checkpoint research-control validation does not: the generated current and
compact frontier artifacts remain bound to `handoff-0849`, and the direct
renderer commands needed to synchronize them are not present in this immutable
AgentJob. The worker invocation was not repeated, and no checkpoint, staging,
or commit occurred. One fresh Validator Engineer recovery must synchronize the
generated frontiers, repeat required validation, and checkpoint only on a full
pass.
