<!-- authority: draft/control -->

# P9-T07 parent fusion: nonlinear compact-token control

The mathematical perspective fixes a three-address uniform finite kernel and
a one-address raw matter token. Its balanced forcing drives the proposal-only
quartic edge action

\[
S_{\mathrm{NL}}=
\frac12\sum_{i<j}C_{ij}(h_i-h_j)^2+
\frac14\sum_{i<j}C_{ij}(h_i-h_j)^4-\rho^Th .
\]

The exact quotient solution is
\(h_\star=(2/3,-1/3,-1/3)\). The Euler residual vanishes componentwise,
the Hessian has positive quotient eigenvalues \(4\) and \(2\), and strict
convexity makes the solution unique. Antisymmetric nonlinear edge fluxes give
an exact finite cut identity. The task-local gradient flow preserves
\(\sum_i h_i=0\), decreases the action, and reduces to
\(d'=2-d-d^3\), which converges monotonically from \(d(0)=0\) to \(d=1\).

The interpretive perspective fixes the model-to-world boundary. A supported
dimensionless token is not yet physical matter; a graph quotient is not yet a
spacetime gauge constraint; antisymmetric edge bookkeeping is not yet
covariant stress-energy conservation; and formal exterior address values are
not a metric, potential, horizon, redshift, or calibrated observable.

The fused no-fog result is
`OBST-P9T07-NONLINEAR-COMPACT-SOURCE-MODEL-TO-WORLD-NONSELECTION-001`.
The source mathematics is an exact nonlinear solution, but the physical
compact-source benchmark remains `INCONCLUSIVE` with secondary label
`FORMAL_ANALOGY`, not `PASS`.

The triangular carrier, raw token, quartic coefficient, and relaxation were
fixed without a target equation, target solution, desired agreement, or
target-informed rerun. Same-context deterministic validation is not
independent reproduction.

This is a current-source obstruction with
`blocked_adoption_open_continuation` status. It is not a global no-go theorem;
a materially new source-derived carrier law, physical matter map, boundary
map, tensor exterior observable, and physical conservation/constraint
semantics can reopen the burden. No canonical ontology, physical compact
object, effective geometry, Einstein equation, exact-GR recovery, benchmark
promotion, proof, publication, push, or completed-derivation authority
follows.
