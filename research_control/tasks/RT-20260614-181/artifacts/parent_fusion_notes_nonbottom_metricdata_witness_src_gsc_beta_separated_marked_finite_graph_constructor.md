# Parent Fusion Notes: Beta-Separated Marked Finite Graph Constructor

Both child perspectives support a `constructed_candidate` result under a
narrow claim boundary.

Consensus:

- The mathematical child constructs the candidate on declared finite acyclic
  source graphs carrying explicit injective source-side `beta` separation.
- The philosophical child requires that `beta` remain domain data, not adopted
  ontology or reconstructed target evidence.
- The support checker may expose examples and malformed cases, but the TeX
  artifact controls the mathematical claim.

Unique contribution preserved:

- The graph family is broader than `D_m^beta` because it permits any declared
  finite acyclic source graph satisfying beta-separation and coherent marked
  transport.
- It is still not an arbitrary finite-DAG theorem because absent, ambiguous,
  external, non-injective, or non-coherent beta fails closed.

No blocking conflict remains. The next role is Smuggling Auditor.
