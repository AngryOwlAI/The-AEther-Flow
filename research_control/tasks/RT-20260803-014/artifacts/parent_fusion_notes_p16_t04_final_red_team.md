<!-- authority: control -->

# P16-T04 parent fusion: final claim-boundary red team

## Fused result

The two internal perspectives agree on the substantive science boundary. The
current sources support a bounded negative conclusion: no declared target path
was detected in the audited benchmark manifests, no benchmark case passed, no
qualifying independent replication exists, the methodology results are
descriptive rather than causal, and the exact-GR package is target-side
adoption rather than first-principles substrate derivation.

Those statements do not compose into anything stronger. In the task-local
typed claim system, source-firewall PASS, control PASS, internal review,
methodology evidence, exact-GR adoption, and scoped obstruction can all be true
while external independence, source derivation, and a global no-go remain false.
This countervaluation satisfies every explicit claim rule, so the stronger
composition is not entailed.

## Conflict resolution

The physicist-mathematician child would allow P16-T04 to close as a bounded
internal audit if no current contract defect remained. The
physicist-philosopher child found such a defect. V21-R38 requires the current
reviewer to be labeled honestly as an internal skeptical role and reserves
external-review language for evidence-backed epistemic independence. The
active base contract still declares `External Red-Team Reviewer` and
`scientific_adversarial_external_review`.

The structured review-context fields correctly classify executed reviews as
same-context or blind-same-model and set external and independent claims to
false. That prevents a scientific promotion, but it does not erase the live
operator-facing role metadata. Because P16-T04 explicitly audits internal
review mislabeled as external, the parent preserves this mismatch as a high
project-system finding.

The historical P11-T07 `independent-review pilot` task label and the P9-T03
`Target-free operational response family` heading remain contextualized by
their exact conservative records. They are not rewritten here. The current
role-contract and display projection must instead be repaired without changing
historical evidence.

## Disposition

P16-T04 has been executed, but it is not qualifying-finalized. The result is
`REPAIR_REQUIRED_P16_T04_INTERNAL_EXTERNAL_REVIEW_LABEL_CONTRACT_DRIFT`.
Emit one non-superseding project-improvement sidecar for a bounded
`improve-project-system` repair, then run a fresh P16-T04 re-audit before
P16-T05.

This fusion is not external review, independent replication, scientific proof,
benchmark promotion, publication authority, push authority, a Gate change, or
a completed derivation.
