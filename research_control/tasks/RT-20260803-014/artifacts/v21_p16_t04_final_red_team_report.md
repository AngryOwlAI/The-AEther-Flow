<!-- authority: control -->

# V21 P16-T04 final claim-boundary red-team report

## Verdict

`REPAIR_REQUIRED_P16_T04_INTERNAL_EXTERNAL_REVIEW_LABEL_CONTRACT_DRIFT`

The final science and public-claim surfaces remain conservative. No current
source turns target-side exact-GR adoption into substrate derivation, no
benchmark case passes, no methodology result is used as physics evidence, and
no review record establishes external human review or independent replication.

P16-T04 nevertheless cannot close cleanly. The active base review contract
still names and types same-context internal AI skeptical pressure as external
review. That conflicts with V21-R38 and the review-independence taxonomy. It is
a high-severity project-system claim-contract defect, not a scientific result.

## Audit inventory

The audit used the P16-T03 reproducible inventory as navigation, then read
claim-bearing sources directly. It covers:

- all 130 pre-P16-T04 V21 task/completion identities and 134 V21 handoffs,
  plus the current task and handoff when finalized;
- the final Distance-to-GR ledger and program state;
- all Gate A-E authority sources preserved by P16-T02;
- proof-normal-form, benchmark, review-context, publication-brief, and source
  registries;
- the P9 source firewall, six benchmark cases, P11 review taxonomy and pilot,
  P15 methodology and package evidence, canonical ontology TeX, README,
  GitHub-facing claim pages, and publication briefs;
- the 19-fixture semantic-smuggling suite and an explicit ten-file ontology
  claim-language scan.

Validator, task, Gate, registry, and checkpoint labels were used only to locate
sources and test internal consistency. They were not treated as scientific
premises.

## Semantic composition test

Let the available positive atoms be:

- `S`: no declared target-to-source path detected in the audited manifests;
- `V`: validator, registry, renderer, and checkpoint control PASS;
- `I`: same-context or blind-same-model internal AI critique completed;
- `M`: descriptive methodology mechanism ratios exist;
- `A`: the exact-GR target-side benchmark is adopted;
- `N`: a fixed derivational line has a scoped obstruction.

Let `E` mean qualifying external independence, `D` mean first-principles
source derivation of the Einstein sector, and `G` mean a global no-go. The
registered rules contain no coercion from `S`, `V`, `I`, `M`, `A`, or `N` to
those stronger atoms. The valuation

`S = V = I = M = A = N = true`, with `E = D = G = false`,

satisfies every explicit claim rule. Therefore the conjunction of all current
positive control statements does not entail external independence, substrate
derivation, benchmark promotion, or global impossibility. This is a task-local
typed non-entailment result, not a theorem about every future source extension.

## Source-purity and benchmark result

The source firewall theorem is conditional on complete parent edges, absent
undeclared side channels, immutable sealing, and no comparison feedback. Its
own source warns that code, lookup tables, training data, priors, human target
knowledge, boundary records, and calibration objects can carry target
information.

The six P9 cases report zero declared target imports and zero post-seal
mutations. They also report six inconclusive outcomes, zero benchmark passes,
zero independent replications, disclosed target-family knowledge, and
unresolved shared-context and shared-code risk. The permitted claim is
therefore manifest-bounded: no declared path was detected. Exhaustive
target-freedom is not established.

The P9-T03 heading `Target-free operational response family` is a lexical risk
because `Clock_src`, `Signal_src`, and `FreeFall_src` were designed with the GR
and Newtonian target family already known. The exact artifact prevents an
overread: no target formula or fitted value enters the definition, the family
is nonselective, the physical map is absent, and the case is `INCONCLUSIVE`.

## Review provenance result

The taxonomy requires evidence, not role title. Existing reviews are exactly
`same_context_role_review` or `blind_same_model_review`; structured external,
human-expert, and independent-replication flags are false. No searched V21
completion, handoff, or review registry row asserts a completed qualifying
external or independent result.

The live base role contract remains inconsistent with that rule. Its metadata
says `External Red-Team Reviewer` and
`scientific_adversarial_external_review`, and its operator-facing prose speaks
as an outside skeptic. The current task overlay correctly calls this execution
internal, but an overlay cannot repair the base contract. The historical
P11-T07 task type also says `independent_review_pilot`, while its completion
correctly reports four internal AI reviews. Historical evidence must not be
rewritten; a project-system repair must fix the current contract and display
projection instead.

## Methodology, ontology, and no-go result

P15-T02 reports six descriptive ratios with valid denominators, but the
unstructured and normalized single-agent arms are absent, blind review is
same-model, diverse review is context-diverse rather than independent, cost is
unavailable, and causal superiority is not identified. The methodology packet
does not count as physics evidence.

README, current frontier, canonical ontology TeX, and public claim pages all
preserve the adoption-versus-derivation distinction. The exact-closure package
adopts one operative GR metric and Einsteinian target dynamics. It does not
derive those objects from substrate structure. Gates D and E remain
`NOT_READY`; six benchmark cases are inconclusive and none passes.

Frozen-line and current-source obstructions remain scoped. They do not reject
the broader ontology or prove future conservative source-extension
impossibility. A genuinely new source law, primitive, or symmetry route remains
open under the tracked authority rules.

## Semantic and public-path validation

The inherited semantic-smuggling suite passes all 22 checks over 19 fixtures:
13 adversarial and six benign. It covers direct imports, aliases, split
encodings, positional and equivalent mathematical forms, misleading no-target
certificates, validator-as-proof attacks, and prompt-level interpretation. It
is finite coverage, not semantic completeness or scientific proof.

The default claim-language scan does not include canonical ontology Markdown
or TeX, so P16-T04 explicitly scanned ten ontology source paths. That explicit
scan produced zero findings. Fifteen stable public claim paths are hash-bound
in the public-claim verdict.

## Required repair

After one governed checkpoint, consume
`PIS-RT-20260803-014-001` through one bounded
`improve-project-system` packet:

1. Preserve all historical task, role, review, completion, and science bytes.
2. Make the current skeptical-review contract and current display projection
   unambiguously internal.
3. Reserve external-human and independent-replication language for evidence
   satisfying the taxonomy.
4. Add fail-closed regression coverage for role names, task display labels,
   schema language, and claim-linter surfaces.
5. Regenerate governed registries and derivatives and checkpoint once.
6. Run a fresh P16-T04 audit before P16-T05.

No canonical science, Gate decision, Distance-to-GR row, ontology, benchmark,
proof, publication, reviewer-contact, external action, or push is authorized by
this result.
