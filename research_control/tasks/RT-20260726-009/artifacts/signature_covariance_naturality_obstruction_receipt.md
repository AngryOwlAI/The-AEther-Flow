# P6-T05 signature, covariance, and naturality receipt

Status: `draft/control`; `blocked_adoption_open_continuation`.

The bounded P6-T05 packet records five new mathematical payloads:

1. `q_L=diag(-1,1,1,1)` and `q_S=diag(-1,-1,1,1)` have equal determinant
   magnitude and identical admitted one-ray, independent-density, and
   constant-phase data, but different inertia.
2. `q_epsilon=diag(-1,1,1,epsilon^2)` approaches a degenerate form while the
   independent source carriers remain fixed; no metric-determinant lower bound
   is derived.
3. The determinant-one diagonal source automorphisms
   `A(a,b,c)=diag(exp(a),exp(b),exp(c),exp(-a-b-c))` preserve a maximally
   symmetric admitted datum. Their weights have no zero pair sum, so the only
   invariant symmetric bilinear form is zero.
4. Conditionally supplied source coframes glue
   `q_U=theta_U^T eta theta_U` exactly when overlap maps preserve `eta` and
   satisfy inverse and cocycle identities.
5. An oriented source ray selects time orientation only conditionally after a
   Lorentzian form and timelikeness are established.

The exact obstruction is
`OBST-P6T05-SIGNATURE-COVARIANCE-NATURALITY-001`. It is candidate-scoped:
current ontology does not derive the required signature selector,
nondegeneracy certificate, source coframe, Lorentz transition representation,
cocycle, or robustness law. It is not a global no-go theorem. The comparison
forms are not adopted source or target geometry, and the conditional coframe
theorem is not a physical metric derivation.

Gate B remains open. Historical scoped `g_eff`, canonical ontology, matter
coupling, Einstein equations, benchmark status, proof authority, publication,
and push state are unchanged. After a governed checkpoint, one fresh bounded
P6-T06 Refuter packet may stress the cumulative obstruction.
