---
authority: control
status: completed
decision_id: DDR-20260726-009
task_id: RT-20260726-009
agent_job_id: AJ-RT-20260726-009-001
director_version: director-of-research@0.3.0
decision_type: task_overlay
selected_role_id: ontology-formalizer
selected_role_version: 0.2.0
supersedes_decision_id: DDR-20260726-008
requires_human_gate: false
role_fit_candidates:
  - ontology-formalizer@0.2.0
  - refuter@0.2.0
  - smuggling-auditor@0.2.0
---

# DDR-20260726-009: execute only v21 P6-T05

## Current objective

Execute immutable generation 129 and only P6-T05. Determine whether the
current source package selects a nondegenerate Lorentzian bilinear form and
its source-induced transformation law. Treat the P6-T03 conformal and P6-T04
scale-calibration nonselection results as hard inputs and return a genuine
conditional theorem or a precise candidate-scoped obstruction.

## Authority surfaces read

The Director read `SOUL.md`, both applicable `AGENTS.md` files, the recursive
relay and `continue-research` skills, the Director and Ontology Formalizer
roles, AgentJob and mathematical-decisiveness schemas, the v21 P6-T05 plan,
the Distance-to-GR burden map and ledgers, `handoff-0882`, the complete P6-T03
and P6-T04 theorem packets, canonical target-side geometry, and the historical
scoped `g_eff` candidate and Gate Chair record. Memory preflight refreshed
retrieval-only state and passed with 1,035 source objects and 5,202
relationship rows. Direct scientific source hashes were held fixed.

Checkpoint commit `cdd4fd0989ed59ed0f1e4b5d7827cf8a1082af07` satisfies the
cumulative P6-T04 dependency and recovery condition recorded by
handoff-0882. Immutable generation 129, route SHA-256
`f8534e0124605ca69bfc93523dac5135d3f46d700c0ff1f954eef3a6dfdd3a8d`,
and handoff-0882 uniquely select P6-T05.

## Role-Fit Matrix

| Candidate role | Fit | Reason |
|---|---|---|
| `ontology-formalizer@0.2.0` | selected | P6-T05 requires typed source data, naturality, signature, determinant, frame-transition, inverse, cocycle, and robustness obligations. |
| `refuter@0.2.0` | next packet | P6-T06 may stress the cumulative obstruction after checkpoint. |
| `smuggling-auditor@0.2.0` | internal boundary only | Target-atlas and physical-metric overreads must be blocked, but the routed packet is a mathematical formalization-or-obstruction test. |

## Selected role and decomposition

Activate one `ontology-formalizer@0.2.0` task overlay and one outer AgentJob.
The internal physicist-mathematician perspective constructs exact
same-source countermodels, proves a source-naturality obstruction, and derives
the conditional coframe gluing theorem. The internal
physicist-philosopher perspective audits object status, coordinate covariance,
time orientation, target imports, and the difference between a conditional
representation and a physical metric. The parent resolves the tensions
without child AgentJobs or subagents.

## Decisive result

On a four-dimensional source tangent space with oriented ray
`[e_0]_+`, an independent positive density, and scalar phases constant on the
comparison datum, the forms

`q_L=diag(-1,1,1,1)` and `q_S=diag(-1,-1,1,1)`

have equal determinant magnitude and identical admitted source data but
different inertia. The family

`q_epsilon=diag(-1,1,1,epsilon^2)`

is Lorentzian for positive `epsilon` and tends to a degenerate form as
`epsilon` tends to zero. The source density is not a metric-volume identity,
so it supplies no determinant lower bound.

There is a stronger naturality obstruction. The determinant-one source
automorphisms

`A(a,b,c)=diag(exp(a),exp(b),exp(c),exp(-a-b-c))`

preserve this admitted source datum. The four weights are
`(1,0,0)`, `(0,1,0)`, `(0,0,1)`, and `(-1,-1,-1)`; no pair sums to zero.
Therefore invariance `A^T q A=q` for all `a,b,c` forces every component of a
symmetric bilinear form `q` to vanish. On this admissible datum there is no
nonzero natural symmetric bilinear form, much less a nondegenerate Lorentzian
one. This is a scoped obstruction, not a global impossibility theorem.

Conditionally, if source-derived coframes `theta_U` and a fixed Lorentzian
form `eta` are supplied, the local tensors
`q_U=eta_ab theta_U^a tensor theta_U^b` glue exactly when every overlap map
obeys `Lambda_VU^T eta Lambda_VU=eta`; triple overlaps require
`Lambda_WV Lambda_VU=Lambda_WU`. This states the missing burden but does not
derive its premises.

The precise disposition is
`OBST-P6T05-SIGNATURE-COVARIANCE-NATURALITY-001`. The exact unchanged route
from one ray, unnormalized density, and dimensionless phases to signature,
nondegeneracy, and a covariant metric is locally frozen.

## Claim boundary and continuation

Current ontology does not derive a source coframe, a Lorentzian signature
selector, a metric-volume identification or determinant lower bound, a
Lorentz-valued transition representation, a cocycle and inverse law, or a
finite-variation robustness law. Adoption remains
`blocked_adoption_open_continuation`: materially richer source-side laws may
shrink the automorphism group and reopen the burden.

P6-T05 does not establish a physical metric, causal cone, proper time, rods,
coordinates, or an unscoped `g_eff`. It does not import target geometry, tune
`G` or `c`, couple matter, derive Einstein equations, promote the exact-GR
benchmark, authorize proof or publication, push, globally reject the theory,
or close future source extensions.

## Next bounded route

After one governed checkpoint, P6-T06 may use one fresh bounded Refuter packet
to stress uniqueness, covariance, finite-variation stability, and
physical-admissibility boundaries of the cumulative P6-T03 through P6-T05
package. It may not pretend that the comparison forms or conditional coframe
construction are an adopted physical metric.
