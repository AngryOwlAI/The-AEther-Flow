<!-- authority: control -->

# Parent Fusion Notes: Detector Replacement Candidate

## Scope

This fusion note records the parent synthesis for `RT-20260706-002`, v17
P4-T02. It fuses the formal finite construction from the physicist-mathematician
child with the ontology and claim-boundary separation from the
physicist-philosopher child.

## Fused Result

The parent selects the positive candidate branch rather than the obstruction
branch. The fused output is:

```text
research_control/tasks/RT-20260706-002/artifacts/detector_semantics_replacement_candidate_v1.tex
```

The constructed object is `SourceDetectorReplacementCandidate_EStar_v1`,
written as a finite source-side tuple
`D_E^src=(Dom_D(E),Read_D(E),Tok_D(E),Cert_D(E),Fail_D(E))`.

## Boundary

The candidate is a source-side placeholder with status
`supplied_source_placeholder`. It is not detector semantics, not a detector
theory, not an empirical protocol, not proper-time calibration, not target
metric behavior, not source-law adoption, not coupling-law adoption, not
matter-coupling derivation, not stress-energy semantics, not a matter action,
not Einstein equations, not benchmark promotion, and not completed derivation.

## Next Route

The logical next packet is P4-T03: one bounded smuggling audit of the detector
replacement candidate.
