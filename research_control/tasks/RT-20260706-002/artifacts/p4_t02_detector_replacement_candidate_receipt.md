<!-- authority: control -->

# P4-T02 Detector Replacement Candidate Receipt

## Result

`RT-20260706-002` constructed exactly one finite source-side detector
replacement candidate:

```text
research_control/tasks/RT-20260706-002/artifacts/detector_semantics_replacement_candidate_v1.tex
```

The candidate is `SourceDetectorReplacementCandidate_EStar_v1` with status
`supplied_source_placeholder`.

## Candidate Fields

```yaml
source_domain: "Dom_D(E_*) = R_{E_*}"
readout_interface: "Read_D(E_*): R_{E_*} -> Tok_D(E_*)"
certificate: "Cert_D(E_*)"
status: "supplied_source_placeholder"
no_empirical_protocol_import: true
no_proper_time_import: true
no_target_metric_import: true
finite_local_witness: "W_D(E_*)"
```

## Non-Conclusions

The candidate is not detector semantics, not source-law adoption, not
coupling-law adoption, not matter-coupling derivation, not stress-energy
semantics, not a matter action, not Einstein equations, not benchmark
promotion, not Gate Chair verdict, not proof authority, and not completed
derivation.

## Next Route

The selected next route is P4-T03: one bounded smuggling audit of the detector
replacement candidate.
