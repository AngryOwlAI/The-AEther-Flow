# Parent Fusion Notes: EqSrc Cross-Complex Reflection-Scope Repair

## Synthesis

The independent mathematical and philosophy-of-physics perspectives agree
that the RT-030 obstruction has one exact proposal-level repair. The fused
output types preservation and reflection for a map between two supplied
source packages, identifies the induced quotient map, and specializes the
reflection criterion to injectivity of \(H_1(F)\) for a chain map.

The result is proposal-only
`EqSrcIntrinsicDiscriminatorAdmissibilityLaw_src^cand,v3`, classified as a
`new_ontology_primitive_candidate` with
`blocked_adoption_open_continuation`. A fresh Smuggling Auditor review remains
required.

## Mathematical Fusion

For \(G:A_E\to A_{E'}\), define
\[
\operatorname{RelPres}_{E,E'}(G)
\Longleftrightarrow
\bigl(z\sim_E z'\Rightarrow Gz\sim_{E'}Gz'\bigr)
\]
and define reflection by the reverse implication. Preservation is equivalent
to a unique factorization
\[
\chi_{E'}G=\bar G\chi_E,\qquad \bar G:K_E\to K_{E'}.
\]
Under that factorization, reflection is equivalent to injectivity of
\(\bar G\).

A chain map \(F:E\to E'\) maps cycles to cycles and boundaries into
boundaries. Hence its degree-one restriction always preserves the relation,
and its induced quotient map is \(H_1(F)\). It reflects the relation exactly
when \(H_1(F)\) is injective, equivalently when
\[
(F_1|_{A_E})^{-1}(B_{E'})=B_E.
\]

A chain isomorphism remains a sufficient certificate because it induces a
homology isomorphism. It is not necessary. In the exact four-state witness,
\(F_1\) collapses the boundary direction \(a\) but fixes the surviving class
\([b]\); it is noninvertible while inducing identity on \(H_1\), with zero
mismatches across all 16 ordered relation tests.

## Fail-Closed Fusion

The v2 rule rejecting reflection for every noninvertible chain map is removed.
The repaired rule distinguishes:

- proved noninjectivity of the induced quotient map, which falsifies
  reflection;
- injectivity not yet established, which withholds the reflection
  certificate; and
- whole-chain noninvertibility, which is inconclusive.

Failure closes only the claimed map or candidate instance. It does not
establish a general EqSrc no-go or future source-extension impossibility.

## Preserved v2 Core

The quotient-label versus kernel-pair distinction, exact translation theorem,
linear stabilizer criterion, corrected \(e_2\) witness, ambient \(L\) contrast,
conditional quotient uniqueness, and finite boundary-move robustness remain
valid under their stated hypotheses. Cross-complex pointwise label fixing is
not introduced because it needs a separately supplied codomain comparison.

## Provenance and Physical Scope

Both \(E\) and \(E'\) are supplied candidate source packages. The prime does
not denote a target manifold. Algebraic naturality is not physical covariance,
relation reflection is not physical reversibility, a chain isomorphism is not
an adopted physical symmetry, and \(H_1\) labels are not established
observables.

Current ontology does not derive the chain-package family, field, truncation,
differentials, homology degree, homology discriminator, boundary variations,
or category of physically admissible chain maps. The shared differential is a
transparent mathematical root, not an independent physical-selection result.

## Parent Decision

The exact typing and reflection-scope defect is claimed repaired in v3,
pending independent audit. The chain-homology route remains `not_frozen`
because this packet adds a falsifiable cross-complex theorem and exact
noninvertible witness rather than repeating v2. Repeating v3 without fresh
audit or new mathematical payload would be orbital and require freeze review.
The earlier quotient-table freeze remains intact.

The next lawful packet is one fresh bounded
`smuggling-auditor@0.2.0` review of exact v3. Refuter stress may not bypass
that audit.

## Preserved Claim Boundary

No canonical ontology edit, source-law adoption, physical-admissibility or
physical-covariance result, general `EqSrc`, RetainH, GenH, or `M_src`
adoption, Distance-to-GR or metric-use ledger change, `g_eff`, matter coupling,
Einstein equations, benchmark promotion, Gate Chair verdict, completed
derivation, future source-extension impossibility, program-wide no-go, or
global theory rejection follows.
