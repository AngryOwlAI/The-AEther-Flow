# EqSrc Orientation-Torsor Descent-Law Smuggling Audit Receipt

- Task: `RT-20260718-040`
- Job: `AJ-RT-20260718-040-001`
- Decision: `DDR-20260718-040`
- Candidate: `EqSrcOrientationTorsorDescentLaw_src^cand,v1`
- Candidate hash: `81b24c69403c304889a94caec8b2a99a6a360a49f40266764da4cfd3ba4edd5f`
- Route: `ontology-law-research-packet`
- Trigger: `derivation_critical_missing_source_law`
- Milestone: `source_equivalence_eqsrc`
- Audit result: `conditional_formal_audit_pass_with_associated_state_retyping_equivariant_line_nonuniqueness_and_variation_nondiscrimination`
- Source-extension classification: `new_ontology_primitive_candidate`
- Adoption: `blocked_adoption_open_continuation`
- Physics progress: `candidate_audited_pending_stress`

## Verified

- The displayed dependency graph contains no explicit target or
  process-authority premise.
- The associated vector operations, line, relation, cocycle, and exact fibre
  pullback are well defined under the stated proposal assumptions.
- No section is used to define the associated object; a section enters only
  through an explicit trivialization back to the original state space.
- Strict scalar non-descent on the original common state space remains true.
- Exactly three nonzero equivariant line families descend to three distinct
  nonzero lines; equivariance does not select the candidate line.
- The source block exchange fixes all eight orientation-state representatives
  at the level of their four associated classes.
- All four translations preserve all three line-kernel relations; the two
  constant-one controls are nonselective.
- `GL(2,2)` has six elements, `AGL(2,2)` has 24, and each line relation has
  eight affine stabilizers and 16 affine changers; each quotient-label fixer
  and affine line-subset stabilizer has order four.
- The nontrivial constant-one translation fixes quotient labels only for the
  diagonal line and swaps them for both coordinate lines.
- Every one-point transfer tested externally between nonempty both-odd blocks
  exits the both-odd domain. These transfers are not admitted candidate
  morphisms.

## Interpretation guard

Associated-state descent is a codomain and identity retyping, not one selected
relation on the original common source-state space. The unordered partition,
its acquisition provenance, the parity domain, physical morphisms, and
physical variations remain explicit but underived source-extension data.
The validator's orientation-zero representative is an enumeration gauge, not
source-canonical data.
Partition-preserving functoriality is not physical covariance, and the
constant-one affine control does not select or physically justify the
candidate relation.

## Not established

This audit does not establish source-only acquisition of the partition,
physical admissibility, covariance, general EqSrc, exact-GR recovery,
downstream GR objects, benchmark promotion, or ontology adoption.

## Routing

One bounded `refuter@0.2.0` ontology-law research packet is recommended against
the unchanged exact candidate. Repeating this audit without new mathematical
payload is frozen. No human gate is required to run the stress packet; a human
Gate Chair remains required before adoption.
