# Parent Fusion Notes: EqSrc Orientation-Torsor Descent-Law Smuggling Audit

## Scope

These notes fuse the independent Physicist-Mathematician and
Physicist-Philosopher reviews of exact proposal-only
`EqSrcOrientationTorsorDescentLaw_src^cand,v1`, hash
`81b24c69403c304889a94caec8b2a99a6a360a49f40266764da4cfd3ba4edd5f`.
The packet has no repair, Refuter-stress, adoption, canonical-ontology, or
promotion authority.

## Consensus

The displayed dependency tree is source-factorized for the expanded proposal
signature. No target, benchmark, detector, workflow, registry, role,
validator, handoff, checkpoint, or generated derivative is a mathematical
premise. The associated vector space, associated line and relation, cocycle,
fibre pullback, strict scalar non-descent, and scoped no-natural-section
results are mathematically correct under the candidate assumptions.

No hidden section is used in the quotient construction. A section is required
only to identify the associated object with the original common state space,
and the candidate marks that trivialization explicitly. Consequently the
formal result is associated-state descent, not strict scalar descent on the
original state space.

The pass is conditional. Current ontology does not derive the unordered
bipartition, its acquisition provenance, the both-odd domain, the block-span
model, a physical morphism category, or a physical variation law. A final
source-record declaration cannot distinguish source-derived from
target-guided acquisition.

## New Mathematical Payload

The two-dimensional \(\mathbb F_2\) fibre has three nonzero proper lines. Each
choice over one torsor orientation determines a unique \(C_2\)-equivariant
two-fibre line family, and all three families descend to distinct lines in the
four-state associated space. Equivariance, cocycle consistency, and descent
therefore do not select the candidate family.

On the symmetric two-point record, the source block exchange maps
\((o,a)\) to \((\sigma o,c a)\), which is the same associated class.
The induced map is identity on all four classes. Associated functoriality
therefore quotients away the main exchange challenge and cannot discriminate
the selected line.

All translations preserve every difference-kernel relation. The candidate's
two constant-one translations preserve all three line relations, while the
full four-element translation group does likewise. For each line, 8 of the 24
affine bijections preserve its relation and 16 change it. The declared control
is independent in definition order but nonmaximal and nonselective. The
quotient-label fixer and affine line-subset stabilizer each have order 4. The
constant-one translation fixes quotient labels only for the diagonal line and
swaps them for either coordinate line, so it does not distinguish the selected
coordinate family from its reverse.

Finally, every one-point transfer tested externally between nonempty both-odd
blocks produces even-even cardinalities and exits the domain. Such transfers
are not admitted candidate morphisms; they expose a robustness burden for any
future, independently derived source-variation category. The internal affine
state controls do not test that burden.

The parity branches also sharpen the failure scope: odd--even cardinalities
point the torsor and make the opposite exact fibre bottom, while even--even
cardinalities admit neither exact fibre. Without independently sourced
partition data, \(\mathrm{GL}(2,2)\) acts transitively on the three lines.
Only the zero and full subspaces are invariant linear subspaces. An additional
invariant set partition \(\{0\}\mid(A\setminus\{0\})\) exists only outside the
candidate's explicitly linear-congruence scope.

## Physical and Ontological Boundary

The associated construction is a valid twisted codomain. It is not a relation
selector on the original common source states and does not derive the
partition primitive. Partition-preserving functoriality is covariance of a
decorated presentation, not covariance under an independently derived
physical gauge category. The constant-one controls are algebraic, not an
established physical variation class.

The candidate remains `proposal-only`, `source-extension data`,
`new_ontology_primitive_candidate`, and
`blocked_adoption_open_continuation`. General EqSrc, exact-GR recovery,
downstream GR objects, and both authoritative ledgers remain unchanged.

## Freeze and Next Route

The exact prior scalar cycles remain locally frozen. Repeating this associated
construction or audit without new payload is frozen. The route remains open
for exactly one materially distinct `refuter@0.2.0`
`ontology-law-research-packet` against the unchanged candidate.

The stress should test the three equivariant descended lines,
source-exchange trivialization, internal complement, associated-state
substitution versus the original EqSrc burden, parity-domain fragility,
full-symmetry scope, partition provenance, and independent physical variation
selection. It may not repair, adopt, or promote the candidate.
