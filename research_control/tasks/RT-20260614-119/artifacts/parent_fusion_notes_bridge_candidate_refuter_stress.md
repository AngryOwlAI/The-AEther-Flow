# Parent Fusion Notes: Bridge Candidate Refuter Stress

Task: `RT-20260614-119`

The mathematical child and philosophy child agree on the central result:
`M_src^{GSC-cand}(E)` survives the named Refuter stress branches only as
fail-closed draft/control bridge-candidate data. The survival is not `M_src`
adoption, not full `M_src` construction, and not a downstream GR unlock.

## Fused Stress Result

The fused output records `candidate_stress_passed_pending_gate`.

The candidate remains stable under the claim boundary because every tested
malformed branch either:

- remains candidate-only source syntax;
- returns `Bottom_src`;
- blocks adoption until a separate proof or certificate exists; or
- fails to supply authority for downstream promotion.

## Preserved Limitations

- `FVR_src^GSC` remains unadopted.
- `RegSold_src^GSC` remains unadopted.
- `M_src` remains unadopted.
- `g_eff`, matter coupling, Einstein equations, and benchmark promotion remain
  blocked.
- Any `M_src` adoption review requires explicit human-gated authority.

## Next Boundary

Autonomous physics continuation on this candidate stops at the human-gated
adoption boundary. The implementation plan's later metrics-separation work is
project-system work and should be routed separately through
`/improve-project-system`.
