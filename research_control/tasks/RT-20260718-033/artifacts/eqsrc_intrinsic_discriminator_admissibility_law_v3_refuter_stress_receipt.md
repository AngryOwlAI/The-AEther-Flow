---
authority: science-draft
status: draft/control
task_id: RT-20260718-033
agent_job_id: AJ-RT-20260718-033-001
related_source: TEX-EQSRC-INTRINSIC-DISCRIMINATOR-ADMISSIBILITY-LAW-V3-REFUTER-STRESS
---

# Intrinsic-discriminator v3 Refuter stress receipt

The exact Refuter classification is `scoped_obstruction`.

The finite calculation fixes `C2=F2`, `C1=F2^2`, `C0=0`, and
`partial_1=0`, then checks all four values of `partial_2(f)`. Every choice is a
valid chain package, and the four choices produce four distinct relations:
equality for the zero image and the three distinct two-class congruences for
the three nonzero images.

Changing `partial_2(f)` from zero to `a` changes four ordered-pair decisions.
The identity on `C1`, together with zero maps in degrees two and zero, is a
chain map from the zero-differential package to the `a` package. It preserves
the source equality relation but does not reflect the destination coset
relation; its induced `H1` map is the noninjective quotient. This agrees with,
rather than refutes, the audited v3 chain-map theorem.

For any finite `F2` vector space `A` and subspace `B`, taking `C2=B`,
`C1=A`, `C0=0`, and `partial_2` equal to inclusion realizes congruence modulo
`B`. This shows representational breadth but supplies no selector among
subspaces or differentials.

The nontrivial `a` package has four states, two classes of size two, and a
two-element declared translation group. Nontrivial source-only instantiability
therefore passes. The relation classes are exactly the declared
boundary-translation orbits, so finite robustness under those moves also
passes exactly. Because the supplied differential determines both the
relation and the move group, that robustness is construction-relative rather
than an independent physical-admissibility result.

Mistyped maps lie outside the typed theorem domain. Returning `Bottom` for
them is a meta-level rule unless a tagged error carrier and total typed
operation are supplied. This is a scoped interface formalization gap, not a
contradiction of the Set, linear, or chain-map theorems.

Current ontology does not derive the chain package, differential, homology
discriminator, physical morphism category, or independently justified
variation class. Exact v3 therefore cannot by itself discharge general EqSrc
or establish physical robustness. It remains `proposal-only` with
`blocked_adoption_open_continuation`.

No canonical ontology edit, source-law adoption, candidate repair, general
EqSrc discharge, downstream GR promotion, ledger change, global no-go, or
future source-extension impossibility follows.

The distinct next packet is one bounded Theoretical Continuation Selector
decision among a source-side selector primitive, source-side irrelevance
theorem, distinct scoped no-go question, or local freeze of this exact v3
line.
