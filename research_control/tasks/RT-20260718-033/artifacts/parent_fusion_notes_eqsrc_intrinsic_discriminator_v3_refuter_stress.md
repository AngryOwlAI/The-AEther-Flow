---
authority: science-draft
status: draft/control
task_id: RT-20260718-033
agent_job_id: AJ-RT-20260718-033-001
---

# Parent fusion notes: intrinsic-discriminator v3 Refuter stress

The children agree that exact v3 is internally coherent once a valid finite
chain package is supplied. The stress does not attack the audited quotient
factorization theorem, its linear specialization, or the induced-homology
reflection criterion.

The decisive finite result changes the supplied differential while holding
the carrier spaces fixed. With
`C2=F2<f>`, `C1=F2<a,b>`, and `C0=0`, each choice
`partial_2(f)` in `{0,a,b,a+b}` is a valid chain package. The four choices
produce four distinct equivalence relations. A rank-one change from zero to
`a` changes equality into coset equivalence modulo `<a>`.

More generally, every linear congruence on a finite `F2` vector space is
realizable: for a subspace `B` of `A`, take `C2=B`, `C1=A`, `C0=0`, and the
inclusion as `partial_2`. This is a representation theorem, not a selector
theorem. The candidate relation classes are exactly the orbits of translations
by `B`, so robustness under declared boundary moves is exact but
construction-relative.

The malformed-map branch is also narrowed. Mistyped maps are not members of
the typed theorem domain. A `Bottom` return can be internalized only after
supplying a tagged error carrier and typing the operation into it; otherwise
it is a meta-level rejection rule. This does not contradict any typed theorem.

The exact classification is `scoped_obstruction`: current ontology does not
derive a rule selecting the chain package, differential, homology
discriminator, physical morphism category, or independently justified
variation class. This blocks use of exact v3 alone as a current-ontology-derived
robust EqSrc selector. It does not refute the proposal as finite algebra,
reject the global ontology, or prove future source-extension impossibility.

The next packet should be one bounded Theoretical Continuation Selector
decision, not another v3 construction, audit, or stress cycle.
