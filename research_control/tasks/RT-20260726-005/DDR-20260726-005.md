---
authority: control
status: completed
decision_id: DDR-20260726-005
task_id: RT-20260726-005
agent_job_id: AJ-RT-20260726-005-001
director_version: director-of-research@0.3.0
decision_type: task_overlay
selected_role_id: ontology-formalizer
selected_role_version: 0.2.0
supersedes_decision_id: DDR-20260726-004
requires_human_gate: false
---

# DDR-20260726-005: execute only v21 P6-T03

## Decision

Activate one `ontology-formalizer@0.2.0` task overlay for immutable generation
124 and execute only P6-T03. The packet tests whether the complete P6-T02
one-ray characteristic datum fixes a nondegenerate Lorentzian conformal class.
It must return a reconstruction theorem or a precise candidate-scoped
obstruction while leaving scale open.

## Evidence

The repository began clean at committed checkpoint
`2c5d8ec75f4c921065945309a77270ea4c4affde`. `handoff-0878`, its passing
ordinary-route guard, immutable generation 124, the v21 plan, the refreshed
memory preflight, the registered P6-T02 candidate, canonical target-side
geometry dictionary, historical scoped `g_eff` candidate and Gate Chair
record, metric-use ledger, and Distance-to-GR burden map uniquely select this
packet. The P6-T02 dependency is complete at that checkpoint.

## Role-Fit Matrix

| Candidate role | Fit | Reason |
|---|---|---|
| `ontology-formalizer@0.2.0` | selected | P6-T03 requires typed source inputs, a conformal reconstruction theorem or exact obstruction, assumption controls, and missing-law identification. |
| `candidate-constructor@0.2.0` | not selected | The immediate question is whether existing one-ray data select conformal geometry, not construction of an unapproved replacement law. |
| `smuggling-auditor@0.2.0` | internal boundary only | Target-import and metric-overread checks are required, but the routed work item is a mathematical formalization. |
| `gate-chair@0.2.0` | blocked | No protected ontology, source-law, physical-causality, or metric adoption decision is requested. |

## Selected Role

Activate one `ontology-formalizer@0.2.0` task overlay and one outer AgentJob.
The internal physicist-mathematician perspective independently proves the
hyperplane/null-cone mismatch and explicit nonconformal comparison family.
The internal physicist-philosopher perspective audits typed-object,
physical-interpretation, scale, historical `g_eff`, and conservative
continuation boundaries. The parent resolves their six tensions without
creating child AgentJobs or subagents.

## Result

The packet proves two linked statements.

First, `Ann(V_x)` is a three-dimensional linear hyperplane and cannot equal a
Lorentzian null cone: polarization would make it a three-dimensional totally
isotropic subspace, whereas a Lorentzian form has Witt index one.

Second, in flow-box coordinates with `V = partial_s`, every

`g_H = -ds^2 + H_ij dy^i dy^j`

for positive-definite `H` realizes the same oriented timelike ray,
annihilator, and forward flow preorder. The pair
`H_0 = diag(1,1,1)` and `H_1 = diag(2,1,1)` is not conformally related and has
different null cones. Thus the complete one-ray input admits infinitely many
conformal outputs.

The precise disposition is
`OBST-P6T03-ONE-RAY-CONFORMAL-NONSELECTION-001`. It is scoped to reconstruction
from only the P6-T02 datum. It is not a global no-go theorem.

## Claim boundary and continuation

Current ontology does not derive the missing full cone, quadratic-form, or
transverse conformal-shape law, nor its compatibility, covariance, and
universality selectors. Adoption remains
`blocked_adoption_open_continuation`: a materially richer source-side law or
distinct multi-sector uniqueness theorem may reopen the milestone.

P6-T03 does not construct physical causality, a conformal metric, or metric
scale. It does not expand the historical scoped `g_eff` object, import target
geometry, couple matter, derive Einstein equations, promote the exact-GR
benchmark, authorize proof or publication, push, globally reject the theory,
or close future source extensions.

## Next bounded route

After one governed checkpoint, P6-T04 may test source-defined operational or
measure structures for the separate scale and calibration burden. It must
carry this conformal nonselection result as a hard input and may not treat a
conformal class as already constructed.
