<!-- authority: project_control -->

# P6-T03 one-ray conformal-insufficiency receipt

Task `RT-20260726-005` executes only v21 work item `P6-T03` under immutable
relay generation 124 and one `ontology-formalizer@0.2.0` outer AgentJob.

## Result

The task proves two linked candidate-scoped results.

1. The P6-T02 characteristic set `Ann(V_x) \ {0}` is a three-dimensional
   linear hyperplane and cannot be the quadratic null cone of a nondegenerate
   Lorentzian inverse form.
2. Even if the oriented P6-T02 ray is favorably stipulated to be future
   timelike, the family `g_H = -ds^2 + H_ij dy^i dy^j` has identical ray,
   annihilator, and flow preorder for every positive-definite `H`, while
   distinct choices of `H` need not be conformally related.

The separating pair `H_0 = diag(1,1,1)` and
`H_1 = diag(2,1,1)` gives an explicit uniqueness failure. Zero-field,
chosen-null-ray, disconnected-component, and multiple-sector controls are
also recorded.

## Disposition

The decisive result is
`OBST-P6T03-ONE-RAY-CONFORMAL-NONSELECTION-001`. It applies only to a
reconstruction whose complete input is the P6-T02 one-ray characteristic
datum. Current ontology does not derive the additional full cone,
quadratic-form, transverse conformal-shape, compatibility, covariance, or
universality law needed to select a conformal class.

Adoption is `blocked_adoption_open_continuation`. A materially richer
source-side law or theorem can reopen the same milestone. Overall metric
scale remains explicitly open.

## Authority

No canonical ontology or source-law adoption, physical time or causality,
effective metric, historical scoped `g_eff` expansion, matter coupling,
Einstein equations, benchmark promotion, Gate Chair verdict, global no-go,
future-extension impossibility, proof publication, push, or completed
derivation is authorized. Validator and checkpoint results are operational
evidence only.
