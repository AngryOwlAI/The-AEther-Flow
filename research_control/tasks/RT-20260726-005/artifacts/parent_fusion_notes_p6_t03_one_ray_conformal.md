<!-- authority: science_draft -->

# P6-T03 parent fusion: one-ray conformal nonselection

The single outer AgentJob combines two internal analytical perspectives. The
physicist-mathematician perspective proves the characteristic-hyperplane
mismatch and constructs the explicit nonconformal `g_H` family. The
physicist-philosopher perspective audits type boundaries, physical
interpretation, scale, historical `g_eff` scope, and conservative
continuation.

## Shared result

The complete reconstruction input under test is only
`P_x(xi)=xi(V_x)`, `Ann(V_x)`, the oriented ray generated by `V_x`, and the
forward flow preorder. This input does not select a Lorentzian conformal
class. The annihilator is a linear hyperplane rather than a Lorentzian null
cone, and in flow-box coordinates every
`g_H=-ds^2+H_ij dy^i dy^j` with positive-definite `H` shares the same
one-ray datum. The explicit choices `diag(1,1,1)` and `diag(2,1,1)` are not
conformally related and have different null cones.

## Preserved tensions

Six tensions are resolved without flattening either perspective:

1. a future-timelike stipulation strengthens the countermodel but does not
   establish physical causality;
2. hyperplane/null-cone mismatch is exact but candidate-scoped;
3. the comparison metrics are mathematical witnesses, not adopted geometry;
4. one-ray nonselection freezes only the unchanged P6-T02 input family;
5. P6-T04 must not assume a conformal class while testing scale or
   calibration structures; and
6. the historical scoped `g_eff` object retains its prior status and cannot
   be expanded by this packet.

## Decisive disposition

The no-fog result is the precise obstruction
`OBST-P6T03-ONE-RAY-CONFORMAL-NONSELECTION-001`. Current ontology does not
derive the missing full cone, quadratic form, transverse conformal shape, or
multi-sector uniqueness rule. Adoption is
`blocked_adoption_open_continuation`: materially richer source-side data or a
distinct theorem may reopen the milestone.

No canonical ontology or source-law adoption, physical time or causality,
conformal metric or scale, target import, `M_src` or unscoped `g_eff`
reconstruction, matter coupling, Einstein equations, benchmark promotion,
global no-go, future-extension impossibility, proof publication, push, or
completed derivation is authorized.
