<!-- authority: draft/control -->

# P9-T05 parent fusion: source cosmology decoder nonselection

The mathematical perspective derives the exact proposal-only amplitude
solution

\[
A(\lambda)=\frac{A_0}{\sqrt{1+2\gamma A_0^2\lambda}}
\]

and the positive decoder counterfamily

\[
b_p=(A_0/A)^p,\qquad
\mathcal E_p=p\gamma A^2,\qquad
\mathcal Q_p=2/p-1 .
\]

It also derives the bookkeeping identity
\(\rho_{p,r}=M_0b_p^{-r}\) and
\(d\rho_{p,r}/d\lambda=-r\mathcal E_p\rho_{p,r}\).
Distinct \(p\) and \(r\) values give distinct exact histories from the same
source amplitude record.

The interpretive perspective fixes the physical boundary. One global amplitude
is single-mode source uniformity, not a physical homogeneous and isotropic
space. The update parameter is not a physical clock. The scale, volume, and
density tokens have no selected model-to-world map. The absence of an
independent constant in the proposal-only amplitude equation does not imply a
vanishing physical vacuum contribution.

The fused no-fog result is
`OBST-P9T05-SOURCE-COSMOLOGY-DECODER-CALIBRATION-NONSELECTION-001`: the current
source package does not derive a total map from its amplitude history to a
unique physical scale, clock, volume, density, curvature, and constant-term
interpretation. The P9-T05 case is `INCONCLUSIVE` with secondary label
`FORMAL_ANALOGY`, not `PASS`.

The source files were sealed before the typed target comparison. No target
equation, desired agreement, tolerance, or branch selected \(p\), \(r\), a
clock calibration, or a rerun. Same-context validation is not independent
reproduction.

This is a current-source obstruction with
`blocked_adoption_open_continuation` status. It is not a global no-go theorem;
conservative source extensions remain open. No canonical ontology, physical
cosmology, effective geometry, Einstein equation, exact-GR recovery, benchmark
promotion, proof, publication, push, or completed-derivation authority follows.
