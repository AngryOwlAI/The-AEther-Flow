# EqSrc Source-Rooted Quotient Selector Smuggling Audit Receipt

## Result

`RT-20260718-024` independently audits exact proposal-only
`EqSrcSourceRootedQuotientSelectorCandidate_v1`.

The exact result is
`formal_source_purity_pass_with_primitive_selection_and_admissibility_limits`.
Every operative value factors through the displayed source roots
`(tau_i, G, H, s)`, and no hidden target, opaque oracle, or workflow-authority
premise is detected. The roots themselves are not derived from current
ontology.

## Qualification

The four-entry profile `s` is a transparent selector-bearing primitive. Its
displayed values prevent an opaque-lookup overread, but the endpoint tokens
and object labels do not derive the table. The proposal supplies no
current-ontology, naturalness, uniqueness, physical, or empirical selection
principle for `G`, `s`, or the admissible variation class.

The exact-v3 lifting passes conditionally under the intended two typed
inverse-cancellation schemes. A strict formal translation must make those
schemes and the evident primitive-leaf equivalences explicit.

## New Mathematical Payload

The audit supplies:

- a profile-enumeration result: 24 bijective profiles realize all three
  two-plus-two partitions, eight profiles per partition;
- a bare-set symmetry obstruction: a permutation-invariant equivalence on
  four unstructured objects is equality or universal;
- an exact stabilizer theorem: 32 of 256 additive profile shifts preserve the
  relation, while `V_H` is the 16-element index-two zero-coset half; and
- counterfactual separation of source factorization, profile selection,
  transported naturality, and physical covariance.

## Stability and Authority

The `V_H` stability theorem is mathematically valid and the candidate
correctly says the class is not maximal. The word admissible is stipulated:
relative answer-preserving stability does not establish physical
admissibility or unrestricted finite-variation robustness.

Transporting the complete decorated datum under a relabeling establishes
reindexing equivariance, not a canonical selector on bare objects or physical
covariance.

No registry row, validator result, role, handoff, checkpoint, generated
derivative, approval, completion state, benchmark result, or Gate Chair state
supplies scientific proof authority. The constant-pass negative-control
branch is a formal device, not empirical validation.

## Source-Extension Classification

The packet is neither a current-ontology derivation nor an established
conservative definitional extension. If adopted, its selector-bearing and
admissibility data would be a new ontology primitive requiring protected
human authority. No adoption is requested.

The candidate remains `proposal-only` with
`blocked_adoption_open_continuation`. The Distance-to-GR ledger is unchanged.

## Next Route

Run one bounded `refuter@0.2.0` stress packet against the exact candidate. It
should test profile-reassignment nonuniqueness, provenance of the primitive
table, independence of physical admissibility from selector preservation,
relation-changing source variations, and the boundary between decorated
naturality and physical covariance.

No human gate is required for stress. Refuter passage would still not
authorize adoption.

## Forbidden Overreads

This receipt does not authorize canonical ontology modification, source-law
adoption, current-ontology derivation, conservative extension, general
`EqSrc`, RetainH, GenH, or `M_src` adoption, Distance-to-GR ledger change,
physical admissibility, unrestricted robustness, physical covariance,
`g_eff`, matter coupling, Einstein equations, benchmark promotion, Gate Chair
verdict, completed derivation, future source-extension impossibility,
program-level no-go, or global theory rejection.

## Validation

Task-local validator:

```text
.venv/bin/python research_control/tasks/RT-20260718-024/artifacts/validate_eqsrc_source_rooted_quotient_selector_smuggling_audit.py --write-report --json
```

Expected status: `PASS`.
