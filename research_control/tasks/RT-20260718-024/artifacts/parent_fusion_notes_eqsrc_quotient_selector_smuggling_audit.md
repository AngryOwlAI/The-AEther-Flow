# Parent Fusion Notes: EqSrc Quotient Selector Smuggling Audit

## Synthesis

The independent mathematical and philosophy-of-physics audits converge without
a blocking conflict. Exact proposal-only
`EqSrcSourceRootedQuotientSelectorCandidate_v1` is formally source-factorized
through its displayed roots `(tau_i, G, H, s)`. No target or workflow datum is
a positive premise.

The fused result is
`formal_source_purity_pass_with_primitive_selection_and_admissibility_limits`.
It advances only this exact proposal to
`candidate_audited_pending_primitive_selection_stress`. It does not establish
current-ontology derivation, physical selection, adoption, or general `EqSrc`.

## Dependency and Provenance Finding

The selector branch is `G,H -> q`, `F_src,s,q -> q composed with s`,
`q composed with s -> Sel_H`, and `Sel_H -> L_AB`. A separate provenance
branch begins at endpoint tokens and generates the exact-v3 term algebra.
These branches meet only in certificate acceptance. In particular, no
displayed map sends `tau_i` to `s(A_i)`.

All downstream values factor through the declared root tuple and are invariant
when target or workflow coordinates vary with that tuple fixed. This is a
formal source-purity pass for the exact proposal. It is not a derivation of
the roots: `G`, `H`, and the four-entry profile table are new source-extension
data.

The profile table is transparent, not operationally opaque. Its values are
displayed and the candidate explicitly denies that object subscripts derive
them. Transparency blocks hidden-authority laundering, but it does not supply
naturalness, uniqueness, current-ontology origin, or physical necessity.

## Fused Mathematical Findings

Three complementary results make the selection burden exact.

- The 24 bijective profiles from four objects to `Z4` realize all three
  two-plus-two matchings, with eight profiles per matching. Thus the fixed
  tokens, group, and subgroup do not select the candidate's pairing.
- An equivalence relation on a bare four-element set that is invariant under
  every permutation is either equality or universal. A nondegenerate
  two-plus-two relation therefore needs symmetry-breaking data such as `s`.
- For additive profile shifts `r`, the pairwise selector is unchanged exactly
  when `q composed with r` is constant. The full relation stabilizer has 32
  elements. `V_H` has 16 elements and is its index-two zero-coset half.

The candidate's relative `V_H` stability, its qualification that `V_H` is not
maximal, and its relation-changing countervariation are correct. Calling
`V_H` admissible remains an underived proposal choice. Stability under a
class constructed from the same kernel as the selector cannot by itself show
that the class is physically admissible.

The exact-v3 finite lifting passes conditionally under both typed
inverse-cancellation schemes. A strict translation should also define
`tau_{A_i}:=tau_i` and display the unique evident primitive-leaf
equivalences. These are nonblocking formalization qualifications at the
displayed cardinalities.

## Naturality and Authority Finding

Transporting the profile and all decorated endpoint data along a bijection is
valid reindexing equivariance. `H`-preserving automorphisms likewise preserve
the quotient test. Neither result selects the profile or establishes
spacetime, Lorentz, gauge, observer, or empirical covariance.

No target atlas, metric, reconstruction, detector, benchmark, registry,
generated derivative, role, validator, handoff, checkpoint, approval,
completion state, or Gate Chair status supplies a scientific root. The
singleton constant-pass negative-control branch is a formal model device, not
empirical validation.

## Parent Decision

The candidate is not a derivation from current ontology and no conservative
definitional-extension theorem is supplied. If adoption were requested, its
profile and admissibility structure would be a new ontology primitive
requiring protected human authority. No adoption is requested here, so the
status remains `proposal-only` and
`blocked_adoption_open_continuation`.

The route is not frozen because this audit adds the profile nonuniqueness,
bare-set symmetry, and exact stabilizer results. The next lawful packet is one
bounded `refuter@0.2.0` stress test of the exact candidate, emphasizing:

- profile-reassignment and pairing nonuniqueness;
- semantic provenance of the extensional profile table;
- independence of a physically admissible variation class from the selector
  it preserves;
- robustness under relation-changing source variations; and
- the distinction between decorated transport equivariance and physical
  covariance.

The Refuter packet may not repair or adopt the candidate. A protected human
gate remains required before any adoption.

## Preserved Claim Boundary

No canonical ontology edit, source-law adoption, current-ontology derivation,
conservative extension, general `EqSrc`, RetainH, GenH, or `M_src` adoption,
Distance-to-GR ledger change, physical admissibility, unrestricted
finite-variation robustness, target metric, `g_eff`, matter coupling,
Einstein equations, benchmark promotion, Gate Chair verdict, completed
derivation, future source-extension impossibility, program-level no-go, or
global theory rejection follows.
