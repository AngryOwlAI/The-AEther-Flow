---
object_id: "MD-V22-P4-T02-B2-PARENT-FUSION-SOURCE-DYNAMICAL-VIABILITY-ADMISSIBILITY-SELECTOR-V1"
authority: "science_draft"
status: "draft/control"
task_id: "RT-20260811-003"
job_id: "AJ-RT-20260811-003-001"
---

# Parent fusion notes — P4-T02 B2 source-dynamical viability selector

The parent fuses two independent physics analyses with no unresolved blocking
conflict. Both support one decisive result: the
`SignedCubicViabilitySelector_v1` is a mathematically complete proposal-only
source-law candidate under its displayed hypotheses, pending one separately
admitted Smuggling Auditor review. It is not derived from current ontology and
is not adopted.

The fused construction works on the registered proposal-only cubic amplitude
line. It adds four disclosed roots fixed before diagnostics: `gamma>0`, a
bounded-step horizon `Delta>0`, a positive representative `a_star>0` of a
nonzero amplitude-reference magnitude, and an orientation token
`sigma in {+1,-1}`. The selected component is
`U_sigma={a:sigma*a>0}`; the bounded-step relation contains the cubic-flow
successors through times in `[0,Delta]`; and the normalized certificate is
`b_sigma(a)=sigma*a/sqrt(a_star^2+a^2)`.

The exact fused theorem is

`b_sigma(phi_t(a)) >= lambda*b_sigma(a)>0`,

for every `a in U_sigma` and `0<=t<=Delta`, with the sharp uniform factor
`lambda=(1+2*gamma*Delta*a_star^2)^(-1/2)`. Iteration proves positivity along
every finite admitted path, and sign preservation gives the exact universal
finite-path identity `Viab_RDelta(U_sigma)=U_sigma`. The margin decays toward
zero over arbitrarily long paths, so the theorem does not assert a positive
infinite-horizon robustness margin.

The parent preserves the Physicist-Mathematician's exact algebra, rational
controls, and two precision corrections. Under simultaneous changes
`a'=d*a`, `a_star'=d*a_star`, `t'=c*t`, and `Delta'=c*Delta`, the correct
coefficient is `gamma'=gamma/(c*d^2)`. This preserves both dimensionless
products in the candidate. Since only `a_star^2` occurs, negative
`a_star` is equivalent to `|a_star|`; positivity is a unique-representative
convention, while only `a_star=0` makes the printed normalization singular at
the origin.

The parent also preserves the Physicist-Philosopher's explanatory boundary.
Sigma performs all component-identity selection and remains an underived
constructive preload. Removing it leaves both reflection-related nonzero
components invariant and viable. Reflection transports the token-indexed
candidate at `sigma` to the candidate at `-sigma`, but selects neither token,
proves no physical gauge equivalence, and supplies no spontaneous-symmetry-
breaking law. Delta and `a_star` change certificate granularity and
normalization, not the selected component.

Component selection is not point occurrence. The construction produces no
initial amplitude, probability measure, empirical frequency, physical time
orientation, causal cone, response map, conformal class, scale, or effective
metric. The source syntax is target-free as written, but an Ontology
Formalizer cannot turn that observation into a formal source-purity verdict.
The next packet is therefore
`PKT-V22-P4T02-B2-SIGNED-CUBIC-VIABILITY-SELECTOR-SOURCE-EXTENSION-SMUGGLING-AUDIT-V1`,
of type `source_extension_smuggling_audit`, under
`smuggling-auditor@0.2.0`. It is selected and not executed here.

All four inherited local freezes remain active and untouched. D7 adequacy is
not reevaluated, B2 is neither activated nor disposed, P4-T03 remains locked,
and all fourteen Distance-to-GR rows remain `no_delta`. No canonical ontology
or source law is modified or adopted, and no Gate, benchmark, proof,
publication, push, external action, global no-go, or completed derivation is
claimed.
