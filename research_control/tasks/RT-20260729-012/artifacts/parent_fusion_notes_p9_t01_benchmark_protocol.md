---
authority: science-draft
status: draft/control
task_id: RT-20260729-012
plan_task_id: P9-T01
artifact_id: ParentFusionNotesP9T01BenchmarkProtocol_v1
---

# P9-T01 parent fusion: source-derived benchmark protocol

## Fused result

The parent accepts both child analyses and resolves all six recorded
differences in one review round. The fused result is
`SourceDerivedBenchmarkProtocol_v1`, a typed case schema, a fail-closed
`TargetImportFirewall_v1`, and a Gate E readiness checklist. All six planned
P9-T02 through P9-T07 cases remain `NOT_RUN`.

The central mathematical payload is `TargetImportFirewallSoundness_v1`.
Under a complete and truthfully classified provenance DAG, a sealed source
output, a preregistered comparator, a target oracle unavailable before the
seal, and immutable case revisions, no forbidden target root can be an
ancestor of the source output. Topological induction establishes invariance
of the source-stage computation under changes to the target roots.

This is a protocol theorem only. It does not show that a future implementation
has no omitted dependency, that a proposal-only source extension follows from
current ontology, that a formal operational token has physical meaning, or
that any source output agrees with GR.

## Shared consensus preserved

Both children require:

- exact source, code, environment, parameter, correction, and provenance
  hashes;
- explicit separation of source execution, source sealing, target-oracle
  reveal, comparison, reproduction, and classification;
- failure on any target atlas, metric, solution, action, equation, boundary,
  parameter, branch, tolerance, or desired agreement reaching source
  computation;
- separate exact analytic-equivalence and numerical-tolerance rules;
- success, failure, invalid, inconclusive, and not-run branches;
- no hidden averaging away of deviations or fatal cases;
- independent reproduction before a decisive physical result; and
- preservation of Gate D `NOT READY` and the absence of Gate E authority.

## Physicist–mathematician contribution

The mathematical child supplied the complete provenance-DAG formalization,
root partition, no-path source-closure condition, seal preimage, post-seal
oracle edge rule, theorem hypotheses and proof sketch, contaminated-code
negative control, analytic/numerical comparison split, and six explicit
downstream case contracts.

The parent preserves these contributions in the TeX theorem, firewall YAML,
case schema, and validator. The fused theorem folds eight fine-grained child
hypotheses into six auditable hypotheses without weakening the completeness,
classification, preregistration, sealing, oracle-order, or immutability
requirements.

## Physicist–philosopher contribution

The philosophical child separated source-model consequences, proposal-only
source extensions, independent calibration, formal operational tokens, target
oracles, post-seal comparison, protocol correctness, physical recovery, and
protected promotion.

The parent adds four controls from that analysis:

1. Target-contaminated code, training data, priors, lookup tables, caches, or
   calibration objects are reclassified as forbidden roots.
2. Every case requires a model-to-world map; absent physical semantics yield
   `FORMAL_ANALOGY` or `INCONCLUSIVE`.
3. Calibration, validation, and held-out cases are partitioned, with
   suite-global parameters frozen before held-out oracles open.
4. Human target knowledge is recorded in a target-exposure ledger, and any
   oracle-informed rerun becomes a new nonblind revision while the original
   result remains preserved.

Independent reproduction is strengthened to require independent code or
method provenance in addition to a fresh environment and no shared
intermediates.

## Current scientific boundary

The fixed P5 through P8 record still supplies no lawful unscoped effective
metric, physical dynamical completion, Einstein equations, controlled
model-to-world semantics, exact-GR recovery, or independent replication.
P9-T01 therefore narrows the benchmark-protocol burden but changes no physical
or Distance-to-GR status.

Gate D remains `NOT READY`. Gate E remains unopened, with two protocol-scope
criteria passing, seven scientific or evidence criteria unmet, and one
protected-authority criterion deferred. No benchmark case is executed, no
benchmark result is established, and no benchmark promotion follows.

## Next bounded route

After a qualifying checkpoint, P9-T02 may attempt the vacuum and Minkowski
case under this protocol using `candidate-constructor@0.2.0`. It must construct
source inputs, produce a sealed output, and return pass, fail, invalid, or
inconclusive evidence without importing Minkowski geometry as a source
premise. Protocol design is not evidence that P9-T02 will succeed.

## Authority boundary

No canonical ontology, reviewed P5 through P8 source, source law, effective
metric, physical dynamics, Einstein equation, exact-GR recovery theorem, Gate
D status, benchmark result, Gate E verdict, proof authority, publication,
push, global no-go, future impossibility, or completed derivation is created
or authorized.
