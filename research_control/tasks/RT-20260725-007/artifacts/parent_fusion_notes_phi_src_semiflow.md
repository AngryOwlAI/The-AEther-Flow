---
authority: science_draft
status: draft_control
task_id: RT-20260725-007
job_id: AJ-RT-20260725-007-001
plan_task_id: P5-T02
---

# P5-T02 parent fusion notes

## Fused result

The two internal perspectives converge on one bounded candidate: `Phi_src` is
tested as a jointly continuous unital forward action of the ordered additive
monoid `(R_ge_0,+,0)` on the P5-T01 admissible source-configuration space
`Q_adm`. The candidate is a topological semiflow in the mathematical sense.
It is `draft/control`, `proposal-only`, `source-extension data`, and
`blocked_adoption_open_continuation`.

The conventional left-action law is

`Phi_(lambda+mu) = Phi_lambda o Phi_mu`.

Because addition is commutative, the fixed-parameter maps commute. That
algebraic fact supplies no physical symmetry or gauge interpretation.

## Mathematical content retained

The fused source requires identity, forward composition, preservation of
admissibility, compatibility with declared source redundancy, and joint
continuity for an explicitly declared source-only topology `T_adm`.

Redundancy compatibility proves that
`bar_Phi_lambda([q])=[Phi_lambda(q)]` is a unique action on the orbit quotient.
Algebraic quotient descent is unconditional under the stated compatibility
law. Joint continuity of the quotient action is asserted only when
`id_Lambda x pi` is itself a quotient map. This explicit hypothesis preserves
the mathematical child’s product-quotient point and the philosophical child’s
fail-closed topology boundary.

The second theorem classifies every continuous additive order-preserving
endomorphism of `R_ge_0` as `r(lambda)=c lambda`, with `c>=0`; it is an
automorphism exactly for `c>0`. Positive rescaling therefore preserves the
action laws without fixing a unit or operational clock.

The continuous witness
`Phi_lambda(x)=max(x-lambda,0)` is jointly continuous and genuinely
noninjective for positive parameter, so the law does not secretly imply a
group. The finite `N_0` specialization shows algebraic iteration and quotient
descent but is not evidence for a unique continuous interpolation or physical
discretization.

## Interpretive boundary retained

The action is on `Q_adm`, not on the P4-T02 source arena. P4-T02 is only an
unadopted proposal precedent. No base-manifold action, bundle lift, target
congruence, or source-point trajectory follows.

The forward parameter orders compositions. It is not established as physical time,
proper time, a clock, causal orientation, thermodynamic arrow, gauge
parameter, or physical history. Scaling freedom is clock-normalization
underdetermination, not physical gauge. Noninvertibility is a mathematical
property, not an entropy law or metaphysical arrow of time.

The quotient removes only declared source redundancy. It is not physical
spacetime, does not carry `g_eff`, and is not a reconstruction. `S_dyn` and
`S_rec` remain open.

## Exceptional cases

Periodic and dense orbits are allowed mathematical behaviors with no physical
period, ergodicity, or clock conclusion. A bi-infinite orbit requires a
separately proved group extension on an invariant subdomain. Branching and
stochastic succession are outside the selected single-valued deterministic
family and require new relation-valued or kernel-valued laws. A discrete
`N_0` action is a specialization, not an automatic replacement for the
continuous mainline.

## Remaining limitation and next burden

The current ontology does not derive this parameter monoid, topology, or
semiflow family, and this packet supplies no uniqueness or physical-preference
theorem. Current adoption is blocked while same-milestone continuation
remains open.

P5-T03 may use this result only as proposal-level input when constructing one
explicit source-dynamics candidate. It must not treat the semiflow as adopted
ontology, a generator, a Hamiltonian, or a physical clock. Metric recovery,
matter coupling, Einstein equations, benchmark promotion, proof publication,
push, and completed derivation remain blocked.
