---
title: "Parent fusion: proposal-only measurable-fiber occurrence-law Refuter stress"
status: "draft/control"
authority: "science_draft"
task_id: "RT-20260816-005"
job_id: "AJ-RT-20260816-005-001"
plan_id: "recommendations_implementation_plan_continue_task-v22"
plan_task_id: "P4-T02"
candidate_id: "CAND-V22-P4T02-B2-PROPOSAL-ONLY-MEASURABLE-FIBER-OCCURRENCE-LAW-V1"
packet_id: "PKT-V22-P4T02-B2-PROPOSAL-ONLY-MEASURABLE-FIBER-OCCURRENCE-LAW-COUPLING-PARAMETER-STATIONARITY-REFUTER-STRESS-V1"
---

# Parent fusion

## Decisive result

The fixed `ProposalOnlyMeasurableFiberOccurrenceLaw_v1` has a
`scoped_obstruction` as a robust natural occurrence-selection law. The
conditional discrete probability construction remains mathematically coherent,
and RT003's narrow `source_pure_as_written` verdict remains fixed. What fails is
the premise that the proposal supplies a source-derived and robustly isolated
choice of coupling, parameter, initial law, realized sample, and clock.

The exact obstruction is
`OB-V22-P4T02-B2-MEASURABLE-FIBER-OCCURRENCE-LAW-ROBUST-SELECTION-EMBEDDABILITY-001`.
It is candidate-local. It is not a global no-go theorem, an ontology rejection,
or a claim that every future source extension is impossible.

## Fixed mathematics preserved

The fusion preserves the full declared proposal-only construction and the
correct RT003 audit boundaries:

1. The locally constant binary fibers are standard Borel on the admitted
   domains.
2. `K_p(x)=p delta_x+(1-p) delta_cx` is normalized and complement-equivariant.
3. The declared restriction, pullback, section gluing, and shared-driver laws
   remain exact within their stated scope.
4. On a nonempty complement orbit,
   `d_TV(K_p(x),K_q(x))=|p-q|`; the empty-open singleton instead gives zero.
5. The token-class coupling interval
   `max(0,2p-1) <= a <= p` is fully restriction-natural and
   complement-equivariant as established in RT003.
6. Exact one-step access to a labeled `K_p` identifies `p`. This internal
   readability does not derive or justify which expansion or `p` the current
   source ontology should select.

The Refuter therefore attacks selection and robustness, not the consistency of
the disclosed written syntax.

## New mathematical payload

### Natural coupling non-isolation

For fixed `p`, write the exchange-symmetric token-class driver law as

`J_a=(a,p-a,p-a,1-2p+a)`.

For any admissible `a,b`, the joint total-variation distance is
`d_TV(J_a,J_b)=2|a-b|`. Since every `p` in `(0,1)` leaves a positive interval
immediately below the shared endpoint `a=p`, the shared-global coupling has
arbitrarily close, distinct, fully natural, same-marginal alternatives. No
source-derived isolating axiom is present.

### Spectrum and finite-lag ambiguity

On a nonempty complement orbit,

`K_p=[[p,1-p],[1-p,p]]`

has eigenvalues `1` and `r_p=2p-1`. Hence

`K_p^n=Pi_+ + r_p^n Pi_-`.

Every even lag is invariant under `p <-> 1-p`, while an exact one-step or
calibrated odd-lag observation would distinguish the two values. The proposal
supplies neither an independently sourced `p` nor a calibrated observational
lag law, so this qualification does not restore source selection.

### Finite homogeneous symmetric CTMC embedding

For `Q_lambda=[[-lambda,lambda],[lambda,-lambda]]`, the nontrivial eigenvalue
of `exp(t Q_lambda)` is `exp(-2 lambda t)`. Therefore a finite homogeneous
symmetric two-state continuous-time embedding exists exactly when `p>1/2`.
At `p=1/2` the required rate-duration product is infinite; for `p<1/2` the
nontrivial eigenvalue is negative and cannot be a finite real exponential.
When embedding exists, only
`lambda t=-(1/2) log(2p-1)` is identified, not the rate and elapsed time
separately.

This theorem is restricted to the stated finite homogeneous symmetric
two-state class. It makes no claim about hidden-state, time-inhomogeneous, or
other enlarged models.

### Stationarity, initial law, and realization

For `p in (0,1)`, a law is stationary under `K_p` exactly when it is
complement-invariant. Stationarity therefore selects neither `p`, an orbit
weight, nor a token. A transition kernel also supplies no initial law,
probability space, sampling map, or realized sample. At the special endpoint
`p=1/2`, one step erases the initial marginal difference, but it still does not
select an initial law or realize an outcome.

Total admission remains formally closed and restriction-natural because it is
the entire fiber. Its characteristic predicate is identically true, so it has
no discriminating admissibility or occurrence force.

## Parent-child synthesis

Both specialist children independently return the same exclusive Refuter
class, obstruction identity, local freeze, and unexecuted successor. There is
no unresolved blocking conflict.

The Physicist-Mathematician contributes the exact coupling metric, spectral
decomposition, lag ambiguity, finite CTMC embeddability theorem, rate-duration
nonidentifiability, and the `p=1/2` initial-law qualification. The
Physicist-Philosopher separates parameter readability from source selection and
distinguishes transition syntax, path law, initial law, realized sample,
empirical frequency, physical probability, and clock meaning. The fused result
preserves both layers.

## Freeze and Distance-to-GR status

All eight inherited freeze labels remain active. This candidate-local
obstruction adds exactly one local freeze:

`NDCL-V22-P4T02-B2-MEASURABLE-FIBER-OCCURRENCE-LAW-ROBUST-SELECTION-EMBEDDABILITY`.

The freeze blocks unchanged replay of this proposal as a standalone robust
occurrence, coupling, parameter, or clock selector. It does not block
diagnostic use or materially distinct future source-law research. All fourteen
Distance-to-GR burdens remain `no_delta`; D7 is not reevaluated, B2 remains
inactive, and P4-T03 remains locked.

## Successor selected, not executed

Exactly one future packet is selected but not created or executed:

`PKT-V22-P4T02-B2-POST-MEASURABLE-FIBER-OCCURRENCE-LAW-REFUTER-THEORETICAL-CONTINUATION-SELECTION-V1`.

It is a `theoretical_continuation_selector` packet for
`theoretical-continuation-selector@0.1.0`. Its future purpose is to choose one
materially distinct, non-promotional continuation while preserving all nine
active freezes and the same claim boundary.

## Authority boundary

This result adopts no source law or ontology and establishes no physical
probability, realized occurrence, empirical frequency, clock, causal response,
cone, conformal geometry, scale, `g_eff`, coupling, Einstein equation,
benchmark, Gate result, proof promotion, publication, push, external action,
global no-go, future-extension impossibility, or completed derivation.
