---
decision_id: "DDR-20260614-117"
task_id: "RT-20260614-117"
director_version: "director-of-research@0.3.0"
decision_type: "task_overlay"
selected_role_id: "candidate-constructor"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260614-117-001"
status: "completed"
supersedes_decision_id: "DDR-20260614-116"
requires_human_gate: false
role_fit_candidates:
  - "director-of-research|rejected|the Director writes the routing record but does not execute the science-draft bridge artifact"
  - "candidate-constructor|accepted|handoff-0155 names one bounded bridge attempt from GenCover_src^GSC toward source_manifold_m_src"
  - "smuggling-auditor|deferred|audit follows only after a bridge candidate or explicit boundary failure exists"
  - "refuter|deferred|stress follows only after audit if the candidate passes conditionally"
  - "ontology-formalizer|rejected|RT-20260614-110 already formalized GenCover_src^GSC and RT-114 adopted only its source-extension-law use"
  - "theoretical-continuation-selector|rejected|the latest handoff names a single Candidate Constructor packet"
  - "gate-chair|rejected|no new human approval exists for adoption, benchmark, closure, or suspension authority"
---

# Director Decision Record DDR-20260614-117

## Current Objective

Create and execute one bounded `candidate-constructor@0.2.0` task overlay
after `RT-20260614-116` and `handoff-0155`.

The packet attempts a bridge from the adopted source-extension-law input
`GenCover_src^GSC` to a source-manifold candidate for the milestone
`source_manifold_m_src`. The output must include the interface table,
no-target-import precheck, proof-obligation matrix, and one decisive
`candidate_constructor_result`.

## Selected Role

The selected role is a task overlay on `candidate-constructor@0.2.0`. The
execution-role record is
`candidate-constructor@0.2.0--RT-20260614-117`.

## Reasoning

`RT-20260614-114` adopted `GenCover_src^GSC` only as an admissible
source-extension law for later `source_manifold_m_src` continuation. It did not
adopt `M_src`, `FVR_src^GSC`, `RegSold_src^GSC`, `g_eff`, matter coupling,
Einstein equations, benchmark status, Gate Chair closure, completed derivation,
future source-extension impossibility, or global theory rejection.

`handoff-0155` requires the next research packet to be one bounded Candidate
Constructor bridge attempt. The packet must produce one of four terminal
candidate-constructor result types. The bridge can construct a draft/control
candidate object or name a smallest missing primitive, but it cannot promote
the candidate to adopted science authority.

## Role-Fit Matrix

| Candidate role | Decision | Reason |
| --- | --- | --- |
| Director of Research | Rejected | The Director creates the routing record but does not execute the science-draft bridge artifact. |
| Candidate Constructor | Accepted | `handoff-0155` names a single bridge attempt from `GenCover_src^GSC` toward `source_manifold_m_src`. |
| Smuggling Auditor | Deferred | The candidate must exist before its hidden-import audit. |
| Refuter | Deferred | Stress follows only after audit if the candidate passes conditionally. |
| Ontology Formalizer | Rejected | `RT-20260614-110` already formalized `GenCover_src^GSC`; this packet consumes it as RT-114 source-extension-law input. |
| Theoretical Continuation Selector | Rejected | The current handoff names a single legal packet. |
| Gate Chair | Rejected | No new adoption, benchmark, closure, or suspension authority is authorized. |

## Memory Preflight

The repository memory preflight returned `core_validation_status: PASS`,
`freshness_status: PASS`, and `local_retrieval_status: PASS`. No retrieval
refresh was needed before Director routing.

Targeted memory use included search for
`GenCover source_manifold_m_src bridge Candidate Constructor` and lookup of
the registered RT-114 human-gate review artifact. Canonical registry rows and
canonical source files were inspected directly after the memory hits.
Generated wiki, semantic, Obsidian, SQLite, and `.local` layers remain
retrieval support only.

## Required Role Decomposition

This is a physics research AgentJob created after `2026-06-17T04:08:16Z`.
Therefore the AgentJob uses
`role_decomposition.mode: "parent_child_parallel_synthesis"` with one outer
AgentJob, one execution-role record, two internal child perspectives, one
parent conflict review, and one fused TeX bridge artifact.

## Target Derivation Milestone

`target_derivation_milestone`: `source_manifold_m_src`

`milestone_burden`: construct a source-manifold bridge candidate from adopted
`GenCover_src^GSC` source-extension-law input or name the smallest remaining
missing primitive.

## Claim Boundary

Allowed:

- consume `GenCover_src^GSC` only as the RT-114 adopted source-extension-law
  input;
- define a draft/control bridge candidate
  `M_src^{GSC-cand}(E)` from non-bottom source-cover output;
- record `Q_src^GSC` only as conditional quotient-support data;
- record interface table, no-target-import precheck, proof-obligation matrix,
  and route consequence; and
- update the Distance-to-GR ledger only as draft/control candidate status.

Forbidden:

- canonical ontology TeX edit;
- adoption of `FVR_src^GSC`, `RegSold_src^GSC`, `M_src`, or full `M_src`;
- `g_eff`, matter coupling, Einstein equations, benchmark promotion, Gate
  Chair closure, completed-derivation language, future source-extension
  impossibility, or global theory rejection;
- use of target topology, target atlas, target differentiable structure,
  target metric, Lorentzian signature, proper time, detector semantics,
  benchmark recovery, validator receipts, registry rows, generated derivatives,
  role labels, handoff labels, or file order as mathematical evidence; and
- reuse of the one-time RT-114 human approval for a new adoption decision.

## Decision

Proceed with one Candidate Constructor packet. The expected decisive result is
`constructed_candidate` only if the candidate remains explicitly draft/control,
audit-pending, and non-adopted.
