<!-- authority: science_draft -->

# Parent Fusion Notes: GenCover to M_src Bridge Attempt

Task: `RT-20260614-117`

The parent synthesis accepts the shared child conclusion: a non-bottom
`GenCover_src^GSC` package can be packaged as a draft/control bridge candidate
`M_src^{GSC-cand}(E)` without treating that package as adopted `M_src`.

The mathematical child supplies the record map:

`B_Msrc^GSC: E -> M_src^{GSC-cand}(E)`.

The philosophical child supplies the authority boundary: `Q_src^GSC` is only
quotient-support data, and `M_src^{GSC-cand}` is candidate notation only.

The fused artifact therefore records `constructed_candidate` and routes next
to Smuggling Auditor. The result does not adopt `M_src`, does not adopt
`FVR_src^GSC` or `RegSold_src^GSC`, and does not unlock `g_eff`, matter
coupling, Einstein equations, or benchmark promotion.
