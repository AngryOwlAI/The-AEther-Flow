# Parent Fusion Notes: EqSrc Cycle–Boundary Selector Smuggling Audit

## Scope

These notes fuse the independent Physicist-Mathematician and
Physicist-Philosopher reviews of exact proposal-only
`EqSrcCycleBoundarySelectorLaw_src^cand,v1`, hash
`64901302bd6f37819ebd30aaaebaf39611d2c71c97d45b6cea6a39df11e0df94`.
The packet has no repair, Refuter-stress, adoption, canonical-ontology, or
promotion authority.

## Consensus

The displayed dependency tree is acyclic. The source record \((X,P)\) defines
the complement indicator, affine action, fixed space, parity functional,
boundary line, quotient, and relation without an explicit target, benchmark,
workflow, registry, role, validator, handoff, checkpoint, or generated
derivative premise.

That is a conditional source-purity pass for the expanded marked signature,
not a derivation of the signature. Current ontology does not derive \(P\), the
odd-cardinality rule, the \(\mathbb F_2\) setting, the marked morphism category,
or a physical variation class. The mark is transparent selector loading.
Because identical final records can arise from source-derived or target-chosen
marks, acquisition provenance remains an external no-target-import
obligation.

## New Mathematical Payload

In the basis \(p=\mathbf1_P\), \(q=\mathbf1_{X\setminus P}\), the quotient
label is \(\pi(\alpha p+\beta q)=\alpha\). Exact enumeration of
\(\operatorname{AGL}(2,2)\) gives 24 affine bijections:

- 8 preserve the kernel-pair relation;
- 4 fix quotient labels pointwise;
- 2 are the candidate's declared translations; and
- 16 change the relation.

Thus the declared variation class is nonmaximal. Translation by \(p\) changes
all four labels while preserving the relation on all 16 ordered pairs.

There are also exactly three nonzero proper lines in
\(A_S\cong\mathbb F_2^2\): \(\langle p\rangle\), \(\langle q\rangle\), and
\(\langle p+q\rangle\). The marked automorphism group fixes \(A_S\) pointwise,
so all three are invariant and each can support an equally acyclic
marked-natural quotient with matching translations. Marked symmetry therefore
does not select \(B_S=\langle q\rangle\); parity supplies substantive selector
data.

No nonempty proper mark is natural on bare finite carriers under all
bijections. On every even carrier, each odd proper mark has an odd complement;
the two orientations have the same four-state \(A_S\) but distinct boundary
lines and relations. The four-point witness is correct, but \(n=2\) already
realizes this complement effect.

## Physical and Ontological Boundary

Marked-bijection naturality is covariance of a decorated presentation. It is
not covariance under an independently derived physical gauge category.
Defining the variations before the boundary removes literal circularity, but
both are co-designed from the same selector-bearing mark. Kernel-aligned
robustness is therefore exact and conditional, not independent physical
validation.

The candidate remains `proposal-only`, `source-extension data`,
`new_ontology_primitive_candidate`, and
`blocked_adoption_open_continuation`. General EqSrc, exact-GR recovery,
downstream GR objects, and both authoritative ledgers remain unchanged.

## Freeze and Next Route

The exact v3 route remains locally frozen. Repeating this construction or audit
without new mathematical payload is frozen. The marked-selector route remains
open for exactly one materially distinct `refuter@0.2.0`
`ontology-law-research-packet` against the unchanged candidate.

The stress should test the three competing boundary lines, complement and
forgetful transformations, the 2/4/8 variation gap, the bare-carrier mark
obstruction, the necessity of odd parity and \(\mathbb F_2\), and whether any
independent source-side physical criterion selects \(P\), \(B_S\), or \(J_S\).
It may not repair, adopt, or promote the candidate.
