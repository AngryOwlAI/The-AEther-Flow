<!-- authority: science_draft -->

# Parent Fusion Notes: V16 DAG Next-Edge Selector

## Consensus

Both internal children selected the same edge:

`mc_source_matter_semantics_equivalence_theorem -> mc_coupling_law_target`

Both children reject immediate detector, stress-energy, action, universal
matter-coupling, Einstein-equation, and benchmark routes as premature under
the current claim boundary.

## Unique Contributions

The mathematical child emphasized payload feasibility: the next packet can
define a source-side coupling-law target signature, certificate preconditions,
and fail-closed branches without adopting a coupling law.

The physics-philosophy child emphasized status discipline: a target
specification is a map of obligations, not an adopted law, physical detector
semantics, stress-energy semantics, matter action, Einstein equations, or a
benchmark result.

## Limitations

This selector does not execute the selected target-specification packet. It
does not prove a coupling-law theorem and does not change the Distance-to-GR
ledger.

## Fused Decision

The fused selector records exactly one next packet:

`source_side_coupling_law_target_specification_under_explicit_certificates`

The next execution family is:

`ontology-formalizer@0.2.0`
