# Parent Fusion Notes: EqSrc Source-Rooted Quotient Selector Candidate

## Synthesis

Both internal perspectives support one positive but sharply bounded result:
`EqSrcSourceRootedQuotientSelectorCandidate_v1` is a mathematically coherent
proposal-only model of exact v3, conditional on the same explicit
inverse-cancellation reading already recorded by the preceding Refuter.

The construction uses four source objects, the finite group
\(\mathbb Z_4\), its subgroup \(H=\{0,2\}\), and the declared source profile
\(s(A_i)=i\). Quotient equality selects exactly the two classes
\(\{A_0,A_2\}\) and \(\{A_1,A_3\}\). The relation is therefore neither
equality nor universal.

## Exact-v3 Lifting

The fused artifact does not stop at an endpoint partition. It supplies
singleton endpoint ledger and proxy fibers, their total typed constructors,
endpoint-tagged source provenance leaves, the exact-v3 provenance term
algebra, one constant passing negative-control channel, and a ledger decision
equal to the quotient selector. Acceptance then agrees certificate-by-
certificate with subgroup membership.

The subgroup axioms prove accepted identity, inverse, and composition
totality. Singleton component fibers supply their coherence equations, the
exact-v3 provenance congruence supplies its component equations, and the
constant negative control supplies outcome coherence.

A formal translation must display both inverse-cancellation schemes. The
result is conditional on that intended meaning of the v3 phrase
“inverse-unit equations,” consistently with RT-20260718-022.

## Stability, Naturality, and Qualified Sharpness

The declared admissible class is
\(\mathcal V_H=H^{\mathcal F}\), containing the identity and 15 nonidentity
objectwise shifts. For every \(h\in\mathcal V_H\),
\[
q(s(A)+h_A)=q(s(A)),
\]
so the selector, ledger decisions, acceptance values, and induced classes are
unchanged. This is a complete relative stability certificate for the declared
class.

Transporting the profile and every endpoint-indexed datum along an object
bijection preserves selector values. Automorphisms of \(\mathbb Z_4\) also
preserve \(H\) and induce quotient automorphisms, giving a second bounded
equivariance result. These are mathematical transport statements, not
physical covariance claims.

The sharpness claim is qualified. \(\mathcal V_H\) is sufficient but not
maximal: all additive shifts whose values lie in one common \(H\)-coset
preserve the relation. A uniform shift by \(1\) is outside \(\mathcal V_H\)
but still preserves it. By contrast, the injectivity-preserving shift
\((1,3,0,0)\) changes the profile to \((1,0,2,3)\) and changes two selector
comparisons. Thus independently varying quotient components can alter the
relation.

## Ontology and Provenance Boundary

The complete definition tree is source-rooted and contains no target atlas,
target metric, reconstructed geometry, detector result, benchmark result,
registry, role, validator, handoff, checkpoint, approval, or adoption datum.
This establishes formal source factorization for the constructed model.

It does not establish that current ontology contains or derives
\(\mathbb Z_4\), \(s\), \(H\), the chosen partition, or the variation class.
The object subscripts are notation, not provenance for the profile values.
The construction has no empirical interpretation, natural-selection
argument, uniqueness theorem, or exact-GR recovery result.

## Parent Decision

The fused result is `constructed_candidate`, not an obstruction. It adds a
nondegenerate finite source model, explicit exact-v3 lifting, a 16-element
relative stability class, transport equivariance, and a discriminating
countervariation. The Distance-to-GR ledger remains unchanged and general
EqSrc remains undischarged.

Freeze criteria are evaluated because this is a repeated EqSrc
construction-audit line. The decision is `not_frozen`: the packet adds new
mathematical payload and changes the next burden from construction to an audit
of the concrete profile, subgroup, provenance, and admissibility restriction.
Another generic signature repair, repeat v3 audit, or repeat abstract stress
remains barred.

The next lawful packet is one bounded `smuggling-auditor@0.2.0` review of this
exact candidate. It must test arbitrary-label, hidden-target,
process-authority, opaque-lookup, and variation-class smuggling and may not
repair or adopt the candidate.

## Preserved Claim Boundary

No canonical ontology edit, source-law adoption, general EqSrc discharge,
RetainH, GenH, or M_src adoption, Distance-to-GR ledger change, target or
effective metric result, matter-coupling or Einstein-equation derivation,
benchmark promotion, Gate Chair verdict, completed derivation, global theory
rejection, or future source-extension impossibility follows.
