# EqSrc Source-Rooted Quotient Selector Candidate Receipt

## Result

`RT-20260718-023` constructed one proposal-only exact-v3 model,
`EqSrcSourceRootedQuotientSelectorCandidate_v1`. It uses
\(\mathcal F=\{A_0,A_1,A_2,A_3\}\), \(G=\mathbb Z_4\),
\(H=\{0,2\}\), and \(s(A_i)=i\). The selector passes exactly when
\(s(B)-s(A)\in H\), producing the nondegenerate classes
\(\{A_0,A_2\}\) and \(\{A_1,A_3\}\).

## Decisive Evidence

Singleton ledger and proxy fibers, endpoint-rooted provenance terms, and one
constant passing negative-control channel lift the selector into the complete
exact-v3 certificate structure. Subgroup closure proves accepted identity,
inverse, and composition totality. The v3 coherence obligations hold under
its intended explicit inverse-cancellation schemes.

The admissible class \(\mathcal V_H=H^{\mathcal F}\) has 16 elements. Every
variation preserves the quotient profile and hence every acceptance value.
Object relabelings with transported source data and automorphisms preserving
\(H\) give bounded transport equivariance.

The stability class is sufficient, not maximal. Any additive shift taking
values in one common \(H\)-coset also preserves the relation. The
injectivity-preserving variation \((1,3,0,0)\), however, changes independently
varying quotient components and changes the relation. This is qualified
sharpness, not physical naturalness.

## Qualifications

Current ontology does not derive \(G\), \(s\), \(H\), the selector, or the
admissible variation class. The object labels do not derive the profile
values. Formal source-root provenance does not establish physical selection,
uniqueness, empirical meaning, or exact-GR recovery.

The candidate remains `proposal-only` with
`blocked_adoption_open_continuation`. General EqSrc remains undischarged and
the Distance-to-GR ledger is unchanged.

## Freeze and Next Route

The freeze decision is `not_frozen` because the packet adds a new
nondegenerate model, complete exact-v3 lifting, a relative stability theorem,
transport equivariance, and a discriminating countervariation. Generic
signature repair, repeat v3 audit, and repeat abstract Refuter stress remain
barred.

The next lawful route is one bounded `smuggling-auditor@0.2.0` packet against
this exact candidate. It must audit the provenance of the profile and subgroup
selection and the bounded meaning of admissibility. It may not repair or
adopt the candidate.

## Claim Boundary

No canonical ontology edit, source-law adoption, general EqSrc discharge,
RetainH, GenH, or M_src adoption, target or effective metric result,
matter-coupling or Einstein-equation derivation, benchmark promotion, Gate
Chair verdict, completed derivation, global no-go, or future source-extension
impossibility follows.

## Validation

Task-local validator:

```text
.venv/bin/python research_control/tasks/RT-20260718-023/artifacts/validate_eqsrc_source_rooted_quotient_selector_candidate.py --write-report --json
```

Expected status: `PASS`.
