# Parent Fusion Notes: EqSrc Intrinsic Discriminator v2 Smuggling Audit

## Synthesis

The independent mathematical and philosophy-of-physics audits agree. Exact
proposal-only `EqSrcIntrinsicDiscriminatorAdmissibilityLaw_src^cand,v2` uses no
explicit target or process datum as a positive mathematical premise. Its
quotient-factorization, translation-classification, linear-stabilizer,
corrected \(e_2\), ambient-\(L\), and forward chain-map naturality results are
valid within their displayed hypotheses.

The fused classification is
`source_pure_as_written_with_precise_repairable_chain_map_reflection_scope_obstruction`.
It records
`OBST-EQSRC-INTRINSIC-DISCRIMINATOR-CHAIN-MAP-REFLECTION-001` and routes one
bounded proposal-only Ontology Formalizer typing and fail-closed scope repair.
No repair is performed here.

## Source Roots and Independence

Once a finite chain package \(E\) and its differentials are supplied,
\(A_E=Z_1(E)\), \(B_E=B_1(E)\), \(K_E=H_1(E)\), the quotient discriminator,
and its kernel pair are conservative mathematical definitions. Current
ontology does not derive or select the package, the use of \(\mathbb F_2\),
the differential, or boundary moves as physical variations.

The variation class is defined without a textual back-reference to the
discriminator or relation. That establishes model-internal non-circularity.
It does not establish an independently motivated physical variation law:
both branches share \(\partial_2\), since its image defines the quotient
kernel and its values generate the admitted translations.

## Decisive Reflection Criterion

For a typed chain map \(F:E\to E'\), naturality gives
\[
\chi_{E'}F_1=H_1(F)\chi_E.
\]
Therefore \(F_1\) preserves the kernel-pair relation. It reflects the relation
exactly when \(H_1(F):K_E\to K_{E'}\) is injective. A chain isomorphism is
sufficient because it induces a homology isomorphism, but invertibility of the
whole chain map is not necessary.

The candidate's quotient-factorization theorem states the correct injectivity
criterion. Its fail-closed item 5 nevertheless rejects relation reflection
whenever a chain map is noninvertible. The two statements are inconsistent.

The smallest counterexample has
\[
C_2=\mathbb F_2\langle f\rangle,\qquad
C_1=\mathbb F_2\langle a,b\rangle,\qquad C_0=0,\qquad
\partial_2f=a.
\]
Define a self-chain-map by \(F_2=0\), \(F_1(a)=0\), and \(F_1(b)=b\).
The degree-one map is noninvertible, but the induced map on
\(H_1=C_1/\langle a\rangle\) is the identity. Exhaustive enumeration finds
zero mismatches across all 16 ordered state pairs.

## Cross-Complex Typing

The candidate defines `RelPres` and `RelAut` for endomaps
\(V:A_E\to A_E\). Its naturality proposition then applies
\(F_1:A_E\to A_{E'}\). The intended cross-package proposition is correct, but
it requires explicit predicates whose domain relation is
\(\mathsf{EqSrc}^{\mathrm{cand}}_E\) and codomain relation is
\(\mathsf{EqSrc}^{\mathrm{cand}}_{E'}\). This is a typing repair, not a new
physics claim.

## Finite Census and Surviving Results

The \(E_\star\) witness has eight states and four two-element quotient classes.
Translation by \(e_2\) changes all eight labels and preserves all 64 ordered
pair relations. The displayed ambient \(L\) is invertible, maps the boundary
generator outside \(B_\star\), and yields 16 relation mismatches.

Enumeration of all 512 linear endomorphisms of \(\mathbb F_2^3\) gives:

- 128 relation-preserving maps;
- 168 invertible maps; and
- 24 invertible relation automorphisms.

More generally, if \(A=\mathbb F_q^n\), \(\dim B=k\),
\(L\in\mathrm{GL}(A)\), and
\(r=\dim(B\cap L^{-1}B)\), then the number of ordered-pair relation mismatches
is
\[
2q^n(q^k-q^r).
\]
The displayed \(E_\star\) witness has \(q=2,n=3,k=1,r=0\), giving 16.

These checks preserve the quotient-factorization, translation, linear
stabilizer, corrected finite witness, and forward naturality core. They do
not establish physical admissibility, physical covariance, or general
source applicability.

## Parent Decision

The packet is a `proposal-only` `new_ontology_primitive_candidate` with
`blocked_adoption_open_continuation`. Physical admissibility is not
established. No target or process smuggling is detected as written, but the
chain-map reflection and cross-complex typing obstruction blocks Refuter
stress.

The next lawful packet is one bounded `ontology-formalizer@0.2.0`
`ontology-law-research-packet`. It must define typed cross-complex preservation
and reflection, replace the noninvertibility fail-closed rule with the exact
induced-\(H_1\) injectivity criterion, retain chain isomorphism as sufficient
rather than necessary, and preserve every primitive-selection and
physical-admissibility limit. A fresh audit must precede Refuter stress.

## Preserved Claim Boundary

No canonical ontology edit, source-law adoption, physical-admissibility or
physical-covariance result, general `EqSrc`, RetainH, GenH, or \(M_{\rm src}\)
adoption, Distance-to-GR or metric-use ledger change, \(g_{\rm eff}\), matter
coupling, Einstein equations, benchmark promotion, Gate Chair verdict,
completed derivation, future source-extension impossibility, program-wide
no-go, or global theory rejection follows.
