<!-- authority: science_draft -->

# P7-T04 parent fusion: common source propagation

## Shared consensus

Both child perspectives support one decisive but narrow result. The explicit
P7-T03 controls define finite Boolean support and reachability relations. A
source-address and transition bijection intertwines every Boolean
reachability power. Under the newly stipulated proposal-only map
`b_RS(r_j)=s_j` and the inherited signal-to-detector map, `Rod_src`,
`Signal_src`, and `Detector_src` share one reflexive three-address-chain
skeleton.

The full five-role suite does not share one relation under the named
bijective-transport predicate `U_bij`. The two-address `Clock_src` relation
has one strongly connected component and a nontrivial two-cycle; the
two-address baseline `FreeFall_src` relation has two strongly connected
components and only one forward edge. Equal carrier cardinality removes the
trivial size objection, while SCC and cycle invariance give the exact
countermodel
`OBST-P7T04-FULL-SUITE-COMMON-PROPAGATION-001`.

## Physicist-mathematician contribution

The mathematical child supplied a formal support relation, Boolean powers,
six explicit intervention-indexed protocol profiles, a transport theorem,
the conditional three-address-subfamily theorem, and two independent
universality witnesses. Its strongest witness compares two two-vertex
relations and enumerates why neither possible vertex bijection can preserve
both Clock edges in the one-edge baseline FreeFall relation.

The parent preserves the child’s exact invariant calculations. The child
uses target rows and source columns; the fused TeX and specification use
source rows and target columns. These are transpose conventions and do not
change carrier size, edge count, SCCs, nontrivial cycles, reachability
diameter, isomorphism, or the obstruction.

## Physicist-philosopher contribution

The semantic child sharpened nine required guards. Protocol roles are not
matter sectors. `FreeFall_src` has a baseline and an intervention relation,
so the extracted object is a relation family rather than one graph selected
by an English name. The rod-to-signal map is new proposal-only source data,
whereas the signal-to-detector map is inherited. Identity loops must be
separated from nontrivial directed cycles, and token-response equality must
not substitute for edge transport.

Most importantly, a finite adjacency matrix is not a differential operator.
Its spectral parameter is not a covector, its characteristic polynomial is
not a principal symbol, and its reachability profile is not a characteristic
cone. The sources supply no energy or scale variable, so the physical
“low-energy” question is underdetermined rather than refuted.

## Resolved differences

Ten differences were resolved in one parent pass. The fused result:

- states every theorem and obstruction for explicit protocol controls, not
  for all matter sectors;
- indexes the baseline and blocked `FreeFall_src` relations separately;
- declares `b_RS` as new proposal-only source-extension data;
- defines full support, reduced support, and nontrivial cycles explicitly;
- fixes a row-source matrix convention while preserving the child’s
  transpose-equivalent calculations;
- proves transport from edges rather than token-response values;
- names the exact predicate `U_bij`;
- leaves quotients, embeddings, covers, coarse grainings, enriched
  structures, continuum limits, and future source extensions open;
- records the absence of an energy/scale and principal-symbol map; and
- limits any later P7-T05 use to proposal-only evidence and fail-closed
  obstruction handling.

There are zero unresolved blocking conflicts.

## Exact result and limitations

P7-T04 returns a constructive theorem for a three-role formal subfamily and
a precise obstruction to one full-suite relation under exact bijective
transport. This discharges the plan’s “common propagation theorem or precise
obstruction” requirement only at formal source-relation scope.

It does not establish physical time, causal order, propagation speed, a
principal symbol, characteristic set, common cone, effective metric,
detector semantics, matter-sector universality, equivalence-principle
behavior, matter action, coupling, stress energy, Einstein equations, or
exact-GR recovery. It also does not establish physical nonuniversality,
birefringence, a global no-go theorem, global theory rejection, or future
source-extension impossibility.

## Reopening and downstream boundary

Reopening the same physical burden requires materially new source-side data:
a propagation carrier for every admitted matter protocol, nontrivial
comparison maps, robustness under presentations and interventions, an
independently justified scale variable, a source-to-operator or explicit
source-analogue map, and a separate model-to-world interpretation.

A later P7-T05 packet may use this result only after the governed checkpoint.
It may not assume a common cone or metric. If universal coupling requires
missing common geometry, it must introduce a genuinely authorized and
materially new source primitive or return a precise obstruction.

## Claim boundary

`CommonSourcePropagationProfileCandidate_v1` remains `draft/control`,
`proposal-only`, and source-extension data. The current ontology does not
derive or adopt it, and adoption remains
`blocked_adoption_open_continuation`. Validator and checkpoint success are
operational evidence only; they create no scientific proof, adoption,
publication, or promotion authority.
