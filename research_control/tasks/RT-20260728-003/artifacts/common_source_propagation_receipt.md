<!-- authority: science_draft -->

# P7-T04 common source propagation receipt

## Result

`RT-20260728-003` returns one `draft/control`, `proposal-only`
`CommonSourcePropagationProfileCandidate_v1` and one precise scoped
obstruction:

`OBST-P7T04-FULL-SUITE-COMMON-PROPAGATION-001`.

Finite enabled source transitions define Boolean support and reachability
relations. Explicit source-address and transition bijections intertwine every
Boolean reachability power. With the new proposal-only map
`b_RS(r_j)=s_j` and the inherited signal-to-detector map, `Rod_src`,
`Signal_src`, and `Detector_src` share one reflexive three-address-chain
skeleton.

The current five-role suite does not satisfy the named `U_bij` predicate of
one relation under exact bijective transport. The two-address `Clock_src`
relation has one strongly connected component and a nontrivial directed
two-cycle. The two-address baseline `FreeFall_src` relation has two strongly
connected components and only one off-diagonal edge. Those invariants cannot
be changed by either bijection of the two-element carriers.

## Reproducible evidence

- Candidate TeX:
  `research_control/tasks/RT-20260728-003/artifacts/common_source_propagation_profile_candidate_v1.tex`
- Machine specification:
  `research_control/tasks/RT-20260728-003/artifacts/common_source_propagation_spec_v1.yaml`
- Controls:
  `research_control/tasks/RT-20260728-003/artifacts/common_source_propagation_controls_v1.yaml`
- Assumption/dependency table:
  `research_control/tasks/RT-20260728-003/artifacts/common_source_propagation_assumption_dependency_table_v1.yaml`
- Validation report:
  `research_control/tasks/RT-20260728-003/artifacts/common_source_propagation_validation_v1.json`
- Compact receipt:
  `research_control/tasks/RT-20260728-003/artifacts/common_source_propagation_compact_receipt_v1.json`

The deterministic validator passes 65 of 65 checks. It reproduces six finite
relation profiles, the exact two-step chain reachability, both transport
maps, the equal-cardinality countermodel, intervention sensitivity, and every
principal-symbol and promotion guard. A two-pass scratch TeX build produces a
five-page PDF with resolved references.

The two internal child perspectives completed independently within the one
outer AgentJob. The parent resolved ten differences in one pass and recorded
zero unresolved conflicts.

## Exact boundary

The finite relation result is not a principal symbol, characteristic set,
physical propagation law, causal cone, metric, detector theorem, matter-sector
universality result, equivalence-principle result, matter action, coupling
law, stress-energy object, Einstein-equation derivation, or GR benchmark
result. The sources contain no energy or scale variable, so physical
low-energy propagation is underdetermined rather than refuted.

The obstruction concerns exact bijective transport of the present explicit
proposal-only protocol relations. It does not exclude quotients, embeddings,
covers, coarse grainings, enriched relation families, continuum limits, or
future conservative source extensions. It is not a global no-go theorem or a
future-impossibility result.

Current ontology does not derive or adopt the relation profile or the new
rod-to-signal map. Adoption remains
`blocked_adoption_open_continuation`. Validator and checkpoint status are
operational evidence only.

## Downstream use

After the governed checkpoint, P7-T05 may use this packet only as scoped
proposal evidence and as a preserved obstruction. It may not assume a common
cone or metric. If an actual universal coupling candidate requires missing
common geometry, the later packet must introduce materially new authorized
source data or return its own precise obstruction.
