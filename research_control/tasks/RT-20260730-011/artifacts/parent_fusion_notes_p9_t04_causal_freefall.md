<!-- authority: science_draft -->

# Parent fusion — P9-T04 causal, local-inertial, and free-fall case

## Fused result

The two internal perspectives agree on one decisive result:
`precise_obstruction`.

Two physically interpreted P7 source sectors, `(1,[0])` and `(2,[0])`, are
evaluated with exact two-state kernels. The movable kernel gives endpoint
response `1-2^(-n)`, while the identity kernel gives zero. The P7 operational
forms give the movable response tuple
`(1, 1/2, 1/4, 1/4, 1/4)` in propagation, free-fall, clock, rod, and signal
order, and the identity-only tuple is zero.

A common task-local equipment record yields exact sector residuals
`(0,0,0,0,0)`. A split record yields
`(1,1/2,1/4,1/4,1/4)`. The failed object is the source-derived total selector
from source sector and operational role to control, equipment, physical
calibration, and model-to-world map.

## Interpretation boundary

The exact values are source arithmetic. The formal slope is address-hops per
update index, not a physical speed. The free-fall, clock, rod, and signal
values are device-token responses, not trajectories, accelerations, proper
times, lengths, or null signals in a physical spacetime.

The common record demonstrates constructible formal equality, not a derived
equivalence principle. The split record demonstrates source nonselection, not
an empirical equivalence-principle violation. The movable-kernel direction
asymmetry is not evidence for a preferred frame or birefringence.

## Conflict resolution

The mathematical perspective supplied the exact kernel powers, operational
traces, two-sector residuals, and selector nonselection theorem. The
philosophical perspective supplied the physical-codomain boundary, the
nonblind exposure disclosure, and the conservative-extension qualification.
Fusion preserves both.

## Case disposition

The source calculation is complete, but the physical characteristic problem,
common time/length calibration, local transformation law, physical
free-fall map, corrections, uncertainty, target equivalence relation,
tolerance, and independent reproduction are absent. The target oracle remains
closed.

P9-T04 is therefore `INCONCLUSIVE`, with secondary label
`FORMAL_ANALOGY`, and zero passes. The status is
`blocked_adoption_open_continuation`: current adoption is blocked, while a
conservative source-side selector or calibration law remains possible.

## Route consequence

The unchanged P9-T04 inference branch is locally frozen against inferring
physical causal propagation, local inertial behavior, universal free fall, or
benchmark promotion. After checkpoint, P9-T05 may run only as a distinct
source-only case carrying the P9-T02, P9-T03, and P9-T04 `INCONCLUSIVE`
dispositions and zero benchmark passes.

## Forbidden overread

No canonical ontology or source law is adopted or rejected. No physical
speed, causal cone, local Lorentz invariance, local inertial frame, universal
free fall, equivalence principle, preferred frame, birefringence, effective
metric, Einstein equation, exact-GR recovery, benchmark pass, Gate E verdict,
promotion, proof, publication, push, global no-go, future-impossibility, or
completed derivation follows.
