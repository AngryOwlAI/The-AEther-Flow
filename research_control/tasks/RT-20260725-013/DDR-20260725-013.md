---
authority: control
status: completed
decision_id: DDR-20260725-013
task_id: RT-20260725-013
agent_job_id: AJ-RT-20260725-013-001
director_version: director-of-research@0.3.0
decision_type: task_overlay
selected_role_id: ontology-formalizer
selected_role_version: 0.2.0
supersedes_decision_id: DDR-20260725-012
requires_human_gate: false
role_fit_candidates:
  - ontology-formalizer@0.2.0
  - smuggling-auditor@0.2.0
  - refuter@0.1.0
  - theoretical-continuation-selector@0.1.0
---

# DDR-20260725-013: execute only v21 P5-T06

## Current Objective

Activate one `ontology-formalizer@0.2.0` task overlay for v21 work item
P5-T06. Commit `895c5f2fa7bf15445ca88ba11a67878f098c30c1` is the successful
governed generation-117 checkpoint required by `handoff-0872`, and immutable
relay generation 118 selects the same work item under `continue-research`.

Use the named `ontology-law-research-packet` route because current ontology
does not derive a typed source-only coarse-graining or reconstruction law with
declared scale, approximation, information loss, surviving observables, and
error. The task defines one candidate law for the exact current
cubic-amplitude source family and a precise limitation. It does not propose a
canonical ontology edit or request adoption.

## Authority Surfaces Read

The exact source boundary comes from the registered P5-T03 cubic-amplitude
dynamics, P5-T04 structural audit, P5-T05 background and mode analysis,
P5-T02 forward-action semantics, P5-T01 compact source-theory object, the v21
plan, the continuum-first P4-T06 integration, the canonical ontology
manuscripts, the burden map, and the Distance-to-GR ledger.
`research_control/support_formalization/` was inspected as absent and supplies
no premise or authority.

Memory preflight refreshed retrieval-only local state and then passed with
1,017 canonical source objects and 5,050 relationship rows. Two
parser-unsafe hyphenated searches and one control-path lookup failed or
returned no canonical object and influenced no result. Parser-safe searches
navigated to the registered P5-T05 source, its parent fusion note, the v21
plan, and the current frontier; each was inspected directly with its registry
row. Generated memory surfaces remained navigation only.

## Role-Fit Matrix

| Candidate role | Fit | Reason |
|---|---|---|
| `ontology-formalizer@0.2.0` | selected | P5-T06 requires exact domains, codomains, maps, covariance statements, approximation theorems, information-loss semantics, and a scoped obstruction. |
| `smuggling-auditor@0.2.0` | next only | P5-T07 owns the independent source-purity and claim-boundary audit after checkpoint. |
| `refuter@0.1.0` | internal adverse-control perspective only | Negative controls and rank/locality failure branches are needed without opening a second AgentJob. |
| `theoretical-continuation-selector@0.1.0` | not selected | The plan, handoff, and immutable route already select P5-T06 and its role. |

## Selected Role

For every source-amplitude resolution `epsilon>0`, define the odd
nearest-lattice quantizer

`N_epsilon(a)=epsilon sgn(a) floor(|a|/epsilon+1/2)`

with half-grid ties away from zero. Set
`O_epsilon=epsilon Z`, `C_epsilon(q_a)=N_epsilon(a)`, and
`R_epsilon(c)=q_c`. The code round trip is exact, and the source-state round
trip has uniform amplitude and `C0` error at most `epsilon/2`.

The map records its fibers and information loss. It preserves the explicit
source reflection, and the exact cubic flow's `1`-Lipschitz property gives a
raw effective-dynamics error at most `epsilon`, a requantized lattice error at
most `3 epsilon/2`, and lattice semigroup defect at most `3 epsilon/2`.

The fixed-resolution quantizer is deliberately discontinuous at half-grid
thresholds. The source resolution is dimensionless amplitude resolution, not
a physical or target-geometric scale.

The decisive scoped limitation is
`OBST-P5T06-QAMP-LOCAL-FIELD-RANK-001`: every `C1` effective-coordinate map
from the one-dimensional source family has differential rank at most one.
Together with P5-T04 patch rigidity and the P5-T05 reconstruction-phase
obstruction, the map cannot recover independent local field data,
characteristic structure, or geometry from the exact current family.

The path to causal structure is explicit but unmet: independently variable
local source data and gluing, a source-defined operator on local variations, a
characteristic set and source-invariant cone theorem, and scale, regularity,
robustness, and operational-response semantics must precede any later
`M_src` or `g_eff` packet.

## Parent-child synthesis

The single outer AgentJob uses `parent_child_parallel_synthesis`. The internal
physicist-mathematician perspective derives typing, reconstruction,
information, covariance, dynamic error, averaging, and rank results. The
internal physicist-philosopher perspective audits physical-scale,
reconstruction, covariance, information-creation, causal, adoption, and
downstream-GR overreads. The parent resolves six tensions and retains no
unresolved conflict.

## Claim Boundary

The result may be a task-local source-observable coarse-graining candidate,
source-state decoder, approximation theorem package, error and information
ledger, scoped local-reconstruction obstruction, and scoped Distance-to-GR
source-ontology evidence delta. It is not canonical ontology, an adopted or
rejected source law, a physical coarse-graining or scale, a physical source
manifold, a local field, causal structure, target covariance, `g_eff`, matter
coupling, Einstein-equation derivation, benchmark promotion, proof
publication, push, completed derivation, global theory rejection, or future
source-extension impossibility.

## Freeze evaluation

The candidate family is not frozen by P5-T06. This packet adds a first typed
map, explicit resolution and error ledger, dynamic approximation theorem, and
a distinct rank-based local-reconstruction obstruction. P5-T07 is a planned
audit of these new objects, not a repetition. A later packet that merely
relabels the amplitude lattice as geometry or fabricates local information
must fail closed.

## Validation

The task-local validator checks candidate identity, map typing, ties and
thresholds, exact code decoding, uniform reconstruction error, information
loss, reflection covariance, bounded-observable error, raw and lattice
dynamic errors, semigroup defect, averaging controls, dimension and
smoothness, the rank/locality obstruction, the causal-structure path,
parent-child synthesis, and claim boundaries. Repository validation,
documentation-impact accounting, claim-language validation, memory
regeneration, TeX scratch compilation, and one governed checkpoint remain
required.

## Completion disposition

The registered source and companion specification and ledger pass all 27
task-local checks. P5-T06 is complete at `draft/control`, `proposal-only`,
`source-extension data` scope with an audit-ready source-observable map and
one precise current-family local-reconstruction obstruction. It is ready for
one governed checkpoint. P5-T07 remains unexecuted until that checkpoint
commits.
