---
title: "Parent fusion: proposal-only measurable-fiber occurrence-law smuggling audit"
status: "draft/control"
authority: "science_draft"
task_id: "RT-20260816-003"
job_id: "AJ-RT-20260816-003-001"
plan_id: "recommendations_implementation_plan_continue_task-v22"
plan_task_id: "P4-T02"
candidate_id: "CAND-V22-P4T02-B2-PROPOSAL-ONLY-MEASURABLE-FIBER-OCCURRENCE-LAW-V1"
packet_id: "PKT-V22-P4T02-B2-PROPOSAL-ONLY-MEASURABLE-FIBER-OCCURRENCE-LAW-SOURCE-EXTENSION-SMUGGLING-AUDIT-V1"
---

# Parent fusion

## Decisive verdict

The fixed `ProposalOnlyMeasurableFiberOccurrenceLaw_v1` construction is
`source_pure_as_written`, narrowly and only at the level of its disclosed
written syntax. No target atlas, target metric, desired cone, empirical answer,
benchmark answer, frozen-candidate output, registry status, role identity,
validator result, or checkpoint result selects its roots.

That verdict is deliberately weaker than every claim that could be mistaken
for progress toward physical occurrence. The binary token set, locally
constant measurable fiber, total admissibility, complement involution,
parameter `p`, shared flip coupling, and total-variation structure are
transparent proposal-only constructive preload. They are not independently
source-provenanced, current-ontology-derived, definitionally conservative,
epistemically selected, adopted, physically interpreted, empirically
calibrated, or P4-accepting.

## Fixed mathematics preserved

The audit preserves the exact conditional RT001 construction:

1. `LC(O,{0,1})` is a standard-Borel proposal fiber on the admitted domains.
2. `K_p(x)=p delta_x+(1-p) delta_cx` is normalized and complement-equivariant.
3. Open restriction and source-diffeomorphism pullback naturality hold for the
   declared section construction.
4. Compatible sections glue uniquely, and the disclosed packet-global flip
   driver gives a section-indexed cover law.
5. On nonempty two-point complement orbits,
   `d_TV(K_p,K_q)=|p-q|`; on the empty-open singleton the distance is zero.
6. The one-step operator `L_p=P_p-I` is an algebraic generator only.

The audit does not broaden shared-driver descent into a claim that arbitrary
local marginals determine a unique joint coupling.

## New mathematical payload

### Coupling nonuniqueness

On a mixed-token section, two fully restriction-natural and token-swap-
equivariant joint laws exist. The shared-global law uses one flip bit for every
token class present and correlates the two token-class drivers perfectly. A
token-class law may instead use a nontrivial exchange-symmetric coupling of
the two drivers. With stay probability `p`, the joint parameter
`a=P(J_0=1,J_1=1)` ranges over
`max(0,2p-1) <= a <= p`. Every member has the same one-token marginals; the
shared law is `a=p`, while independent token-class drivers give `a=p^2`.
Restrictions discard unused token classes and therefore preserve this family.
Naturality and local marginals do not select `a`.

Fresh independent bits per connected component are an exact countercontrol,
not a valid full-category alternative: when a connected open restricts to a
disconnected subopen, the pushed-forward law retains one bit, whereas fresh
componentwise independence uses two. This proves the required naturality
failure without weakening the genuine token-class nonuniqueness result.

### Parameter, stationarity, occurrence, and clock nonselection

For `p in (0,1)`, a measure is stationary under `K_p` exactly when it is
complement-invariant. The stationary class is therefore independent of `p`.
In particular, `p=1/3` and `p=2/3` define distinct kernels with the same uniform
stationary law on each two-point orbit. Arbitrary weights among distinct
complement orbits remain stationary as well.

A Markov kernel determines conditional weights, not an initial law or a
realized sample. `delta_0 K_p` and `delta_1 K_p` differ, so the kernel selects no
initial token. Likewise, assigning a duration `tau>0` replaces the rate form by
`L_p/tau` without changing the dimensionless one-step law. No fixed source root
selects `tau`.

### Same-reduct nondefinability

One current source reduct admits distinct expansions with different `p` values
and different token-class couplings. Kernel evaluation separates those
expansions. Thus the proposal is not a conservative definitional extension of
the current ontology. It is a `new_ontology_primitive`, held only as
`proposal-only` source-extension data.

## Parent-child synthesis

The Physicist-Mathematician and Physicist-Philosopher children independently
select the same decisive verdict and the same successor identity. The
mathematical child supplies the exact component-splitting naturality failure,
token-class natural coupling, stationarity classification, clock rescaling,
and same-reduct nondefinability theorem. The philosophical child sharpens the
distinction between disclosed preload and source derivation, identifies total
admissibility as semantically nondiscriminating, and separates formal
probability from occurrence and physical probability.

Two boundary conditions are preserved rather than treated as conflicts. First,
the shared flip coupling is explicitly disclosed outside the compact six-root
list and remains an additional unselected proposal choice. Second, total
admissibility satisfies the formal closure requirements trivially but supplies
no discriminating admissibility semantics. Neither condition is hidden target
smuggling; neither earns source-provenance or P4 credit.

## Freeze and Distance-to-GR status

All eight inherited freeze labels remain active and unopened. No new freeze is
created by this first focused audit. Every one of the fourteen Distance-to-GR
burdens remains `no_delta`. D7 is not reevaluated, B2 remains inactive, and
P4-T03 remains locked.

## Successor selected, not executed

Exactly one later packet is selected but not created or executed:

`PKT-V22-P4T02-B2-PROPOSAL-ONLY-MEASURABLE-FIBER-OCCURRENCE-LAW-COUPLING-PARAMETER-STATIONARITY-REFUTER-STRESS-V1`

It is a `source_extension_refuter_stress` packet for `refuter@0.2.0`. Its
bounded purpose is to stress coupling uniqueness, full restriction naturality,
parameter and stationary-law identifiability, initial-law and realization
dependence, clock normalization, total-admissibility nonselection, and the
empty-open metric edge case without repairing, adopting, or promoting the
candidate.

## Authority boundary

This audit proves no physical probability, realized occurrence, empirical
frequency, causal mechanism, clock, cone, conformal geometry, scale, `g_eff`,
coupling, Einstein equation, benchmark, Gate result, or completed derivation.
It provides no proof or publication authority, and it authorizes no push or
external action.
