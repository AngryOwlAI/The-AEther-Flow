<!-- authority: control -->

# Parent Fusion Notes: Source Detector/Readout Candidate Setup

## Fusion Result

The parent review fused the physicist-mathematician and physicist-philosopher
outputs into one setup artifact:

```text
research_control/tasks/RT-20260708-009/artifacts/source_detector_readout_candidate_setup_v1.md
```

## Shared Consensus

Both child reviews select exactly one candidate target branch:
`SourceReadoutCandidate_EStar_v1`.

Both reviews require the setup to remain a draft/control envelope. P5-T03
does not construct a readout law, does not adopt `Readout_src(E_*)`, does not
adopt `Det_src(E_*)`, and does not derive matter coupling.

## Unique Contributions Preserved

The mathematical review supplies the source-domain, readout, detector, witness,
compatibility, and fail-closed obligations for P5-T04.

The philosophical/semantic review supplies the ontology/model/empirical
separation guard: source-side readout notation is not detector semantics, and
compatibility with an earlier candidate is not adoption of a coupling law.

## Residual Limitation

The setup names the construction target only. P5-T04 must either construct a
source-side candidate artifact or record one precise scoped obstruction.
