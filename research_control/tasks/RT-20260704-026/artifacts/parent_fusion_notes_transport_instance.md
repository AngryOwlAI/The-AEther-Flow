<!-- authority: control -->

# Parent Fusion Notes: Transport Certificate Instance

## Summary

The child perspectives agree that RT-20260704-026 constructs exactly one
finite/local valid source transport certificate instance. The fused artifact is:

```text
research_control/tasks/RT-20260704-026/artifacts/finite_local_transport_certificate_instance_v1.tex
```

The mathematical contribution is the explicit finite context
`E_src={0,1,2}`, source objects `A_src` and `B_src`, the row-wise map
`tau(A,i)=(B,i)`, and direct finite verification of guard and response-token
preservation.

The philosophy-of-physics contribution is the authority separation: no target
metric, detector semantics, stress-energy semantics, matter action, benchmark
behavior, process authority, generated derivative, validator, role, handoff,
approval, commit, or local cache is certificate payload.

## Boundary

The result is one finite/local declared source-object equivalence inside the
declared scope. It is not source-law adoption, an unrestricted `RR_E` theorem,
matter semantics, detector semantics, a coupling law, matter coupling,
stress-energy semantics, matter action, Einstein equations, benchmark
promotion, or completed derivation.
