<!-- authority: control -->

# Parent fusion notes: P4-T02 B2 common-hyperbolicity envelope

Task `RT-20260810-005` used one outer `candidate-constructor@0.2.0` AgentJob with two internal evidence-producing perspectives. Neither child created a separate AgentJob or held checkpoint, adoption, Gate, publication, push, or external-action authority.

## Shared result

Both children independently select `constructed_candidate` for `CAND-V22-B2-COMMON-HYPERBOLICITY-ENVELOPE-V1`. On the exact equipped family `S_eq={R,S,D}`, three independently split proposal-only degree-one forms have a hyperbolic cubic product and a nonempty common oriented component. The source-intrinsic strict-margin argument allows independent sector perturbations and therefore does not replay the locally frozen shared-line or diagonal-only route.

The positive result remains conditional source-extension mathematics. The distinct sector characteristic hyperplanes remain distinct. Common time-covector admissibility is not universal propagation, physical causality, conformal geometry, or `g_eff`.

## Two-layer quotient resolution

The physicist-mathematician fixes the sector vector representatives as proposal-only data and constructs the positive-projective map

`Q_ray(k) = [Resp_prin(k)]_{R_{>0}}`.

It removes one common positive scale and retains two independent relative response ratios. The physicist-philosopher confirms that this is legitimate formal source bookkeeping on the declared nonzero-response domain, while emphasizing that it does not remove independent sector normalization, unequal degree, multiplicity, units, transition weights, or representative-selection burdens.

The parent therefore preserves two nested quotients instead of collapsing either contribution:

1. `Q_ray` is the ratio-retaining candidate for the explicitly fixed sector representatives. Its positive projective cell has exact preimage `Gamma_intersection`. It is not canonical across independent sector rescalings without a coherent relative-normalization law.
2. `Q_sign(k) = [Resp_prin(k)]_{(R_{>0})^3}` is the coarser ordered-sign orbit. It discards relative magnitudes but is invariant under independent positive sector normalization. Its all-positive orbit has the same exact preimage `Gamma_intersection`.

This resolves the only normalization tension without claiming more than either layer supplies. Neither quotient is an existing P7 response, empirical device readout, physical observable, or complete physical compatibility law.

## Preserved stress and scope boundaries

The fused packet retains the explicit empty-intersection control, orientation ambiguity, zero-factor collapse, repeated-root and reducibility debt, inverse and cocycle failures, unequipped-sector boundary, target-import rejection, and physical-overread rejection. The candidate covers only the equipped Rod, Signal, and Detector controls. A zero response remains a boundary record outside the nonzero quotient domain.

The fixed local freeze `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR` and scoped obstruction `OBST-V22-P4T02-B2-NATURAL-LINE-LOCK-001` are preserved. This materially distinct branch is not frozen because it produced new mathematical payload, but that does not weaken the prior freeze or create a global no-go theorem.

## Result and next role

The fused no-fog result is exactly `constructed_candidate`, at `draft/control`, `proposal-only`, `source-extension data` status. Current ontology does not derive or adopt the sector forms, source orientation, relative-normalization rule, or protocol-to-principal-response law. Adoption remains `blocked_adoption_open_continuation`.

After a successful checkpoint, the next bounded packet should be `source_extension_smuggling_audit` executed by `smuggling-auditor@0.2.0`. That audit must test the fixed representatives, component choice, response vocabulary, transition factors, units, normalization, sector scope, and any physical interpretation for hidden target or goal-property imports. It is not executed in this invocation.

## Non-conclusion

No full P7 sector constructor, physical causal quotient, complete descriptor instance, D7 or P2-T01 adequacy verdict, B2 activation, P4-T03 unlock, source-law adoption, canonical ontology edit, target atlas, metric, scale, matter coupling, Einstein equation, benchmark promotion, Gate Chair verdict, proof authority, global no-go theorem, publication, push, or external action follows.
