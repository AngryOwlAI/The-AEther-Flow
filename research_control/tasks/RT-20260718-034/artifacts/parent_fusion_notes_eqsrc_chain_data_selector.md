---
authority: science-draft
status: draft/control
task_id: RT-20260718-034
agent_job_id: AJ-RT-20260718-034-001
---

# Parent fusion notes: EqSrc post-v3 chain-data selector

Both perspectives select one distinct constructive and non-promotional next
packet: an `ontology-law-research-packet` executed by
`ontology-formalizer@0.2.0`.

The selected proposal-only target is
`EqSrcCycleBoundarySelectorLaw_src^cand,v1`. It should define a partial natural
source-side selector

`CycleBoundarySel_src(S) = (A_S, B_S subseteq A_S)`

or a tagged failure, before evaluating the candidate relation. The induced
relation is the kernel pair of `A_S -> A_S / B_S`. A differential may be used
to present `B_S` as an image, but the packet must not demand unjustified
uniqueness of the full chain presentation when only the induced relation is
quotient-relevant.

The next packet must include source-only provenance, naturality under source
isomorphisms, equivariance under source automorphisms, a fixed-carrier
discrimination result, and a nondegenerate finite witness with
`0 proper-subset B_S proper-subset A_S` or a precise obstruction. It must
separately define source variations without using the selected relation or its
stabilizer, and prove conditional robustness or return a fragility
obstruction. Failure branches must be tagged and fail closed.

This is a new ontology primitive candidate, not a derivation from current
ontology and not an adopted source law. Current adoption remains blocked while
same-milestone formalization, later target-import audit, and later Refuter
stress remain open.

The exact `EqSrcIntrinsicDiscriminatorAdmissibilityLaw_src^cand,v3`
construction, audit, and stress cycle is locally frozen from repetition. The
freeze is local to that route. The conditional typed algebra survives if a
genuinely independent selector is later supplied. The result is not a general
EqSrc no-go, global ontology rejection, or proof that future source extensions
are impossible.

No Distance-to-GR ledger row changes. No source law, physical admissibility,
`RetainH`, `GenH`, `M_src`, `g_eff`, matter coupling, Einstein equations,
benchmark status, Gate Chair verdict, or completed derivation is promoted.
