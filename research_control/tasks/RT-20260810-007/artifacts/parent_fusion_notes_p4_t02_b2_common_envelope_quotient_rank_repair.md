<!-- authority: control -->
<!-- object-id: MD-V22-P4-T02-B2-PARENT-FUSION-COMMON-ENVELOPE-QUOTIENT-RANK-REPAIR-V1 -->

# Parent fusion notes: P4-T02 B2 common-envelope quotient-rank repair

Task `RT-20260810-007` uses one outer `candidate-constructor@0.2.0` AgentJob with two internal child perspectives. Neither child creates an independent AgentJob or receives Refuter, ontology-adoption, adequacy, Gate, proof, publication, push, or external-action authority.

## Fused result

The decisive result is `constructed_candidate`. The exact bounded correction selected by RT006 is mathematically constructible without rewriting the closed RT005 or RT006 artifacts.

For the supplied source vectors

`v_R = u + epsilon z`, `v_S = u`, `v_D = u - epsilon z`,

each sector score is first retained in its own formal line. Declared proposal-only maps `iota_s:L_s -> L_0` move those values into one bookkeeping line before cross-sector addition. Writing `a=k(u)` and `b=k(z)`, the relabelled response is

`Resp_prin(k) = (a + epsilon b, a, a - epsilon b)`.

Its image is exactly `r_R+r_D=2r_S`, its rank is two, and its kernel is `ann(span{u,z})`, of dimension two. The predecessor's two-independent-ratio description is replaced for continuation by this exact information record.

## Positive ray and sign image

The positive common-scale quotient retains more than the continuous coordinate alone. On the all-components-nonzero domain `D`, `a=r_S` cannot vanish. Define

`sigma = sgn(a)` and `t = epsilon b/a = (r_R-r_D)/(2r_S)`.

After division by `|a|`, the positive ray has representative

`[sigma(1+t), sigma, sigma(1-t)]_+`.

Thus the image has one continuous dimension and a discrete orientation sheet. The expression `[1+t,1,1-t]_+` covers only `sigma=+1`; discarding `sigma` would incorrectly replace positive projectivization by ordinary real projectivization. The `a` chart covers all of `D`, with `t != +/-1`. A complementary `b` chart records the `a=0` boundary rays in the broadened projective closure, but those rays remain outside `D` because the Signal response vanishes.

The independent-positive-scale quotient realizes exactly six ordered nonzero sign cells:

`---`, `--+`, `-++`, `+--`, `++-`, and `+++`.

The alternating patterns `-+-` and `+-+` contradict the midpoint relation. Zero-response boundaries are not repaired into either quotient domain.

## Preserved conditional mathematics

The repair preserves:

- `P_product(k)=k(u)(k(u)^2-epsilon^2 k(z)^2)` and its three distinct characteristic hyperplanes;
- `Gamma_intersection={k:k(u)>|epsilon k(z)|}`;
- `Gamma_intersection=Q_ray^{-1}(C_+++)=Q_sign^{-1}(O_+++)`;
- source-presentation covariance with simultaneous transport of `h_0`, sector data, typed lines, and comparison maps; and
- the local independent-sector strict-margin witness, including the reproduction bound `109/150>0`.

The child synthesis adds one necessary property separation. Common positivity is product-open under independent sufficiently small changes of the finite sector family. The exact midpoint plane and six-cell image are not: they require structured changes preserving `v'_R+v'_D=2v'_S`. Three independent response vectors can instead give rank three and all eight sign cells. The repair is exact for the admitted split family, not a generic three-sector theorem.

## Semantic and authority boundary

The formal response-line maps make the algebra type-correct by declaration. They do not establish empirical calibration, common physical units, device gain matching, protected P7 semantics, or an adopted ontology equivalence. `Q_ray` remains fixed-representative syntax modulo one common positive scale. Independent positive sector factors generally change it; `Q_sign` preserves only signs and discards every magnitude.

The common cone is a shared formal time-covector admissibility region. It does not identify the three characteristic hyperplanes, derive dynamics, or establish universal propagation or physical causality. Only the equipped Rod/Signal/Detector controls are present. An added sector can destroy the intersection, so no all-P7 or B2 universality credit is available.

Roles, registries, executable checks, child agreement, parent fusion, compilation, generated derivatives, handoffs, validation, and checkpoint state are process evidence only. They create no theorem, empirical, physical, adoption, Gate, benchmark, or promotion authority.

## Conflict resolution

The final child artifacts contain no scientific disagreement. Both select `constructed_candidate`, preserve the same mathematical and semantic limits, and select the same unexecuted successor.

Before fusion, the children sharpened the positive-projective statement by retaining `sigma` alongside the one continuous coordinate `t`; the parent adopted that correction. One clerical source-reference mismatch was also resolved in round 1: the Physicist-Philosopher child replaced a noncanonical alias with the authoritative RT006 finding ID `FIND-V22-P4T02-B2-RESPONSE-IMAGE-RANK-001`. No substantive position changed, and no blocking conflict remains.

## Source-extension, freeze, and successor status

The exact classification remains `draft/control`, `proposal-only`, and `source-extension data`. Current ontology does not derive the sector forms, `h_0`, typed response trivializations, relative normalization, transitions, or response semantics. No source law is adopted or rejected; status remains `blocked_adoption_open_continuation`.

The prior `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR` freeze is preserved. The repaired common-envelope branch is not frozen because it adds and validates the exact payload selected by RT006. It earns no Distance-to-GR delta.

The next materially distinct packet is one `source_extension_refuter_stress` under `refuter@0.2.0`. It is selected but not executed in RT007.

## Non-conclusion

No predecessor, ontology, response-semantics, empirical, physical-causality, universal-sector, D7, B2, P4-T03, `g_eff`, coupling, Einstein-equation, Distance-to-GR, Gate, benchmark, proof, promotion, external-action, global-no-go, future-impossibility, or completed-derivation status changes.
