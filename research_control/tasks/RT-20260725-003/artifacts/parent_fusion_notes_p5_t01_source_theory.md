<!-- authority: control -->

# P5-T01 parent fusion notes

The parent fused the two required internal perspectives into one source-side
formal object. The mathematical perspective supplied the groupoid/subgroupoid
typing, invariant-observable rule, tagged open-slot construction, concrete
witnesses, and fail-closed branches. The philosophical perspective supplied
the component-by-component status discipline and the separation between
declared source redundancy and physical gauge interpretation.

Three tensions were resolved in one review round:

1. `Red_src` is called declared source redundancy. Gauge equivalence is the
   internal quotient relation, while physical gauge interpretation remains
   open.
2. The reconstruction interface carries only an abstract codomain and a
   source-provenance obligation. Its slot is `open`; it supplies no target
   metric or benchmark value.
3. The torus-amplitude and two-state examples are proposal-only type
   witnesses. They establish non-emptiness and internal consistency, not
   physical truth.

The fused object therefore discharges P5-T01's compact-object typing burden at
`draft/control` scope. It does not adopt a source law, select
`Phi_src` semantics, fill dynamics or reconstruction, establish physical
gauge symmetry, define `M_src` or `g_eff`, couple matter, derive Einstein
equations, promote the exact-GR benchmark, or create proof, publication, push,
or completed-derivation authority.

P5-T02 is the next distinct burden: select the semantic kind of `Phi_src` and
prove the corresponding basic laws without target import.
