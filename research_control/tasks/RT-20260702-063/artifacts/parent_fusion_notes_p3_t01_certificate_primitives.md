<!-- authority: control -->

# Parent Fusion Notes: P3-T01 Certificate Primitives

The parent synthesis fused the mathematical child recommendation for a small
source certificate carrier with the epistemic-boundary child recommendation
that certificates must remain source-side formal witnesses rather than
physical semantics or process authority.

The fused TeX artifact defines:

- declared source object index;
- certificate domain and codomain;
- certificate record fields;
- source transport, invariance, and factorization certificate kinds;
- primitive identity, composition, and restriction expressions;
- malformed, missing, and fail-closed certificate states.

The fusion deliberately does not assert operation laws. Identity preservation,
composition closure, restriction behavior, missing-certificate behavior, and
target-import invalidity are routed to P3-T02.

The packet preserves the P2 certificate-gap witness and the Gate Chair
boundary: `NarrowMSCertEq_v1` has only scoped source-extension
evidence-status and does not become source-law adoption, matter-semantics
adoption, detector-semantics adoption, coupling-law adoption, matter coupling,
Einstein-equation evidence, benchmark promotion, or completed derivation.
