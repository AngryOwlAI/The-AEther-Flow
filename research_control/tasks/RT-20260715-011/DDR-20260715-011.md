---
authority: control
decision_id: "DDR-20260715-011"
task_id: "RT-20260715-011"
created_at: "2026-07-16T01:20:44Z"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "memory-system-maintainer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260715-011-001"
status: "completed"
completed_at: "2026-07-16T01:24:48Z"
supersedes_decision_id: ""
requires_human_gate: false
validation_status: "PASS"
---

# DDR-20260715-011: Deduplicate checkpoint memory synchronization and validation

## Decision

Execute one bounded Memory-System Maintainer AgentJob for v19 `P3-T05`.
Checkpoint convergence will call the existing write-only `memory_sync()`
operation, retain the required second synchronization after a targeted PDF
build, and run the legacy-consolidated memory validation recipe once after the
final staged Git tree is known. The checkpoint receipt will identify generator
passes, the final tree hash, the runtime gate, the satisfied obligation, and
compact performance metrics.

## Role-Fit Matrix

| Role | Decision | Reason |
| --- | --- | --- |
| `memory-system-maintainer@0.2.0` | Selected | The registered v19 plan assigns P3-T05 to this role, and the implementation binds checkpoint convergence to the memory system's existing write-only synchronizer. |
| `validator-engineer@0.2.0` | Not selected | The classifier recognizes a checkpoint change, but this packet does not change validator findings or weaken a gate; it removes redundant invocation while preserving the final compatibility validator. |
| `project-control-maintainer@0.2.0` | Not selected | Make-target decomposition remains P3-T06. |
| Physics roles | Rejected | No derivation, ontology, proof, benchmark, or scientific promotion occurs. |

## Evidence and boundary

- P3-T04 completed with PASS at commit
  `068685a83bb031a1a0d4d15012904ed53bef7c4f`.
- Entry Git state was clean and local `main` was nine commits ahead of
  `origin/main`.
- The compatibility bootstrap performs synchronization and validation in one
  invocation. The prior checkpoint then ran validate-only in post-sync and
  final-staged phases, yielding three memory-validation traversals in the
  ordinary one-pass path.
- `bootstrap_memory_system.py` already exposes `memory_sync()` as a write-only
  tracked-state operation. P3-T05 therefore needs no bootstrap CLI semantic or
  compatibility-alias change.
- The Makefile contains no same-tree immediate bootstrap-plus-validate pair;
  its target decomposition remains assigned to P3-T06.
- The memory preflight and emitted-signal validator passed. The advisory
  project-improvement resolver had no competing open signal.
- `handoff-0740` remains the ordinary research authority and is not
  superseded by this project-system task.

## Scope-alarm review

The v19 plan explicitly names three workflow skill contracts, the checkpoint,
and focused orchestration coverage. The existing checkpoint stabilization,
rollback, and residue fixtures live in `tests/test_research_control.py`; their
bootstrap expectations must be mechanically updated to the new write-only sync
operation or the established safety coverage would become stale. This tracked
decision therefore adds that one test path to the plan's named test surface.
The patch is larger than a local branch edit because compact mutation receipts,
performance counters, final index identity, multi-pass proof, PDF re-sync proof,
rollback proof, and residue proof are required. It introduces no dependency,
public schema, persistent-data format, or physics-authority change.

## Preserved boundaries

- Preserve the compatibility bootstrap and `--validate-only` command for
  ordinary direct use, read-only checks, and diagnosis.
- Preserve one blocking final legacy-consolidated validation after the final
  staged tree is identified.
- Preserve exact entry-index restoration on every blocked path.
- Preserve the second synchronization after a targeted PDF build.
- Preserve the three-pass convergence bound and block unexplained residue.
- Preserve `handoff-0740` and `EqSrc_family_closure_repair_or_stress` as the
  ordinary research authority.
- Do not edit Make, CI, planner, bootstrap CLI semantics, canonical ontology,
  science drafts, or generated derivatives by hand.
- Do not treat validator results, tests, registries, derivatives, or checkpoint
  evidence as physics proof.

## Outcome rule

PASS requires zero compatibility-bootstrap invocations during checkpoint
convergence, one final memory compatibility validation on the recorded staged
tree, exact index restoration when sync fails, retained post-PDF re-sync,
bounded stabilization, blocked unstaged residue, compact performance evidence,
and all current project-system checkpoint obligations. Otherwise issue
`REPAIR_REQUIRED`, `ROLLBACK_REQUIRED`, or `BLOCKED` without checkpointing
invalid state.
