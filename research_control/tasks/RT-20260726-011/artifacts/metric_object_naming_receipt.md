<!-- authority: control -->

# P6-T07 metric-object naming receipt

## Result

`ScopedMetricStructureRecord_src` is now the primary current name for the
exact ten-slot scoped source-extension record historically written as
`g_eff^{GSC-cand}`. The historical symbol remains an exact legacy alias under
the same declared source-side scope. Bare `g_eff` remains an unresolved
effective-metric burden, not an extant unscoped physical metric.

## Gate B

The readiness verdict is 0 of 8 criteria met. The task makes no physical
metric, cone, clock, rod, matter-coupling, Einstein-equation, benchmark, or
completed-derivation claim. The protected historical scoped adoption verdict
is preserved, and historical artifacts are not renamed in place.

## Alias audit

The migration is additive. Current reader-facing control surfaces name the
scoped record first, retain explicit legacy resolution, and separate
`GateBComparisonForm` controls and the exact-GR target metric from the scoped
record. The task-local validator writes the bounded path-level audit to
`g_eff_reference_audit_v1.json`.

## Next packet

The selected materially distinct continuation is P6-T08 as one independent
`source_extension_smuggling_audit` on the `external-review-packet` route,
using `external-red-team-reviewer@0.1.0`. It may review readiness or record an
exact repair; it receives no promotion authority from this receipt.
