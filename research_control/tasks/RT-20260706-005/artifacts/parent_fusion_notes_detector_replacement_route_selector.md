<!-- authority: science_draft -->

# Parent Fusion Notes: Detector-Replacement Route Selector

## Synthesis

Both child execution units select `metric_use_ledger_integration_packet` as the
single P4-T05 route. The shared basis is that
`SourceDetectorReplacementCandidate_EStar_v1` exists as a finite source-side
candidate, P4-T03 found it `source_pure_as_written`, and P4-T04 found it
`survives_as_source_replacement_candidate` with a
`bridge_facing_candidate_path` classification.

The parent accepts that route and records immediate continuation to P5-T01.
This does not integrate the detector replacement into `K_E`, adopt detector
semantics, create a physical metric premise, or promote any GR claim.

## Route Consequence

The metric-use ledger route is the narrowest useful next packet because later
agents need a durable ledger before using `g_eff`, source readout tokens,
detector-replacement placeholders, proper-time language, target-metric
language, or process receipts near matter-coupling work.

The v17 backlog names `schema-maintainer@0.1.0` for P5-T01, but the active
role registry does not currently register that role. The next Director packet
should therefore bind P5-T01 through an active project-control role overlay,
currently `project-control-maintainer@0.2.0`, unless a separate
project-system route first registers a schema-maintainer role.

## Hard Blocks

The fused result preserves:

- no canonical ontology edit
- no source-law adoption
- no detector-semantics adoption
- no matter-semantics adoption
- no coupling-law adoption
- no matter-coupling derivation or adoption
- no stress-energy semantics
- no matter action
- no Einstein equations
- no benchmark promotion
- no Gate Chair verdict
- no completed derivation

## Conflict Review

No blocking conflict remains between the child outputs. The mathematical child
emphasized ledger preconditions before any `K_E` repair integration. The
philosophical child emphasized preventing a source-side candidate from being
mistaken for physical detector theory. These are compatible and jointly
support the selected route.
