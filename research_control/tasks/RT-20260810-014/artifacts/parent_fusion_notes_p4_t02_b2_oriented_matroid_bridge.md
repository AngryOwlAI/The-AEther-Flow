---
authority: science_draft
task_id: RT-20260810-014
job_id: AJ-RT-20260810-014-001
status: completed
result: constructed_candidate
---

# Parent fusion — finite oriented-matroid bridge

## Answer first

The parent accepts `constructed_candidate`. On the corrected finite exact
domain, the complete proposal-only classifier

`Bridge_OM(A)=(Cstar(A),Cir_plus(A),Tot(A),Ref(A),Strat(A))`

is constructed. It returns all realized sign covectors, all minimal positive
circuits, one exclusive feasible-or-obstructed certificate branch,
presentation-equivariant certificate orbits, finite-refinement maps, and
finite-complexity path-wall records.

This is source-extension mathematics pending an independent Smuggling Auditor.
It is not a current-ontology derivation, physical common-time selector,
empirical response model, universal P7 sector result, conformal structure, or
effective metric. Distance-to-GR is unchanged.

## Required type corrections

Both specialists independently found the same three hazards, and the parent
adopts all three repairs:

1. A presentation quotient cannot return a raw `k` or `alpha`. The corrected
   bridge is first equivariant on the configuration groupoid; quotient-level
   values contain certificate orbits or isomorphism classes. Raw certificates
   transport as `k -> T^{-T}k` and `alpha -> P D^{-1}alpha`.
2. Arbitrary continuous compact-interval paths may cross determinant walls
   infinitely often. `Strat` is restricted to predeclared finite semialgebraic
   or piecewise-rational exact paths with root isolation.
3. Decidable sign and rank are insufficient for complete covector feasibility
   and wall enumeration. The admitted exact domain must also decide
   homogeneous linear feasibility and isolate and order the relevant roots.

Removing any repair triggers the corresponding fail-closed branch—FB-06,
FB-08, or FB-11—and invalidates this constructed-candidate result.

## Mathematical synthesis

For a finite predeclared oriented configuration `A`, complete covectors are
obtained by deciding every word in the finite sign cube. Minimal positive
circuits are obtained by exact support, rank, kernel, coefficient-sign, and
proper-subset tests through the rank-plus-one bound. Gordan's strict
alternative makes the feasible and obstructed branches exhaustive and
exclusive.

Presentation arrows consist of source-linear isomorphisms, independent
positive column scaling, and sector relabeling. They preserve the transported
bridge data, but the project assigns them no physical-gauge meaning. Negative
reorientation remains new source data and can change the total branch.

For finite inclusions, covector restriction is surjective, the strict chamber
shrinks under added sectors, old supported positive circuits persist, and
nested restrictions compose. These are finite refinement statements, not a
continuum or universality theorem.

## Independent exact controls

The parent preserves two separately counted rational conformance suites:

- The Physicist-Mathematician suite uses five configurations, exhaustively
  lists 295 covectors, contains three fixture-level positive circuits, checks
  four refinement maps, and isolates three path walls at `1/4`, `1/3`, and
  `1/2`.
- The parent executable suite uses six configurations in feasible and
  obstructed refinement towers. It exhaustively constructs 442 covectors,
  checks six refinement relations, verifies both total branches and
  presentation transport, and includes a negative-reorientation control and
  one isolated wall at `0`.

The counts are not merged into a physical census. Both suites are engineered
conformance evidence; neither is ontology-derived sector data, empirical
calibration, or evidence that the source naturally realizes a preferred
branch.

## Philosophy-of-physics synthesis

The complete chamber and a returned feasible witness do not select a natural
shared `tau`. Sign covectors record sides of a finite central hyperplane
arrangement; positive circuits record minimal linear incompatibility. Without
dynamics, detector semantics, units, calibration, or a model-to-world map,
neither object is physical causality or empirical response.

The configuration, orientations, provenance, coverage boundary, refinement
family, exact-domain choice, and bridge-facing interpretation are
proposal-only inputs not derived by the current ontology. Symmetric acceptance
of both certificate branches prevents favorable-outcome filtering, but it
does not convert engineered inputs into source evidence.

## Freeze and authority result

All three inherited freezes remain active and are not reopened:

- `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR`
- `NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-COMMON-CHARACTER-HOLONOMY-PROTECTION`

No D7 adequacy reevaluation, B2 activation or disposition, P4-T03 unlock,
physical cone, empirical response, conformal reconstruction, scale, `g_eff`,
coupling, Einstein equation, Gate verdict, benchmark promotion, ontology
adoption, proof promotion, publication, push, external action, global no-go,
or completed derivation is created.

## Next lawful packet

Select exactly one separately admitted
`PKT-V22-P4T02-B2-ORIENTED-MATROID-BRIDGE-SMUGGLING-AUDIT-V1` packet under
`smuggling-auditor@0.2.0`. It must audit the sector, orientation, provenance,
exact-domain, presentation, refinement, fixture, and bridge-status choices for
hidden target, desired-positivity, empirical-answer, frozen-route, or workflow
authority imports. The audit is selected but not executed in RT014, and no
adoption follows automatically.
