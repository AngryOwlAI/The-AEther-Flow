---
object_id: "MD-V22-P4-T02-B2-PARENT-FUSION-COMMON-CHARACTER-INDEPENDENT-VARIATION-SMUGGLING-AUDIT-V1"
authority: "science_draft"
status: "draft/control"
task_id: "RT-20260810-011"
job_id: "AJ-RT-20260810-011-001"
---

# Parent fusion notes — P4-T02 B2 common-character smuggling audit

The parent fuses two independent Smuggling Auditor analyses into one exact
verdict: `source_pure_as_written`. The phrase “as written” is load-bearing. It
means only the fixed, explicitly pointed, framed, anchored, finite,
proposal-only existence witness. Both children find no hidden target, empirical,
physical, benchmark, generated-artifact, registry, role, validator, checkpoint,
or protected-authority import.

The pass does not turn RT010 into a derivation. The universal-character equation
is the desired formal common-descent property stated directly as the candidate
law. The base object, positive frames, anchors, cochain arrays, balance matrix,
gauge generator, source covector, and sector scope are new preparation data.
They are disclosed before evaluation and are therefore source-pure at the
proposal level, but their independent epistemic motivation is absent. The
acyclic dependency graph proves definitional noncircularity; it does not prove
that the roots were selected by current ontology, observation, symmetry, or an
independent source law.

The mathematical child sharpens the remaining burden cohomologically. Positive
one-dimensional representations of the connected one-cycle groupoid are
classified, up to positive object cochains, by one `H^1` holonomy class. There
are two compatible codimension counts:

- allowing the common class to vary, equal three-sector holonomy is the
  one-dimensional diagonal in a three-dimensional sector-class space and has
  codimension two;
- holding the RT010 `chi` class fixed, the three equations
  `[rho_s]=[chi]` have codimension three in the four-class space.

There is no child conflict: the counts use different ambient spaces. The parent
records both. A single-edge perturbation
`rho_R^epsilon=rho_R exp(epsilon omega)` changes the loop-holonomy ratio by
`exp(epsilon)`. For every nonzero, arbitrarily small `epsilon`, no positive
object cochain restores the fixed common intertwiner. This is a target-free
robustness countermodel outside the fixed universal-character axiom, not a
retroactive hidden import into the disclosed law.

The anchor analysis is equally exact. When common holonomy already holds,
unanchored intertwiners form one positive-constant torsor per sector and retain
two relative moduli after one common scale is removed. The declared
`a_s(0)=1` anchors select one representative in the framed pointed candidate.
They do not change cycle holonomy and cannot repair unequal sector classes.
Moreover, the bare oriented cycle has no automorphism-natural selected vertex,
and an oriented line has no automorphism-natural positive frame. The point and
frames are genuine added source structure. Structure-relative transport is
valid naturality, but it is not a selector from the undecorated source.

The philosophical child supplies a sharp internal collapse branch. At the
admitted positive-flow parameter `t=-1/2`,
`x(t)=(3/2,3/2,3/2,3/2)` and `mu=0`. Hence `chi`, every `a_s`, and every
`rho_s` equal one while the formal descent equations still hold. This proves
that a formally successful common descent may lose all nontrivial transition
content along its own admitted flow. It is a Refuter burden, not a purity
failure.

The parent adds two further controls. First, within the source-typed parameter
space `Bd=0` and `m(d)=0`, the conditions `rank(B)=2` and
`rank([B;m])=3` are open; the nonzero variation quotient is not an isolated
coefficient accident. Second, the alternative covector
`m_0=(1,-1,1,-1)` has the same type and acyclic dependency shape but obeys
`m_0 d=m_0 v=m_0 x_*=0`. The DAG therefore cannot select the RT010 covector
or convert designed relevance into independent explanation.

The source-extension classification is `new_ontology_primitive_candidate`, with
allowed language “proposal-only new ontology primitive candidate.” It is not
derived from current ontology and is not a conservative definitional extension.
Adoption remains human-gated and was neither requested nor performed. The
research status remains `blocked_adoption_open_continuation`.

Both inherited freezes remain unchanged:

- `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR`
- `NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS`

This distinct common-character branch is not frozen before its first independent
Refuter stress. The exact next packet is
`PKT-V22-P4T02-B2-COMMON-CHARACTER-INDEPENDENT-VARIATION-REFUTER-STRESS-V1`
under `refuter@0.2.0`; it is selected but not admitted or executed in RT011. It
must stress the fixed candidate against arbitrarily small `H^1` holonomy
perturbations, the `t=-1/2` collapse, reanchoring, point and frame nonselection,
alternative covectors, rank-preserving balance variations, inverse branches,
and every frozen-route control.

Distance-to-GR is unchanged. D7 and P2-T01 adequacy are not reevaluated; B2
remains inactive and undisposed; P4-T03 remains locked. No all-P7 universality,
empirical response, physical causality, calibration, conformal geometry, scale,
`g_eff`, coupling, Einstein equation, source-law or ontology adoption, Gate
verdict, benchmark, proof promotion, publication, push, external action, global
no-go, future-extension impossibility, or completed derivation follows.
