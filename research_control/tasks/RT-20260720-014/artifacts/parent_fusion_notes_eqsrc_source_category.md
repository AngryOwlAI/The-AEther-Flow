<!-- authority: control -->

# Parent fusion notes: P2-T01 EqSrc source category

The parent fused the two bounded child perspectives into
`EQSRC-SOURCE-CATEGORY-DEFINITION-V1` without expanding the outer
`ontology-formalizer@0.2.0` AgentJob.

Shared consensus was preserved: objects and morphisms are target-neutral and
candidate-blind; `Core(C_Sigma)` is the exact domain for covariant transport of
equivalence relations by bijections; structural automorphisms are not physical
gauge transformations; variations are independent typed change records; and
`S_chi(X)` denotes eligible choices rather than a selector.

The mathematical child supplied the category-closure proof, explicit inverse
provenance obligations, the core relation-transport theorem with proof, the
variance guard for noninvertible maps, and four mechanically enumerable finite
controls. The philosophy child supplied the ontology/model/presentation/file
layering, transformation taxonomy, extension-classification boundary, and
exact underdetermination language.

Four apparent conflicts were resolved in one review round:

1. Covariant relation transport is restricted to the invertible core; arbitrary
   maps receive pullback plus explicit fail-closed forward-law options.
2. A forgetful functor is only an extension interface, not a conservativity
   proof.
3. Structural automorphisms and physical gauge transformations remain distinct.
4. The eligible-choice functor is not called a selected section.

No unique child contribution was discarded. No unresolved conflict remains.
The fused theorem proves only formal category, core, transport, and action
functoriality. It does not prove the P2-T02 selector theorem, derive the domain
from current ontology, establish physical covariance, discharge general
`EqSrc`, change a scientific ledger, or authorize promotion.
