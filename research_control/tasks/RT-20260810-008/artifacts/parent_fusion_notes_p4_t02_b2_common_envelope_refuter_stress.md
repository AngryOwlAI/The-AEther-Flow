<!-- authority: science_draft -->

# Parent fusion notes — P4-T02 B2 repaired-quotient Refuter stress

The two independent children agree on the decisive result: `scoped_obstruction`. The committed RT007 rank-two response plane, two-sheet positive-projective atlas, six-cell sign image, common-positive cone, product hyperbolicity, and positive-preimage identities remain exact for the fixed equipped split family. The stress blocks only the stronger use of that fixed object as a presentation-independent and unrestricted-independent-variation-robust source-operational law.

## Fused mathematical result

Collapse at `epsilon z=0` lowers the response rank from two to one and reduces the six sign cells to the diagonal `+++` and `---` cells, while a nonempty common-positive half-space survives. For the noncollapsed fixed candidate, `Resp_prin` has two-dimensional affine covector fibers, `Q_ray` fibers add a positive scale, and `Q_sign` fibers are full-dimensional sign cones. These quotient classes do not reconstruct a unique source covector, line trivialization, normalization, unit, empirical response, or physical equivalence class.

Independent positive response-line transitions preserve `Q_sign` but preserve `Q_ray` for every fixed response only when all three transition factors agree. The common factor must also satisfy the usual overlap cocycle. Thus the fixed RT007 relabelings provide local proposal syntax but no source-derived global `Q_ray` descent certificate.

The minimal finite-variation countermodel changes one sector in one new source direction:

`v_R(delta)=u+epsilon z+delta w`, `v_S(delta)=u`, `v_D(delta)=u-epsilon z`,

with `w in ker(h_0)` independent of `z`. The exact minor is `-epsilon delta`, so every nonzero, arbitrarily small `delta` raises the response rank from two to three and realizes all eight nonzero sign cells. Yet `h_0(v_s(delta))=1` for every sector, so strict common positivity and finite-product hyperbolicity survive. The midpoint family has codimension four in `V^3` and codimension three among `h_0`-normalized triples.

## Resolved differences

The children proposed semantically equivalent freeze labels. The fused stable label is `NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS`. It freezes only promotion of this exact repaired quotient as presentation independent and ambiently robust. It does not freeze the fixed RT007 mathematics, reject B2, modify the prior shared-tau freeze, or prohibit materially distinct future source extensions.

The mathematical child supplied the normalized codimension-three refinement and minimality result. The physicist-philosopher child supplied the operational identification boundary: a formal quotient records information deletion, not physical gauge equivalence; visible goal preload supports a conditional witness, not derivation or universality; and no-target-import hygiene is not empirical or physical evidence. All are retained.

The narrow fact that the `Q_ray` coordinate is unnecessary for deciding membership in the `+++` common-positive cell is retained. It does not make `source_side_irrelevance_theorem_path` the present completion class because broader bridge irrelevance has not been proved.

## Route consequence

The next separately admitted role is `theoretical-continuation-selector@0.1.0`. It must choose a materially distinct same-milestone packet: a source-side irrelevance theorem or deliberately weaker sign/common-admissibility bridge; a new source-derived common-line descent and admissible structured-variation candidate; or another distinct source bridge family. The selector is not executed in RT008.

D7 and P2-T01 adequacy, B2 activation or disposition, P4-T03, empirical P7 semantics, physical causality, conformal geometry, `g_eff`, ontology or source-law adoption, matter coupling, Einstein equations, Gate action, benchmark promotion, proof authority, publication, push, external action, global no-go, future source-extension impossibility, and completed derivation remain blocked. All fourteen Distance-to-GR burdens have zero task delta.
