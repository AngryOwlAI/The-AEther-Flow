---
decision_id: "DDR-20260627-001"
task_id: "RT-20260627-001"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "project-control-maintainer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260627-001-001"
status: "completed"
supersedes_decision_id: ""
requires_human_gate: false
role_fit_candidates:
  - "project-control-maintainer|accepted_for_control_record_only|can record the boundary decision and task-local no-op rationale but cannot edit canonical ontology TeX PDFs or generated derivatives directly"
  - "project-system-director|routing_scope|can decide that the requested implementation crosses project-system authority and must stop before source edits"
  - "documentation-curator|rejected|the requested write scope is current ontology TeX and PDF derivatives rather than explanatory Markdown or source-backed publication specs"
  - "physics roles|rejected|the requested work is format/provenance presentation only and does not authorize physics derivation claim adoption or benchmark changes"
---

<!-- authority: control -->

# DDR-20260627-001: APA 7 Ontology Retrofit Authority Check

## Current Objective

The user requested implementation of an APA 7 title-page and references
retrofit for the 8 current managed ontology documents under `ontology/tex` and
`ontology/pdfs`.

The requested visible title-page attribution is:

- `Alexander Samuel Ricciardi`
- `Project Creator`
- disclosure of `GPT-5.4` and `GPT-5.5` as model-assisted
  research/drafting tools rather than authors.

## Role-Fit Matrix

| Candidate role | Decision | Reason |
| --- | --- | --- |
| Project-Control Maintainer | Accepted for control record only | The role may maintain control records and registries. It explicitly must not alter canonical ontology TeX, benchmark sources, science drafts, PDFs, or generated derivatives directly. |
| Project-System Director | Routing scope | The director can resolve the boundary and create a bounded task/AgentJob, but its active contract has `may_modify_sources: false` and forbids canonical ontology plus generated derivative source classes. |
| Documentation Curator | Rejected | The packet is not an explanatory Markdown or source-backed HTML publication update. The requested write scope is canonical ontology TeX plus PDF derivatives. |
| Physics roles | Rejected | The packet changes format and provenance presentation only. It must not alter physics claims, ontology status, benchmark status, Gate Chair status, or derivational conclusions. |

## Decision

Stop before TeX or PDF edits.

This is the exact stop condition described in the user-provided plan: if the
role or validator boundary rejects canonical TeX formatting under this
project-system packet, stop before edits and record a no-op rationale requiring
explicit source-format authority.

## Claim Boundary

This decision does not authorize:

- editing `ontology/tex/**`;
- editing or adding `tex_shared/**` fragments;
- rebuilding `ontology/pdfs/**`;
- editing `legacy_ontology/**`;
- editing historical `research_control/tasks/**` TeX or PDF artifacts;
- changing scientific claims, ontology status, benchmark status, Gate Chair
  status, or derivational conclusions.

## Recommended Authorization Text

To proceed safely, the next packet should use an explicit authorization like:

> I authorize one bounded source-format retrofit packet for the 8 current
> `ontology/tex/*.tex` files, new shared `tex_shared/` APA title/reference
> fragments, and corresponding managed `ontology/pdfs/*.pdf` derivatives only.
> Do not edit `legacy_ontology/**` or historical `research_control/tasks/**`
> artifacts. Do not change scientific content, ontology status, benchmark
> status, Gate Chair status, or derivational conclusions.

## Completion

Completed at `2026-06-27T15:36:02Z`. No ontology TeX, shared TeX fragment,
legacy snapshot, historical research-control artifact, or PDF derivative was
modified.
