<!-- authority: control -->

# Parent Fusion Notes: P6-T03 Finite Toy Response v2 Refuter Stress

## Result

P6-T03 is completed by a stress-survival result:
`survives_as_finite_toy_model`.

The model survives tag-removal and source-relabeling stress because the
response relation is induced from the unlabeled finite path relation. The
names `a`, `b`, `c`, `O_0`, `O_1`, and `O_2` are presentation handles, not
independent response tags.

## Synthesis

The mathematics child supplied the tag-erasure and relabeling invariance
stress arguments, plus the finite edge-perturbation pressure. The philosophy
child supplied the authority boundary: the result is a draft/control finite
toy model stress result, not a physical metric, `g_eff`, matter coupling,
empirical readout, benchmark evidence, or Gate Chair verdict.

The parent resolution keeps both points:

- source isomorphism preserves the response relation;
- arbitrary edge perturbation changes the source object or falls to bottom;
- target-distance, physical-metric, empirical-readout, `g_eff`, and
  matter-coupling substitutions are blocked overreads;
- P6-T04 may integrate the stress status into a source-model zoo, but this
  packet does not perform that integration.

## Conflict Resolution

Two possible conflicts were resolved.

First, orbit labels could be mistaken for hidden tags. The fused artifact
states that those labels are derived class names and can be erased if the
source relation is retained.

Second, finite variation could be overclaimed. The fused artifact treats edge
edits as changed-source or fail-closed branches and does not claim general
robustness.

## Next Route

The next logical route is P6-T04:
`finite_toy_response_v2_model_zoo_integration` under
`ontology-formalizer@0.2.0`.
