<!-- authority: control -->

# Parent Fusion Notes: P6-T02 Finite Toy Response v2 Model-or-Obstruction

## Result

P6-T02 is completed by a positive model, not by an obstruction.

The fused construction uses the P6-T01 source path
`S_v2={a,b,c}` with adjacency `A_v2={{a,b},{b,c}}`. It defines the
response relation `R_v2({x,y})=d_A(x,y)`, where `d_A` is source graph
distance. The response classes are source automorphism orbit-distance
classes, not independent target tags.

## Synthesis

The mathematics child supplied the finite path object, the source relations,
the orbit-distance structure, and the relabeling-invariance proof. The
philosophy child supplied the model-status boundary: this is a draft/control
finite source toy model, not a physical metric, detector semantics, or
benchmark claim.

The parent resolution keeps both points:

- response values are induced from source adjacency and orbit structure;
- class names are presentation names only;
- finite relation edits are represented by a fail-closed probe for P6-T03;
- no target metric, `g_eff`, matter coupling, Einstein-equation, benchmark,
  or completed-derivation claim is authorized.

## Conflict Resolution

Two possible conflicts were resolved.

First, orbit-distance class labels could be mistaken for a reintroduced tag
route. The fused artifact states that these are invariant classes determined
by the unlabeled finite path, not independent response tags.

Second, finite variation could be overclaimed. The fused artifact defines a
stress target only and assigns adversarial evaluation to P6-T03.

## Next Route

The next logical route is P6-T03:
`finite_toy_response_v2_invariance_and_tag_removal_stress` under
`refuter@0.2.0`.
