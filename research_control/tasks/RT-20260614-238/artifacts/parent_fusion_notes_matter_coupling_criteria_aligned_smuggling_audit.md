<!-- authority: science_draft -->

# Parent Fusion Notes: Criteria-Aligned Coupling-Law Smuggling Audit

The parent fused the physicist-mathematician audit of candidate slots with the
physicist-philosopher separation of ontology, mathematical model, empirical
semantics, and benchmark status.

Consensus:

- `CouplingLaw_src^{GSC-crit-cand}` is source-pure as written pending Refuter
  stress.
- The finite/local token semantics, admissibility, transition transport,
  scoped compatibility, optional source conservation slot, and bottom labels
  remain draft/control candidate data.
- No hidden target import, stress-energy semantics, detector semantics,
  coupling-law adoption laundering, source-law adoption laundering, process
  authority, `MetricData(E)` adoption, `g_eff` scope change, Einstein-equation
  pressure, benchmark promotion, or completed-derivation claim is detected as
  written.

Unresolved limitations:

- Refuter stress must still test transition inverse and cocycle behavior,
  nontriviality, universality overread, scoped-`g_eff` overread, bottom
  completeness, and adoption/promotion pressure.

The fused output is
`267_NONBOTTOM_METRICDATA_WITNESS_SRC_GSC_POST_GATE_MATTER_COUPLING_COUPLING_LAW_CRITERIA_ALIGNED_SMUGGLING_AUDIT.tex`.
