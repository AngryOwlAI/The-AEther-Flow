---
authority: science-draft
status: proposal-only
task_id: RT-20260718-044
candidate: EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1
---

# Parent fusion: rooted-partition law smuggling audit

## Fused result

The exact displayed candidate contains no explicit target atlas, target
metric, proper time, detector, benchmark-success, generated-derivative,
registry, validator, role, handoff, file-order, or task-process premise. Its
parity and kernel-pair theorems are mathematically correct on admitted finite
records.

That local source-factorization pass is non-discriminating. Every nontrivial
ordered partition of a finite token set has a canonical admitted realization:
choose a root in the even block, one bridge token in the odd block, direct
edges from the root to every odd token, and edges from the bridge to every
other even token. The supplied transition and root data can therefore encode
any desired ordered partition before the relation is written. Listing those
data first does not prove that current ontology or source acquisition produced
them.

## Category and variation scope

Candidate morphisms are required to preserve the graph, root, and
pre-relation data that determine parity. Relation preservation then follows by
construction. This is valid category-by-preservation functoriality, but it
does not independently identify physical morphisms.

For four tokens, 72 of 336 partition-permutation cases stabilize the selected
boundary line and 264 change it. All 48 valid one-token transfers between
nonempty blocks change the boundary. Candidate-preserving variations exclude
those transfers, so the finite-variation theorem is conditional closure rather
than robustness evidence.

Strictly increasing relabeling of the ordering parameter with fixed transition
endpoints leaves the graph unchanged. It is label invariance, not physical
covariance.

## Authority separation and next route

The audit classification is a
`conditional_source_factorization_pass_with_universal_partition_encoding_and_category_scope_nondiscrimination`.
The packet is still a `proposal-only new ontology primitive candidate`; current
ontology does not derive its finite provenance package. Adoption remains
`blocked_adoption_open_continuation`.

No Distance-to-GR or metric-use ledger row changes. General EqSrc remains
undischarged. The exact orientation-torsor route remains locally frozen. One
bounded Refuter packet may now stress the unchanged rooted-partition candidate
against universal encoding, independent provenance, broader source morphisms,
and partition-changing variations. No repair, adoption, or promotion is
authorized.
