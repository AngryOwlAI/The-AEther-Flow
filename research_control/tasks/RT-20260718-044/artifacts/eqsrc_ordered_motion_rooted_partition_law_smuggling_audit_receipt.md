---
authority: project-control
status: draft/control
task_id: RT-20260718-044
candidate: EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1
---

# Ordered-motion rooted-partition smuggling-audit receipt

## Verdict

The unchanged exact candidate passes a conditional displayed-source audit:
its definitions contain no explicit target-GR or project-process premise, and
its finite parity and relation theorems are correct on admitted records.

The audit also proves that the admission data are non-selecting. Every
nontrivial ordered finite partition can be realized by a canonical admitted
rooted DAG when transition and root data are freely supplied. Morphism
naturality is category-by-preservation, monotone reparameterization with
unchanged endpoints is label invariance, and the finite-variation theorem
excludes every partition-changing case tested.

## Exact finite evidence

- Sizes two through five have 2, 6, 14, and 30 nontrivial ordered partitions;
  every one is canonically realized.
- On four tokens, the 14 partitions yield 336 permutation cases: 72 stabilize
  the boundary and 264 change it.
- All 48 valid one-token transfers between nonempty blocks change the boundary
  line.
- The unrooted two-cycle has zero automorphism-invariant singleton roots.

These results show possible desired-partition encoding and underived category
scope. They do not prove an explicit target import, a global no-go theorem, or
future source-extension impossibility.

## Status

The candidate remains a `proposal-only new ontology primitive candidate`.
Current ontology does not derive the transition, projection, root, morphism,
or variation package. Adoption remains
`blocked_adoption_open_continuation`. General EqSrc remains undischarged, both
authoritative ledgers remain unchanged, and no physical covariance or
admissibility is established.

## Next route

Run one bounded `refuter@0.2.0` `ontology-law-research-packet` against the
unchanged candidate. Stress universal partition encoding, provenance
selection, root and transition alternatives, broader source morphisms, and
partition-changing variations. Do not repair, adopt, or promote the candidate
inside that stress packet.
