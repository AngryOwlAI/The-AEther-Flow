<!-- authority: control -->

# Parent Fusion Notes: Source Model Zoo Schema

## Summary

The child perspectives agree that P10-T01 must define a finite/local source
model zoo schema, not construct the model zoo itself. The fused design schema
therefore fixes the required fields, eight model kinds, status vocabularies,
certificate-instance compatibility rules, validation contract, and blocked
overread list.

## Fusion Result

The fused output is:

```text
research_control/design/source_model_zoo_schema_v1.md
```

The mathematics child contributed the typed model record grammar, model-kind
vocabulary, relation expectation fields, and fail-closed validation rules. The
philosophy-of-physics child contributed the authority-separation guard:
target metric data, detector semantics, stress-energy data, matter action,
benchmark behavior, registries, validators, roles, handoffs, approvals,
commits, generated derivatives, and local caches are never source model data.

## Preserved Boundaries

- finite/local source fixtures only
- no concrete model zoo construction in P10-T01
- no source-law adoption
- no `EqSrc` discharge
- no `RetainH` or `GenH` adoption
- no unrestricted `RR_E` theorem
- no matter-semantics or detector-semantics adoption
- no stress-energy semantics or matter action
- no Einstein equations
- no benchmark promotion
- no completed derivation

## Next Packet

Run P10-T02 as one bounded `ontology-formalizer@0.2.0` packet that constructs
the initial eight-model source zoo using the schema and P4 certificate
instances.
