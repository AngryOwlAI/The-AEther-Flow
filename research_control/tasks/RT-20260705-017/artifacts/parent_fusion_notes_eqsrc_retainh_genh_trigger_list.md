<!-- authority: control -->

# P9-T01 Parent Fusion Notes

## Inputs

- `child_phys_math_eqsrc_retainh_genh_trigger_list.yaml`
- `child_phys_phil_eqsrc_retainh_genh_trigger_list.yaml`
- `parent_conflict_review_eqsrc_retainh_genh_trigger_list.yaml`

## Fusion

The mathematical child supplies the exact dependency trigger map:
`EqSrc` for record-independent or family-wide equivalence, `RetainH` for
`H`-indexed retention or preservation, and `GenH` for generated-family
construction, enumeration, closure, or theorem input.

The ontology/status child supplies the overread guards: the policy is not a
proof, not primitive adoption, not source-law adoption, and not downstream
physics promotion.

## Fused Output

The fused output is
`research_control/design/eqsrc_retainh_genh_trigger_list_v16.md`.

## Boundary

No upstream primitive route is selected in P9-T01. P9-T02 must integrate the
trigger-status block into selector packets and DAG edge ranking.
