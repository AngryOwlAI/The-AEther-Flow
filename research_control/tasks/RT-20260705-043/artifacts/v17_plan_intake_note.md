<!-- authority: control -->

# V17 Plan Intake Note

## Analysis

`implementations_plans/recommendations_implementation_plan_continue_task-v17.md`
exists in the local working tree and is the user-named implementation plan for
the current goal. Before this packet, the path was ignored by `.gitignore`, so
the plan was not tracked repository evidence. The v15 implementation plan is
explicitly unignored and tracked. The v16 implementation plan is registered as
project-control guidance and remains historical context for the v17 source
basis.

## Intake Result

This packet adds the v17 ignore exception and registers v17 using the same
source-first retrieval pattern as prior registered implementation plans:

- object ID: `MD-RECOMMENDATIONS-IMPLEMENTATION-PLAN-CONTINUE-TASK-V17`;
- source path:
  `implementations_plans/recommendations_implementation_plan_continue_task-v17.md`;
- role: `implementation_plan`;
- authority status: `project_control`;
- audience: `agents`;
- generated metadata note:
  `wiki/markdown/md-recommendations-implementation-plan-continue-task-v17.md`.

## Authority Boundary

The v17 plan is implementation guidance only. It is not a physics proof, a
canonical ontology edit, a source-law adoption,
`RR_ETransportCompletenessOrInvarianceLaw_v1` adoption, unrestricted `RR_E`
theorem status, matter-semantics adoption, detector-semantics adoption, a
coupling-law adoption, matter-coupling derivation, Einstein-equation evidence,
benchmark authority, Gate Chair authority, proof authority, or completed
derivation evidence.

`handoff-0615` remains the deferred scientific route authority for the later
concrete coupling-law candidate construction packet. This packet does not
execute that candidate-construction packet. It records that v17 P0 must build
the execution backlog and run active-state preflight before P1 candidate setup
or construction unless later tracked state proves equivalent work has already
been completed.

## Logical Next Step

Run one bounded P0-T02 backlog materialization packet. That packet should
create `research_control/design/v17_recommendation_backlog.yaml` and
`research_control/design/v17_recommendation_backlog_schema.md`, map every v17
phase task exactly once, and route to P0-T03 active-state preflight.
