<!-- authority: control -->

# Phase 5A Memory-System Page Before/After Review

## Page

`memory-system-explainer`

## Before

The memory-system subject existed in source, registry, wiki, and local
retrieval machinery, and older pre-publication surfaces existed historically.
The active publication-process registry did not contain a reviewed Phase 5A
publication packet for the memory, registry, wiki, semantic, Obsidian, SQLite,
and `.local` retrieval relationship.

## After

The Phase 5A packet adds:

- `markdown/publication-briefs/memory-system.publication-brief.md`
- `markdown/html-explainer-specs/memory-system-explainer.md`
- `github-facing/memory-system-explainer.md`
- `html/memory-system-explainer.html`
- desktop screenshot evidence
- mobile screenshot evidence
- a reviewed publication-brief registry row

The GitHub-facing page reads as a native reference catalog. The HTML page uses
a layered architecture, source matrix, memory preflight workflow, and stale
local retrieval troubleshooting section. Both surfaces state that generated
wiki notes, semantic extracts, Obsidian mirrors, SQLite indexes, and `.local`
caches are retrieval aids only.

## Boundary Review

PASS:

- Source-first memory principle is explicit.
- Registry rows are described as routing, provenance, generated-output, and
  memory metadata.
- Wiki, semantic, Obsidian, SQLite, and `.local` layers are explicitly
  non-authority.
- Memory preflight is framed as navigation followed by canonical source and
  registry inspection.
- Freshness warnings are described as retrieval warnings.
- Bootstrap and validate-only are separated.
- No memory-system behavior, registry schema, validator, routing, role,
  checkpoint, generated-output authority, or physics claim status changed.

Remaining risk:

- The page is an operator reference, not an implementation guide. Readers who
  need script internals must inspect `.codex/skills/project-memory-system/`
  and the relevant scripts directly.
