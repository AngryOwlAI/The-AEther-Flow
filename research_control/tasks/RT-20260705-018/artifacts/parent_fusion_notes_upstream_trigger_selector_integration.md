<!-- authority: control -->

# Parent Fusion Notes: P9-T02 Upstream Trigger Selector Integration

The Physicist-Mathematician child supplied the operational selector rule:
future selectors evaluate `EqSrc`, `RetainH`, and `GenH` trigger status before
route selection, and `dependency_on_missing_eqsrc_retainh_genh` is interpreted
as a route-selection penalty, not as proof.

The Physicist-Philosopher child supplied the ontology and status boundary:
trigger-status recording is not primitive adoption, non-trigger status is not a
permanent no-go theorem, and score/validator/commit evidence is not physics
evidence.

The fused artifact preserves both views. It defines the required completion
fragment, the future theorem-setup integration rule, and the current P9-T02
non-trigger evaluation. No conflicts remain.
