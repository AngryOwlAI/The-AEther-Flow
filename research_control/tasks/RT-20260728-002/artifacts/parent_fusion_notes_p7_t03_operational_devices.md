<!-- authority: science_draft -->

# P7-T03 parent fusion: source operational-device suite

## Shared consensus

Both child perspectives support one decisive but narrow result: a finite,
`draft/control`, `proposal-only` source protocol tuple can implement all five
plan-required role names with explicit preparation, intervention, finite
history, formal token readout, success, and failure data. A total
single-valued readout partitions finite rational bookkeeping history weights;
equal-mass intervention differences sum to zero; and transport of the entire
protocol tuple gives source-presentation naturality.

The shared boundary is equally important. `Clock_src`, `Rod_src`,
`Signal_src`, `Detector_src`, and `FreeFall_src` are guarded formal role names
only. Their tokens are not time, duration, length, causality, probability,
observation, empirical detection, force freedom, geodesic behavior, geometry,
or coupling. The P7-T01 and P7-T02 basis remains proposal-only, and adoption
remains `blocked_adoption_open_continuation`.

## Physicist-mathematician contribution

The mathematical child supplied the complete finite protocol tuple
\(D=(C,P,I,A,r,S,F)\), the history weight and response formulas, exact finite
rational closure, token partition, zero-sum intervention differences, and
full presentation-naturality proof. It constructed five executable witnesses,
including a two-state cycle protocol, two three-address protocols, a
signal-token transducer, and a baseline/intervention comparison.

It also supplied two independent finite systems with an exact Cartesian
success-cell response \(1/16=(1/4)(1/4)\), plus an early-trigger consistency
control whose paired responses are \(1/4\) and \(3/4\). The parent preserves
the general product proof, selects Clock_src and Signal_src as the displayed
disjoint pair, and retains the child's Clock_src and Rod_src pair as an
equally valid unselected instance.

## Physicist-philosopher contribution

The semantic child supplied the model-to-world ladder, protected adoption
ladder, total-readout guard, role-specific smuggling audit, exact
underdetermination statement, and exact-GR recovery obligations. Its central
contribution is that familiar device words do not create their familiar
physical referents.

The parent adopts the stronger language `no declared intervention` or
`unintervened baseline` for the plan-required `FreeFall_src` role. It also
requires an explicit typed source-history and token map before any
signal--detector comparison, and requires presentation transport of
preparations, interventions, alphabets, readouts, success tokens, and failure
classes—not merely transport of the underlying control graph.

## Resolved differences

Thirteen differences were resolved in one parent pass. The principal choices
are:

- use total single-valued readouts and fail partial or multivalued attempts
  closed;
- group the two-step clock histories into `tick_1` and `idle_2` in the fused
  compact control while preserving the child's finer phase-token refinement;
- use a two-state identity-only blocking intervention for the compact
  FreeFall_src witness while retaining the child's added-transition witness
  as a lawful unselected variant;
- use `inconsistent_D` for a formal cross-device contract failure;
- display Clock_src and Signal_src as the independent pair while retaining
  the general proof that covers Clock_src and Rod_src; and
- call P7-T03 complete only at constructed-candidate proposal scope.

Every child concern is resolved in the fused TeX, specification, controls,
assumption table, or explicit authority flags. There are zero unresolved
blocking conflicts.

## Unresolved limitations

The finite preparations, intervention labels, token alphabets, readout maps,
success subsets, failure classifiers, and cross-device identifications are
stipulated proposal inputs. Current ontology does not derive or uniquely
select them. No long-history, continuum, robustness, empirical calibration,
or physical interpretation result is supplied.

P7-T04 common-propagation analysis remains unexecuted. A future formal common
structure would still not by itself establish a physical cone, effective
metric, detector semantics, universality, equivalence principle, matter
action, stress energy, coupling, Einstein equations, or exact-GR recovery.

## Claim boundary

The fused result is `SourceOperationalDeviceSuiteCandidate_v1`, with exact
candidate-relative finite token-response and source-presentation-naturality
theorems. It is not canonical ontology, an adopted source law, physical
operational semantics, independent review, proof authority, empirical
confirmation, publication, or a completed derivation. Validator and
checkpoint success remain operational evidence only. No target geometry,
empirical detector result, desired benchmark behavior, or generated
derivative is used as a scientific premise.
