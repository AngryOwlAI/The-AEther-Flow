# Parent Fusion Notes: Phase 6 MetricData Route Selector

## Analysis

Both child perspectives converge on the same route. The accepted
`PreMetricData_src^{GSC-cand}(G^beta)` evidence is sufficient to justify one
bounded construction-or-obstruction test, but it is not itself `MetricData(E)`,
not an adoption-ready object, and not `g_eff`.

## Fusion Decision

Select one `candidate-constructor@0.2.0` packet for v9 Phase 7:

```text
draft/control MetricData(E) construction or precise OB-GEFF obstruction
```

Required boundary sentence:

```text
This route may construct a draft/control MetricData(E) candidate but may not adopt MetricData(E) and may not define or construct g_eff.
```

## Claim Boundary

No `BranchDisc_src^{GSC}` adoption, no `MetricData(E)` adoption, no checker
proof authority, no arbitrary finite-DAG theorem, no `g_eff`, and no downstream
GR promotion are created by this selector.
