# Parent Fusion Notes: EqSrc Relation–Label Scope Repair

## Synthesis

The independent mathematical and philosophy-of-physics perspectives agree
that the RT-028 obstruction has one exact proposal-level repair. The fused
output defines pointwise discriminator-label fixing separately from
kernel-pair relation preservation and reflection. It retains translation by
\(e_2\) as a pointwise-label sharpness witness, not as a relation-fragility
witness.

The result is proposal-only
`EqSrcIntrinsicDiscriminatorAdmissibilityLaw_src^cand,v2`, classified as a
`new_ontology_primitive_candidate` with
`blocked_adoption_open_continuation`. A fresh Smuggling Auditor review remains
required.

## Mathematical Fusion

Let \(A=Z_1(E)\), \(B=B_1(E)\), \(K=A/B\), and
\(\chi:A\rightarrow K\). For a map \(V:A\rightarrow A\), relation
preservation is equivalent to the existence of a unique induced map
\(\bar V:K\rightarrow K\) satisfying
\(\chi V=\bar V\chi\). Relation reflection is injectivity of \(\bar V\);
pointwise label fixing is the stronger condition \(\bar V=\mathrm{id}_K\).

For a translation \(\tau_c(z)=z+c\),
\[
\chi(\tau_cz)=\chi(z)+[c].
\]
It fixes quotient values pointwise exactly when \(c\in B\), but every
translation preserves and reflects the kernel-pair relation because pairwise
differences are unchanged. Thus the admitted boundary translations are
exactly the pointwise-label-fixing translations, not the full translation
subgroup of relation automorphisms.

In the eight-state witness, \(\tau_{e_2}\) changes all eight quotient values
while producing zero relation mismatches across all 64 ordered pairs. Its
induced quotient map is \(q\mapsto q+[e_2]\).

The fused artifact also retains a stronger algebraic contrast. The invertible
linear map
\[
L(e_0)=e_0,\qquad L(e_1)=e_0+e_2,\qquad L(e_2)=e_1
\]
maps the boundary generator \(e_0+e_1\) to \(e_2\notin B\), so it changes the
relation. It is an ambient source-state bijection only: it is not a boundary
move, an admitted chain map, a source automorphism in the candidate category,
or an established physical variation.

## Provenance and Physical Scope

Once the finite chain package is supplied, the cycle space, boundary subgroup,
homology quotient, quotient map, and kernel pair are conservative finite
algebra. Current ontology does not derive the chain-package family, select its
differential, select homology as the physical discriminator, or establish
boundary moves as physically admissible variations.

Definition order establishes formal non-reference. It does not establish
independent physical selection because the quotient kernel and admitted moves
share \(\partial_2\) as a supplied root. Chain-map naturality remains a formal
theorem rather than physical covariance. Codomain gauge means a mathematical
bijective relabeling here, not an adopted physical gauge symmetry.

## Parent Decision

The exact scope defect is claimed repaired in v2, pending independent audit.
The surviving chain-map naturality, conditional quotient-uniqueness, and
finite admitted boundary-move pointwise robustness results are retained under
their stated hypotheses.

The distinct chain-homology route remains `not_frozen` because this packet adds
new factorization, translation-classification, and linear-stabilizer theorem
payload and corrects the finite witness semantics. The exact earlier
quotient-table cycle remains locally frozen.

The next lawful packet is one fresh bounded
`smuggling-auditor@0.2.0` review of exact v2. Refuter stress may not bypass
that audit.

## Preserved Claim Boundary

No canonical ontology edit, source-law adoption, physical-admissibility or
physical-covariance result, general `EqSrc`, RetainH, GenH, or `M_src`
adoption, Distance-to-GR or metric-use ledger change, `g_eff`, matter coupling,
Einstein equations, benchmark promotion, Gate Chair verdict, completed
derivation, future source-extension impossibility, program-wide no-go, or
global theory rejection follows.
