# Parent Fusion Notes: Phase 11 Effective-Metric Preflight Selector

The parent fused the two child perspectives into one selector artifact.

Shared consensus:

- scoped `M_src` adoption is now available only under the RT-20260614-134
  source-only boundary;
- `g_eff` is still not defined;
- the live missing law is the source-to-effective-metric or metric-response
  law, tracked historically as `MSL-MSRC-METRIC-RESPONSE-LAW`;
- the first legal packet is proposal-only source-law formalization or a
  precise obstruction; and
- any target metric, proper-time, benchmark, registry, validator, role,
  handoff, generated-derivative, or file-order premise must fail closed.

The mathematical child contributed the minimum prerequisite interface:
signature or orientation selector, interval scale selector, causal-cone or
preorder analogue, source-transition covariance, nondegeneracy or controlled
degeneracy, and `Bottom_src` or `OB-GEFF-*` failure branches.

The philosophical child contributed the claim boundary: source-side `M_src`
adoption is not metric existence, metric-law formalization is not empirical
recovery, and a metric-law obstruction is not global theory rejection.

The fused selector therefore chooses one next packet:
`ontology_law_research_packet` executed by `ontology-formalizer@0.2.0` for a
proposal-only `MetricLaw_src^{GSC}` formalization or a precise obstruction.
No `g_eff` construction is authorized.
