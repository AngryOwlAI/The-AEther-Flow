<!-- authority: control -->

# Parent fusion notes: P3-T01 selector-resolution taxonomy

The parent fused two independent internal perspectives into one registered
taxonomy, one machine decision schema, and five required worked controls. The
children did not create or dispatch AgentJobs and did not expand the outer
job's authority.

Shared consensus retained:

- the primary class set is exactly added source structure, representative
  irrelevance, dynamical symmetry breaking, probabilistic selection, and
  scoped obstruction;
- one complete record receives exactly one primary class or fails closed;
- the primary order is RI, PS, DSB, AS, then SO;
- supporting classes retain auxiliary mechanisms, evidence, and assumption
  costs without inheriting the primary class's proof;
- representative irrelevance is a nonvacuous factorization or complete
  all-choice independence result, not gauge terminology;
- dynamical and probability routes require explicit state, evolution, measure,
  kernel, stability, and sampling data appropriate to their claims;
- obstruction language remains inside exact declared quantifiers and names
  concrete reopening criteria; and
- no class label changes ontology, physics, empirical, authority, EqSrc,
  Distance-to-GR, benchmark, promotion, or publication status.

Unique physicist-mathematician contributions retained:

- five explicit evidence predicates and failure conditions;
- the nonvacuous RI guard and probability-target typing guard;
- a deterministic exact-one-primary/fail-closed proposition with proof;
- a componentwise assumption-cost vector that preserves incomparability; and
- seven auditable controls, including RI-over-DSB and PS-over-stochastic-
  dynamics overlap cases.

Unique physicist-philosopher contributions retained:

- separate ontology, model, physical, empirical, and authority claim layers;
- explicit prohibitions on treating structural automorphisms as physical
  gauge or a formal class as empirical adequacy;
- distinctions among spontaneous, explicit, boundary-induced, and
  noise-induced selection; and
- class-specific advancement evidence and invalid promotional language.

The parent resolved five synthesis differences in one round. Target types were
normalized to five top-level result kinds without erasing finer mathematical
typing. Per-assumption provenance records and a componentwise cost vector were
both retained. RI now fails on a vacuous empty selector image. DSB remains the
plan-mandated route label, but claims use the weaker phrase
"dynamics-mediated selection" unless equivariant-law and non-invariant-state
evidence is present. Overlaps are supporting fields with explicit rationales,
never multiple primary classes.

There are no unresolved blocking conflicts within P3-T01. Remaining limits are
deliberate scope boundaries: the source-category package is proposal-only;
current ontology derives none of the five mechanisms; P3-T02 still owns the
source-extension lattice; P3-T03 and P3-T04 remain unexecuted; and protected
scientific or publication decisions remain human-gated.

Fused taxonomy identity:
`EQSRC-SELECTOR-RESOLUTION-TAXONOMY-V1`.
