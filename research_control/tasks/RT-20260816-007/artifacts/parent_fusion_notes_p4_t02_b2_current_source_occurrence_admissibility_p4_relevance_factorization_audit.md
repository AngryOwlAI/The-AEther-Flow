<!-- authority: science_draft -->

# Parent fusion: current-source occurrence and admissibility P4-relevance factorization audit

## Decisive result

RT-20260816-007 returns exactly `current_source_combined_extension_typing_or_provenance_obstruction`, with obstruction `OB-V22-P4T02-B2-CURRENT-SOURCE-COMBINED-OCCURRENCE-EXTENSION-TYPING-PROVENANCE-001`.

The registered P4-T01 source narrowly supplies the fixed six-channel source-law presentation `D_source`, its lawful reduced principal construction `Prin`, and the determinant-line source-principal codomain `Y_PS`. The first missing current-source primitive is not `Y_PS`; it is a target-free combined occurrence/admissibility extension carrier `X_OCC`, together with its source-arrow morphism class and an independently provenanced attachment rule over `D_source`. Because `X_OCC` is not current-source formed, neither `U : X_OCC -> D_source` nor the actual recomputation map `R : X_OCC -> Y_PS` has a current-source instance.

The result is scoped. It does not prove that no conservative source extension can ever form `X_OCC`, and it does not turn missing provenance into global P4 irrelevance. Current adoption remains blocked while materially distinct same-milestone continuation remains open.

## Exact conditional mathematics

For arbitrary typed maps `U : X -> D` and `R : X -> Y`, a unique factor `f_im : im(U) -> Y` exists exactly when `R` is constant on every `U`-fibre. This theorem is canonical only on `im(U)`. An all-`D` factor additionally requires an extension away from `im(U)`; surjectivity gives global uniqueness, while a non-surjective `U` and a codomain with at least two elements permit multiple extensions. A singleton codomain is the exact degenerate exception.

A disclosed product control `X_ctrl = D_source x E` is factorable when every source equation, highest-derivative coefficient, constraint, gauge generator, redundancy, and lawful reduced quotient is held fixed. In that control, the actual P4-T01 recomputation satisfies `R_PS = Prin after U`. This is a conditional theorem over proposal-only source-extension data, not a derivation of `X_OCC`, its arrows, or its attachment rule.

The exact falsifiers are sharp. At `k=(1,2,3,5)`, the registered six-factor polynomial has value `60480`; changing the first source vector from `(1,1,0,0)` to `(1,2,0,0)` changes the value to `100800`. That is a source-principal coefficient change, not an occurrence-only variation. Introducing a constraint, gauge, or redundancy direction that removes one reduced channel changes determinant degree from six to five and is likewise outside an honest conservative fibre. Finally, the same two-root unselected fibre supports either a constant factorable relevance map or a separating nonfactorable relevance map, proving that nonselection alone determines neither relevance nor irrelevance.

## Parent-child synthesis

The Physicist-Mathematician and Physicist-Philosopher children independently converged on the same exclusive result, first failed primitive, P4-T01 boundary, conditional theorem family, controls, nine inherited freezes, fourteen literal no-delta rows, and authority limits. One identity-only round canonicalized the obstruction ID; no scientific result, hypothesis, count, freeze, or authority boundary changed.

The fused result preserves both children’s distinctive contributions. The mathematical child supplies the exact extension and uniqueness qualifications, singleton exception, naturality control, and operator/reduction arithmetic. The philosophical child supplies the layer-by-layer separation between current-source law, combined-carrier formation, conditional mathematics, source-principal relevance, physical meaning, and workflow authority.

## Claim and freeze boundary

`Y_PS` remains registered source-principal data only. No physical characteristic cone, Lorentzian conformal class, operational response, physical probability, realized occurrence, empirical frequency, common matter cone, time orientation, or effective metric is obtained. No source law or ontology is adopted. D7 is not reevaluated, B2 remains inactive, P4-T02 remains incomplete, and P4-T03 remains locked.

All nine inherited candidate-local freezes remain active and untouched. No new freeze is created because this result identifies a first current-record formation prerequisite rather than refuting a formed candidate family. An unchanged replay without newly source-provenanced `X_OCC` data remains outside scope. Every one of the fourteen Distance-to-GR rows remains `no_delta`.

## Selected continuation

Exactly one later packet is selected but not executed: `PKT-V22-P4T02-B2-POST-OCCURRENCE-P4-RELEVANCE-FACTORIZATION-OBSTRUCTION-THEORETICAL-CONTINUATION-SELECTION-V1`, packet type `theoretical_continuation_selector`, under `theoretical-continuation-selector@0.1.0`.

That selector may compare a narrowly conservative source-principal irrelevance theorem, a source-provenance acquisition or ontology-law research packet, another current-source carrier construction, and a protected human-gated stop. It may not reconstruct a frozen candidate, assume the missing carrier, import target geometry, claim a global no-go, execute a successor, act on B2, unlock P4-T03, promote, publish, push, or take external action.
