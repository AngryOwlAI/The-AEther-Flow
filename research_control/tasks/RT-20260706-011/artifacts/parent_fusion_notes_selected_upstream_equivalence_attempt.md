<!-- authority: science_draft -->

# Parent Fusion Notes: Selected Upstream Equivalence Attempt

Both child perspectives select the same bounded result for v17 P6-T02:
execute `EqSrc_theorem_attempt` as a record-local relabeling theorem candidate
with explicit premises and preserve an obstruction to general `EqSrc`
promotion.

The mathematical child supplies the constructive theorem shape: an explicit
finite source-record relabeling orbit with reflexive, symmetric, and
transitive branches only when the relabeling maps and their compositions are
supplied as source-side data.

The philosophical child supplies the authority boundary: this is draft/control
formalization, not canonical ontology and not downstream physics recovery.
General `EqSrc` remains open because family-wide comparison closure,
`RetainH`, and `GenH` are not derived by the current ontology.

No conflict remains. The fused output is:

`research_control/tasks/RT-20260706-011/artifacts/selected_upstream_equivalence_attempt_v1.tex`

