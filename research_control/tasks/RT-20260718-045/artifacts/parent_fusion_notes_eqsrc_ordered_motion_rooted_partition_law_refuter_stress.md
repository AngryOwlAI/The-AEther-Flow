---
authority: science-draft
status: proposal-only
task_id: RT-20260718-045
candidate: EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1
---

# Parent fusion: ordered-motion rooted-partition-law Refuter stress

## Fused result

The unchanged candidate's conditional algebraic core survives. Once a finite
transition graph, event projection, singleton root, morphism category, and
variation family are loaded and pass admission, root parity and the
kernel-pair relation are well-defined.

The Refuter result is nevertheless `scoped_obstruction`. Current ontology does
not derive the loaded data, and the unchanged candidate does not select a
unique relation from transition structure alone.

## New mathematical pressure

For a reachable bipartite transition graph, root parity gives the unique
bipartition up to exchange. Roots on the same side produce the same ordered
blocks; roots on opposite sides exchange them. Since the boundary lines
\(\langle\mathbf 1_Q\rangle\) and
\(\langle\mathbf 1_P\rangle\) are distinct, a graph with admissible roots on
both sides supports two candidate relations unless an independent root law
selects one side.

The exact four-token census enumerates all 4,096 loop-free directed graphs and
all 16,384 graph-root configurations. It finds:

- 1,088 structurally admitted rooted records;
- 557 transition graphs with at least one admitted root;
- 292 graphs inducing one relation; and
- 265 graphs whose alternative roots induce two distinct relations.

For fixed odd-block size one, two, or three, each specific partition has 24,
160, or 8 rooted transition realizations respectively. Thus even after a
relation is fixed, the proposal syntax does not select transition provenance.

## Countermodel and symmetry

The minimal two-token directed cycle and the strengthened four-token directed
cycle have no automorphism-invariant singleton root. For the four-cycle, all
four roots pass structural reachability and path parity, yet they induce two
relation lines. The order-four automorphism group acts transitively on roots,
while only an order-two subgroup preserves a selected relation.

Loading a root certificate resolves the mathematical ambiguity by adding
proposal data. It does not derive that certificate from current ontology.
Choosing the first observed token would break the symmetry, but the candidate
correctly labels such a choice observer truncation and fails closed.

## Variation and covariance scope

The fourteen nontrivial four-token partitions have 182 ordered distinct
relation changes. Their Hamming-distance distribution is 48, 72, 48, and 14
at distances one through four. Every partition change changes the boundary
line; the candidate's variation class excludes the entire changing orbit.

The prior 336-case permutation census is recovered: 72 cases stabilize the
relation and 264 change it. Naturality under graph-and-root-preserving maps is
therefore a valid conditional statement, not independently selected physical
covariance.

## Classification, freeze, and next route

The failed premise is a source-derived transition, projection, root, morphism,
and variation law that selects one relation independently of the relation
being sought. The candidate remains `proposal-only`,
`new_ontology_primitive_candidate`, and
`blocked_adoption_open_continuation`.

The exact candidate-v1 construction, audit, and stress route is locally frozen.
No Distance-to-GR or metric-use ledger row changes. The next bounded route is
one `theoretical-continuation-selector@0.1.0`
`ontology-law-research-packet` to select a genuinely source-derived
transition/root provenance law, a source-side irrelevance theorem, or a
distinct scoped no-go target with new mathematical payload. It may not repeat,
repair, adopt, or promote exact candidate v1.
