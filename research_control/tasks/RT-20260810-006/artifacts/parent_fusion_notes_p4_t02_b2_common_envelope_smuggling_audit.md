<!-- authority: control -->

# Parent fusion notes: P4-T02 B2 common-envelope smuggling audit

Task `RT-20260810-006` uses one outer `smuggling-auditor@0.2.0` AgentJob with two internal child perspectives. Neither child creates a separate AgentJob or receives repair, adoption, adequacy, Gate, publication, push, or external-action authority.

## Fused result

The final verdict is `repair_required`, not `source_pure_as_written`. The written candidate contains no forbidden target-GR, empirical-answer, benchmark, or process-authority premise. Its common-positive goal property is transparently loaded through explicit proposal-only `h_0`, `u`, `z`, `v_s`, and orientation data, so the construction remains a legitimate conditional source-side existence witness with no derivation credit.

One mathematical statement is nevertheless false. The exact sector vectors obey

`v_R + v_D = 2 v_S`,

and therefore every response obeys

`r_R + r_D = 2 r_S`.

The response map has rank two, kernel `ann(span{u,z})` of dimension two, and positive-projective image dimension one. On the nonzero domain its ray is parameterized by

`t = epsilon k(z)/k(u) = (p_R-p_D)/(2p_S)`.

Thus the repeated RT005 statement that `Q_ray` retains two independent relative response ratios overcounts the actual image. Two coordinate ratios may be displayed, but they satisfy one relation. The corrected count is one independent continuous projective parameter, with the discrete sign of `p_S` separating opposite ray orientations. The exact `Q_sign` image contains six nonzero ordered sign orbits; the alternating patterns `(+,-,+)` and `(-,+,-)` cannot satisfy the response relation.

## Preserved conditional mathematics

The rank correction does not invalidate the product polynomial, common component, `Q_ray` or `Q_sign` definitions, or either all-positive preimage identity. The source-presentation pullback and local strict-margin result also remain valid at their written conditional scope.

The parent preserves the semantic audit consensus:

- `Q_ray` is formal fixed-representative syntax modulo one common positive scale; it is not invariant under independent sector normalization.
- `Q_sign` is formal ordered-sign syntax modulo independent positive scales; it discards every magnitude and ratio.
- Formal principal scores are a new proposal-only request/readout primitive, not exact P7 or empirical device responses.
- A common formal time-covector component does not identify the three characteristic hyperplanes or establish physical causality.
- Three equipped controls do not establish all P7 sector coverage.
- Roles, registries, validators, generated derivatives, handoffs, child agreement, and checkpoints supply no physics or adoption authority.

## Conflict resolution

The Physicist-Mathematician child initially returned `repair_required` from the exact rank lemma. The Physicist-Philosopher child initially returned `source_pure_as_written` because all semantic and authority imports were absent. The parent requested one bounded parallel revision round. Both children now agree that absence of smuggling and correctness of the written information claim are separate predicates. The exact aggregate verdict is `repair_required`, while `target_import_detected` remains false.

No blocking conflict remains. The repair is narrow and known; it is not executed in this audit.

## Source-extension and freeze status

Finite product, intersection, group-orbit formation, and pullback remain conservative operations once the inputs are supplied. The sector forms, common orientation, fixed representative and formal-unit convention, transition data, covector request, and response-token law remain unadopted new ontology-primitive candidate data. Current adoption stays `blocked_adoption_open_continuation`.

The local `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR` freeze and `OBST-V22-P4T02-B2-NATURAL-LINE-LOCK-001` obstruction are preserved. The independently split branch is not frozen because this audit adds a new exact rank lemma and exposes a bounded repair. No global no-go or future-extension impossibility follows.

## Exact repair and next role

The next unexecuted packet is one `source_extension_candidate` repair under `candidate-constructor@0.2.0`. It may only:

1. replace every two-independent-ratio claim with the one-parameter response-image statement;
2. record the image plane, rank, kernel, projective dimension, parameter `t`, and six realized sign orbits;
3. state that `Q_ray` is formal syntax in fixed commensurately typed trivializations and has no physical cross-sector unit meaning;
4. transport `h_0` explicitly on overlaps and state sector relabeling as typed equivariance; and
5. preserve the product, common cone, quotient definitions, preimage identities, finite sector scope, prior freeze, and every downstream block.

Refuter stress should receive the corrected artifact, not one known to misstate its information content. The Candidate Constructor repair is selected but not executed in RT006.

## Non-conclusion

No D7 or P2-T01 adequacy evaluation, descriptor completion, B2 activation, viability, adoption, rejection, or disposition; P4-T03 unlock; physical causal quotient; empirical response; universal propagation; Lorentzian conformal structure; scale; `g_eff`; matter coupling; Einstein equation; Distance-to-GR change; Gate or benchmark verdict; proof authority; global no-go theorem; future-extension impossibility; publication; push; or external action follows.
