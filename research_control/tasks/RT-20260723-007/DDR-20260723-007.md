---
authority: "control"
decision_id: "DDR-20260723-007"
task_id: "RT-20260723-007"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "process-integrity-auditor"
selected_role_version: "0.1.0"
agent_job_id: "AJ-RT-20260723-007-001"
status: "completed"
requires_human_gate: false
created_at: "2026-07-23T13:36:01Z"
activated_at: "2026-07-23T13:36:01Z"
completed_at: "2026-07-23T14:34:49Z"
validation_status: "FAIL"
supersedes_decision_id: "DDR-20260723-006"
role_fit_candidates:
  - "process-integrity-auditor@0.1.0|selected|The correct handoff identity and four one-byte EOF normalizations are uniquely determined by tracked and hash-sealed evidence."
  - "validator-engineer@0.2.0|not_selected|No validator behavior or test semantics require modification."
  - "project-control-maintainer@0.2.0|not_selected|No control policy, schema, or routing rule requires modification."
  - "documentation-curator@2.0.0|not_selected|The handoff change is identity metadata, not explanatory documentation."
---

# DDR-20260723-007 — Repair the exact checkpoint integrity findings

## Current Objective

Repair only the two generation-74 blockers: make the latest handoff Markdown
identify `handoff-0839`, and remove one final blank line from each of the four
paths named by the immutable checkpoint blocker. Prove all other bytes and
metric semantics unchanged, rerun the required gates, and invoke one normal
checkpoint before any P12-T06 execution.

## Authority Surfaces Read

The repository control instructions, `improve-project-system`, Project-System
Director and Process Integrity Auditor roles, the generation-74 immutable
route and 85-path manifest, `program_state.yaml`, `handoff-0839` YAML and
Markdown, the RT-20260723-006 AgentJob and completion, the hash-sealed EOF
blocker, the ordinary-route policy, relevant registries, and the live
continuation boundary were inspected. Generation 74 was claimed and consumed
exactly once before this worker step.

## Source-Backed Resolution

The YAML handoff identity is `handoff-0839`, while the paired Markdown omits
that literal identifier. The checkpoint blocker names four files whose last
four bytes end in two newline bytes and authorizes removal of exactly one.
These five repairs have one unique state and require no policy, validator,
scientific, ontology, or protected-authority decision.

## Role-Fit Matrix

| Role | Fit | Reason |
| --- | --- | --- |
| `process-integrity-auditor@0.1.0` | selected | Owns uniquely determined control-state and handoff metadata repairs. |
| `validator-engineer@0.2.0` | not selected | Validator behavior and tests remain unchanged. |
| `project-control-maintainer@0.2.0` | not selected | Policy and routing semantics remain unchanged. |
| `documentation-curator@2.0.0` | not selected | No explanatory source update is required. |

## Required Role Decomposition

Not applicable. This is a non-physics project-system integrity recovery.

## Target Derivation Milestone

Not applicable. The recovery creates no physics payload and no Distance-to-GR
delta.

## Claim Boundary

The AgentJob may add one canonical identity marker, remove four exact final
newline bytes, create task-local equivalence evidence, synchronize the
transaction, and invoke one checkpoint. It may not alter metric content,
tests, runtime behavior, route history, science sources, ledgers, P12-T06, or
any protected authority.

## Validation

PASS requires exact expected post-repair hashes, one terminal newline in each
EOF target, a visible `handoff-0839` identifier, unchanged metric/test
semantics, the focused and 155-test P12 gates, project-control gates, and one
normal checkpoint commit without legacy validation.

## Resolution

The identity marker and four exact newline bytes match their predetermined
post-repair state. The task-local validator and receipt preserve every other
target byte and all authority nonclaims. The one checkpoint invocation then
stopped without commit when the full repository shard exposed a distinct
P10-T05 event-store architecture dependency. The entry index was restored.
This decision authorizes no retry, legacy validation, P10 repair, or P12-T06
execution; one fresh bounded recovery is required.
