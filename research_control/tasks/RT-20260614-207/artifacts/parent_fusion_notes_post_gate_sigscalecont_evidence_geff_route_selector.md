<!-- authority: science_draft -->

# Parent Fusion Notes: Post-SigScaleCont g_eff Route Selector

Both child perspectives select one bounded `candidate-constructor@0.2.0`
packet as the next lawful route.

The mathematical reason is that `RT-20260614-206` accepted
`SigScaleCont_src^{GSC}` only as scoped source-extension selector
evidence/precondition. That removes the immediate human-gate precondition for a
new construction-or-obstruction attempt, but it does not construct `g_eff`.

The ontological reason is that the accepted evidence remains weaker than
source-law adoption. The next packet can test whether the accepted scoped
`MetricData(E)` evidence plus scoped `SigScaleCont_src^{GSC}` selector
evidence/precondition determines a draft/control effective-metric candidate or
instead yields one precise `OB-GEFF` obstruction.

No conflict remains. The fused artifact selects Candidate Constructor and keeps
all downstream GR promotion blocks active.
