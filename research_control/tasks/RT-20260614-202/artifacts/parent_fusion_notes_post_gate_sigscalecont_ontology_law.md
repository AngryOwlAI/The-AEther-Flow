<!-- authority: science_draft -->

# Parent Fusion Notes: RT-20260614-202

The child analyses agree that `SigScaleCont_src^{GSC}` can be drafted only as proposal-only source-extension data. It is a selector-law interface, not an adopted law and not a `g_eff` object.

The fused artifact therefore defines:

- a source-side signature-label token;
- a source-side scale-normalization token;
- a source-side transition-continuum obligation record;
- fail-closed bottom behavior;
- audit obligations for hidden target import and authority laundering.

The packet preserves `blocked_adoption_open_continuation`. The next lawful packet is Smuggling Auditor audit, not adoption and not effective metric construction.
