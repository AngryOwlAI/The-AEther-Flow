<!-- authority: control -->

# V18 Plan Intake Note

## Analysis

`implementations_plans/recommendations_implementation_plan_continue_task-v18.md`
exists in the local working tree and is the user-named implementation plan for
the active goal. Before this packet, the path was ignored by `.gitignore`, so
the plan was not tracked repository evidence. The v17 implementation plan is
explicitly unignored, tracked, and registered as project-control guidance.

`handoff-0672` remains the latest normal research-continuation handoff. The
later `RT-20260707-003` CI registry path visibility repair is project-system
sidecar evidence only; it has no ordinary research handoff and does not
supersede `handoff-0672` for v18 scientific routing.

## Intake Result

This packet adds the v18 ignore exception and registers v18 using the same
source-first retrieval pattern as prior registered implementation plans:

- object ID: `MD-RECOMMENDATIONS-IMPLEMENTATION-PLAN-CONTINUE-TASK-V18`;
- source path:
  `implementations_plans/recommendations_implementation_plan_continue_task-v18.md`;
- role: `implementation_plan`;
- authority status: `project_control`;
- audience: `agents`;
- generated metadata note:
  `wiki/markdown/md-recommendations-implementation-plan-continue-task-v18.md`.

## Recommendation Coverage

P0-T01 records the v18 recommendation set without implementing downstream
recommendation work:

- `V18-R01`: upstream `EqSrc` / `RetainH` / `GenH` theorem-or-countermodel.
- `V18-R02`: typed source-equivalence object.
- `V18-R03`: minimal countermodel obligations.
- `V18-R04`: source detector/readout semantics frontier burden.
- `V18-R05`: non-tag-fragile finite toy response model v2.
- `V18-R06`: authority-limited support formalization tooling.
- `V18-R07`: physics-payload ratio and route-orbit control.
- `V18-R08`: active-state semantics after sidecars.
- `V18-R09`: public status-card v2 and cognitive-load reduction.
- `V18-R10`: focused external-review packet preparation without outreach.

## Authority Boundary

The v18 plan is implementation guidance only. It is not a physics proof, a
canonical ontology edit, source-law adoption, general `EqSrc` discharge,
`RetainH` adoption, `GenH` adoption, `MetricData(E)` adoption, `g_eff` scope
expansion, source detector/readout semantics adoption, coupling-law adoption,
matter-coupling derivation, Einstein-equation evidence, benchmark authority,
Gate Chair authority, proof authority, external outreach authorization, or
completed derivation evidence.

`handoff-0672` remains the scientific source basis for the later upstream
`EqSrc` / `RetainH` / `GenH` theorem-attempt packet. This P0-T01 packet does
not execute that theorem attempt. It records that v18 P0 must build the
execution backlog and run active-state preflight before P1 active-state
bifurcation or later physics packets unless later tracked state proves
equivalent work has already been completed.

## Logical Next Step

Run one bounded P0-T02 backlog materialization packet. That packet should
create `research_control/design/v18_recommendation_backlog.yaml` and
`research_control/design/v18_recommendation_backlog_schema.md`, map every v18
phase task exactly once, prove dependency acyclicity, and route to P0-T03
active-state preflight.
