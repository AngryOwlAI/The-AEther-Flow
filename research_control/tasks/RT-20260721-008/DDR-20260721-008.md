---
authority: "control"
decision_id: "DDR-20260721-008"
task_id: "RT-20260721-008"
director_version: "director-of-research@0.3.0"
decision_type: "task_overlay"
selected_role_id: "validator-engineer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260721-008-001"
status: "completed"
requires_human_gate: false
created_at: "2026-07-22T05:04:54Z"
activated_at: "2026-07-22T05:04:54Z"
completed_at: "2026-07-22T05:20:54Z"
validation_status: "PASS"
supersedes_decision_id: "DDR-20260721-007"
role_fit_candidates:
  - "validator-engineer@0.2.0|selected|P10-T06 is a bounded deterministic implementation parity and failure-reporting packet under the already fixed P10-T05 architecture."
  - "project-control-maintainer@0.2.0|not_selected|P10-T05 already selected storage identity authority migration and rollback contracts so no new architecture or policy choice is required."
  - "memory-system-maintainer@0.2.0|not_selected|A disposable SQLite index is exercised but the primary work is event validation and shadow-view parity rather than retrieval-memory maintenance."
  - "documentation-curator@2.0.0|not_selected|The Markdown views are machine-generated task-local compatibility evidence rather than explanatory publication material."
---

# DDR-20260721-008 — Activate v21 P10-T06 read-compatible event-store pilot

## Decision

Activate exactly one bounded `validator-engineer@0.2.0` AgentJob for v21
`P10-T06`. The job may create one task-local shadow event stream and manifest,
one disposable untracked SQLite query index, deterministic task, candidate,
handoff, and status views, exact parity and loss receipts, fixtures, focused
tests, standard control records, and one next handoff.

## Evidence

- `P10-T05` is finalized by `AJC-AJ-RT-20260721-007-001` at SHA-256
  `2517f883a575fa8e82379f91d59b2cb4ba85f1f8da7d64a2a2373907b8ec3de2`.
- `handoff-0808` selects exact `P10-T06` and restricts it to a bounded,
  non-authoritative read-compatible pilot with no cutover.
- Commit `0cfe0fe53f31ea2e1d3855d397ba30258dd4f444` is the clean checkpoint that
  contains the complete selected RT-005 through RT-007 source slice.
- The current frontier and Director summary both report a clean
  `director_decision_required` boundary for this packet.
- Memory retrieval identified the registered P10-T05 architecture decision
  and v21 plan; their canonical source hashes were inspected directly.

## Role-Fit Matrix

| Role | Fit | Reason |
| --- | --- | --- |
| `validator-engineer@0.2.0` | selected | The architecture is fixed; this packet implements deterministic replay, stable hashes, source-link checks, parity checks, and focused failure tests. |
| `project-control-maintainer@0.2.0` | not selected | No storage, authority, migration, or rollback policy choice remains inside P10-T06. |
| `memory-system-maintainer@0.2.0` | not selected | SQLite is disposable pilot output, not retrieval-memory authority. |
| `documentation-curator@2.0.0` | not selected | Generated shadow Markdown is compatibility evidence, not publication content. |

## Boundary

The source slice is read from the pinned checkpoint rather than mutable
working-tree registries. The pilot may write only task-local tracked evidence
and an ignored `.local` SQLite cache. It may not replace a live registry,
modify a shared renderer, activate dual writes, switch a reader, begin
historical backfill, infer missing history, reinterpret candidate status,
change registered science or Distance-to-GR, execute protected `P4-T05`, or
authorize ontology selection, promotion, proof, publication, or completed
derivation.

## Role decomposition and derivation status

Parent-child physics synthesis is not applicable because this is project-system
validation work. The inherited milestone label is navigational only; the job
produces no mathematical payload and no Distance-to-GR delta.

## Success and stop conditions

Success requires a stable full-hash event chain, exact pinned source hashes,
byte-identical clean rebuilds, a verified disposable SQLite projection, zero
unmapped fields inside the declared slice, explicit exclusions outside it, and
passing focused plus governed validation. Stop on source-hash drift, ambiguous
identity, unknown schema, non-deterministic bytes, parity loss, authority
violation, sensitive-data capture, repository mismatch, or checkpoint failure.

## Completion

The pilot produced 20 immutable source-linked shadow events across seven
domains, four deterministic compatibility views, and one reproducible ignored
SQLite index. Thirteen internal checks and 13 focused tests pass with zero
mismatches and zero unmapped fields. The receipt records three legacy-shape
issues and five migration risks, including ordering, handoff aliases, decimal
normalization, and untested writer or protected-authority behavior. The result
is qualifying project-control feasibility evidence only: no reader, writer,
registry, history, science, or protected-authority surface changed. `P10-T08`
is selected as the dependency-ready successor under a fresh AgentJob.
