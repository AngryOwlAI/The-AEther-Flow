---
authority: "control"
decision_id: "DDR-20260723-010"
task_id: "RT-20260723-010"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "process-integrity-auditor"
selected_role_version: "0.1.0"
agent_job_id: "AJ-RT-20260723-010-001"
status: "completed"
requires_human_gate: false
created_at: "2026-07-23T17:22:12Z"
activated_at: "2026-07-23T17:22:12Z"
completed_at: "2026-07-23T17:31:49Z"
validation_status: "PASS"
supersedes_decision_id: "DDR-20260723-009"
role_fit_candidates:
  - "process-integrity-auditor@0.1.0|selected|The correct handoff identity is uniquely fixed by the canonical YAML ID and requires only two heading-byte replacements."
  - "validator-engineer@0.2.0|not_selected|The validator correctly detected the missing literal and needs no behavior change."
  - "project-control-maintainer@0.2.0|not_selected|No policy schema routing or permission semantics require modification."
  - "documentation-curator@2.0.0|not_selected|The repair is an exact control-state identity synchronization rather than discretionary explanatory copy."
---

# DDR-20260723-010 — Restore the canonical handoff-0842 Markdown identity

## Current Objective

Repair only the checkpoint finding preserved by `handoff-0842`: normalize the
Markdown heading from `Handoff 0842` to the exact canonical `handoff-0842`
identity by changing one letter's case and one separator byte. Preserve every
other Markdown byte, the YAML handoff, the completed RT-008 parity recovery,
all protected P10 recovery bytes, and invoke one normal checkpoint before
P12-T06.

## Authority Surfaces Read

The repository control instructions, `improve-project-system`, Project-System
Director and Process Integrity Auditor role contracts, the generation-77
immutable route and exact 123-path manifest, `program_state.yaml`,
`handoff-0842`, the RT-20260723-009 task, AgentJob, completion and blocker,
relevant registries, the validation-obligation policies, and the live
continuation boundary were inspected. Generation 77 was claimed and consumed
exactly once before this worker step.

## Source-Backed Resolution

The YAML handoff declares `handoff_id: "handoff-0842"`. The blocker records
that the Markdown mirror's current heading is semantically correct but lacks
that exact literal, and explicitly identifies a punctuation-or-case-only
repair. Replacing the initial uppercase `H` with lowercase `h` and the
intervening space with `-` is therefore the unique minimum correction.

## Role-Fit Matrix

| Role | Fit | Reason |
| --- | --- | --- |
| `process-integrity-auditor@0.1.0` | selected | Owns uniquely determined control-state identity and mirror-parity repairs. |
| `validator-engineer@0.2.0` | not selected | Validator behavior is already correct. |
| `project-control-maintainer@0.2.0` | not selected | No contract or policy semantics change. |
| `documentation-curator@2.0.0` | not selected | The exact canonical ID leaves no copy choice. |

## Required Role Decomposition

Not applicable. This is a non-physics project-system integrity recovery.

## Target Derivation Milestone

Not applicable. The repair creates no physics payload and no Distance-to-GR
delta.

## Claim Boundary

The AgentJob may replace exactly two heading bytes in
`research_control/handoffs/handoff-0842.md`, create task-local preservation
evidence, synchronize normal control derivatives, and invoke one checkpoint.
It may not change `handoff-0842.yaml`, any other source byte in the Markdown
mirror, validator behavior, completed recovery outputs, P12-T06, science
sources, ledgers, or protected authority.

## Validation

PASS requires the exact canonical literal, exactly two replaced bytes with
unchanged length and every other Markdown byte preserved, unchanged protected
hashes, a passing live routing snapshot, the required project-control gates,
and one governed checkpoint.

## Completed Resolution

The selected AgentJob replaced exactly the two intended heading bytes. The
Markdown mirror now contains `handoff-0842`, its length is unchanged, reversing
the replacements reconstructs the sealed pre-repair hash, and every other byte
is identical. The YAML handoff, completed RT-008 parity evidence, and protected
P10 recovery hashes are unchanged. The live routing snapshot and four focused
regressions pass. One normal governed checkpoint remains required before
P12-T06.
