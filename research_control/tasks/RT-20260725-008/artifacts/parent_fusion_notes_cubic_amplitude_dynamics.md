---
authority: science_draft
status: draft_control
task_id: RT-20260725-008
job_id: AJ-RT-20260725-008-001
plan_task_id: P5-T03
---

# P5-T03 parent fusion notes

## Fused construction

The internal perspectives converge on one bounded candidate built on the exact
P5-T01 torus-amplitude witness:

`X_gamma(q_a) = -gamma a^3 sin(theta_1)` for `gamma>0`.

The resulting equation `da/dlambda=-gamma a^3` has the unique global forward
solution

`a(lambda)=a0/sqrt(1+2 gamma a0^2 lambda)`.

Its integrated maps form a jointly smooth unital forward action compatible
with the proposal-only P5-T02 semantics. This fills `S_dyn` only in one named
task-local witness instance. It is `draft/control`, `proposal-only`,
`source-extension data`, and `blocked_adoption_open_continuation`.

## Mathematical content retained

Direct substitution proves composition. The right derivative at zero recovers
the generator. For every positive parameter the amplitude map is injective
but has the proper range

`(-1/sqrt(2 gamma lambda), 1/sqrt(2 gamma lambda))`.

Accordingly, it has no inverse self-map of the whole state space. Every
nonzero backward orbit blows up at finite negative parameter, so the candidate
does not extend to a global real-parameter action on the full amplitude
family.

The source control functional `L=a^2/2` satisfies
`dL/dlambda=-gamma a^4=-4 gamma L^2`. The positive control
`gamma=1, a0=1, lambda=1` gives `a=1/sqrt(3)`. Negative-rate,
noncomposing, off-tangent, target-import, triviality, and physical-overread
controls fail closed for their exact reasons.

## Interpretive boundary retained

The maps are injective. Nonsurjectivity is therefore not information loss, and
failure of a global backward extension is not physical irreversibility,
entropy production, causal order, a physical beginning, or a spacetime
singularity.

`L` is a source control or Lyapunov functional, not physical energy, an
action, a Hamiltonian, entropy, or a thermodynamic potential. No state-space
metric or gradient operator is selected. The packet uses a direct update rule,
not `X=-grad L`.

The parameter remains mathematical source order. The coefficient has only the
formal relation `[gamma]=[lambda]^-1[a]^-2`; neither amplitude nor parameter
has a physical unit here. Active action rescaling and passive parameter
regraduation transform the numerical coefficient in reciprocal conventions,
and neither fixes a physical rate or clock.

## Alternative families and locality

Linear, other nonlinear, absorbing, local, group, discrete, branching,
stochastic, categorical, and order-only candidates remain possible. The
cubic law is not uniquely selected or derived. Its autonomous amplitude rule
does not establish locality on the four-dimensional source arena.

## Remaining limitation and next burden

P5-T04 should analyze this exact unchanged candidate for source-manifold
locality or its absence, constraints, first integrals, symmetry and
nontrivial-redundancy extension, and conserved structure. It must not replace
the candidate, call the Lyapunov functional physical energy, or import
conservation from target covariance.

Canonical ontology or source-law adoption, physical time, reconstruction,
`M_src`, `g_eff`, matter coupling, Einstein equations, benchmark promotion,
proof, publication, push, and completed derivation remain blocked.
