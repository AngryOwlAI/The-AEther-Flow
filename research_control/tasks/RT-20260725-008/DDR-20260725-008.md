---
authority: control
status: completed
decision_id: DDR-20260725-008
task_id: RT-20260725-008
agent_job_id: AJ-RT-20260725-008-001
director_version: director-of-research@0.3.0
decision_type: task_overlay
selected_role_id: candidate-constructor
selected_role_version: 0.2.0
requires_human_gate: false
---

# DDR-20260725-008: execute only v21 P5-T03

## Decision

Activate one `candidate-constructor@0.2.0` task overlay for v21 work item
P5-T03. The generation-112 checkpoint at
`48799f729758a06efd31c234c1724f590516ebcd` satisfies the dependency recorded
by `handoff-0867`, and immutable relay generation 113 selects the same work
item under `continue-research`.

Use the named `candidate-construction-packet` route. P5-T02 supplies a
proposal-only forward-action semantic kind but deliberately leaves the
dynamics generator and law open. P5-T03 is therefore ordinary planned
construction under the existing source-only boundary, not a canonical
ontology adoption packet.

## Evidence and route resolution

The exact source boundary comes from the registered P5-T01 compact
source-theory object, the P5-T02 semiflow semantics, the v21 plan, the
continuum-first P4-T06 integration, the canonical ontology manuscripts, the
burden map, and the Distance-to-GR ledger. The P5-T01 torus-amplitude witness
already supplies a one-dimensional configuration space, observable,
variation line, and identity-only redundancy. The absent planned path
`research_control/support_formalization/` was inspected as absent and supplies
no premise; this ordinary repository gap does not block a source-only
candidate constructed directly from registered sources.

Memory preflight refreshed retrieval-only local state and then passed with
1,013 canonical source objects and 5,008 relationship rows. One search
containing `P5-T03` failed because the hyphen was parsed as an FTS column
operator and supplied no authority. The successful lookup and source-dynamics
search led back to the registered P5-T02 and P5-T01 objects, the current
frontier, the burden map, and canonical dynamics boundary, all of which were
inspected directly.

## Role-Fit Matrix

| Candidate role | Fit | Reason |
|---|---|---|
| `candidate-constructor@0.2.0` | selected | P5-T03 requires one explicit, nontrivial, source-only dynamics construction with equations, solution, and controls. |
| `ontology-formalizer@0.2.0` | internal mathematical boundary only | Systematic locality, constraints, and conservation belong to P5-T04 after checkpoint. |
| `philosopher-of-physics@0.1.0` | internal interpretation perspective only | Time, energy, irreversibility, locality, and adoption overreads require explicit audit without a second AgentJob. |
| `theoretical-continuation-selector@0.1.0` | not selected | The plan, handoff, and immutable route already select P5-T03. |

## Selected candidate

Use only the P5-T01 amplitude family
`q_a(theta)=a sin(theta_1)`. Transport the smooth structure of `R` to that
family and define, for `gamma>0`,

`X_gamma(q_a)=-gamma a^3 sin(theta_1)`.

The update equation `da/dlambda=-gamma a^3` has the explicit solution
`a(lambda)=a0/sqrt(1+2 gamma a0^2 lambda)`. Its maps are jointly smooth,
compose additively, preserve admissibility, and respect the identity-only
redundancy. For positive parameter they are injective but nonsurjective, and
nonzero backward trajectories blow up at finite negative parameter. This is a
genuinely forward source-order action, not information loss or physical
irreversibility.

The functional `L=a^2/2` is retained only as a source control or Lyapunov
functional. No state-space metric, gradient, physical energy, action,
Hamiltonian, entropy, or probability is selected.

## Parent-child synthesis

The single outer AgentJob uses `parent_child_parallel_synthesis`. The internal
physicist-mathematician perspective independently derived the solution,
semigroup, exact range, backward-extension obstruction, Lyapunov law, scales,
and controls. The internal physicist-philosopher perspective audited the
source-order, energy, irreversibility, locality, adoption, and downstream-GR
boundaries. The parent resolves six tensions and retains no unresolved
conflict.

## Claim boundary

The result may be one task-local source-dynamics candidate and a scoped
Distance-to-GR source-ontology delta. It is not an adopted law, canonical
ontology, physical time, physical energy, physical gauge, source-manifold
locality theorem, reconstruction, metric, matter coupling, Einstein-equation
derivation, benchmark promotion, proof publication, push, or completed
derivation.

## Validation

The task-local validator checks exact typing, solution, finite semigroup
evaluations, positive control, range, Lyapunov behavior, scale provenance,
failure controls, parent-child synthesis, and claim boundaries. Repository
validation, documentation-impact accounting, claim-language validation,
memory regeneration, TeX scratch compilation, and one governed checkpoint
remain required.

## Completion disposition

The registered source and companion specification pass all 19 task-local
checks. The decisive no-fog result is `constructed_candidate`. P5-T03 is
complete at `draft/control`, `proposal-only`, `source-extension data` scope
and is ready for one governed checkpoint. P5-T04 remains unexecuted until that
checkpoint commits.
