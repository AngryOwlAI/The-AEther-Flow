<!-- authority: science_draft -->

# Parent Fusion Notes: Upstream-Burden Selector

The parent synthesis accepts the shared conclusion of the physicist-mathematician
and physicist-philosopher children: the bounded P6-T01 route is
`EqSrc_theorem_attempt`.

The mathematical reason is that `EqSrc` already has a draft-object row with a
missing general equivalence theorem, while `RetainH` and `GenH` are missing
primitive rows whose immediate necessity has not yet been isolated by a theorem
setup. The ontology-status reason is that selecting EqSrc is lower authority
than adopting or inventing RetainH or GenH primitives, and it avoids silently
overreading P2, P4, or P5 candidate evidence as matter-coupling adoption.

No conflict remains. The fused artifact must preserve that route selection is
not proof, trigger status is not adoption, and deferred primitive status is not
a no-go theorem.

Immediate continuation is P6-T02: one bounded `ontology-formalizer@0.2.0` task
overlay writing `selected_upstream_equivalence_attempt_v1.tex`.
