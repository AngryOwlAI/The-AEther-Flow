---
authority: control
decision_id: "DDR-20260706-018"
task_id: "RT-20260706-018"
director_version: "director-of-research@0.3.0"
decision_type: "provisional_role"
selected_role_id: "formalization-engineer"
selected_role_version: "0.1.0"
agent_job_id: "AJ-RT-20260706-018-001"
status: "completed"
created_at: "2026-07-06T12:20:00Z"
activated_at: "2026-07-06T12:20:00Z"
completed_at: "2026-07-06T12:20:00Z"
supersedes_decision_id: "DDR-20260706-017"
requires_human_gate: false
validation_status: "PASS"
---

# DDR-20260706-018

## Analysis

`handoff-0649` selected v17 `P8-T02`: mechanize the
`fail_closed_certificate_evaluation` fragment with a custom finite checker
implemented in Python. The plan requires a local runnable fragment, tests or a
checker report, `support_only: true`, `proof_authority: false`, and no
scientific Gate Chair claim.

The selected role family `formalization-engineer@0.1.0` is not registered in
the active role registry. The safe implementation path is therefore a Director
`provisional_role` decision with explicit expiration after
`AJ-RT-20260706-018-001` and no reusable authority until the role is
registered.

## Role-Fit Matrix

| Role | Fit | Reason |
| --- | --- | --- |
| `formalization-engineer@0.1.0` | selected as one-job provisional | The plan and handoff name this role for mechanizing the support-only fragment. The Director decision type is `provisional_role`, and the execution role is not reusable because it is not registered. |
| `validator-engineer@0.2.0` | not selected | The packet includes tests and validation, but the main work is the finite formalization itself. |
| `project-control-maintainer@0.2.0` | not selected | Project-control traceability belongs to P8-T03, not P8-T02. |
| `gate-chair@0.1.0` | not selected | No protected adoption, benchmark status, source-law promotion, or completed-derivation verdict is requested. |

## Decision

Create one bounded `formalization-engineer@0.1.0` one-job provisional role
execution:

- implement `support_formalization_fail_closed_certificate_evaluation_v1`;
- use Python standard-library code only;
- encode positive declared-source-scope behavior and fail-closed negative
  branches for missing, malformed, target-import, detector-semantics,
  process-authority, downstream-physics, generated-authority, and scoped
  evidence misuse cases;
- emit a deterministic `validation_report.json`;
- publish focused unit tests;
- preserve `support_only=true`, `proof_authority=false`, and
  `physics_promotion_authorized=false`;
- defer formalization traceability-registry insertion to P8-T03.

## Claim Boundary

Allowed:

- support-only finite checker implementation;
- focused local tests;
- deterministic validation report;
- task-local parent-child synthesis artifacts;
- P8-T03 route recommendation.

Forbidden:

- treating the checker as proof authority;
- source-law adoption;
- `MetricData(E)` adoption;
- `g_eff` adoption or scope expansion;
- matter-coupling derivation or adoption;
- stress-energy semantics, stress-energy tensor, detector semantics, matter
  action, Einstein equations, benchmark promotion, Gate Chair verdict,
  completed derivation, future source-extension impossibility, or global
  theory rejection.

## Verification

Focused P8-T02 checks completed before this DDR was closed:

- `.venv/bin/python -m unittest discover -s research_control/formalization/fail_closed_certificate_evaluation -p 'test_*.py'`;
- `.venv/bin/python research_control/formalization/fail_closed_certificate_evaluation/fail_closed_certificate_evaluation.py --json-output research_control/formalization/fail_closed_certificate_evaluation/validation_report.json --json`;
- `python -m json.tool research_control/formalization/fail_closed_certificate_evaluation/validation_report.json`.

Post-write repository validation is recorded in the completion receipt.
