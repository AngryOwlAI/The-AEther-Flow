<!-- authority: control -->

# Parent Fusion Notes: EqSrc Closure Accepted-Totality Repair

## Inputs

The parent fused the proposal-only v1 candidate, the RT-20260718-017
Smuggling Auditor obstruction, handoff-0742, and the two bounded child outputs
for RT-20260718-018.

## Shared Consensus

The children independently agree that v1 did not state the accepted identity,
inverse, and composite existence used by its proof. The exact repair is to
replace necessary-only admissibility language with universal accepted-totality
clauses over a closed typed source grammar. This repairs
`OBST-EQSRC-CLOSURE-TOTALITY-001` at the proposal’s statement level.

The result remains `proposal-only` source-extension data with
`blocked_adoption_open_continuation`. It does not establish that current
ontology derives or instantiates the repaired law.

## Preserved Mathematical Contribution

The fused v2 artifact defines:

- a declared source signature for object, ledger-label, proxy-incidence,
  negative-control, and provenance domains;
- the least typed provenance term algebra generated by base, identity,
  inverse, and composition constructors;
- an exact fail-closed acceptance conjunction;
- an accepted identity for every declared object;
- an accepted inverse for every accepted certificate;
- an accepted composite for every composable accepted pair;
- unit, double-inverse, associativity, certificate-congruence, and
  ledger-congruence compatibility;
- a conditional proof that accepted certificates induce an equivalence
  relation on the declared source family.

The proof uses only the explicit accepted-totality hypotheses. Coherence
conditions remain separately visible so they cannot conceal the exact
quantifier burden.

## Preserved Ontology and Authority Boundary

The closed grammar forbids target atlases, target metrics, detector outcomes,
benchmark results, reconstructed geometry, registries, role declarations,
validators, generated derivatives, handoffs, checkpoints, or approvals from
creating source witnesses or acceptance.

The candidate is neither canonical nor adopted. No general `EqSrc` discharge,
RetainH adoption, GenH adoption, `M_src` adoption, `g_eff` result,
matter-coupling derivation, Einstein-equation derivation, finite-variation
robustness theorem, Distance-to-GR ledger change, benchmark promotion, Gate
Chair verdict, completed derivation, global no-go, or future source-extension
impossibility follows.

## Conflict Review

No blocking conflict was found. The mathematical child allows nonunique
inverse and composite witnesses modulo source congruence; the fused candidate
therefore asserts existence rather than uniqueness. The philosophical child’s
source-purity condition is implemented by a least constructor grammar and
explicit source-domain acceptance rules rather than an opaque predicate named
“source.”

## Next Route

Route exactly one fresh bounded Smuggling Auditor packet to test the complete
v2 grammar, quantifiers, process-authority guards, and certificate congruence.
Refuter stress follows only after that audit. Any ontology or source-law
adoption remains a separate protected human-gated decision.
