# Parent Fusion Notes: EqSrc Closure Candidate v3 Refuter Stress

## Synthesis

The mathematical and ontology-authority perspectives converge on
`scoped_obstruction`. Exact proposal-only `EqSrcClosureLaw_src^cand v3` is
abstractly instantiable beyond the one-object degenerate case, but it is not a
source-side selector that determines which equivalence relation current
ontology entails, and it does not establish stability under finite changes to
its primitive decision data.

The obstruction is local to exact v3 as a standalone robust selector. It does
not refute every v3 model, close the EqSrc milestone globally, reject the
project ontology, or establish future source-extension impossibility.

## Nontrivial Witness

The fused witness has two source objects, singleton ledger and proxy fibers,
one primitive source-rooted cross-object provenance edge, the generated
identity/inverse/composition grammar, and all-Pass decisions. Under the
candidate's intended groupoid-style cancellation equations, every endpoint
pair has one provenance congruence class and the full law holds.

The witness is finite only after congruence quotienting. Its literal raw
provenance grammar is countably infinite because constructors can be nested.
It is therefore recorded as a finite-congruence-quotient abstract witness, not
a finite raw-syntax model.

The phrase "inverse-unit equations" must include the two inverse-cancellation
schemes for the nonidentity witness. This is consistent with the full
inverse-compatibility clauses and prior audit. A proof-assistant translation
must display the schemes; a weaker reading would strengthen the local
formalization obstruction.

## Source-Provenance and Malformed-Input Findings

Every root of the constructed witness is explicitly enumerated source data.
All internal nodes are typed source constructors, and no target, workflow,
registry, role, validator, derivative, handoff, checkpoint, or adoption datum
appears. This proves source-only factorization for the abstract witness.

It does not show that current ontology supplies those roots or decisions.
Source hygiene is not current-ontology grounding, physical necessity,
uniqueness, empirical meaning, or adoption.

`Accept_src` is total on its typed certificate domain. A malformed external
value is outside that domain. Missing declarations, partial maps, type errors,
cyclic definitions, or forbidden provenance reject a proposed interpretation
at model level. An object-level malformed-input result would require a tagged
error carrier not present in v3.

## Decisive Countermodel

Hold the complete two-object algebraic structure fixed. In the universal
interpretation, all four singleton ledger fibers pass. In the discrete
interpretation, the diagonal fibers pass and the two off-diagonal fibers
block. Both interpretations satisfy the full law, remain source-only and
congruence-invariant, and differ in exactly two ledger-decision entries.

The universal interpretation relates both objects. The discrete
interpretation relates only each object to itself. Thus intra-model
certificate congruence does not provide inter-model selection or stability.
Exact v3 lacks both a provenance-backed rule selecting the primitive decisions
and an explicit admissible-variation class with a stability certificate.

## Parent Decision

The Refuter classification is `scoped_obstruction`, with
`OBST-EQSRC-CLOSURE-V3-VARIATION-SELECTOR-001` as the exact record. The
Distance-to-GR ledger is unchanged. General EqSrc remains undischarged.

Freeze criteria are evaluated because this is a repeated EqSrc construction
and audit line. The decision is `not_frozen`: the witness and countermodel are
new mathematical payload, and one materially different bridge-facing route
remains. Another generic signature repair or repeat audit is barred.

The next lawful packet is one bounded `candidate-constructor@0.2.0`
ontology-law packet. It must attempt a proposal-only, nondegenerate,
provenance-complete source interpretation with a cross-fiber acceptance
selector and an explicit admissible finite-variation class, then prove
stability or return a precise scoped obstruction.

## Preserved Claim Boundary

No canonical ontology edit, source-law adoption, general EqSrc discharge,
RetainH, GenH, or M_src adoption, Distance-to-GR ledger change, effective
metric result, matter-coupling or Einstein-equation derivation, benchmark
promotion, Gate Chair verdict, completed derivation, global theory rejection,
or future source-extension impossibility follows.
