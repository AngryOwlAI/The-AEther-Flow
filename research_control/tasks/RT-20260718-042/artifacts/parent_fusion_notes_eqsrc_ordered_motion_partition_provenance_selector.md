---
authority: science-draft
status: draft/control
task_id: RT-20260718-042
agent_job_id: AJ-RT-20260718-042-001
---

# Parent fusion notes: EqSrc ordered-motion partition-provenance selector

Both required perspectives select one distinct constructive and
non-promotional next packet: an `ontology-law-research-packet` executed by
`ontology-formalizer@0.2.0`.

The proposal-only target is
`EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1`. It asks whether the
program's source-side ordered-motion structure can produce the missing ordered
partition before any orientation-torsor relation is evaluated. The packet must
begin from a finite directed source record whose edges carry explicit
\(\Phi_\lambda\)-compatible transition certificates and whose event-to-record
projection is fixed independently of the desired line.

The packet may define a parity partition only on a branch with a unique
source-intrinsic root certificate. Every directed path from that root to a
record token must have the same length parity. The even and odd tokens then
form \(P_U\) and \(Q_U\), and a candidate pointing may use
\(B_U=\operatorname{span}_{\mathbb F_2}\{\mathbf 1_{Q_U}\}\). This line and
its kernel-pair relation remain proposal-only outputs of the candidate law;
they are not adopted `EqSrc`.

The root certificate is essential. A first event created only by truncating an
observer's sampling window is not intrinsic source provenance. Repository file
order, task chronology, validator output, benchmark success, target time
orientation, and a desired relation are also forbidden as root evidence. The
formalizer must state the exact source certificate and prove its naturality
under every admitted source isomorphism.

Two finite controls are mandatory. A two-event directed path with an
independently certified root is the minimal positive branch. An unrooted
directed two-cycle with exchange symmetry is the minimal negative branch: it
has no natural root and must return tagged bottom rather than either
coordinate line. Odd-cycle or mixed-path parity, disconnected components,
nonunique roots, truncation artifacts, and relation-defined variations must
also fail closed.

The formalizer must prove or precisely obstruct root uniqueness, automorphism
naturality, path-parity well-definedness, order-preserving
\(\lambda\)-reparameterization invariance, projection and truncation
noncircularity, partition and line functoriality, and independently declared
variation robustness. It must classify each new datum as derived from current
ontology, conservative, a new primitive candidate, or a forbidden import.

The canonical foundations source provides only an intentionally schematic
ordered-motion map. It does not presently derive the finite projection, root
certificate, parity-coloring law, morphism category, or variation category.
The fused selection therefore classifies the target restrictively as a
`proposal-only` `new_ontology_primitive_candidate`, with adoption blocked and
same-milestone continuation open.

A source-side line-irrelevance theorem is deferred because no independently
derived physical equivalence or downstream functor exists under which the
three line relations could be shown irrelevant. The unrooted symmetry result
is retained as a negative control rather than overread as a general no-go.
The exact orientation-torsor candidate-v1 construction, audit, and stress
cycle remains locally frozen.

No Distance-to-GR or metric-use ledger row changes. No source law, canonical
ontology, physical admissibility, `RetainH`, `GenH`, `M_src`, `g_eff`, matter
coupling, Einstein equations, benchmark status, Gate Chair verdict, or
completed derivation is promoted.
