<!-- authority: scientific_draft_control -->

# Parent fusion: idempotent equation-decoration null-semantics, rank-topology, and bridge-force Refuter stress

## Decisive result

The exclusive Refuter class is `scoped_obstruction`.

The fixed RT009 construction and RT011’s narrow `source_pure_as_written`
verdict remain correct. The algebraic carrier is coherent and target-free as
written, while its occurrence/admissibility and null-attachment meanings
remain proposal-only `new_ontology_primitive` debt. The new stress establishes
that the unchanged candidate supplies no naturally selected nontrivial
attachment, no derived occurrence semantics, no admitted perturbation law, and
no source-factored projector/image/kernel/quotient/rank bridge descendant.

This is a candidate-local obstruction. It is not a global no-go theorem, a
rejection of all source extensions, or an ontology decision.

## Full-frame naturality selects no endpoint

On a real rank-six equation fibre, any endomorphism fixed under conjugation by
the full `GL(6)` frame group is scalar. Idempotency reduces the scalar to zero
or one. Hence both `P=0` and `P=I` are full-arrow natural exact idempotents.
Naturality proves coherence of either declared endpoint but does not select the
null endpoint.

The complement map `C(P)=I-P` sharpens the symmetry. It is an involution,
commutes with conjugation and restriction, preserves pairwise norm and rank
distance, swaps zero with identity, swaps image with kernel, and sends rank
`r` to rank `6-r`. Naming zero as the attachment point breaks this symmetry by
stipulation, not by source-side discovery.

## Semantic relabeling supplies no occurrence law

Relabeling the decoration as occurrence, nonoccurrence, readiness, or inert
bookkeeping changes no carrier, arrow, idempotency equation, zero section,
rank control, model result, or validation check. The algebra therefore does
not derive the semantic interpretation. It establishes neither epistemic
selection nor realized or physical occurrence.

This does not reverse RT011’s narrow source-purity verdict. The proposal roots
were transparently disclosed. It instead blocks the stronger claim that the
disclosed syntax selects or justifies its intended meaning.

## Inert-or-bridge-force theorem

Let `U` forget the decoration and retain the old source datum. A downstream
construction `B` factors through `U` exactly when it is constant on every
`U`-fibre (with uniqueness on all old data only when `U` is surjective).

For the same old source datum, `P=0` and `P=I` give:

- ranks `0` and `6`;
- images `0` and `W_eq`;
- kernels `W_eq` and `0`;
- quotients `W_eq/ker(P)` equal to `0` and `W_eq`;
- distinct projector values.

Every faithful endpoint descendant therefore fails old-source factorization.
This yields an exact dichotomy:

1. If all old-language laws ignore the decoration, the expansion can be
   model-conservative but the decoration has no bridge force.
2. If a downstream law distinguishes decorations, it reads the new primitive
   and must independently prove source provenance, admissible arrows, and any
   semantic, physical, or P4 interpretation.

The unchanged proposal cannot obtain both conservativity and bridge force for
free.

## Exact topology does not supply perturbative robustness

Every nonzero exact idempotent has eigenvalue one, so its induced operator norm
is at least one. Zero and identity are therefore isolated from different-rank
exact strata. This is a valid algebraic fact, not a source perturbation law.

Both children provide compatible exact fixed-rank counterfamilies. A shear
family has `P_t -> P_0` in norm while `rank(P_t-P_0)=1` for every nonzero `t`.
A rational rotation family gives the analogous effect with rank difference
two. Thus the integer-valued rank distance stays positive along arbitrarily
small ordinary-norm changes.

The external family `Q_epsilon=epsilon I` approaches zero with idempotency
defect `epsilon(epsilon-1)I`. It is not part of the exact candidate. Its role is
only to expose the missing source-derived tolerance, variation, projection, or
restoration law; RT012 does not import one.

## Parent-child synthesis

Both children independently select the same exclusive Refuter class, exact
obstruction ID, candidate-local freeze ID, and theoretical-selector successor.
They preserve RT009, RT011, all nine inherited freezes, fourteen literal
`no_delta` rows, and every downstream authority block.

The mathematical child emphasizes fibre factorization, exact endpoint and
rank controls, fixed-rank norm-continuous families, and the approximate-law
boundary. The philosophical child adds complement symmetry, semantic
relabeling, pointedness versus discovery, and the precise force/provenance
dichotomy. These contributions are compatible. The initial catalog-path typo
was corrected before final child write. There is no unresolved scientific,
identity, control, or authority conflict.

## Local freeze and Distance to GR

All nine inherited freeze labels remain active. One additional candidate-local
freeze is applied:

`NDCL-V22-P4T02-B2-IDEMPOTENT-EQUATION-DECORATION-NULL-SEMANTICS-RANK-TOPOLOGY-BRIDGE-FORCE`

It freezes only replay of the unchanged candidate as a standalone natural
occurrence-attachment selector or projector/quotient P4 bridge. Diagnostic use
and a materially distinct independently source-provenanced law remain open.

Every one of the fourteen Distance-to-GR burdens remains literal `no_delta`.
D7 is not reevaluated, B2 remains inactive, and P4-T03 remains locked.

## Successor selected, not executed

Exactly one successor is selected but not created or executed:

`PKT-V22-P4T02-B2-POST-IDEMPOTENT-EQUATION-DECORATION-REFUTER-THEORETICAL-CONTINUATION-SELECTION-V1`

It is a `theoretical_continuation_selector` packet under
`theoretical-continuation-selector@0.1.0` on the
`ontology-law-research-packet` route. Its later task is to choose one materially
distinct continuation without replaying any active freeze.

## Authority boundary

RT012 adopts no source law or ontology and establishes no physical occurrence,
probability, empirical response, cone, conformal geometry, scale, `g_eff`,
matter coupling, Einstein equation, benchmark, Gate result, or completed
derivation. It creates no proof or publication authority and authorizes no push
or external action.
