<!-- authority: science_draft -->

# Parent fusion: post-census P4-T02 continuation selector

## Decisive result

RT-20260812-001 selects Route C,
`C_FINITE_TYPED_PRESENTATION_ADMISSION_BRIDGE_WITNESS`, as one separately
admitted, unexecuted `source_extension_candidate` for
`candidate-constructor@0.2.0`. Its canonical packet identity is
`PKT-V22-P4T02-B2-FINITE-TYPED-PRESENTATION-ADMISSION-BRIDGE-WITNESS-CONSTRUCTION-OR-PROVENANCE-OBSTRUCTION-V1`.

The packet is not a license to stipulate a root and call it source-derived. It
is a fail-closed construction-or-obstruction test. The minimum typed interface,
theory-scope conservativity, complete automorphism and naturality
classification, independent noncircular source provenance, target-import
exclusion, and blocked-adoption boundary are mandatory before any positive
finite witness may be constructed. If provenance for any required asymmetric
datum is absent, the required result is `source_provenance_obstruction` and the
positive branch is prohibited.

## Fixed predecessor result

RT-20260811-013 remains an exact, bounded current-signature typing
obstruction. The frozen grammar has one `SrcArena` term, two `Nat` terms, 357
`Bool` terms, and 360 terms total. The Boolean terms yield exactly the two
constant semantic classes on the declared dimension-four reduct class. The
current signature has no `PresCand_src`, `AdmissionPredicate_src`, or
`EqSrcArrow`; `Phi_src` remains unresolved and untyped. Identity-only
naturality is vacuous. These results are language- and grammar-relative, not a
global no-go and not a proof that every richer source extension must fail.

## Exact selector mathematics

Four exact controls govern the selection.

1. **Reduct-fibre nondefinability.** If two expansions lie in one fibre of a
   forgetful map but their added presentation-admission data differ, those data
   cannot factor through the current reduct. This proves current-language
   explicit nondefinability only.
2. **Minimum-interface deletion lemma.** Presentation objects, presentation
   arrows, endpoint maps, identity and composition, Boolean admission, and
   transport are componentwise necessary and jointly syntactically sufficient
   for the declared formation and naturality expressions. They do not select
   a source law or justify adoption.
3. **Two-object swap classification.** With trivial Boolean transport, the
   two-object swap admits exactly two natural predicates: constant false and
   constant true. It admits no unique natural selection.
4. **Conditional rooted control.** A disclosed root and root-preserving arrow
   class make `admit_rho(p)` iff `p=rho` a unique natural predicate. This is a
   tractability fixture only; it leaves the independent source provenance of
   the root completely open.

## Parent-child synthesis

The Physicist-Mathematician initially preferred immediate Route C because it
has exact finite construction and obstruction branches. The
Physicist-Philosopher initially preferred Route B because every uniquely
selecting finite witness already needs asymmetric data, and silently
stipulating those data would beg the central provenance question.

Two bounded resolution rounds produced consensus. Route C remains selected,
but Route B is absorbed as six mandatory preconstruction gates. Both children
agree that the rooted fixture is conditional, that a missing independent
provenance path must terminate as `source_provenance_obstruction`, and that the
selected packet remains unexecuted. The second round only converged the packet
identifier. No blocking conflict remains.

The fusion preserves the mathematical child's reduct-fibre, minimum-interface,
swap-orbit, and rooted-control results. It also preserves the philosophical
child's separation of current ontology, extension syntax, mathematical
consistency, epistemic provenance, physical meaning, empirical meaning,
adoption, and workflow authority.

## Route dispositions

- Route A is not selected as a standalone theorem because it would largely
  strengthen the already decisive RT013 nondefinability result without
  constructing new source data. Its fibre theorem remains a guard.
- Route B is not selected as a standalone formalization because its full typed
  interface and deletion proof are already fixed at selector level. Its
  conservativity and provenance burdens become mandatory gates inside Route C.
- Route C is selected because it adds the strongest bounded constructive
  consequence: one finite witness or one exact obstruction, with provenance
  checked before construction.
- Route D is premature because proposal-only construction and refutation do
  not require canonical ontology adoption.

## Freeze and Distance-to-GR status

All six local freezes remain active:

- `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR`
- `NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-COMMON-CHARACTER-HOLONOMY-PROTECTION`
- `NDCL-V22-P4T02-B2-ORIENTED-MATROID-BRIDGE-SELECTION-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-SIGNED-CUBIC-VIABILITY-SELECTOR-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-SOURCE-LAW-SPACE-ROBUST-INVARIANCE-PROTECTION-ROBUSTNESS`

All fourteen Distance-to-GR rows remain `no_delta`. This selector constructs no
source primitive, nonidentity `EqSrc`, retention or generation law,
localization or response map, physical source spacetime, cone, conformal
class, scale, `g_eff`, matter coupling, field equation, robustness theorem,
benchmark evidence, or protected verdict.

## Separately admitted continuation

After the RT001 checkpoint, one separate Candidate Constructor AgentJob may
execute the selected packet. That later job must bind the committed RT001
transaction, hold the RT013 obstruction and all six freezes fixed, and return
exactly one proposal-only finite witness or one precise obstruction. A positive
result would still require a separate Smuggling Auditor before any later route
could use it. RT001 neither creates nor executes the successor.

## Exact authority boundary

No current ontology or source law is modified, proved, adopted, rejected, or
promoted. A finite admission bit is not physical response, causality, time
orientation, a cone, conformal geometry, or `g_eff`. D7 is not reevaluated, B2
remains inactive, and P4-T03 remains locked. No global no-go, future-extension
impossibility, Gate verdict, benchmark promotion, proof authority,
publication, push, external action, or completed derivation follows.
