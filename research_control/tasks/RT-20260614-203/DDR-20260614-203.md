---
decision_id: "DDR-20260614-203"
task_id: "RT-20260614-203"
agent_job_id: "AJ-RT-20260614-203-001"
director_version: "director-of-research@0.3.0"
decision_type: "task_overlay"
selected_role_id: "smuggling-auditor"
selected_role_version: "0.2.0"
status: "completed"
requires_human_gate: false
created_at: "2026-06-27T00:50:00Z"
---

# Director Decision Record: RT-20260614-203

## Analysis

The active authority surface is `handoff-0235`. It requires one bounded
`smuggling-auditor@0.2.0` packet to audit the proposal-only
`SigScaleCont_src^{GSC}` selector candidate before any Refuter stress,
source-law adoption, `MetricData(E)` adoption, protected gate, `g_eff`
construction, or downstream GR promotion.

The required claim boundary is narrow. The audited object is proposal-only
source-extension data. A clean audit may route only to Refuter stress; it is
not source-law adoption, `MetricData(E)` adoption, `g_eff` construction, or
evidence of completed derivation.

## Role-Fit Matrix

| Candidate role | Decision | Reason |
| --- | --- | --- |
| `smuggling-auditor` | accepted | The handoff requires hidden-import and laundering audit of the proposal-only selector candidate. |
| `refuter` | deferred | Appropriate only after the source-purity audit is complete. |
| `candidate-constructor` | rejected | The next packet is not a `g_eff` construction attempt. |
| `ontology-formalizer` | rejected | The selector candidate already exists in `RT-20260614-202`. |
| `gate-chair` | rejected | No adoption or protected source-law verdict is authorized by this packet. |
| `theoretical-continuation-selector` | rejected | The handoff already identifies the next role. |

## AgentJob Contract

Create and execute one bounded `smuggling-auditor@0.2.0` AgentJob that:

- consumes `handoff-0235` and the `RT-20260614-202` proposal-only
  `SigScaleCont_src^{GSC}` artifact directly;
- audits signature-label, scale-normalization, transition-continuum, and
  fail-closed bottom components for hidden target import;
- audits proper-time, detector, empirical-calibration, benchmark, generated
  derivative, registry, validator, role, handoff, approval, local cache,
  file-order, and commit-state laundering;
- tests whether transition-covariance language is only a source-side
  obligation, not an adopted target atlas or smooth structure;
- routes next to Refuter stress only if no hidden import or laundering is
  detected as written;
- preserves `blocked_adoption_open_continuation`;
- does not adopt `SigScaleCont_src^{GSC}`, `MetricData(E)`, or `g_eff`;
- does not derive matter coupling, Einstein equations, benchmark promotion, or
  completed derivation.

## Claim Boundary

Allowed: source-purity audit of the proposal-only selector candidate and
Refuter stress routing.

Forbidden: source-law adoption, canonical ontology edit, target topology
import, target atlas import, target metric import, Lorentzian signature import,
proper-time import, detector semantics import, benchmark success import,
role/validator/generated derivative authority as source premise,
`MetricData(E)` adoption, `g_eff` construction/adoption, matter coupling,
Einstein equations, benchmark promotion, completed derivation, future
source-extension impossibility, and global theory rejection.
