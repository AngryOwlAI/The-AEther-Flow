---
authority: "control"
decision_id: "DDR-20260801-013"
task_id: "RT-20260801-013"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "validator-engineer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260801-013-001"
status: "completed"
supersedes_decision_id: "DDR-20260801-012"
requires_human_gate: false
role_fit_candidates:
  - "validator-engineer@0.2.0|selected|The blocker is one integrated-validator interpretation defect requiring byte-bound compatibility logic and focused fail-closed regression tests while the canonical dual-budget evaluator remains unchanged."
  - "process-integrity-auditor@0.1.0|not_selected|The protected evidence and preservation boundary are exact, but resolving the gate requires validator code and regression tests beyond receipt-only audit authority."
  - "project-control-maintainer@0.2.0|not_selected|No dual-budget policy, schema, workflow, skill, permission, or control-markdown semantics change is needed."
  - "documentation-curator@2.0.0|not_selected|No explanatory publication or human-facing source rewrite is required."
created_at: "2026-08-02T03:56:18Z"
activated_at: "2026-08-02T03:56:18Z"
completed_at: "2026-08-02T03:56:18Z"
validation_status: "PASS"
---

# DDR-20260801-013 — Reconcile protected dual-budget checkpoint accounting

## Current Objective

Resolve only the exact generation-211 blocker. Preserve the immutable blocked
RT-20260801-011 AgentJob and completion and the RT-20260801-012 recovery
receipt; retain the original dual-budget evaluator's rejected result; add one
byte-, path-, status-, error-, and evidence-bound integrated-validator
compatibility rule for that historical record; prove any drift fails closed;
then invoke at most one checkpoint only after the complete validation boundary
passes.

## Authority Surfaces Read

The root and research-control instructions, recursive-relay and
project-system skills, Project-System Director, Validator Engineer, Process
Integrity Auditor, and Project-Control Maintainer contracts, AgentJob and
execution-role schemas, generation 212's immutable route and 58-path manifest,
the registered v21 plan, the canonical dual-budget policy and evaluator,
RT-20260801-011 and RT-20260801-012 control records and evidence, the live
classifier, resolver, signal validator, integrated research-control validator,
and the existing immutable-historical ordinary-route compatibility pattern
were inspected.

## Role-Fit Matrix

| Role | Fit | Reason |
| --- | --- | --- |
| `validator-engineer@0.2.0` | selected | Owns deterministic validator behavior and focused regression tests. |
| `process-integrity-auditor@0.1.0` | not selected | Receipt-only audit cannot repair the integrated validator. |
| `project-control-maintainer@0.2.0` | not selected | No reusable policy or contract semantics change. |
| `documentation-curator@2.0.0` | not selected | No explanatory publication change. |

## Selected Role

Use one Validator Engineer task overlay. It may edit only the integrated
research-control validator, its focused tests, ordinary recovery records,
registries, cumulative documentation-impact inventories, and approved
generated derivatives. It may not edit the dual-budget evaluator or policy,
the protected RT-20260801-011 AgentJob or completion, the RT-20260801-012
recovery receipt, scientific sources, or P13-T08.

## Claim Boundary

The packet may claim only that one exact immutable blocked completion remains
rejected and nonqualifying under `dual_budget_policy_v1`, while repository
validation treats its sealed truthful zero-checkpoint history as compatible
because the exact recovery receipt proves why the prospective checkpoint
output was absent. Any path, byte, status, error-set, evidence, or authority
drift remains a hard failure. The packet may not generalize this treatment,
count the predecessor as accepted, change physics or Distance-to-GR status,
execute P13-T08, or create promotion, proof, publication, push, hosted
workflow, Gate Chair, or completed-derivation authority.

## Validation

PASS requires the original evaluator to return exactly one missing-checkpoint
error; integrated validation to emit one compatibility warning and no error;
tampered completion bytes, altered status, additional errors, and unsealed
records to fail closed; the 150-test dual-budget and research-control shard,
complete repository tests, task taxonomy, documentation impact, claim
language, affected profile, memory, generated-report freshness, generated
views, signal parity, and whitespace gates to pass; and the protected job,
completion, and recovery receipt hashes to remain exact before checkpoint.

## Result

The compatibility rule is limited to the exact protected completion path,
SHA-256, job path and SHA-256, sole evaluator error, blocked/FAIL status,
truthful zero-checkpoint fields, and exact generation-211 recovery-receipt
SHA-256 and contents. The dual-budget evaluator remains unchanged and still
returns `rejected`. Focused tests prove exact acceptance and drift rejection;
the dual-budget plus research-control shard passes 150 tests. Full governed
validation and the one cumulative checkpoint remain the final transaction
boundary.

The repository-wide regression boundary also requires the fail-closed
research-control shard inventory to include the two new compatibility policy
tests. That inventory synchronization changes no validator policy or science
claim.

Stable repository validation then exposed a separate historical-binding
failure outside this decision's immutable accounting route: the P10-T05
event-store audit still pins the pre-RT11 hashes of
`render_current_frontier.py` and `render_task_index.py`. The event-store audit
and downstream P10 migration-readiness check therefore fail. This frame does
not broaden itself into that distinct repair and invokes no checkpoint; one
fresh bounded project-system recovery must resolve those exact bindings before
checkpointing can be reconsidered.
