# Parent Fusion Notes: Marked Reconvergent Constructor

## Analysis

Both child perspectives agree that the prior unmarked diamond obstruction was
specific to missing source-side branch discrimination. The RT-20260614-173 to
RT-20260614-176 sequence supplies a stress-survived proposal-only
`BranchDisc_src^{GSC}` input, but does not adopt that input as source law.

## Fusion

The fused output constructs a bounded marked reconvergent diamond family
candidate `D_m^beta`, `m >= 2`. Its non-bottom status depends on explicit
source-side marking `beta`, injective branch labels, and coherent marked
transition transport. If those conditions fail, the route returns `Bottom_src`
or preserves `OB-GEFF-RECONVERGENT-BRANCH-DISCRIMINATOR-MISSING`.

## Boundary

The construction is `draft/control` and `proposal-only`. It is not
`BranchDisc_src^{GSC}` adoption, `MetricData(E)` adoption, arbitrary finite-DAG
inhabitation, `g_eff` construction, or downstream GR promotion.

## Next Route

The logical next role is one bounded Smuggling Auditor packet for the marked
family candidate. The audit should test hidden target import, source-law
adoption laundering, marked-source overread, fail-closed behavior, and
premature promotion.
