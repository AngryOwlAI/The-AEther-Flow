---
decision_id: "DDR-20260701-005"
task_id: "RT-20260701-005"
created_at: "2026-07-01T04:56:56Z"
director_version: "director-of-research@0.3.0"
decision_type: "task_overlay"
selected_role_id: "project-control-maintainer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260701-005-001"
status: "completed"
supersedes_decision_id: "DDR-20260701-004"
requires_human_gate: false
validation_status: "PASS"
---

# Director Decision Record: RT-20260701-005

## Decision

Route one bounded `project-control-maintainer@0.2.0` task-overlay packet for
v13 P0-T03 recommendation trace-matrix construction.

## Basis

Tracked state after `RT-20260701-004` and `handoff-0413` requires one bounded
P0-T03 recommendation trace matrix before P0-T04 execution gating. The v13 plan
requires a task-local matrix linking every recommendation to phases, tasks,
outputs, validators, status, dependencies, and claim boundaries. This work is
project-control traceability. It does not select a physics route and does not
execute the deferred `SourceMatterSemanticsAdoptionReadinessLaw_v1` Refuter
stress.

## Role-Fit Matrix

| Role | Decision | Reason |
| --- | --- | --- |
| `project-control-maintainer@0.2.0` | Selected | The work creates a control artifact, updates research-control state and registries, and preserves routing boundaries without changing scientific claims. |
| `documentation-curator@2.0.0` | Rejected | The plan names a documentation-curator family, but the artifact is task-local control traceability and the transaction touches control registries and handoff state. |
| `theoretical-continuation-selector@0.1.0` | Rejected | P0-T03 does not choose among scientific packets. P0-T04 will select exactly one next task. |
| `refuter@0.2.0` | Rejected | The deferred Refuter stress remains a later scientific route after P0-T04 if still current. |
| `gate-chair@0.1.0` | Rejected | No protected verdict, adoption, benchmark promotion, or closure decision is requested. |

## Authority Boundary

This decision authorizes only project-control traceability. It does not
authorize canonical ontology edits, source-law adoption, source-extension data
adoption beyond exact scoped gate results, `MetricData(E)` adoption, `g_eff`
adoption or scope expansion, matter-semantics adoption, detector-semantics
adoption, coupling-law adoption, matter-coupling derivation or adoption,
stress-energy semantics, stress-energy tensor, matter action, Einstein
equations, benchmark promotion, Gate Chair closure, completed derivation,
future source-extension impossibility, or global theory rejection.

## Expected Output

The execution artifact must record:

- all recommendation IDs `R1` through `R15`;
- every v13 phase and task, including P0 and P16 all-phase control tasks;
- expected artifacts and validators at a planning granularity;
- status values distinguishing completed prior control work from pending future
  work;
- a clear statement that the matrix is a control aid only.

## Next Expected Route

The next route after this packet is P0-T04 V13 execution gate. P0-T04 must read
the P0-T02 baseline reconciliation and P0-T03 trace matrix, then select exactly
one next task without treating the whole v13 plan as globally authorized.
