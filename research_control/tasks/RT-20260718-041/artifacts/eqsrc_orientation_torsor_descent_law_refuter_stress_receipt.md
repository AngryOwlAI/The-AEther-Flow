---
authority: supporting
status: draft/control
task_id: RT-20260718-041
job_id: AJ-RT-20260718-041-001
completion_id: AJC-AJ-RT-20260718-041-001
---

# Receipt: orientation-torsor descent-law Refuter stress

## Result

The exact Refuter classification is `scoped_obstruction`.

The conditional associated-state theorem core survives. The minimal two-point
both-odd record nevertheless supports three distinct equivariant descended
line relations. `GL(2,2)` acts transitively on them; the internal complement
swaps the coordinate pair and fixes the diagonal; every translation preserves
all three; and the common affine stabilizer is precisely the four-element
translation group. The candidate coordinate line is therefore not selected by
equivariance, complement, source exchange, or the declared translation
controls.

The obstruction is
`OB-EQSRC-ORIENTATION-TORSOR-LINE-SELECTION-001`. It blocks only the reading
that the current ontology derives a unique physically admissible selector.
It does not refute the conditional associated-state quotient theorem and does
not establish a general EqSrc no-go.

## Exact finite checks

- `|GL(2,2)| = 6`.
- `|AGL(2,2)| = 24`.
- There are three nonzero proper lines.
- Each line congruence has eight ordered pairs.
- Each affine relation stabilizer has order eight.
- Their common stabilizer has order four and consists exactly of translations.
- The three-line meet is equality with four ordered pairs.
- Their generated join is universal with sixteen ordered pairs.
- A one-point transfer makes both nonempty odd blocks even, or empties a
  singleton and exits the admitted domain.

## Authority and route

Candidate status remains `proposal-only`, `source-extension data`, and
`blocked_adoption_open_continuation`. Canonical ontology, both authoritative
ledgers, general EqSrc, and downstream GR claims are unchanged.

The exact candidate-v1 construction-audit-stress cycle is locally frozen. One
bounded `theoretical-continuation-selector@0.1.0` packet may choose a genuinely
source-derived pointing/provenance law, a source-side irrelevance theorem, a
distinct scoped no-go, or local freeze. It may not repair or adopt the
candidate.

## Validation

The task-local validator exhaustively enumerates the finite state space,
`GL(2,2)`, `AGL(2,2)`, all three line relations, their stabilizers, meet, and
join, and checks the registered source hashes and control-token boundaries.
The durable validation report is
`eqsrc_orientation_torsor_descent_law_refuter_stress_validation.json`.
