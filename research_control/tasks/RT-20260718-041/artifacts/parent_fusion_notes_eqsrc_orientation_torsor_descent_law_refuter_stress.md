---
authority: supporting
status: draft/control
task_id: RT-20260718-041
job_id: AJ-RT-20260718-041-001
---

# Parent fusion notes: orientation-torsor descent-law Refuter stress

## Fused result

The two child analyses agree on a `scoped_obstruction`. The exact
proposal-only `EqSrcOrientationTorsorDescentLaw_src^cand,v1` remains
conditionally coherent as an associated-state construction: quotient
well-definedness, strict scalar non-descent on the original common state
space, fibre pullback, cocycle consistency, admitted-source functoriality, and
declared-translation preservation survive.

The surviving algebra does not select the candidate line. On the minimal
two-point both-odd record, the same unpointed source record supports three
distinct equivariant line families:

\[
L_p=\langle p\rangle,\qquad L_q=\langle q\rangle,\qquad
L_d=\langle p+q\rangle.
\]

All three descend to associated-state congruences and obey the same cocycle,
pullback, and translation controls. `GL(2,2)` has order six and acts
transitively on the lines. Each line relation has an eight-element affine
stabilizer, while the common stabilizer of all three is exactly the
four-element translation group. Hence every translation is nonselective.
The internal complement swaps the coordinate lines and fixes the diagonal.
The meet of the three congruences is equality; their generated join is the
universal relation.

## Obstruction

`OB-EQSRC-ORIENTATION-TORSOR-LINE-SELECTION-001` blocks the claim that current
ontology plus the displayed candidate assumptions uniquely derives the
candidate coordinate line or establishes it as physically admissible. The
unordered partition and its provenance, the both-odd domain, the block-span
model, and a physical source-record variation category remain underived.
One-point transfers expose domain fragility: they make both nonempty blocks
even, or empty a singleton and leave the admitted domain.

This obstruction does not refute the conditional bundle theorem and is not a
general EqSrc no-go. A richer source-derived pointing or provenance law, a
source-side irrelevance theorem, or a distinct scoped no-go remains logically
possible.

## Route and freeze

The exact candidate-v1 construction, audit, and stress cycle is locally
frozen. The next lawful packet is one bounded
`theoretical-continuation-selector@0.1.0` packet selecting among:

1. a genuinely source-derived pointing or partition-provenance law;
2. a source-side theorem proving line choice irrelevant to the required
   physical equivalence;
3. a distinct scoped no-go target; or
4. explicit local freeze.

No candidate repair, ontology adoption, physical-admissibility claim, general
EqSrc discharge, ledger change, downstream GR promotion, or global rejection
is authorized.
