---
authority: control
decision_id: "DDR-20260715-008"
task_id: "RT-20260715-008"
created_at: "2026-07-15T20:23:36Z"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "memory-system-maintainer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260715-008-001"
status: "completed"
completed_at: "2026-07-15T20:29:53Z"
supersedes_decision_id: ""
requires_human_gate: false
validation_status: "PASS"
---

# DDR-20260715-008: Extract pure memory-core validation

## Decision

Execute one bounded Memory-System Maintainer AgentJob for v19 `P3-T02`.
The job may extract the tracked memory invariant checks from `validate_all()`
into a callable `memory_validate_core(snapshot=None)` gate, provide an
immutable registry snapshot reuse surface, return stable finding IDs and
structured counts, and add focused equivalence tests. It must leave
publication policy and local-retrieval freshness outside the new core gate.

## Role-Fit Matrix

| Role | Decision | Reason |
| --- | --- | --- |
| `memory-system-maintainer@0.2.0` | Selected | P3-T02 changes only source-first memory validation orchestration and focused tests. |
| `validator-engineer@0.2.0` | Not selected | The implementation remains inside the registered memory-system ownership boundary. |
| `project-system-director@0.2.0` | Routing authority | It bounds the planned implementation without expanding role authority. |
| Physics roles | Rejected | No derivation, ontology, proof, benchmark, or scientific promotion occurs. |

## Evidence and boundary

- P3-T01 completed with PASS at commit
  `4bb806ecc1710ef5f3c036810b1ba9f80e5dc821`.
- Entry Git state was clean and local `main` was six commits ahead of
  `origin/main`.
- The registered v19 plan names P3-T02, the selected role, the exact three
  source/test write paths, pure-core obligations, and no-physics boundary.
- Memory preflight passed core validation. Its warnings were ignored `.local`
  retrieval freshness only and were not used as authority.
- The advisory project-improvement resolver had no competing open signal.

## Scope-alarm review

The patch exceeds the narrow-change line heuristic because the plan requires
three inseparable contracts: an immutable reusable snapshot, stable structured
finding identity, and legacy-composite equivalence tests. The change remains
inside exactly the three planned files, adds no dependency or public schema,
and does not widen into P3-T03 ownership changes.

## Preserved boundaries

- Preserve `handoff-0740` and `EqSrc_family_closure_repair_or_stress` as the
  ordinary research authority.
- Preserve `validate_all()` as the legacy composite compatibility path in the
  `legacy_consolidated` epoch.
- Do not execute P3-T03, add publication or local-retrieval adapters, edit
  Make or CI, retire legacy paths, or hand-edit generated derivatives.
- Do not treat validator results, tests, registries, derivatives, or
  checkpoint evidence as physics proof.

## Outcome rule

PASS requires a side-effect-free gate with ID `memory_core`, all former core
hard checks represented, immutable preloaded snapshot reuse, stable finding
IDs, structured counts, no publication or local-cache warnings in the pure
gate, legacy composite compatibility, existing failure-fixture coverage, a
live read-only acceptance, and all project-system gates required by current
policy. Otherwise issue `REPAIR_REQUIRED`, `ROLLBACK_REQUIRED`, or `BLOCKED`
and do not checkpoint invalid state.
