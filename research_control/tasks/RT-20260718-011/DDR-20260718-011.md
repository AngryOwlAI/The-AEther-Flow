---
authority: control
decision_id: "DDR-20260718-011"
task_id: "RT-20260718-011"
created_at: "2026-07-18T20:18:11Z"
activated_at: "2026-07-18T20:18:11Z"
director_version: "director-of-research@0.3.0"
decision_type: "task_overlay"
selected_role_id: "project-control-maintainer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260718-011-001"
status: "completed"
completed_at: "2026-07-18T20:18:11Z"
validation_status: "PASS"
supersedes_decision_id: ""
requires_human_gate: false
role_fit_candidates:
  - "project-control-maintainer@0.2.0|selected|P0-T02 creates bounded project-control data, schema, validation evidence, and registry rows."
  - "validator-engineer@0.2.0|rejected|The focused validator supports the transaction, but the primary payload is backlog materialization rather than standing validator infrastructure."
  - "process-integrity-auditor@0.1.0|rejected|The task requires canonical control-source creation rather than an evidence-only audit."
  - "director-of-research@0.3.0|director_only|The Director authorizes one task-scoped project-control packet while preserving ordinary research authority."
---

# DDR-20260718-011: Materialize the v20 backlog and dependency DAG

## Current objective

Implement only v20 `P0-T02`: create a typed task-local backlog, schema,
deterministic dependency and coverage report, and focused validator for all 104
v20 task cards. Do not execute `P0-T03` or any later task.

## Evidence and routing

The memory preflight refreshed ignored local retrieval state from WARN to PASS.
Targeted lookup resolved the canonical v20 plan object
`MD-RECOMMENDATIONS-IMPLEMENTATION-PLAN-CONTINUE-TASK-V20`; direct source and
registry inspection confirmed source SHA-256
`6ef3fc85f89dcfc71832298a3ff846e0425e93a61a7409801f737c2132f8ba6a`.
The predecessor `P0-T01` completion is committed at
`593084c827c5dc690f159cd77404735cbf3cf84b` and validates the dependency root.

The tracked `continue_research.py --summary` boundary remains
`director_decision_required` at `RT-20260709-006` and `handoff-0740`. This is
expected: the v20 plan is a project-system sidecar and does not supersede the
ordinary `EqSrc_family_closure_repair_or_stress` route. The v19 protected
publication boundary also remains unchanged.

## Role-fit decision

Select Project-Control Maintainer through one task overlay. The role may create
the task-local backlog, schema, validator, report, standard control records,
one schema registry row, and policy-generated memory derivatives. It may not
edit program state, the ordinary handoff, canonical science, standing relay
contracts, external systems, or protected authority.

## Role-Fit Matrix

| Candidate | Disposition | Evidence |
|---|---|---|
| `project-control-maintainer@0.2.0` | selected | P0-T02 creates bounded project-control data, schema, validation evidence, and registry rows. |
| `validator-engineer@0.2.0` | rejected | The validator is task-local and does not change standing validation infrastructure. |
| `process-integrity-auditor@0.1.0` | rejected | The task requires canonical control-source creation rather than evidence-only review. |
| `director-of-research@0.3.0` | director only | The Director authorizes exactly one bounded task and preserves the active research route. |

## Scope alarm resolution

The task necessarily exceeds three files, one hundred lines, and the usual
narrow-change threshold because its definition of done requires a complete
machine-readable representation of 104 task cards and 110 dependency edges,
plus one task, decision, AgentJob, execution-role record, completion,
documentation-impact receipt, registry rows, generated memory derivatives,
and a governed checkpoint. The implementation remains bounded to one generated
backlog, one schema, one focused validator/report pair, and standard evidence.

## Claim boundary

The backlog records planned dependencies, roles, paths, gates, terminal states,
and human preconditions. Those rows are data, not reusable authorization. Each
future v20 task still requires a distinct immutable goal record, a fresh worker
discussion, one outer AgentJob per generation, and current tracked authority.
No validation result, graph property, coverage count, plan completion, or relay
receipt is physics proof or Distance-to-GR progress.

## Outcome

The task-local validator reports PASS: 17 phases, 104 unique task cards, 110
dependency edges, no dangling dependencies, no cycles, no unreachable required
tasks, 104 unique goal-boundary hashes, all 13 expected role families active,
and complete direct plus `P16-T03` final-audit coverage for `V20-R01` through
`V20-R21`. Human/external authority is represented only as a precondition.
`P0-T03` is the next separate task after the governed checkpoint.
