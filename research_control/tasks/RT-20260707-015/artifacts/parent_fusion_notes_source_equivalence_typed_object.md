<!-- authority: science_draft -->

# Parent Fusion Notes: V18 P2-T03 Source-Equivalence Typed Object

## Analysis

The physicist-mathematician child supplied the formal tuple, relation, closure
obligations, and countermodel slots. The physicist-philosopher child supplied
the authority separation: source-side mathematical model only, no ontology
edit, no target import, no benchmark status, and no registry-as-proof
premise.

## Fused Decision

The fused artifact defines `SourceEquivalenceTypedObject_v1` as a draft/control
typed object. It makes `EqSrc_T` a source-only comparison relation guarded by
object, morphism, invariant, annotation, influence, negative-control, and
no-target checks.

The registry row is retained as navigational metadata for the artifact. It is
not proof authority.

## Limitations

Identity, inverse, and composition still require future family witnesses or
countermodels. `RetainH` and `GenH` remain boundary conditions and possible
primitive requirements. The next route is P2-T04 smuggling audit.
