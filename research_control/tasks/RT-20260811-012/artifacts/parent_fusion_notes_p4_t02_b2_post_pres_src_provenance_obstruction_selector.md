---
authority: science_draft
status: draft/control
task_id: RT-20260811-012
job_id: AJ-RT-20260811-012-001
claim_boundary_id: CB-V22-P4-T02-B2-POST-PRES-SRC-PROVENANCE-OBSTRUCTION-THEORETICAL-SELECTION-001
---

# Parent fusion: post-`Pres_src` provenance-obstruction selector

## Decisive result

Both independent physics perspectives and the parent select Route C:

`PKT-V22-P4T02-B2-BOUNDED-NATURAL-INVARIANT-PRESENTATION-ADMISSION-CENSUS-V1`

The future packet type is `bounded_theoretical_calculation`; its next role is
`ontology-formalizer@0.2.0`. It is selected but not executed.

The packet must freeze an exact finite typed grammar, complexity bound, reduct
class, equations, test groupoid, codomains, and admission predicates over the
live registered current source signature before any admission or P4 outcome is
examined. It then exhaustively enumerates the declared terms and returns either
one explicit source-derived natural admission term or one exact
grammar-relative obstruction. It introduces no new primitive.

## Why Route C wins

Route A's fibre-factorization theorem is exact but substantially develops the
same-reduct consequence already earned by RT011. It is retained as a guard
inside the selected census. Route B is constructive only after a carrier,
operations, and covariance are independently source-provenanced; otherwise it
renames the missing admission primitive. Route D is premature because this
bounded nonpromotional calculation remains available.

Route C uniquely combines constructive preference, no new primitive, exact
finite termination, a positive witness branch, a complete bounded negative
branch, and strict independence from RT011 roots, `kappa`, `RobInv`, and all
six frozen families.

## Exact mathematical payload

For maps `U:E->R` and `F:E->Y`, a unique factor `F_bar:im(U)->Y` exists with
`F=F_bar o U` exactly when `F` is constant on every nonempty `U`-fibre. A
factor is unique on all of `R` only when `U` is surjective or extra extension
data are supplied. This corrects any overclaim from a factor known only on the
image.

No nonempty finite subset of affine `R^4` is invariant under every
translation: invariance would contain `a+n v` for all integers `n`. This is an
affine-translation control only, not a theorem about source models with
additional fields.

A finite typed terminal set, finitely many finite-arity constructors, and a
fixed finite depth generate finitely many terms; exhaustive constructor
induction is complete relative to that grammar. The executable fixture has
cumulative counts `3`, `12`, and `93`. Term finiteness does not automatically
decide semantic naturality.

The Philosopher child adds the orbit-quotient form: an invariant binary
predicate factors uniquely through the orbit map. A transitive two-point swap
admits only constant invariant predicates, whereas the identity-only action
admits singleton predicates. Thus every negative census depends on the exact
declared action and cannot be globalized.

## Conflict review

There is no blocking conflict. Both children independently choose the same
route, packet identity, packet type, and next role. The parent records three
precision normalizations: factorization is exact on `im(U)`; the translation
control is limited to the affine `R^4` action; and enumeration completeness is
syntax-relative while naturality remains a separate proof obligation. No child
revision round is required.

## Result contract and limits

The future packet must return exactly one of an explicit bounded
source-natural admission-term construction, a grammar-relative symmetry
obstruction, or a precise census-typing obstruction. A positive result remains
proposal-only and requires a separate Smuggling Auditor. A negative result
quantifies only over the frozen grammar, depth, reduct class, equations, and
arrow coverage; it is not a global no-go, future-extension impossibility, or
proof that protected ontology change is necessary.

All six inherited freezes remain active, all fourteen Distance-to-GR rows
remain `no_delta`, D7 and P2-T01 adequacy are not reevaluated, B2 remains
inactive and undisposed, and P4-T03 remains locked.

## Exact authority boundary

RT012 performs no census, constructs no source term, adopts no law or ontology,
and changes no scientific or physical status. It derives no occurrence rule,
observer or detector response, causal cone, universal matter propagation,
conformal structure, scale, `g_eff`, coupling, action, stress energy, or
Einstein equation. It creates no Gate Chair, benchmark, proof-promotion,
publication, release, push, external-action, or completed-derivation authority.
