# Parent Fusion Notes: EqSrc Intrinsic Discriminator and Admissibility Law

## Scope

The parent fused the two internal perspectives into proposal-only
`EqSrcIntrinsicDiscriminatorAdmissibilityLaw_src^cand,v1`. The packet answers
the exact handoff-0751 formalization burden. It does not adopt a source law,
edit canonical ontology, establish physical admissibility, or discharge
general EqSrc.

## Shared Mathematical Result

The fused proposal uses a finite two-truncated chain complex over
\(\mathbb F_2\). Its source states are cycles, its independently generated
variations are finite boundary moves, and its discriminator is the canonical
map from cycles to first homology. The candidate EqSrc relation is the
discriminator kernel pair.

Three theorem-level results are explicit:

1. chain maps induce a natural discriminator square, with functorial
   composition and inverse certificates;
2. any surjective discriminator with the same boundary-orbit kernel pair
   factors uniquely through homology by a codomain bijection; and
3. every finite word in the independently declared boundary moves preserves
   the discriminator.

The finite witness has one vertex, three loop edges, and one two-cell with
boundary \(e_0+e_1\). Its eight cycles form four two-element classes. The
nonidentity boundary move preserves classes, while translation by \(e_2\)
changes them and supplies a precise out-of-class sharpness witness.

## Parent Resolutions

“Intrinsic” is restricted to determination by the displayed source chain
package. It does not mean that current ontology derives or physically selects
that package.

The variation-class certificate establishes formal non-circularity because
the generators use only chain boundary data and are fixed before the
discriminator or relation. It does not establish physical admissibility.

The uniqueness theorem is not absolute uniqueness of all discriminators. It
is the quotient universal property under the exact hypothesis that a
surjective discriminator has the same kernel pair.

## Authority and Next Route

The result is
`candidate_formalized_pending_fresh_smuggling_audit`,
`proposal-only`, and `blocked_adoption_open_continuation`. The prior exact
quotient-table cycle remains locally frozen, while this structurally distinct
chain route is not frozen because it supplies new definitions, proofs, and a
finite witness.

The next lawful step is one fresh bounded Smuggling Auditor audit of the exact
chain-homology candidate. That audit must test hidden primitive selection,
target or process import, physical-admissibility overread, naturality scope,
and unjustified extension beyond the finite witness. It may not repair or
adopt the candidate.
