# EqSrc Intrinsic Discriminator and Admissibility Law Candidate Receipt

## Result

`RT-20260718-027` formalizes proposal-only
`EqSrcIntrinsicDiscriminatorAdmissibilityLaw_src^cand,v1`. The result is
`candidate_formalized_pending_fresh_smuggling_audit`.

For a finite two-truncated chain complex over \(\mathbb F_2\), the proposal
uses cycles \(Z_1\) as source states, boundaries \(B_1\) as independently
generated source-local moves, and the canonical quotient
\(\chi_E:Z_1\to H_1=Z_1/B_1\) as the discriminator. The candidate relation is
the kernel pair of \(\chi_E\).

## Decisive Mathematical Payload

The packet proves:

- naturality of \(\chi_E\) under typed chain maps, including composition,
  identity, and inverse certificates for chain isomorphisms;
- uniqueness up to a unique codomain bijection among surjective
  discriminators with exactly the boundary-orbit kernel pair; and
- robustness under every finite word in the independently declared boundary
  moves.

The admissible variation definition is fixed from chain boundary data before
the discriminator or relation is evaluated. This proves formal
non-circularity, not physical admissibility.

The concrete witness has one vertex, three loop edges, and one two-cell with
boundary \(e_0+e_1\). Its eight source states form four two-element homology
classes. The nonidentity boundary move preserves the classes; translation by
\(e_2\) is outside the admitted class and changes them.

## Claim and Authority Boundary

Current ontology does not derive the chain-package primitive, the homology
discriminator law, or the physical selection of boundary moves. The candidate
remains `proposal-only` source-extension data with
`blocked_adoption_open_continuation`.

No canonical ontology edit, source-law adoption, physical-admissibility
result, general EqSrc discharge, RetainH, GenH, M_src, g_eff, matter coupling,
Einstein equations, benchmark promotion, Gate Chair verdict, completed
derivation, future source-extension impossibility, or global theory rejection
follows. The Distance-to-GR and metric-use ledgers are unchanged.

## Freeze and Next Route

The prior exact quotient-table cycle remains locally frozen. This distinct
chain-homology route is `not_frozen` because it adds a new formal object,
three theorem-level payloads, an independence certificate, and a distinct
finite witness.

The next lawful packet is one fresh bounded
`smuggling-auditor@0.2.0` audit of this exact candidate. It must audit the
selection of chain data and boundary moves, transitive target/process
provenance, formal-independence versus physical-admissibility language,
naturality scope, and finite-witness generalization. It may not repair or
adopt the candidate.

## Validation

Task-local validator:

```text
.venv/bin/python research_control/tasks/RT-20260718-027/artifacts/validate_eqsrc_intrinsic_discriminator_admissibility_law_candidate.py --write-report --json
```

Expected status: `PASS`.
