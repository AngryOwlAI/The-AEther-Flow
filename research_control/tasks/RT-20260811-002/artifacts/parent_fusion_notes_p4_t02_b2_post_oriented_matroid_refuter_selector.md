---
object_id: "MD-V22-P4-T02-B2-PARENT-FUSION-POST-ORIENTED-MATROID-REFUTER-SELECTOR-V1"
authority: "science_draft"
status: "draft/control"
task_id: "RT-20260811-002"
job_id: "AJ-RT-20260811-002-001"
---

# Parent fusion notes — P4-T02 B2 post-oriented-matroid Refuter selector

The parent fused two independent physics analyses with no blocking conflict and
no child-revision round. Both children select exactly one future, unexecuted
packet:
`PKT-V22-P4T02-B2-SOURCE-DYNAMICAL-VIABILITY-ADMISSIBILITY-SELECTOR-FORMALIZATION-V1`
under `ontology-formalizer@0.2.0`. It is an
`ontology_law_research_packet` on route `ontology-law-research-packet`, with
trigger `derivation_critical_missing_source_law`.

The shared discriminator is that RT018 exposed two independent gaps. A source
occurrence or component law must select which state is realized, while a
source transition or admissibility law must determine which finite changes are
allowed. Viability without occurrence selects nothing; occurrence without
viability protects nothing. The future packet must define both from disclosed
source premises or return the smallest exact missing primitive or
countermodel.

The parent preserves the Physicist-Mathematician's reachability-component
theorem: a diagnostic branch is robust from a state exactly when it is
constant on the entire source-reachable component, which is the least
forward-invariant carrier. It also preserves the exact rational barrier
control and the RT018 wall countermodel showing that an admitted path from
`A(1/n)` to `A(-1/n)` defeats branch robustness.

The parent preserves the Physicist-Philosopher's
representation-occurrence separation lemma: even an injective representation
distinguishes only states already supplied; it selects no occurrence state and
defines no admissible transition relation. This establishes the explanatory
order behind the route decision. A realization-sensitive bridge may later be
useful, but retaining more information cannot precede or replace a source law
that determines occurrence and admissible dynamics.

Route A is selected because it is constructive, materially independent of all
four freezes, and directly addresses both missing structures through a
decisive theorem-or-obstruction contract. Route B adds an unselected norm,
topology, or margin and still does not select occurrence. Route C is less
constructive and lacks an independently delimited broad source-observable
class beyond RT018's narrow obstruction. Route D is premature while
proposal-only target-free formalization remains lawful without canonical
ontology authority.

The dominant risk is circular goal-property preload. The future packet fails
if `U_src`, `b_src`, `R_src`, or `O_src` is chosen because it preserves a
desired `Bridge_OM` branch, causal cone, empirical response, metric, or
benchmark result. A successful candidate remains proposal-only and must be
routed separately to a Smuggling Auditor before any Refuter stress or protected
adoption question.

The selected packet is not executed here. It formalizes no current-ontology
primitive or source law, assigns no physical or empirical meaning, and adopts
nothing. Source viability is not physical causality; a positive barrier is not
time orientation or scale; a viable component is not a physical cone,
conformal structure, or `g_eff`. D7 is not reevaluated, B2 is neither activated
nor disposed, P4-T03 remains locked, all four freezes remain unchanged, all
fourteen Distance-to-GR rows remain `no_delta`, and no Gate, benchmark, proof,
publication, push, external action, global no-go, or completed derivation is
claimed.
