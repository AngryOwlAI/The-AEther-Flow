<!-- authority: control -->

# V13 Plan Intake Note

## Analysis

`implementations_plans/recommendations_implementation_plan_continue_task-v13.md`
exists in the local working tree and is the user-named implementation plan for
the current goal. Before this packet, the path was ignored by `.gitignore`, so
the plan was not tracked repository evidence. The prior v11 and v12
implementation plans are explicitly unignored, and v12 is registered in
`registries/MARKDOWN_SOURCE_REGISTRY.csv` as `implementation_plan`
project-control guidance.

## Intake Result

This packet adds the v13 ignore exception and registers v13 using the same
source-first retrieval pattern as v12:

- object ID: `MD-RECOMMENDATIONS-IMPLEMENTATION-PLAN-CONTINUE-TASK-V13`;
- source path:
  `implementations_plans/recommendations_implementation_plan_continue_task-v13.md`;
- role: `implementation_plan`;
- authority status: `project_control`;
- audience: `agents`;
- generated metadata note:
  `wiki/markdown/md-recommendations-implementation-plan-continue-task-v13.md`.

## Authority Boundary

The v13 plan is implementation guidance only. It is not a physics proof, a
canonical ontology edit, a source-law adoption, a source-extension adoption, a
Gate Chair verdict, benchmark authority, or completed derivation evidence.

`handoff-0411` remains the immediate scientific authority for the later
Refuter stress of `SourceMatterSemanticsAdoptionReadinessLaw_v1`. This packet
does not execute that stress. It records that v13 P0 must reconcile the live
baseline before later tasks are scheduled.

## Historical Evidence

`implementations_plans/recommendations_implementation_plan_continue_task-v12.md`
remains historical implementation-plan evidence. This packet does not overwrite
or reinterpret v12 task completions. It only registers v13 as the current
plan-context surface for the new goal.

## Logical Next Step

Run one bounded P0-T02 live baseline reconciliation packet. That packet should
compare the v13 assumptions against tracked state and decide whether the plan
baseline matches, has advanced, conflicts, or needs a human gate.
