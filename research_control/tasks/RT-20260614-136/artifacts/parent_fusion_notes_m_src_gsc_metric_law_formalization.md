# Parent Fusion Notes: MetricLaw_src Formalization

The parent fused the Physicist-Mathematician and Physicist-Philosopher child
outputs into one proposal-only source-law formalization artifact.

Shared consensus:

- `MetricLaw_src^{GSC}` may be formulated as a partial source-only interface
  over scoped adopted `M_src^{GSC}(E)`.
- The interface requires orientation or signature selection, scale or measure
  selection, causal preorder or cone analogue, transition covariance,
  nondegeneracy or controlled degeneracy, finite-variation compatibility, and
  fail-closed obstruction behavior.
- No effective metric is constructed.

Unique Physicist-Mathematician contribution:

- Formal object, domain, map, selector-family, and obstruction-label
  structure for the source-law interface.

Unique Physicist-Philosopher contribution:

- Separation between source ontology, mathematical model, empirical recovery,
  benchmark success, and process authority.

Resolution:

- The final artifact states a constructive proposal-only law interface, not an
  adopted law. Any target metric import, process-authority laundering, selector
  underdetermination, covariance failure, nondegeneracy failure, or
  finite-variation fragility returns `Bottom_src` or a named `OB-GEFF-*`
  obstruction.

Next route:

- One bounded Smuggling Auditor packet for the proposed `MetricLaw_src^{GSC}`
  interface before any Refuter stress, adoption request, or `g_eff`
  construction.
