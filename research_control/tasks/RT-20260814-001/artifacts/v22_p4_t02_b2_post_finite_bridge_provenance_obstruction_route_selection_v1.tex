\documentclass[11pt]{article}
\usepackage[margin=0.92in]{geometry}
\usepackage{amsmath,amssymb,booktabs,array,enumitem,microtype}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.48em}
\setlength{\emergencystretch}{3em}
\newcommand{\Sub}{\mathcal{A}}
\newcommand{\EqSrc}{\ensuremath{\mathrm{Eq}_{\mathrm{src}}}}
\newcommand{\NoDelta}{\texttt{no\_delta}}
\newcommand{\ModCur}{\mathbf{Mod}_{\mathrm{cur}}}
\title{Post-Provenance-Obstruction P4--T02\\Theoretical Continuation Selection}
\author{RT-20260814-001 parent synthesis}
\date{14 August 2026}

\begin{document}
\maketitle

\section{Decisive result}

The parent synthesis selects Route A, the current-source equivariant-asymmetry
derivation audit.  Its future packet identifier is:
{\small\ttfamily\raggedright
PKT-V22-P4T02-B2-CURRENT-SOURCE-EQUIVARIANT-ASYMMETRY-\linebreak
DERIVATION-AUDIT-V1.\par}
The packet type is \texttt{bounded\_theoretical\_calculation}, its next role
is Ontology Formalizer version 0.2.0, and it is selected but unexecuted.

The packet must form, or precisely fail to form, the semantic model and
isomorphism groupoid of the exact registered current-source signature.  It
then asks whether any predeclared presentation-relevant asymmetry is a natural
current-source construction.  A positive result must exhibit the typed natural
transformation and its factorization through registered source data.  A
negative result must identify the first exact typing, automorphism, naturality,
or factorization obstruction and remain scoped to the current signature.

\section{Fixed predecessor result}

The RT-20260812-002 finite mathematics remains valid.  The minimum typed
presentation interface is coherent, and exhaustive analysis of its two-object
pair groupoid leaves only the two constant Boolean admissions.  A disclosed
root makes a unique admission conditionally tractable, but GATE-04 fails: the
registered source package supplies no independently source-provenanced root,
orientation, order, label, admission bit, \EqSrc{} transport, or
source-to-presentation map.  The positive rooted construction is therefore
prohibited.

The selected audit is not permission to repeat that fixture.  Its material
difference is the full current-source semantic scope.  It must test every
isomorphism preserving exactly the registered current structure, not merely
an identity arrow or the finite pair swap.

\section{Exact selector mathematics}

\subsection{Equivariant terminal sections and fixed points}

Let $G$ act on a set $P$, and let $1$ be the singleton with trivial $G$ action.
An equivariant map $s:1\to P$ is determined by $p=s(*)$.  Equivariance is
equivalent to
\[
  g\cdot p=p \qquad \text{for every }g\in G.
\]
Thus
\[
  \operatorname{Hom}_{G}(1,P)\cong P^G.
\]
If $P$ is a nonempty free torsor for a nontrivial group, then $P^G$ is empty
and no equivariant root exists.  In the exact task-local control, $G=C_2$ acts
freely and transitively on $P=\{0,1\}$ by addition modulo two.  The fixed-point
set and the equivariant-terminal-map set are both empty.

This is a source-side symmetry theorem.  It does not identify $G$ with a
physical gauge group and does not turn a presentation root into a physical
time orientation.

\subsection{Orbit quotient and selector information loss}

For the same torsor, $P/G$ contains one orbit.  The quotient map sends both
points to that orbit.  Suppose a decoder $d:P/G\to P$ recovered every root.
Then $d([0])=0$ and $d([1])=1$, while $[0]=[1]$, a contradiction.  Therefore
no decoder satisfies $d\circ\pi=\operatorname{id}_P$.

The quotient is a valid invariant and may support classification.  It does not
recover a root or unique admission.  Route B consequently needs an
independently registered downstream map before it can have P4--T02 selector
force; none is supplied by the current packet.

\subsection{Factorization through a current reduct}

Let $U:E\to R$ forget expansion data, and let $q:E\to Q$ encode a proposed
asymmetry.  A factor $\bar q:U(E)\to Q$ with $q=\bar q\circ U$ exists if and
only if $q$ is constant on every $U$-fibre.

The forward implication follows from
\[
  U(e_0)=U(e_1)
  \quad\Longrightarrow\quad
  q(e_0)=\bar q(U(e_0))=\bar q(U(e_1))=q(e_1).
\]
Conversely, fibre constancy makes
$\bar q(U(e)):=q(e)$ well defined.  The exact model exhausts all sixteen
Boolean functions on a four-point domain partitioned into two two-point
fibres and verifies both directions.  In particular, distinct root data over
one current reduct cannot be current-reduct derived.

\subsection{Equivariant law existence is not law selection}

The exact model also exhausts equivariant maps into the two-point torsor.  A
trivial one-point $C_2$ source admits none.  A free two-point $C_2$ source
admits exactly two: the identity and the swap.  Hence equivariance alone can
fail to provide any law, and even a free source action does not select a unique
law.  Any positive future result must name the registered current structure
that types and distinguishes its transformation.

\section{Four-route comparison}

\begin{center}
\small
\begin{tabular}{>{\raggedright\arraybackslash}p{0.10\linewidth}
                >{\raggedright\arraybackslash}p{0.19\linewidth}
                >{\raggedright\arraybackslash}p{0.57\linewidth}}
\toprule
Route & Disposition & Decisive reason \\
\midrule
A & Selected, not executed & Audits the full current-source model groupoid,
uses no new primitive, and returns either a derivation theorem or a minimal
scoped obstruction.  This is epistemically prior to proposing a new law. \\
B & Not selected & The orbit output is equivariant but loses root information.
Without an independently sourced downstream map it is a classifier, not a
selector, and risks another standalone-bridge replay. \\
C & Conditional later fallback & A proposal-only source-generated asymmetry
law is lawful after an exact negative current-source audit.  Selecting it now
would risk defining the missing law by the desired output. \\
D & Not triggered & Human-gated ontology change is premature while a bounded,
non-promotional current-source calculation remains open. \\
\bottomrule
\end{tabular}
\end{center}

\section{Parent-child conflict resolution}

The Physicist--Mathematician initially selected Route C and the
Physicist--Philosopher initially selected Route A.  In round one they
independently exchanged preferences after testing replay risk against
goal-smuggling risk.  The parent then imposed the decisive novelty condition:
Route A is selectable only if the future packet forms or precisely obstructs
the full semantic model and isomorphism groupoid of the registered source
signature, predeclares all candidate codomains, and never reuses the RT002
pair-groupoid calculation as its purported new result.  Route C remains an
explicit later fallback after a scoped negative result.  Both children
converged on that Route A identity in round two.  No blocking conflict remains.

\section{Future audit contract}

The Ontology Formalizer must bind the registered source signature and:
\begin{enumerate}[leftmargin=2em]
  \item separate the smooth four-manifold primitive from the unresolved
        $\Phi_{\mathrm{src}}$ slot and all target benchmark structure;
  \item form $\ModCur$ and its complete declared isomorphism groupoid, or
        return the first precise formation obstruction;
  \item predeclare exactly six candidate classes: point or finite point set,
        orientation, order, root or label, nonconstant admission, and proper
        arrow subclass;
  \item type each candidate as a functor on $\ModCur$ or record the first
        exact type failure;
  \item compute natural transformations under every declared isomorphism and
        test current-reduct factorization;
  \item use translations, orientation reversal, permutations, and same-reduct
        pairs only within their exact scoped hypotheses;
  \item return exactly one current-source derivation theorem, scoped
        naturality-factorization obstruction, or groupoid-typing obstruction;
  \item preserve the target-import, frozen-family, physical-meaning, adoption,
        and external-authority blocks.
\end{enumerate}

The selected packet is not run by RT-20260814-001.  A negative result may
license later consideration of Route C, but does not select, construct, or
adopt that source law.

\section{Freeze and Distance-to-GR status}

The following six local freezes remain unchanged:
\begin{enumerate}[leftmargin=2em]
  \item \texttt{NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR};
  \item \texttt{NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS};
  \item \texttt{NDCL-V22-P4T02-B2-COMMON-CHARACTER-HOLONOMY-PROTECTION};
  \item \texttt{NDCL-V22-P4T02-B2-ORIENTED-MATROID-BRIDGE-SELECTION-}
        \texttt{ROBUSTNESS};
  \item \texttt{NDCL-V22-P4T02-B2-SIGNED-CUBIC-VIABILITY-SELECTOR-}
        \texttt{ROBUSTNESS};
  \item \texttt{NDCL-V22-P4T02-B2-SOURCE-LAW-SPACE-ROBUST-INVARIANCE-}
        \texttt{PROTECTION-ROBUSTNESS}.
\end{enumerate}

Every expanded Distance-to-GR burden remains \NoDelta{}:
\begin{center}
\small
\begin{tabular}{ll}
\toprule
Burden & RT014 status \\
\midrule
Source ontology primitives & \NoDelta{} \\
Source equivalence $\EqSrc$ & \NoDelta{} \\
$\mathrm{RetainH}$ & \NoDelta{} \\
$\mathrm{GenH}$ & \NoDelta{} \\
$\mathrm{ObsLoc}_{\mathrm{lc}}$ & \NoDelta{} \\
$\mathrm{Resp}_{\mathrm{lc}}$ & \NoDelta{} \\
$M_{\mathrm{src}}$ & \NoDelta{} \\
$g_{\mathrm{eff}}$ & \NoDelta{} \\
Matter coupling & \NoDelta{} \\
Einstein equations & \NoDelta{} \\
Finite-variation robustness & \NoDelta{} \\
Benchmark promotion & \NoDelta{} \\
Gate Chair status & \NoDelta{} \\
Current route freeze or hard-fail status & \NoDelta{} \\
\bottomrule
\end{tabular}
\end{center}

\section{Non-conclusions}

No source model groupoid, \EqSrc{} law, source primitive, source law, or
presentation ontology is adopted.  A semantic automorphism group is not a
physical gauge group.  A mathematical orientation is not physical time
orientation.  The orbit quotient is not causality or empirical response.  D7
is not reevaluated, B2 remains inactive, and P4--T03 remains locked.  No
effective metric, matter coupling, Einstein equations, global no-go,
future-extension impossibility, Gate verdict, benchmark promotion, proof
authority, publication, push, external action, or completed derivation
follows.

\end{document}
