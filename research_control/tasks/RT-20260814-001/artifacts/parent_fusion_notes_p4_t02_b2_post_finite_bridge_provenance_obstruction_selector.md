<!-- authority: science-draft -->

# Parent fusion — post-finite-bridge provenance-obstruction selector

## Result

RT-20260814-001 selects Route A,
`A_CURRENT_SOURCE_EQUIVARIANT_ASYMMETRY_DERIVATION_AUDIT`, and the future
packet
`PKT-V22-P4T02-B2-CURRENT-SOURCE-EQUIVARIANT-ASYMMETRY-DERIVATION-AUDIT-V1`.
The packet type is `bounded_theoretical_calculation`, its next role is
`ontology-formalizer@0.2.0`, and it is selected but unexecuted.

This selector adds no source primitive, source law, ontology edit, physical
interpretation, or protected status. It chooses the least assumption-expanding
calculation that can distinguish two possibilities: the registered current
source already defines a natural equivariant asymmetry, or its bounded semantic
model groupoid supplies an exact scoped naturality, factorization, or typing
obstruction. Route C, a proposal-only source-generated asymmetry law, remains a
lawful later fallback only if the audit is negative; it is neither selected nor
executed here.

## Fixed predecessor

The exact RT-20260812-002 result remains fixed. Its finite typed
presentation-admission interface and pair-groupoid calculations are valid, but
GATE-04 fails because the registered source package supplies no independently
source-provenanced root, orientation, order, label, admission bit, EqSrc
transport, or source-to-presentation map. The positive rooted construction
remains prohibited. This selector neither repairs nor replays that fixture.

## Exact four-route comparison

1. Route A audits the full semantic model and isomorphism groupoid of the
   registered current-source signature. It must predeclare its candidate
   asymmetry codomains and test naturality and reduct factorization under every
   declared current-structure automorphism. This route is selected.
2. Route B replaces a root by an orbit or groupoid-valued classifier. The exact
   two-point control shows that this preserves symmetry but loses the very
   information needed to recover a root or unique admission. No registered
   downstream map gives it physical or P4-T02 selector force.
3. Route C proposes a new target-free source law whose state-dependent output
   could transform equivariantly. It is potentially constructive, but selecting
   it before the current-source audit risks defining the missing law by the
   desired output. It remains a conditional later fallback with blocked
   adoption and a mandatory smuggling audit.
4. Route D is the protected human-gated ontology stop. It is premature while
   Route A remains a bounded non-promotional calculation.

## New mathematical payload

For a group (G) acting on a set (P), equivariant maps from the terminal
(G)-set to (P) are exactly the fixed points of (P). A nonempty free
torsor for a nontrivial group therefore admits no equivariant root. In the
exact (C_2) control on two points, the torsor is free and transitive, has no
fixed point, and has no equivariant terminal root.

The orbit quotient of that torsor has one element. Both roots map to the same
orbit, and no decoder from the singleton quotient recovers both roots
uniformly. Thus a root-free quotient may be a valid equivariant classifier
while carrying zero unique-selection force.

For finite maps (U:E\to R) and (q:E\to Q), (q) factors through (U)
exactly when it is constant on every (U)-fibre. The task-local model exhausts
all sixteen Boolean maps on a four-point domain with two two-point fibres and
verifies both directions. Two expansion asymmetries over one current reduct
therefore refute current-reduct factorization.

Finally, the trivial one-point (C_2)-source admits no equivariant map to the
torsor, whereas the free two-point source admits exactly two. Equivariance
alone therefore guarantees neither existence nor a unique law. Any positive
current-source theorem must identify the registered structure that selects and
types the natural transformation.

## Parent-child resolution

The two required child reviews initially divided between Route A and Route C.
After exchanging the replay and goal-smuggling objections, they exchanged
preferences rather than converging. The parent resolved the scientific issue
in round two by imposing a strict material-distinctness boundary on Route A:
the successor must audit the full current-source semantic groupoid, not the
RT002 pair swap, and must return exactly one current-source derivation theorem
or minimal scoped obstruction. Both children then converged on the same Route
A packet identity, type, role, and unexecuted status. No blocking conflict
remains.

## Source and meaning boundary

The future semantic model groupoid is a theorem-audit device. It is not adopted
EqSrc, a physical gauge group, a causal structure, an empirical response map,
or a presentation ontology. A natural orientation in that audit would be a
source-mathematical construction only; it would not establish physical time
orientation. A negative audit is scoped to the exact registered current
signature and predeclared candidate grammar. It is not a global no-go and does
not show that every conservative source extension is impossible.

All six inherited freezes remain active. All fourteen Distance-to-GR burdens
remain `no_delta`. D7 was not reevaluated, B2 was not activated, and P4-T03
remains locked. No effective metric, matter coupling, Einstein equations,
benchmark promotion, Gate verdict, proof authority, publication, push,
external action, or completed derivation follows.
