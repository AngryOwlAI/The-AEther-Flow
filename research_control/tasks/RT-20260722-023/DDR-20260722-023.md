---
authority: control
decision_id: "DDR-20260722-023"
task_id: "RT-20260722-023"
created_at: "2026-07-23T03:31:12Z"
activated_at: "2026-07-23T03:31:12Z"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "process-integrity-auditor"
selected_role_version: "0.1.0"
agent_job_id: "AJ-RT-20260722-023-001"
status: "blocked"
completed_at: "2026-07-23T04:14:12Z"
validation_status: "FAIL"
supersedes_decision_id: "DDR-20260722-022"
requires_human_gate: false
role_fit_candidates:
  - "process-integrity-auditor@0.1.0|selected|The blocker requires exact preservation evidence and a narrowly scoped Git classification with no bypass."
  - "validator-engineer@0.2.0|not_selected|The validators now pass and the remaining failure is staged Git policy compatibility."
  - "project-control-maintainer@0.2.0|not_selected|No routing algorithm handoff schema or validator implementation changes are needed."
  - "documentation-curator@2.0.0|not_selected|The immutable manuscript payload must not be edited."
---

# DDR-20260722-023 — Preserve the hash-bound P15-T01 checkpoint payload

## Current Objective

Clear the sole normal-checkpoint blocker without altering any of the four
hash-bound P15-T01 files. Add an explicit Git whitespace classification for
those four literal repository paths, prove that the classification does not
apply to adjacent or wildcard paths, and rerun the normal checkpoint with no
legacy-validation bypass.

## Authority Surfaces Read

The repository SOUL and control instructions, the recursive relay and
project-system skills, Project-System Director and Process Integrity Auditor
roles, AgentJob and execution schemas, `handoff-0831`, the complete
RT-20260722-022 packet, its hash-bound blank-at-EOF blocker, the four immutable
P15-T01 files, `.gitattributes`, Git's installed `gitattributes` and whitespace
documentation, live program state, registries, and checkpoint machinery were
read. Generation 64 was claimed and consumed exactly once before this worker
step.

## Role-Fit Matrix

| Role | Decision | Reason |
| --- | --- | --- |
| `process-integrity-auditor@0.1.0` | Selected | Owns preservation proof and a uniquely determined exact-path control repair. |
| `validator-engineer@0.2.0` | Not selected | The prior live-route repair and required test shard already pass. |
| `project-control-maintainer@0.2.0` | Not selected | No routing, schema, or checkpoint algorithm change is authorized. |
| `documentation-curator@2.0.0` | Not selected | The four hash-bound source artifacts are immutable evidence. |

## Selected Role

Use one Process Integrity Auditor task overlay. It may append only four
literal path classifications to `.gitattributes`, create the task-local proof
and recovery records, synchronize standard generated derivatives, and invoke
the normal checkpoint for the already authorized combined transaction. It may
not normalize the protected files, introduce a wildcard, modify the checkpoint
or validator implementation, or use legacy validation.

## Claim Boundary

A passing whitespace-policy proof establishes only that Git's staged
whitespace gate preserves four immutable files while retaining blank-at-EOF
enforcement everywhere else. It does not execute P15-T03, change any P15-T01
scientific statement, establish ontology or physical interpretation, change a
scientific ledger, authorize external review or publication, promote a physics
claim, or complete a derivation.

## Validation

Require exact before-and-after hashes for the four protected files and the
predecessor completion and handoff, literal `git check-attr` results for four
intended paths plus an adjacent control path, an isolated Git-diff proof that
the four intended paths are exempted while the control still fails
`blank-at-EOF`, the P15-T01 package checker, the exact 171-test shard, affected
validation planning, project-change classification, signal and documentation
gates, changed-scope research-control validation, claim-language validation,
memory synchronization, normal Git diff checking, and one normal governed
checkpoint for `AJ-RT-20260722-023-001`.

The four literal rules, protected and predecessor hashes, adjacent control,
P15-T01 package checker, static control validation, and exact 171-test shard
pass. The one normal checkpoint invocation then stopped before staging because
the validation classifier reports root `.gitattributes` as
`unknown_governed_path`, making the shadow checkpoint plan unsafe. This
decision does not authorize classifier or checkpoint implementation changes,
so the exact planner failure is preserved for a fresh Validator Engineer
recovery. P15-T03 remains unexecuted.
