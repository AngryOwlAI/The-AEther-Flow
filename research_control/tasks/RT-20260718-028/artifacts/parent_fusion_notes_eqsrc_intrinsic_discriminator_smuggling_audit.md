# Parent Fusion Notes: EqSrc Intrinsic Discriminator Smuggling Audit

## Synthesis

The independent mathematical and philosophy-of-physics audits agree. Exact
proposal-only `EqSrcIntrinsicDiscriminatorAdmissibilityLaw_src^cand,v1` uses no
explicit target or process datum as a positive mathematical premise. Its
chain-map naturality, conditional quotient uniqueness, and finite
boundary-move pointwise robustness arguments are valid within their stated
hypotheses.

The fused classification is
`source_pure_as_written_with_precise_repairable_relation_label_obstruction`.
It records
`OBST-EQSRC-INTRINSIC-DISCRIMINATOR-RELATION-LABEL-001` and routes one bounded
proposal-only Ontology Formalizer scope repair. No repair is performed here.

## Source Roots and Independence

Once a finite chain package \(E\) and its differentials are supplied,
\(Z_1(E)\), \(B_1(E)\), \(H_1(E)\), the quotient discriminator, and its kernel
pair are conservative mathematical definitions. Current ontology does not
derive or select the package, the use of \(\mathbb F_2\), the differential, or
boundary moves as physical variations.

The variation class is defined without a textual back-reference to the
discriminator or relation. That establishes model-internal non-circularity.
It does not establish an independently motivated physical variation law:
both branches share \(\partial_2\), since its image defines the quotient kernel
and its values generate the admitted translations.

## Decisive Relation-Label Separation

Let \(A=Z_1(E)\), \(B=B_1(E)\), and
\(\chi:A\rightarrow A/B\). For a fixed cycle \(c\), define
\(S_c(z)=z+c\). Then
\[
\chi(S_c z)=\chi(z)+[c].
\]
Pointwise discriminator values are fixed exactly when \(c\in B\). However,
for every \(c\in A\),
\[
S_c(z)-S_c(z')=z-z',
\]
so \(z\sim z'\) if and only if \(S_c(z)\sim S_c(z')\). Every translation is
an automorphism of the kernel-pair relation.

For the RT-027 witness, \(c=e_2\) changes all eight individual discriminator
values but preserves all 64 ordered-pair relation tests. The four quotient
blocks are permuted rather than broken. The witness therefore shows failure
of pointwise class-label fixing, not relation fragility.

A genuinely relation-changing source-state bijection exists: define
\(L(e_0)=e_0\), \(L(e_1)=e_0+e_2\), and \(L(e_2)=e_1\). It is invertible, but
maps the boundary generator \(e_0+e_1\) to \(e_2\), outside \(B_1\). Thus
\(0\sim e_0+e_1\) while \(L(0)\not\sim L(e_0+e_1)\). This audit witness is not
a candidate repair and is not an admitted chain map.

## Naturality, Uniqueness, and Generalization

Homology functoriality is valid for typed chain maps. Chain isomorphisms
preserve and reflect the relation. This is a theorem in the supplied category,
not a selection of that category or a proof of physical covariance.

The quotient-uniqueness theorem is valid for surjective set-valued
discriminators with exactly the same kernel pair. Its codomain bijection need
not be linear, pointed, or natural in a family. The theorem does not select the
kernel pair from current ontology.

The eight-state witness proves nondegenerate finite-model existence only. It
does not establish physical admissibility, ontology-wide applicability,
continuous or infinite-system robustness, or exact-GR recovery.

## Parent Decision

The packet is a `proposal-only` `new_ontology_primitive_candidate` with
`blocked_adoption_open_continuation`. Physical admissibility is not
established. No target or process smuggling is detected as written, but the
relation-level sharpness overread blocks immediate Refuter stress.

The next lawful packet is one bounded `ontology-formalizer@0.2.0`
`ontology-law-research-packet`. It must distinguish pointwise discriminator
stability from kernel-pair relation preservation, correct or withdraw the
\(e_2\)-translation sharpness claim, and retain every primitive-selection and
physical-admissibility limit. A fresh audit must precede Refuter stress.

## Preserved Claim Boundary

No canonical ontology edit, source-law adoption, physical-admissibility or
physical-covariance result, general `EqSrc`, RetainH, GenH, or `M_src`
adoption, Distance-to-GR or metric-use ledger change, `g_eff`, matter coupling,
Einstein equations, benchmark promotion, Gate Chair verdict, completed
derivation, future source-extension impossibility, program-wide no-go, or
global theory rejection follows.
