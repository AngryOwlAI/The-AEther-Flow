# Parent Fusion Notes: EqSrc Closure Candidate v3 Smuggling Audit

## Synthesis

The independent mathematical and ontology-authority audits converge without a
blocking conflict. Proposal-only `EqSrcClosureLaw_src^cand v3` repairs the
exact statement-level defects recorded for v2: every proxy constructor is
typed, the ledger, proxy, provenance, outcome, and certificate congruences are
declared, negative-control evaluators act on raw certificate fibers, and the
rank-0 through rank-4 dependency graph excludes an acceptance back-edge.

The fused result is
`statement_level_smuggling_audit_pass_with_instantiation_provenance_limit`.
It advances the proposal to `candidate_audited_pending_stress`; it does not
establish current-ontology derivation, instantiation, adoption, or general
`EqSrc`.

## Fused Mathematical Findings

The parent preserves three mathematical contributions.

- Certificate congruence and the invariance of the component decisions imply
  invariance of `Accept_src`; the typed inverse and composition constructors
  preserve certificate congruence componentwise.
- A one-object singleton construction satisfies the full law abstractly and
  therefore witnesses formal consistency and non-vacuity. It is physically
  degenerate and supplies no current-ontology instance.
- A one-object construction with a two-element ledger, all certificates
  accepted, and constantly-identity ledger composition has an equivalence
  object relation but violates unit compatibility. Thus the three
  accepted-totality clauses suffice for the coarse theorem while the full
  coherence law is strictly stronger.

The audit also narrows the fail-closed wording: `Accept_src` is total on its
typed certificate domain. Ill-typed or missing data outside that domain are a
meta-level reason to reject a proposed model unless a later formalization adds
an ambient error carrier.

## Source-Factorization Finding

Let an ambient interpretation space carry a projection to its declared
source-root tuple. A lower-rank interpretation is source-only exactly when it
factors through that projection; for a surjective projection, this is
equivalent to being constant on every projection fiber. A finite acyclic
definition graph then propagates factorization from source-only roots to every
rank-0 through rank-2 value, `Accept_src`, and candidate-law satisfaction.

Counterfactual invariance alone is insufficient if a target-derived hash,
lookup result, learned parameter, or oracle output is frozen and relabeled as
a source root. The candidate correctly adds the jointly necessary transitive
root-provenance exclusion. This passes as a proposal-level admissibility
condition. No concrete current-ontology definition trees are supplied, so
instantiated source purity remains a later audit burden.

## Authority Finding

No target geometry, reconstructed observation, benchmark, downstream field
equation, registry, generated derivative, role, validator, handoff, checkpoint,
approval, adoption, or Gate Chair state is a positive premise. Internal
certificate acceptance remains distinct from audit passage and ontology
adoption.

The proposal is substantive source-extension data. No definitional translation,
unique expansion, or conservativity theorem is present. If adoption is later
requested, it remains a new ontology primitive requiring protected human
authority.

## Parent Decision

`OBST-EQSRC-CLOSURE-SIGNATURE-001` does not recur at statement level. The route
is not frozen because v3 is materially repaired and this audit adds a
factorization lemma, acceptance-invariance lemma, consistency witness, and
strictness countermodel.

The next lawful packet is one bounded `refuter@0.2.0` stress test of exact v3,
emphasizing:

- nontrivial source-only model existence;
- transitive provenance pressure for aliases, hashes, lookups, learned
  parameters, and opaque oracles;
- malformed-definition rejection and typed fail closure;
- coherence independence and degenerate models; and
- finite-variation robustness.

No human gate is required to run that stress packet. A protected human gate is
still required before any adoption.

## Preserved Claim Boundary

No canonical ontology edit, source-law adoption, general `EqSrc` discharge,
RetainH, GenH, or `M_src` adoption, Distance-to-GR ledger change, `g_eff`
result, matter-coupling or Einstein-equation derivation, benchmark promotion,
Gate Chair verdict, completed derivation, future source-extension
impossibility, program-level no-go, or global theory rejection follows.
