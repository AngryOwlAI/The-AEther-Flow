<!-- authority: control -->

# Parent fusion notes: P2-T04 finite countermodel atlas

The parent fused the mathematical group-action inventory with the ontology and claim-boundary review into one atlas. The fusion keeps the P2-T03 theorem domain unchanged and treats every finite family through the same four coordinates: declared automorphism group, eligible-choice space, orbit decomposition, and fixed set.

Five records are direct empty-fixed theorem instances: the two independent `X_empty` swaps, the `GL(2,2)` line action, and the two- and four-cycle root actions. Three records are fixed-point-positive controls only: explicit response tags, a supplied root certificate, and the proposal-only four-chain minimum. The trivial two-choice action is the required multiple-fixed control and explicitly demonstrates that `K` may preserve or collapse selector multiplicity.

The periodic, multiple-minimum, and non-graded graded-orbit controls are guarded noninstances. Their candidate admission fails before the eligible selector functor used by P2-T03 is formed. This classification preserves their original fail-closed meaning and prevents an invalid theorem overread.

Every original task, candidate, countermodel, obstruction, and freeze identifier remains intact. The atlas is a cross-family integration record, not a replacement or reopening of any exact family. Structural automorphisms are not physical gauge transformations. Supplied tags or roots are not current-ontology derivations. File names, serialization order, registry order, validator state, roles, handoffs, and process metadata are excluded from every group action.

The decisive result is a constructed finite atlas with twelve machine fixtures and a theorem-instance map. It sharpens theorem scope and negative-result provenance but creates no Distance-to-GR delta, ontology adoption, general `EqSrc` discharge, physical covariance result, or promotion authority.
