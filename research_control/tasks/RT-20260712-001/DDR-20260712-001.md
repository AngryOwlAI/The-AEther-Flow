---
authority: control
decision_id: "DDR-20260712-001"
task_id: "RT-20260712-001"
created_at: "2026-07-12T20:36:04Z"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "project-control-maintainer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260712-001-001"
status: "completed"
supersedes_decision_id: ""
requires_human_gate: false
validation_status: "PASS"
---

# DDR-20260712-001: V19 Plan Registration

## Analysis

The user authorized implementation of the first logical task in
`implementations_plans/recommendations_implementation_plan_continue_task-v19.md`.
The working tree was clean, the project-improvement resolver reported no
competing signal, and P0-T01 has no dependencies or human gate. The plan and
its 2026-07-12 audit were ignored local files; the plan was not tracked or
registered.

`handoff-0740` remains the ordinary research-continuation authority and selects
`EqSrc_family_closure_repair_or_stress`. V19 explicitly declares itself a
non-superseding project-system sidecar.

## Decision

Create `RT-20260712-001` and bind one bounded AgentJob to a
`project-control-maintainer@0.2.0` task overlay for P0-T01 registration.

The draft plan names `project-system-director@0.2.0` as its role family. The
live role contract allows that role to route and create an AgentJob but states
that it does not execute the selected job and may not modify sources. The
Project-Control Maintainer is therefore the execution role for the registry
and control-source changes; the Project-System Director remains the routing
authority.

## Role-Fit Matrix

| Role | Decision | Reason |
| --- | --- | --- |
| `project-control-maintainer@0.2.0` | Selected | The bounded work changes `.gitignore`, the Markdown source registry, and task-local project-control records. |
| `project-system-director@0.2.0` | Routing authority | Its live contract creates and routes one AgentJob but does not execute source modifications. |
| `documentation-curator@2.0.0` | Rejected | This is control-source registration rather than explanatory or publication documentation. |
| Physics roles | Rejected | The packet contains no derivation, scientific audit, candidate, stress test, or promotion decision. |

## Write-Boundary Rationale

P0-T01 names the plan and Markdown source registry as its planned source scope.
One adjacent `.gitignore` line is also necessary because the repository ignores
`implementations_plans/*`; without the exception the required tracked-plan
success condition cannot be met. No other substantive source is changed.

Normal task-local records and bootstrap-generated derivatives are required by
repository policy. A normal research handoff and `program_state.yaml` are
deliberately excluded because changing either would incorrectly displace
`handoff-0740`.

## Authority Boundary

This decision authorizes project-control registration only. It does not
authorize canonical ontology edits, source-law adoption, general `EqSrc`
discharge, `RetainH` or `GenH` adoption, `MetricData(E)` adoption, `g_eff`
scope expansion, detector or readout semantics adoption, coupling-law adoption,
matter-coupling derivation, stress-energy semantics, Einstein-equation
derivation, benchmark promotion, Gate Chair authority, proof authority, or a
completed-derivation claim.

## Next Route

On PASS, the next v19 sidecar packet is P0-T02 backlog materialization.
`handoff-0740` and its ordinary `EqSrc_family_closure_repair_or_stress` route
remain unchanged and do not resume until the v19 sidecar closes or a later
lawful decision changes that ordering.
