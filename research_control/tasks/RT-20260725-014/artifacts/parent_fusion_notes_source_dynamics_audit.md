<!-- authority: science-draft -->

# P5-T07 parent fusion notes

Status: `draft/control`, proposal-only audit and negative-result evidence.

The mathematical and philosophical perspectives agree on a decisive scoped
result. The exact P5-T01 through P5-T06 package contains no explicit target or
process premise and its candidate-specific formulas, structural theorems, and
error bounds survive. Those facts do not make the package intrinsically
source-derived or robust enough for geometry.

The new mathematical payload is exact:

- The cubic flow is one member of the family
  `da/dlambda = -gamma a^(2m+1)`, `m >= 1`, whose members share total forward
  action, reflection oddness, a unique zero stationary state, monotone
  attraction, and finite backward horizons.
- Under `b = d a` and `lambda' = c lambda`,
  `gamma' = gamma/(c d^(2m))`.
- The P5-T06 quantizer obeys
  `N_(d epsilon)(d a) = d N_epsilon(a)`, so resolution must transform as
  amplitude-coordinate data.
- The one-dimensional family has no transverse generator or local
  characteristic data.

The fused verdict is
`source_pure_as_written_but_not_intrinsically_selected_or_geometry_robust`.
The exact unchanged one-mode route from cubic dynamics and fixed-resolution
coding to geometry support is locally frozen. This does not reject the
candidate-specific mathematics, canonical ontology, the global project, or
future conservative source extensions.

P5-T08 may integrate the scoped negative result after a governed checkpoint.
It may not reopen or repair the frozen route inside this task.
