<!-- authority: control -->

# P2-T02 EqSrc natural-selector fixed-point receipt

Task `RT-20260720-015` executes only v21 work item `P2-T02` under
`ontology-formalizer@0.2.0` and the `ontology-law-research-packet` route.

The registered fused artifact is
`EQSRC-NATURAL-SELECTOR-FIXED-POINT-CONDITION-V1`. On the unchanged P2-T01
certified-isomorphism core, it defines a deterministic source-natural selector
as `sigma:1_GSigma -> S_chi` and proves that every component value is fixed by
the declared structural automorphism action. It distinguishes empty,
singleton, and multiple fixed loci; separates selector-level from `K`-image
uniqueness; and proves a bounded reduct/stabilizer inclusion for added marks.

Seven finite controls are recorded: two empty-fixed transitive actions, one
singleton positive control, one two-choice nonuniqueness and `K`-image split,
two added-mark stabilizer controls, and one noninvertible coherence guard.
The finite enumerations instantiate the conditional theorem; they are not
empirical evidence, a general theorem proof, or physical recovery.

The result is `draft/control`, `proposal-only`, and `source-extension data`.
Current ontology does not derive the P2-T01 category, choice functor, selector,
added marks, or structural automorphism group as physical gauge. Adoption
remains `blocked_adoption_open_continuation`.

The following remain false: post-hoc domain alteration, the complete P2-T03
theorem, P2-T06 audit, P2-T07 stress test, physical gauge or covariance status,
current-ontology derivation, frozen-family reopening, source-law adoption,
canonical ontology edit, general `EqSrc` discharge, `M_src`, `g_eff`, matter
coupling, Einstein equations, scientific-ledger change, benchmark promotion,
Gate Chair verdict, publication, and completed derivation.

The only next dependency-ready work item is `P2-T03`, which may state and
prove the complete no-selector and nonuniqueness theorem with explicit global
component and relation-image quantifiers. P2-T06 and P2-T07 remain independent
later audit and stress packets.
