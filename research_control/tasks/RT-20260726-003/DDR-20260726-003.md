---
authority: control
status: completed
decision_id: DDR-20260726-003
task_id: RT-20260726-003
agent_job_id: AJ-RT-20260726-003-001
director_version: director-of-research@0.3.0
decision_type: task_overlay
selected_role_id: candidate-constructor
selected_role_version: 0.2.0
supersedes_decision_id: DDR-20260726-002
requires_human_gate: false
role_fit_candidates:
  - candidate-constructor@0.2.0
  - ontology-formalizer@0.2.0
  - smuggling-auditor@0.2.0
---

# DDR-20260726-003: execute only v21 P6-T02

## Current Objective

Execute immutable generation 122 and only P6-T02. Add materially new
source-local law content, calculate one characteristic relation, principal
polynomial, or order from the actual equation, and return a decisive candidate
or obstruction. Do not reopen the frozen inference from unchanged P5 diagonal
data and do not import the target GR cone.

## Authority Surfaces Read

The Director read `SOUL.md`, both applicable `AGENTS.md` files, the recursive
relay and `continue-research` skills, the Director and Candidate Constructor
roles, AgentJob and mathematical-decisiveness schemas, the v21 P6-T02 plan,
the Distance-to-GR burden map and ledger, `handoff-0876`, the complete P6-T01
target and completion, the P5-T08 synthesis and P6 input contract, canonical
foundations, dynamics, and geometry TeX, the historical scoped `g_eff`
candidate and Gate Chair record, and the metric-use ledger. Memory preflight
refreshed retrieval-only state and passed with 1,028 source objects and 5,143
relationship rows. Direct source hashes were held fixed.

The P6-T01 checkpoint commit
`6f50f4997ae97e870b317616c8a895fe641b00f1` satisfies the only plan
dependency. Immutable generation 122, route SHA-256
`5b6106fdbf0104ac31f6ef06a9769cc16c002fc8089a4e6eec5086a9c0e684eb`,
and `handoff-0876` uniquely select P6-T02.

## Role-Fit Matrix

| Candidate role | Fit | Reason |
|---|---|---|
| `candidate-constructor@0.2.0` | selected | P6-T02 requires one explicit new source-local equation, characteristic calculation, example, approximation, and failure controls. |
| `ontology-formalizer@0.2.0` | next packet | P6-T03 may test whether the resulting one-ray structure fixes a conformal class or is precisely insufficient. |
| `smuggling-auditor@0.2.0` | internal boundary only | Target import and physical-overread checks are required, but the immutable work item is construction rather than a separate audit AgentJob. |
| `gate-chair@0.2.0` | blocked | No source-law adoption, physical-causality verdict, metric promotion, or protected decision is requested. |

## Selected Role

Activate one `candidate-constructor@0.2.0` task overlay and one outer
AgentJob. The internal physicist-mathematician perspective independently
derives the equation, solution, principal polynomial, characteristic set,
hyperbolicity half-space, dual ray, response, and approximation bound. The
internal physicist-philosopher perspective audits orientation, time,
chronology, operational, universality, geometry, and adoption overreads. The
parent resolves their six tensions without creating child AgentJobs.

The selected proposal-only extension on the smooth source arena is

`L_V a + gamma a^3 = 0`,

where `a` is a source scalar and `V` is a nowhere-zero positively oriented
source vector field. Along every integral curve it exactly recovers the P5
cubic flow. Linearization gives `P_x(xi)=xi(V_x)`, characteristic covectors
annihilating `V_x`, a cotangent hyperbolicity half-space, and a single
nonnegative dual propagation ray.

## Claim Boundary

The construction is `draft/control`, `proposal-only`, and `source-extension
data`. It is not a canonical-ontology candidate, adopted source law, target
congruence, preferred physical frame, clock, physical causal cone, conformal
class, or metric. Current ontology does not derive or select `V`, orientation,
normalization, nonvanishing, chronology, operational meaning, or common-sector
universality.

The zero-field control destroys the principal polynomial, a closed orbit
destroys antisymmetry, competing `V` and `W` fields preserve restricted P5
data while changing characteristics, and a two-sector system produces
multiple rays. These are scoped controls, not a global theory rejection or
future-extension impossibility theorem.

## Validation

The task-local deterministic validator passes 41 exact-source, solution,
semigroup, tangent-response, principal-symbol, hyperbolicity, dual-ray,
approximation, control, parent-child, TeX-marker, and authority checks. The
registered TeX compiles to a five-page scratch PDF. Repository-wide source,
documentation, signal, claim-language, research-control, memory, test,
whitespace, and checkpoint gates remain mandatory before the transaction is
final.

The decisive no-fog disposition is `constructed_candidate`.
