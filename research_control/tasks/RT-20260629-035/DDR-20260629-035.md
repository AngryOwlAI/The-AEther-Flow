---
decision_id: "DDR-20260629-035"
task_id: "RT-20260629-035"
agent_job_id: "AJ-RT-20260629-035-001"
director_version: "director-of-research@0.3.0"
decision_type: "task_overlay"
selected_role_id: "validator-engineer"
selected_role_version: "0.2.0"
status: "completed"
requires_human_gate: false
created_at: "2026-06-29T16:13:30Z"
---

# Director Decision Record: DDR-20260629-035

## Decision

Select `validator-engineer@0.2.0` for one bounded v12 P1-T04 current-frontier
renderer update.

## Basis

`research_control/program_state.yaml` names `RT-20260629-034` and
`handoff-0329`. The handoff authorizes exactly one next packet: update the
current-frontier renderer so layered Distance-to-GR statuses and overread
guards are visible.

The P1-T04 plan requires `research_control/current_frontier.md` generation to
display layered Distance-to-GR status layers or a compact equivalent, make
overread guards visible, and preserve generated-snapshot non-authority. This
is renderer and regression-test work, directly within
`validator-engineer@0.2.0`.

## Scope

Allowed:

- update `scripts/research_control/render_current_frontier.py` to render
  control, mathematical, physical, promotion, and overread-guard ledger
  fields;
- update `tests/test_render_current_frontier.py` to prove layered rendering,
  non-authority boundaries, and drift detection;
- regenerate `research_control/current_frontier.md` from tracked state;
- regenerate memory, dependency graph, and registry derivatives required by
  validation and checkpoint;
- create normal Continue Research receipts.

Blocked:

- canonical ontology edit;
- source-law adoption;
- `MetricData(E)` adoption;
- unscoped `g_eff` adoption or `g_eff` scope expansion;
- coupling-law adoption;
- matter-coupling derivation or adoption;
- stress-energy semantics;
- stress-energy tensor;
- matter action;
- detector semantics;
- Einstein equations;
- exact-GR benchmark promotion;
- benchmark Gate Chair closure;
- completed derivation;
- treating the current-frontier snapshot, renderer output, validator output,
  generated memory, dependency graph, checker, commit, handoff, approval, or
  registry row as scientific proof.

## Role-Fit Matrix

| Candidate role | Fit judgment | Reason |
| --- | --- | --- |
| `validator-engineer@0.2.0` | selected | P1-T04 changes a deterministic renderer and tests for a control snapshot. |
| `documentation-curator@2.0.0` | acceptable but not selected | The visible text is reader-facing, but the implementation surface is renderer code and drift tests. |
| Physics roles | not selected | The packet has no derivation milestone and must not produce physics evidence. |

## Result

The selected AgentJob updated current-frontier rendering so the generated
Distance-to-GR table exposes the layered status columns and overread guards.
Focused renderer tests now prove layered field visibility, high-risk row
boundary text, machine-readable payload fields, and drift detection.

## References

The AEther-Flow Research Project. (2026, June 29). *Distance-to-GR layered
status migration report* [Internal control migration report].

The AEther-Flow Research Project. (2026, June 29). *Distance-to-GR status
layers v1* [Internal control design note].

The AEther-Flow Research Project. (2026, June 29). *Handoff 0329* [Internal
research-control handoff].

The AEther-Flow Research Project. (2026, June 29). *Recommendations
implementation plan continue task v12* [Internal implementation plan].
