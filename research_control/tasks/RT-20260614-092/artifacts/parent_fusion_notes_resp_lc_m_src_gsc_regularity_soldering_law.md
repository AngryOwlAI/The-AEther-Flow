<!-- authority: control -->

# Parent Fusion Notes: RegSold_src^GSC Formalization

## Analysis

The Physicist-Mathematician child supplied the formal interface for
`RegSold_src^GSC`: `Sep_src`, `TopReg_src`, `ChartSupp_src`, `Reg_src`,
`Solder_src`, `Var_reg_src`, and `Bottom_src`, with source-only domains and
certificate obligations.

The Physicist-Philosopher child supplied the authority boundary: the law is
proposal-only draft/control source-extension candidate data. It is not adopted
ontology, not adopted `M_src`, and not a downstream GR shortcut.

## Fusion

The fused artifact keeps both contributions. It defines a law package and a
regularized candidate notation `M_src^{GSC-reg-cand}` only as proposal-only
data. It also states theorem obligations, bottom branches, failure branches,
and a packet-interface no-target-import lemma.

## Boundary

The fused artifact does not claim canonical ontology edit, source-law
adoption, `M_src` adoption, full `M_src` construction, `g_eff`, matter
coupling, Einstein equations, benchmark promotion, Gate Chair closure,
completed derivation, future source-extension impossibility, or global theory
rejection.

## Next Route

The logical next route is one bounded Smuggling Auditor packet for
`RegSold_src^GSC`.
