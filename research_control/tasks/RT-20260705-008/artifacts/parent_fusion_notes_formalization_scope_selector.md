# Parent Fusion Notes: P6-T01 Formalization Scope Selector

Both child perspectives select the same target:

```text
certificate record type and validity predicate
```

Both also select the same toolchain:

```text
Python typed algebraic spec plus unittest
```

The mathematical reason is minimality. The record and validity predicate are
the kernel needed before checking transport, invariance, factorization,
composition, missing, malformed, target-import, detector-import, process
authority, and scoped-evidence failures.

The philosophical/process reason is boundary clarity. The executable result
must classify finite/local records, not promote ontology, law, matter coupling,
or benchmark status.

The fused artifact records the route to P6-T02 and leaves implementation
deferred to a bounded Validator Engineer packet.
