---
object_id: "MD-V22-P4-T02-B2-PARENT-FUSION-COMMON-CHARACTER-INDEPENDENT-VARIATION-REFUTER-STRESS-V1"
authority: "science_draft"
status: "draft/control"
task_id: "RT-20260810-012"
job_id: "AJ-RT-20260810-012-001"
---

# Parent fusion notes — P4-T02 B2 common-character Refuter stress

The parent fuses two independent Refuter analyses into one exact result:
`scoped_obstruction`. The fixed RT010 pointed, framed, anchored law remains
conditionally correct, and the RT011 `source_pure_as_written` provenance verdict
remains valid in its exact proposal-only scope. Neither child finds an inverse,
cocycle, positivity, target-import, or hidden-authority defect in the stipulated
law. The obstruction is instead the absence of a source-derived invariant that
protects equal, nontrivial sector holonomy in the enlarged target-free variation
family required by P4-T02.

The minimal countermodel changes one oriented generator pair. If `omega` is the
additive path cochain with `omega(e_01)=1` and zero on the other two displayed
generators, then

`rho_R^epsilon = rho_R exp(epsilon omega)`

is a positive one-dimensional representation for every real `epsilon`. Its
cycle log holonomy is `3 mu + epsilon`, while the S and D sector values remain
`3 mu`. Every nonzero `epsilon`, however small, therefore leaves both the
three-sector common-class diagonal and the fixed-`chi` locus. Object cochains
cannot repair the defect because their loop sum telescopes to zero.

The mathematical child quantifies this transversality. In a standard Euclidean
coordinate norm on log-holonomy space, the squared distance from
`(h+epsilon,h,h)` to the common diagonal in `R^3` is `2 epsilon^2/3`. In the
four-class space `(chi,R,S,D)`, the corresponding squared distance to the
all-equal diagonal is `3 epsilon^2/4`; holding `chi` fixed gives distance
`|epsilon|`. These are bookkeeping norms on the displayed coordinate spaces,
not source-derived geometry or a physical metric. They show that the failure
has zero robustness radius while tending continuously to zero with `epsilon`.

The candidate's own admitted flow supplies a second obstruction. On
`x(t)=x_*+t v`, `mu(t)=1+2t` over the positive interval `-2<t<1`. At
`t=-1/2`, the state is `(3/2,3/2,3/2,3/2)`, `mu=0`, and every `chi`, `a_s`,
and `rho_s` value is one. Formal descent, inverse, composition, balance, and the
non-gauge tangent survive. Nontrivial transition content does not. Moreover,
the nontrivial locus is the disconnected union `(-2,-1/2) union (-1/2,1)`.
Removing the crossing therefore requires a new invariant domain or protection
law rather than a harmless representative choice.

Anchor and presentation controls sharpen the scope. Removing `a_s(0)=1`
restores three positive sector constants and two relative moduli after common
diagonal scale; changing anchors selects representatives only when holonomies
already agree. The bare oriented cycle has a transitive `C_3` vertex action and
selects no point, while an oriented real line has positive-scalar automorphisms
and selects no nonzero frame. Transporting all decorations remains coherent
covariance, but it is weaker than deriving them from the undecorated source.

The philosopher adds a refinement control. If the rule `ell=+1` is reapplied
to every displayed generator after subdividing one edge, the displayed cycle
winding changes from three to four. The fixed RT010 presentation is not thereby
invalidated. Presentation-independent refinement requires `ell` to be retained
and transported as independent source decoration, or a new normalization law
must be supplied.

The covector analysis separates two different nonselection effects. First,
`m_0=(1,-1,1,-1)` is a row of `B` and annihilates `d`, `v`, and `x_*`; it makes
the character trivial on every `B` root. Thus the declared covector type and
acyclic dependency shape do not select a nonzero quotient functional. Second,
for every `r` in `Row(B)`, `m+r` agrees with `m` on all of `ker(B)`. Root-locus
behavior therefore identifies only `[m]` modulo `Row(B)`, not an off-root
representative, sign, normalization, physical unit, or operational scale.

The parent exact model supplies the finite balance family. With rows
`m_0` and `(1-s)r_2+s m`, every `B_s` has rank two, contains `d` in its kernel,
and has a one-dimensional quotient. The exact witness
`w_s=(2-3s,s-2,3s-2,2-s)` obeys `m(w_s)=4(1-s)`. At `s=1`, the quotient remains
nonzero but `m` enters the balance row space and vanishes on the quotient. This
does not contradict local openness near the RT010 coefficients. It proves that
rank, gauge preservation, and quotient dimension alone do not protect character
relevance across a finite source-typed family.

The two children use compatible branch bookkeeping. The handoff names eight
combined families. The parent matrix records nine atomic rows by separating
point/frame nonselection and frozen-route control while keeping inverse and
cocycle together. Every required branch is covered. No conflict-resolution
round is needed.

The conceptual result is a four-level ladder:

1. fixed-equation validity passes;
2. source purity in the disclosed proposal passes;
3. enlarged-family robustness fails candidate-scopingly;
4. physical or empirical interpretation remains wholly unestablished.

A quotient, cohomology class, or isomorphism class records formal distinctions
deleted by a definition. It does not by itself establish that those distinctions
are physically gauge, empirically indistinguishable, or ontologically unreal.

The exact obstruction is
`OB-V22-P4T02-B2-COMMON-CHARACTER-HOLONOMY-PROTECTION-001`. Its failed premise
is that the RT010 balance law, anchors, and pointed framed presentation protect
equal nontrivial sector holonomy under the target-free variations required by
P4-T02. The result is candidate-scoped. It neither reverses source purity nor
refutes the fixed conditional equations, and it proves neither a global no-go
nor future source-extension impossibility.

Both inherited freezes remain active:

- `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR`
- `NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS`

The completed constructor-auditor-refuter cycle meets the new local freeze
condition:

- `NDCL-V22-P4T02-B2-COMMON-CHARACTER-HOLONOMY-PROTECTION`

This third label freezes only replay of the exact common-character protection
route under the tested roots and enlarged variation scope. It does not freeze
all P4-T02 research, reject the ontology, or dispose B2.

Exactly one later packet is selected and remains unexecuted:
`PKT-V22-P4T02-B2-POST-COMMON-CHARACTER-REFUTER-THEORETICAL-CONTINUATION-SELECTION-V1`
under `theoretical-continuation-selector@0.1.0`. It must compare materially
distinct same-milestone routes—a source-side irrelevance theorem, a
non-character bridge-facing candidate, an independently provenanced protection
law, or a protected human-gated stop—without automatically replaying any frozen
branch.

Distance-to-GR is unchanged. D7 and P2-T01 adequacy are not reevaluated; B2
remains inactive and undisposed; P4-T03 remains locked. No full-P7 universality,
empirical response, physical causality, calibration, conformal geometry, scale,
`g_eff`, coupling, Einstein equation, ontology or source-law adoption or
rejection, Gate verdict, benchmark, proof promotion, publication, push,
external action, global theory rejection, future-extension impossibility, or
completed derivation follows.
