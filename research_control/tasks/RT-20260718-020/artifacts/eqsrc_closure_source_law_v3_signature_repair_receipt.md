# EqSrc Closure Source-Law Candidate v3 Signature Repair Receipt

## Result

`RT-20260718-020` produces proposal-only
`EqSrcClosureLaw_src^cand v3`. The result is
`statement_level_signature_and_noncircularity_repair_supplied`.

Version 3 declares the identity, inverse, and composition proxy constructors
with exact endpoint-indexed types; declares ledger, proxy, provenance,
negative-outcome, raw-certificate, and full-certificate congruences; types each
negative-control outcome map on a displayed raw-certificate fiber; and places
acceptance after every component decision in a finite rank-0 through rank-4
dependency order. The candidate remains
`declaration_closed_pending_fresh_audit`, not an audited or adopted source law.

## New Mathematical Payload

The packet supplies:

- an endpoint-indexed formal signature for ledger, proxy, provenance, outcome,
  and certificate objects and maps;
- an acyclic-acceptance proposition showing that the explicit finite Boolean
  conjunction is total and uniquely determined from lower-rank data;
- a declaration-closure proposition proved by induction over the displayed
  ranks; and
- a conditional equivalence theorem preserving the v2 accepted identity,
  inverse, and composition totality core.

This is a `conditional_theorem_candidate` and proposal-level
source-extension object. It repairs
`OBST-EQSRC-CLOSURE-SIGNATURE-001` at statement level but still requires a
fresh independent audit.

## Source-Purity and Authority

The complete no-target/no-process guard applies to every nonlogical carrier,
constructor, congruence, component decision, certificate witness, and
definition-tree leaf. It excludes target geometry, detector or benchmark
success, downstream field equations, generated derivatives, registry
metadata, roles, validators, handoffs, checkpoints, approvals, adoption
status, and acceptance or law satisfaction as lower-rank premises.

The candidate remains `proposal-only` source-extension data with
`blocked_adoption_open_continuation`. Current ontology does not derive or
instantiate it. Registration, validation, and checkpoint evidence establish
transaction integrity only.

## Distance-to-GR and Freeze Result

The effect is `no_distance_delta`; no Distance-to-GR ledger row changes.
General `EqSrc`, RetainH, GenH, `M_src`, `g_eff`, matter coupling, Einstein
equations, finite-variation robustness, benchmark promotion, and Gate Chair
status remain open or blocked.

The repeated EqSrc line is not frozen because v3 adds the exact missing typed
signature and a new acyclic-dependency theorem. Freeze review becomes
appropriate if the next audit again finds undeclared component terms,
acceptance recursion, or a required forbidden target/process premise.

## Next Route

Run one fresh bounded Smuggling Auditor review of the complete v3 declaration
set, rank graph, source-purity guard, component congruences, accepted-totality
clauses, and proofs. Refuter stress follows only if that audit passes. Any
ontology or source-law adoption remains a separate protected human-gated
decision.

## Forbidden Overreads

This receipt does not authorize canonical ontology modification, source-law
adoption, general `EqSrc` discharge, RetainH or GenH adoption, `M_src` or
`g_eff` adoption, matter coupling, Einstein equations, Distance-to-GR ledger
promotion, benchmark promotion, Gate Chair verdict, completed derivation,
program-level no-go, future source-extension impossibility, or global theory
rejection.

## Validation

Task-local validator:

```text
.venv/bin/python research_control/tasks/RT-20260718-020/artifacts/validate_eqsrc_closure_source_law_v3_signature_repair.py --write-report --json
```

Expected status: `PASS`.
