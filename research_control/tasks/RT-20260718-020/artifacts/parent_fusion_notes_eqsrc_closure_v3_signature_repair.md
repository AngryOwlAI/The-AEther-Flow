# Parent Fusion Notes: EqSrc Closure v3 Signature Repair

## Scope

The parent fused two independent child analyses into proposal-only
`EqSrcClosureLaw_src^cand v3`. The scope is exactly the statement-level repair
of `OBST-EQSRC-CLOSURE-SIGNATURE-001`; no ontology adoption, general `EqSrc`,
or downstream physics promotion is included.

## Shared Result

Both children preserved the v2 accepted identity, inverse, and composition
totality core. They agreed that the remaining defect is not the three-line
conditional equivalence proof, but the language in which its hypotheses were
stated: free proxy terms, missing component comparisons, and an unstratified
negative-control dependency.

The fused v3 therefore uses:

- rank-0 endpoint-indexed carriers, primitive leaves, outcome sets, pass sets,
  and equivalences;
- rank-1 total ledger and proxy constructors plus the least provenance algebra
  and congruence;
- rank-2 raw certificate constructors, product-first certificate congruence,
  total component decisions, and negative-control evaluators;
- rank-3 `Accept_src` as a finite Boolean conjunction; and
- rank-4 candidate-law assertions, the induced relation, and conditional
  theorems.

Every semantic dependency points forward through this order. Negative
evaluators respect the already defined certificate congruence; certificate
congruence is not defined from their results. This resolves the most important
potential fusion circularity.

## Parent Resolutions

The mathematical child noted that existential proxy witnesses would avoid
unjustified determinism. The parent retained total proxy functions because v2
already selected distinguished inverse and composite witnesses and the task
requires preserving that interface. V3 makes the cost explicit: those total
functions are proposal-only source-law data, not consequences of current
ontology.

The philosophy child recommended renaming “acceptance” to prevent authority
confusion. The parent retained the established notation for continuity but
defines it explicitly as internal certificate admissibility, never audit PASS,
approval, adoption, or benchmark acceptance.

The child reports used different rank granularities. The fused five-rank
presentation retains all ordering constraints by allowing only visibly earlier
structural declarations within rank 2. No same-rank semantic recursion is
allowed.

## Source-Purity Guard

Syntax alone is insufficient. The fused guard combines declaration closure
with transitive provenance exclusion and semantic factorization: holding all
declared source leaves fixed while varying target or workflow state must leave
every source interpretation, certificate decision, `Accept_src` value, and
candidate-law truth value unchanged. Opaque or aliased data is classified by
its provenance, not its source-looking name.

This is still a proposal condition pending fresh audit. It does not prove that
current ontology instantiates the signature.

## Mathematical Payload

The fused artifact adds:

1. an endpoint-indexed exact typed signature;
2. an acyclic-acceptance totality and uniqueness proposition;
3. a declaration-closure proposition by rank induction; and
4. the preserved conditional equivalence theorem with explicit witnesses.

The result is
`statement_level_signature_and_noncircularity_repair_supplied`, not an audit
pass or milestone discharge.

## Authority, Freeze, and Next Route

The candidate remains `proposal-only`, `draft/control`, source-extension data
with `blocked_adoption_open_continuation`. The Distance-to-GR ledger is
unchanged.

The repeated EqSrc route is not frozen because v3 supplies materially new
typed maps and a dependency theorem. Freeze review becomes appropriate if a
fresh audit again finds undeclared constructors, circular or opaque acceptance
dependencies, or a required target/process premise.

The next lawful role is one fresh bounded Smuggling Auditor review of v3.
Refuter stress follows only after that audit passes. Any adoption remains a
separate protected human-gated decision.
