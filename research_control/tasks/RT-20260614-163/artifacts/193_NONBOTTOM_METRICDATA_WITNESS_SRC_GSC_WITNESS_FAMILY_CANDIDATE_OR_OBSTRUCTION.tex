% authority: science_draft
\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{array}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{enumitem}

\newcommand{\MsrcGSC}{M_{\mathrm{src}}^{\mathsf{GSC}}}
\newcommand{\MetricLaw}{\mathrm{MetricLaw}_{\mathrm{src}}^{\mathsf{GSC}}}
\newcommand{\MetricRespTok}{\mathrm{MetricRespTok}_{\mathrm{src}}^{\mathsf{GSC}}}
\newcommand{\MetricTokenVal}{\mathrm{MetricTokenVal}_{\mathrm{src}}^{\mathsf{GSC}}}
\newcommand{\Witness}{\mathrm{NonBottomMetricDataWitness}_{\mathrm{src}}^{\mathsf{GSC}}}
\newcommand{\MetricData}{\mathcal{L}_{\mathrm{src}}^{\mathsf{GSC}}}
\newcommand{\ValRecord}{\mathrm{ValRecord}_{\mathrm{src}}^{\mathsf{GSC}}}
\newcommand{\NoTarget}{\Pi_{\mathrm{no\mbox{-}target}}^{\mathsf{GSC}}}
\newcommand{\BottomSrc}{\bot_{\mathrm{src}}}
\newcommand{\OBGEFF}{\mathrm{OB\mbox{-}GEFF}}
\newcommand{\geff}{g_{\mathrm{eff}}}
\newcommand{\En}{E_n}
\newcommand{\Diamond}{D_{\diamond}}

\newtheorem{definition}{Definition}
\newtheorem{lemma}{Lemma}
\newtheorem{theorem}{Theorem}
\newtheorem{status}{Status}

\title{Draft/Control Non-Bottom Metric-Data Witness-Family Candidate or Obstruction}
\author{Candidate Constructor Packet RT-20260614-163}
\date{2026-06-26}

\begin{document}
\maketitle

\section{Control Status}

This artifact is the fused Candidate Constructor output for
\texttt{RT-20260614-163}. It consumes \texttt{handoff-0196}, the
\texttt{RT-20260614-162} post-stress selector, and the audited and
stress-survived finite/local \(\Witness(E_0)\) candidate from
\texttt{RT-20260614-159} through \texttt{RT-20260614-161}.

The result is a draft/control finite/local witness-family candidate for the
declared directed chain family \(\En\), \(n\ge 2\). It is not adopted
\(\MetricData(E)\). It is not a theorem for arbitrary finite source packages,
arbitrary finite directed acyclic graphs, cyclic graphs, branching graphs, or
continuum packages. It does not define, construct, or adopt \(\geff\). It does
not derive matter coupling, Einstein equations, benchmark promotion,
benchmark Gate Chair closure, completed derivation, future source-extension
impossibility, or global theory rejection.

\begin{verbatim}
candidate_constructor_result:
  result_type: "constructed_candidate"
  constructed_object: "NonBottomMetricDataWitness_src^{GSC}(E_n)"
  family_scope: "arbitrary finite directed chain family n >= 2"
  metric_data_adopted: false
  arbitrary_source_package_theorem: false
  g_eff_constructed: false
  next_required_role: "smuggling_auditor"
\end{verbatim}

\section{Input Boundary}

The admitted inputs are:
\begin{itemize}[leftmargin=*]
  \item scoped source-only \(\MsrcGSC(E)\);
  \item adopted fail-closed \(\MetricLaw\);
  \item adopted fail-closed \(\MetricRespTok\);
  \item adopted \(\MetricTokenVal\) only as source-extension law data;
  \item the strict \(\Witness(E)\) draft/control schema;
  \item the finite/local \(E_0\) construction, audit, stress, and selector
  artifacts; and
  \item the current no-target-import and fail-closed obstruction discipline.
\end{itemize}

No target topology, target atlas, coordinate transition, target metric,
Lorentzian signature, proper time, detector semantics, benchmark success,
target-GR reconstruction, generated derivative, registry metadata, validator
status, role identity, handoff status, approval status, local cache state,
file order, or commit status is used as a mathematical premise.

\section{Prior \(E_0\) Evidence Consumed}

The previous \(E_0\) candidate supplied a three-site chain
\[
  s_0\to s_1\to s_2
\]
with source response profiles \((2,0),(1,1),(0,2)\). That candidate was
audited for hidden target import and stress-tested for collapse,
nonuniqueness, covariance failure, finite-variation fragility, bottom
incompleteness, finite/local overread, and premature promotion. It survived
only as finite/local draft/control candidate data.

This packet uses \(E_0\) as the \(n=2\) member of the chain-family template.
It does not use \(E_0\) survival as adoption, benchmark evidence, or proof of
arbitrary \(E\) inhabitation.

\section{Claim Boundary and Forbidden Overreads}

The following conclusions are blocked:
\begin{itemize}[leftmargin=*]
  \item the current ontology derives \(\MetricTokenVal\) without
  source-extension law data;
  \item \(\MetricData(E)\) is adopted;
  \item every finite source package inhabits \(\Witness(E)\);
  \item arbitrary finite DAGs, branching packages, cyclic graphs, or
  continuum packages are covered by this theorem;
  \item \(\geff\) is defined, constructed, Lorentzian, unique, empirical, or
  adopted;
  \item matter coupling or Einstein equations are derived;
  \item exact-GR benchmark recovery is proved or promoted;
  \item future source-extension continuation is impossible; and
  \item the global theory is accepted, rejected, completed, or suspended.
\end{itemize}

\section{Declared Family Scope}

The primary family scope is the arbitrary finite directed chain family
\[
  \mathcal{E}_{\mathrm{chain}}
  = \{\En : n\in\mathbb{N},\ n\ge 2\}.
\]

This is a family theorem for finite source chains only. The non-chain diamond
pressure case in Section~\ref{non-chain-pressure} is included to prevent broad
overread from chain success.

\section{Finite Source Chain Family \(E_n\)}

\begin{definition}[Finite directed source chain]
For every finite \(n\ge 2\), define
\[
  \En=(S_n,\rho_n,\mathcal{T}_n,\mathcal{V}_n,B_{\mathrm{src},n})
\]
where
\[
  S_n=\{s_0,s_1,\ldots,s_n\}.
\]
The primitive directed response tokens are
\[
  \rho_{i,i+1}:s_i\to s_{i+1},\qquad 0\le i<n.
\]
The derived source reachability tokens are
\[
  \rho_{ij}:s_i\leadsto s_j,\qquad 0\le i<j\le n,
\]
where \(\rho_{ij}\) records source-chain reachability through the primitive
response tokens. It is not a target-geometric segment, distance, causal cone,
or proper-time interval.
\end{definition}

\begin{lemma}[Domain lemma]
For every finite \(n\ge 2\), \(\En\) is an admitted finite/local source
package under this packet boundary.
\end{lemma}

\begin{proof}
The site set \(S_n\) is finite. The primitive response tokens and derived
reachability tokens are source-side syntactic relations over \(S_n\). The
transition tokens, finite variations, and bottom behavior are declared inside
the same source package below. The construction does not require target
topology, target atlas, target metric, Lorentzian signature, detector
semantics, benchmark fit, process authority, or generated derivatives.
Therefore \(\En\) is admitted as a draft/control finite/local source package.
\end{proof}

\section{Admitted Source Transition Tokens}

The admitted transition tokens are source relabeling isomorphisms between
copies of \(\En\) that preserve:
\begin{itemize}[leftmargin=*]
  \item source-site cardinality;
  \item directed response incidence;
  \item the marked source-chain class;
  \item endpoint orientation when endpoints are marked; and
  \item bottom and variation branch labels.
\end{itemize}

For a marked directed chain, internal automorphisms may be trivial. This is a
source-gauge fact, not a target-covariance claim.

\section{Admitted Finite Variations}

The admitted finite variations are:
\begin{enumerate}[leftmargin=*]
  \item source relabeling of a finite chain package;
  \item chain-preserving single-edge replacement by an equivalent source
  response token; and
  \item fail-closed mutation for chain deletion, incidence breakage,
  response-profile collapse, non-source evidence, target import, process
  authority laundering, or source-domain exit.
\end{enumerate}

Refinement and coarsening are not admitted in this packet. A future packet may
define them source-side, but this construction does not require them.

\section{Candidate Valuation Record}

\begin{definition}[Valuation record on \(\En\)]
For every finite \(n\ge 2\), define the draft/control valuation record
\[
\MetricTokenVal(\En)=
(T_{\Sigma}(\En),T_{\Lambda}(\En),R_C(\En),K_{\tau}(\En),
D_N(\En),V_{\mathrm{ML}}(\En),B_{\mathrm{src}}(\En)).
\]
The fields are:
\[
\begin{aligned}
T_{\Sigma}(\En) &= [s_0\to s_1\to\cdots\to s_n]_{\mathrm{or}},\\
T_{\Lambda}(\En) &= [\mu_n]_{\mathrm{scale}},\qquad
  \mu_n(s_i)=\frac{1}{n+1},\\
R_C(\En) &= \preceq_{\rho},\qquad
  s_i\preceq_{\rho}s_j \Longleftrightarrow i\le j,\\
K_{\tau}(\En) &= \kappa_{\mathrm{chain}},\\
D_N(\En) &= \Delta_{\mathrm{profile},n},\\
V_{\mathrm{ML}}(\En) &= \mathcal{V}_{\mathrm{repair}/\mathrm{bottom},n},\\
B_{\mathrm{src}}(\En) &= \mathcal{B}_{\mathrm{src}}^{\En}.
\end{aligned}
\]
\end{definition}

\begin{longtable}{p{0.16\linewidth}p{0.20\linewidth}p{0.46\linewidth}}
\toprule
Field & Classification & Source-side justification \\
\midrule
\(T_{\Sigma}\) & derived &
Oriented finite directed source-chain token class; not Lorentzian signature. \\
\(T_{\Lambda}\) & derived &
Finite source counting-measure class up to positive source scale gauge; not
proper time, detector normalization, volume form, or metric determinant. \\
\(R_C\) & derived &
Source reachability preorder generated by response tokens; not target causal
cone. \\
\(K_{\tau}\) & derived &
Admitted source relabeling or isomorphism action preserving the chain package;
not target atlas covariance. \\
\(D_N\) & derived &
Source response-profile distinguishability. \\
\(V_{\mathrm{ML}}\) & derived &
Source finite-variation repair-or-bottom rule; not dynamics or action
variation. \\
\(B_{\mathrm{src}}\) & definitional &
Named bottom and obstruction branch table. \\
\bottomrule
\end{longtable}

\section{No-Target and No-Process-Authority Checks}

\begin{lemma}[No-target lemma]
For every valuation field \(F\) in the record above,
\[
  \NoTarget(\En,F)=\mathrm{true}
\]
under the declared packet boundary.
\end{lemma}

\begin{proof}
Each field is computed from \(S_n\), directed source response tokens, source
reachability, source relabeling, source profile counts, the admitted
variation list, and named bottom branches. None of the definitions uses target
topology, target atlas, target transition maps, target metric, Lorentzian
signature, proper time, detector semantics, benchmark behavior, target-GR
reconstruction, generated derivatives, registry rows, validator status, role
identity, handoff status, approval status, local cache state, file order, or
commit status as a mathematical premise.
\end{proof}

\section{Source Transition Covariance Lemma}

\begin{lemma}[Source transition covariance]
If \(\tau:\En\to E'_n\) is an admitted source transition token between copies
of the declared chain family, then
\[
  \MetricTokenVal(\tau\En)=\tau\MetricTokenVal(\En)
\]
as source-gauge classes.
\end{lemma}

\begin{proof}
The oriented chain class is preserved by admissibility. The source counting
measure class is preserved under finite bijection. The reachability preorder
is preserved because directed response incidence and chain order are
preserved. Response-profile counts are carried to the corresponding relabeled
sites. The finite-variation rule is stated over chain-preserving relabeling,
equivalent source response-token replacement, and named bottom behavior, each
of which is transported by \(\tau\). Hence every valuation component is
preserved up to the declared source gauge.
\end{proof}

\section{Source Distinguishability Lemma}

\begin{lemma}[Source distinguishability]
For every finite \(n\ge 2\), \(D_N(\En)\neq\BottomSrc\) for the declared
chain family.
\end{lemma}

\begin{proof}
Omitting reflexive identities, define the response profile of \(s_i\) by
\[
  p_n(s_i)=
  \left(\#\{j:i<j\},\#\{j:j<i\}\right)=(n-i,i).
\]
If \(p_n(s_i)=p_n(s_k)\), then \(n-i=n-k\) and \(i=k\). Therefore
\(p_n(s_i)\ne p_n(s_k)\) whenever \(i\ne k\). The sites of \(\En\) are
distinguished by source reachability profiles. This is not Lorentzian
nondegeneracy; it is only the source non-bottom condition declared by this
candidate.
\end{proof}

\section{Finite-Variation Repair-or-Bottom Lemma}

\begin{lemma}[Finite-variation behavior]
For every admitted variation \(v\in\mathcal{V}_n\), the valuation record is
preserved up to source gauge, repaired inside the source package, or returned
to a named bottom branch.
\end{lemma}

\begin{proof}
If \(v\) is source relabeling, transition covariance preserves the valuation
record up to source gauge. If \(v\) replaces one chain edge by an equivalent
source response token and the chain class persists, the reachability preorder
and response profiles are recomputed inside the same source package, so the
record repairs source-side. If \(v\) deletes a chain edge, breaks incidence,
collapses response profiles, introduces non-source evidence, imports target
structure, launders process authority, or exits the source domain, the
valuation is not repaired by target geometry or status metadata; it returns
the corresponding branch in \(B_{\mathrm{src}}\).
\end{proof}

\section{Source-Gauge Uniqueness Lemma}

\begin{lemma}[Source-gauge uniqueness]
Within the declared chain family, any two valuation records satisfying the
domain, field definitions, transition rule, distinguishability condition, and
finite-variation rule are equal up to admitted source-gauge relabeling and
positive source-scale rescaling.
\end{lemma}

\begin{proof}
The oriented chain fixes \(T_{\Sigma}\). The cardinality-normalized finite
counting-measure class fixes \(T_{\Lambda}\) up to positive source scale. The
chain response relation fixes \(R_C\). The admitted transition-token action is
the chain-preserving source relabeling action. The response profile formula
fixes \(D_N\). The finite-variation rule has only the declared preserve,
repair, and bottom branches. No additional valuation choice remains unless a
new source-side equivalence or discriminator is separately defined in a later
packet.
\end{proof}

\section{Bottom and Obstruction Branch Completeness}

\begin{longtable}{p{0.34\linewidth}p{0.50\linewidth}}
\toprule
Failure case & Returned branch \\
\midrule
Missing source package &
\(\OBGEFF\)-NONBOTTOM-METRICDATA-DOMAIN-MISSING \\
Malformed source package &
\(\OBGEFF\)-NONBOTTOM-METRICDATA-DOMAIN-FAILURE \\
Missing adopted \(\MetricTokenVal\) source-extension law data &
\(\OBGEFF\)-TOKEN-VALUATION-MISSING \\
Missing valuation field &
\(\OBGEFF\)-NONBOTTOM-METRICDATA-TOKEN-VALUATION-MISSING \\
Target topology, atlas, metric, signature, proper time, detector semantics,
benchmark premise, or target-GR reconstruction &
\(\OBGEFF\)-NONBOTTOM-METRICDATA-TARGET-IMPORT \\
Registry, validator, role, handoff, approval, cache, file order, generated
derivative, or commit status used as evidence &
\(\OBGEFF\)-NONBOTTOM-METRICDATA-PROCESS-AUTHORITY-LAUNDERING \\
Transition covariance failure &
\(\OBGEFF\)-NONBOTTOM-METRICDATA-COVARIANCE-FAILURE \\
Response-profile collapse &
\(\OBGEFF\)-NONBOTTOM-METRICDATA-COLLAPSE \\
Finite variation breaks family without repair &
\(\OBGEFF\)-NONBOTTOM-METRICDATA-FINITE-VARIATION-FRAGILE \\
Nonunique valuation records beyond source gauge &
\(\OBGEFF\)-NONBOTTOM-METRICDATA-NONUNIQUE \\
Premature promotion to \(\MetricData(E)\), \(\geff\), matter coupling,
Einstein equations, benchmark, completed derivation, or theory status &
invalid-under-claim-boundary or \(\OBGEFF\)-PREMATURE-PROMOTION \\
\bottomrule
\end{longtable}

\section{Non-Chain Pressure Case}\label{non-chain-pressure}

\begin{definition}[Diamond pressure package]
Let \(\Diamond\) be the finite source package with directed edges
\[
  s_0\to s_1,\quad s_0\to s_2,\quad s_1\to s_3,\quad s_2\to s_3.
\]
Use the same reachability-profile test with identities omitted.
\end{definition}

\begin{lemma}[Diamond collapse pressure]
The same simple response-profile distinguishability schema used for the chain
family does not construct a non-bottom witness for \(\Diamond\).
\end{lemma}

\begin{proof}
The diamond profiles are
\[
  p(s_0)=(3,0),\qquad p(s_1)=(1,1),\qquad
  p(s_2)=(1,1),\qquad p(s_3)=(0,3).
\]
The middle sites \(s_1\) and \(s_2\) have identical profiles. Therefore the
simple \(D_N\) field collapses on \(\Diamond\). This is a minimal pressure
case against arbitrary finite-DAG overread. It does not defeat the chain
family theorem because \(\Diamond\notin\mathcal{E}_{\mathrm{chain}}\).
\end{proof}

\section{Decisive Candidate Constructor Result}

\begin{theorem}[Finite directed chain-family candidate]
For every finite \(n\ge 2\), the declared chain package \(\En\) has a
draft/control source-side candidate
\[
  W(\En)=
  (\En,\MsrcGSC(\En),\MetricLaw,\MetricRespTok,\MetricTokenVal(\En),
  \ValRecord(\En),\NoTarget,B_{\mathrm{src}}(\En))
\]
for \(\Witness(\En)\), under the stated source-extension and no-target
boundary.
\end{theorem}

\begin{proof}
The domain lemma admits \(\En\) as a finite/local source package. The
valuation definition supplies all required fields. The no-target lemma
excludes target and process-authority premises. Transition covariance holds
for admitted source transition tokens. Response-profile distinguishability
holds for all sites in every finite chain member. Finite variations preserve,
repair, or bottom out. Source-gauge uniqueness removes independent valuation
choice inside the declared gauge. The bottom table names every declared
failure branch. Hence the Candidate Constructor result is a constructed
draft/control candidate for the declared finite directed chain family only.
\end{proof}

\begin{status}[No-fog result]
The decisive result is \(\mathrm{constructed\_candidate}\). The construction
is the chain-family candidate above. The diamond pressure case prevents
arbitrary finite-DAG overread. No `MetricData(E)` adoption, `g_eff`
construction, or downstream GR promotion occurs.
\end{status}

\section{Distance-to-GR Status}

\begin{longtable}{p{0.30\linewidth}p{0.56\linewidth}}
\toprule
Burden & Status after this packet \\
\midrule
Source ontology primitives & Unchanged; no canonical ontology TeX edit is made. \\
Source equivalence EqSrc & Unchanged; no general EqSrc theorem is supplied. \\
RetainH & Unchanged. \\
GenH & Unchanged. \\
ObsLoc\_lc & Unchanged. \\
Resp\_lc & Prior accepted source-extension context only. \\
\(M_{\mathrm{src}}\) & Accepted only within the scoped source-only
\(M_{\mathrm{src}}^{\mathsf{GSC}}(E)\) boundary. \\
\(\MetricTokenVal\) & Adopted only as source-extension law data. \\
\(\Witness(\En)\) & Arbitrary finite directed chain-family candidate
constructed as draft/control only, pending family-level audit and stress. \\
\(\Witness(E)\) for arbitrary finite packages & Not proved; diamond pressure
shows the simple schema collapses outside the chain family. \\
\(\MetricData(E)\) & Not adopted; general inhabitation is not proved. \\
\(\geff\) & Not constructed and not adopted. \\
Matter coupling & Blocked; no coupling law is derived. \\
Einstein equations & Blocked; no field-equation derivation is supplied. \\
Finite-variation robustness & Repair-or-bottom behavior defined only for
declared chain-family variations. \\
Benchmark promotion & Blocked; no benchmark evidence or promotion is supplied. \\
Gate Chair status & No new Gate Chair verdict; any adoption question remains protected. \\
Current route freeze or hard-fail status & Not newly frozen; family-level audit is required next. \\
\bottomrule
\end{longtable}

\section{Next Route}

The next logical packet is one bounded Smuggling Auditor family-level audit of
this constructed chain-family candidate. The audit must check hidden target
import, process-authority laundering, finite-family overread,
candidate-reconstruction circularity, diamond/non-chain overread, and
premature promotion. The audit pass, if obtained, would still not adopt
\(\MetricData(E)\) or construct \(\geff\).

\section{References}

Candidate Constructor Packet RT-20260614-159. (2026). \textit{Draft/control
non-bottom metric-data witness candidate or obstruction} [Research-control
TeX artifact]. The AEther-Flow Interpretation of Relativity Research Project.

Smuggling Auditor Packet RT-20260614-160. (2026). \textit{Smuggling audit of
the draft/control non-bottom metric-data witness candidate} [Research-control
TeX artifact]. The AEther-Flow Interpretation of Relativity Research Project.

Refuter Packet RT-20260614-161. (2026). \textit{Refuter stress test of the
finite/local non-bottom metric-data witness candidate} [Research-control TeX
artifact]. The AEther-Flow Interpretation of Relativity Research Project.

The AEther-Flow Research Project. (2026, June 25). \textit{Handoff 0196}
[Internal research-control handoff].

\end{document}
