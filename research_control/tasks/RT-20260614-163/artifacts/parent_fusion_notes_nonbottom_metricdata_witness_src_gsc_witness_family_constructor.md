# Parent Fusion Notes

Task `RT-20260614-163` fuses the Physicist-Mathematician and
Physicist-Philosopher child outputs for one Candidate Constructor packet.

The mathematical child supplies the finite directed chain-family construction:
`E_n` with source sites `s_0,...,s_n`, directed response tokens, source
reachability, source-profile distinguishability `p_n(s_i)=(n-i,i)`, admitted
chain-preserving transition tokens, and repair-or-bottom finite variation.

The philosophical child supplies the authority separation: the candidate is
draft/control chain-family evidence only. It is not `MetricData(E)` adoption,
not arbitrary finite-DAG inhabitation, not a `g_eff` construction, and not
downstream GR recovery.

The fused output keeps both contributions. It returns exactly one decisive
Candidate Constructor result:

```yaml
candidate_constructor_result:
  result_type: "constructed_candidate"
```

The diamond pressure case is included because it is the simplest check against
chain-family overread. The same profile schema collapses the diamond middle
sites, so the construction does not claim arbitrary finite-source or
finite-DAG witness inhabitation.
