<!-- authority: control -->

# Parent Fusion Notes: GSC Source-Manifold Candidate Smuggling Audit

The parent fused both child outputs into a single Smuggling Auditor artifact.
The Physicist-Mathematician child supplied the formal dependency audit:
`M_src^GSC-cand` uses non-bottom `GSC_src` output, `Q_src^GSC`, source chart
candidates, selected source transition tokens, inverse and cocycle guards,
`Var_GSC`, and `Stab_GSC`.

The Physicist-Philosopher child supplied the authority separation. The audit
pass is conditional source-purity as written, not adopted `M_src`, not full
`M_src` construction, not `g_eff`, not matter coupling, not Einstein
equations, and not benchmark promotion.

No blocking conflict was found. The missing primitive remains a
source-manifold regularity and soldering law. The next required role is
Refuter.
