---
authority: science_draft
status: draft_control
task_id: RT-20260725-012
job_id: AJ-RT-20260725-012-001
plan_task_id: P5-T05
---

# P5-T05 parent fusion notes

## Fused background and mode result

The two internal perspectives analyze the exact unchanged P5-T03
cubic-amplitude dynamics and preserve every P5-T04 structural finding.

For `gamma>0`, `q_0` is the unique stationary background. The positive,
zero, and negative amplitude strata are forward invariant. Every nonzero
trajectory retains its sign and converges to `q_0`; reflection swaps the two
nonzero strata and fixes the zero state.

Along an exact trajectory background,

`eta' = -3 gamma a_b(lambda)^2 eta`,

with

`eta(lambda) = eta_0/(1+2 gamma a_b0^2 lambda)^(3/2)`.

Thus there is one strictly decaying admitted source-tangent mode along each
nonzero trajectory and one neutral linear mode at `q_0`. The zero background
is nevertheless globally Lyapunov stable and globally asymptotically
attractive under the nonlinear flow. Its decay is polynomial, not
exponential.

## Exact counting boundary

The current candidate has one admitted source-tangent dimension. Its declared
identity-only redundancy gives zero gauge modes. A physical mode count is not
available, Hamiltonian constrained-mode counts are not applicable, and
off-family modes are undefined because no off-family dynamics is supplied.

The positive and negative strata are transient sign sectors, not stationary
ordered phases. Neither is dynamically preferred, and the unique fixed point
preserves reflection. Forward source order is not physical time orientation,
causality, or entropy production.

## Precise reconstruction-phase obstruction

`OBST-P5T05-QAMP-NO-RECONSTRUCTION-PHASE-001` records the decisive result.
Inside the exact candidate there is no stationary nonzero phase, the sole
stationary background has a degenerate zero linearized generator, and the
only nonzero-background perturbation is a single global decaying amplitude
mode. Combined with the P5-T04 patch-amplitude obstruction, the family
contains no independent local perturbations, momentum-indexed spectrum,
local characteristic operator, propagating sector, or source-defined
resolution hierarchy.

This blocks a nontrivial local reconstruction-suitable phase only for the
exact `Q_amp` candidate. It does not rule out multi-mode, locally patchable,
coupled, stochastic, or other conservative source extensions, and it is not
a global theory rejection.

## Parameter controls

`gamma=0` produces a degenerate identity action with a continuum of neutral
fixed states and fails P5-T03 nontriviality. `gamma<0` produces finite
positive-parameter blowup for every nonzero initial amplitude and fails
global forward totality. The admitted `gamma>0` branch is nonexpanding in its
one tangent direction and attracts every trajectory to zero.

## Remaining burden and next route

P5-T05 is complete because its backgrounds, exact linearization, source-mode
classification, stability, symmetry behavior, parameter controls, and
reconstruction-phase disposition are explicit. P5-T06 may next attempt a
typed source-only coarse-graining or reconstruction map with declared domain,
codomain, scale, approximation, information loss, and error. It must preserve
the obstruction and must not fabricate local modes or select a map from a
desired target metric.

Canonical ontology or source-law adoption, physical background, phase, mode,
time, `M_src`, `g_eff`, matter coupling, Einstein equations, exact-GR
recovery, benchmark promotion, Gate Chair closure, proof, publication, push,
completed derivation, global theory rejection, and future source-extension
impossibility remain blocked.
