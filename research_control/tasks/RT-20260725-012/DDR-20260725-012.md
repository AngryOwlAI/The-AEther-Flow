---
authority: control
status: completed
decision_id: DDR-20260725-012
task_id: RT-20260725-012
agent_job_id: AJ-RT-20260725-012-001
director_version: director-of-research@0.3.0
decision_type: task_overlay
selected_role_id: candidate-constructor
selected_role_version: 0.2.0
requires_human_gate: false
---

# DDR-20260725-012: execute only v21 P5-T05

## Decision

Activate one `candidate-constructor@0.2.0` task overlay for v21 work item
P5-T05. Commit `dd7197d40794b529f06d805840d5e228ee0792c4` is the successful
governed cumulative checkpoint required by `handoff-0871`, and immutable relay
generation 117 selects the same work item under `continue-research`.

The task uses the plan's `candidate-construction-packet` route. It does not
alter the exact P5-T03 cubic-amplitude dynamics or erase the P5-T04 structural
obstruction. Its bounded construction target is a complete source-defined
background, phase, linearization, mode, stability, symmetry, and parameter
catalog, ending in either a reconstruction-suitable phase or one precise
candidate-specific obstruction.

## Evidence and route resolution

The exact source boundary comes from the registered P5-T03 dynamics candidate,
the P5-T04 structural analysis, the P5-T02 forward-action semantics, the P5-T01
compact source-theory object, the v21 plan, the continuum-first P4-T06
integration, the canonical ontology manuscripts, the burden map, and the
Distance-to-GR ledger. `research_control/support_formalization/` was inspected
as absent and supplies no premise or authority.

Memory preflight refreshed retrieval-only local state and then passed with
1,015 canonical source objects and 5,035 relationship rows. A parser-unsafe
query containing `P5-T05` failed, and a handoff-path lookup returned no
canonical object; neither influenced the result. Direct lookup navigated to
the registered P5-T04 source, which was inspected directly with its registry
row. The remaining required sources were inspected directly.

## Role-Fit Matrix

| Candidate role | Fit | Reason |
|---|---|---|
| `candidate-constructor@0.2.0` | selected | P5-T05 requires explicit background construction, exact perturbative equations, mode and stability classification, and a decisive viable-or-obstructed phase result. |
| `ontology-formalizer@0.2.0` | next only | P5-T06 will require a typed coarse-graining or reconstruction map after this phase datum is checkpointed. |
| `refuter@0.1.0` | internal adverse-control perspective only | Parameter and interpretation controls are needed without opening a second AgentJob. |
| `theoretical-continuation-selector@0.1.0` | not selected | The plan, handoff, and immutable route already select P5-T05 and its role. |

## Selected construction

Define a stationary background as a root of the exact source generator. This
gives only `q_0`. Preserve the positive, zero, and negative amplitude strata
as forward-invariant source sectors; treat the nonzero sectors as transient
trajectory families rather than stationary physical phases.

Linearize along the exact background trajectory:

`eta'=-3 gamma a_b(lambda)^2 eta`,

with

`eta(lambda)=eta_0(1+2 gamma a_b0^2 lambda)^(-3/2)`.

The one admitted source-tangent mode decays along nonzero trajectories and is
neutral at the zero background. The nonlinear flow nevertheless makes the
zero background globally Lyapunov stable and globally asymptotically
attractive, with polynomial rather than exponential decay.

Count zero gauge modes from identity-only declared redundancy. Mark physical
mode classification unavailable, Hamiltonian constrained-mode counts not
applicable, and off-family modes undefined because the candidate supplies no
off-family dynamics.

## Decisive result

The packet returns `precise_obstruction`, not an open-ended research note.
`OBST-P5T05-QAMP-NO-RECONSTRUCTION-PHASE-001` records that the exact candidate
has no stationary nonzero phase, its sole stationary background has zero
linearized generator, and it supplies only one global decaying amplitude mode
off zero. Together with P5-T04 patch-amplitude rigidity, this blocks a
nontrivial local reconstruction-suitable phase inside `Q_amp`.

The obstruction is scoped to the current candidate. It does not forbid a
multi-mode, locally patchable, coupled, stochastic, or other conservative
source extension and does not reject the wider project.

## Parent-child synthesis

The single outer AgentJob uses `parent_child_parallel_synthesis`. The internal
physicist-mathematician perspective derives the background, linearization,
nonlinear stability, parameter controls, and precise obstruction. The internal
physicist-philosopher perspective audits background, phase, mode, time,
symmetry-breaking, reconstruction, adoption, and downstream-GR overreads. The
parent resolves six tensions and retains no unresolved conflict.

## Claim boundary

The result may be a task-local theorem package, exact source-mode and stability
catalog, scoped reconstruction-phase obstruction, and scoped Distance-to-GR
source-ontology evidence delta. It is not canonical ontology, an adopted or
rejected source law, physical vacuum, physical phase, physical mode count,
physical time, physical gauge or constraint structure, `M_src`, `g_eff`,
matter coupling, Einstein-equation derivation, benchmark promotion, proof
publication, push, completed derivation, global theory rejection, or future
source-extension impossibility.

## Freeze evaluation

The candidate family is not frozen by P5-T05. This packet adds first-time
background, exact linearization, nonlinear stability, symmetry, and parameter
results. It can define a complete admitted perturbative sector, and P5-T06 is
a distinct typed-map obligation rather than a repetition of the P5-T04 or
P5-T05 obstruction. Any later packet that merely renames this global mode or
fabricates local degrees without a source extension must fail closed.

## Validation

The task-local validator checks candidate identity, the complete fixed-point
catalog, invariant strata, exact linearization, numerical derivative controls,
mode-count boundaries, nonlinear and polynomial stability, symmetry and
orientation, coefficient branches, the precise obstruction, parent-child
synthesis, and claim boundaries. Repository validation, documentation-impact
accounting, claim-language validation, memory regeneration, TeX scratch
compilation, and one governed checkpoint remain required.

## Completion disposition

The registered source and companion catalogs pass all 23 task-local checks.
P5-T05 is complete at `draft/control`, `proposal-only`, `source-extension
data` scope with a precise candidate-specific obstruction and is ready for one
governed checkpoint. P5-T06 remains unexecuted until that checkpoint commits.
