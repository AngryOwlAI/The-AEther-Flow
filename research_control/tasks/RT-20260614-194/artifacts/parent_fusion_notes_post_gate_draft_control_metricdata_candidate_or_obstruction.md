# Parent Fusion Notes: Draft/Control MetricData Candidate

## Analysis

Both child perspectives converge on construction rather than obstruction. The
accepted `PreMetricData_src^{GSC-cand}(G^beta)` evidence supplies a declared
source package sufficient to define a source-only draft/control
`MetricData(E)` candidate with fail-closed boundaries.

## Fusion Decision

Construct:

```text
MetricData_src^{GSC-cand}(E;G^beta)
```

This construction is draft/control only. It is not adoption and not `g_eff`.

## Next Route

Route one bounded Smuggling Auditor packet for source-purity and laundering
audit before any stress test, selector, adoption question, or downstream route.
