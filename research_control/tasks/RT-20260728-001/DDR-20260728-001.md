---
authority: control
status: completed
decision_id: DDR-20260728-001
task_id: RT-20260728-001
agent_job_id: AJ-RT-20260728-001-001
director_version: director-of-research@0.3.0
decision_type: task_overlay
selected_role_id: candidate-constructor
selected_role_version: 0.2.0
supersedes_decision_id: DDR-20260727-008
requires_human_gate: false
role_fit_candidates:
  - candidate-constructor@0.2.0
  - ontology-formalizer@0.2.0
  - theoretical-continuation-selector@0.1.0
---

# DDR-20260728-001: construct the P7-T02 finite transition kernel

## Decision

Activate one `candidate-constructor@0.2.0` task overlay and exactly one outer
AgentJob for the selected P7-T02 source-transition-law packet. The packet must
construct or precisely obstruct one proposal-only
`SourceMatterFiniteTransitionKernelCandidate_v1` over the fixed
`SourceMatterIncidenceCandidate_v1` transition grammar.

Generation 142 was validated, atomically claimed, and consumed exactly once
under immutable route SHA-256
`405ce28987b5e4cb87842bcfd713f90f388427f8bc2cce275ab8548e9aaaec01`.
The selector checkpoint is commit
`4da35201d2c701eaea7814fd0401584206a1d0d2`; the worktree was clean at
entry.

## Evidence and construction question

P7-T01 supplies finite configurations
\(\Psi=(m,z)\), the sector coordinates \((Q,[z])\), and typed admissible
transition records \((\Psi^-,J,w,\Pi,\Psi^+)\). It supplies no selector,
weights, composition law, physical time, or dynamics. The completed P7-T02
selector compares action, evolution-equation, and transition-law branches and
selects the transition-law branch because it reuses the exact finite grammar
with the smallest explicit assumption delta.

The construction question is therefore whether a finite, source-only,
presentation-equivariant row-normalized kernel can be defined without target
geometry or empirical calibration. Uniform positive weights on a finite
source-declared enabled-transition row are a lawful proposal-only candidate:
they depend only on row membership, are preserved by row bijections, and
separate a new normalization consequence from the inherited P7-T01 support
constraints.

## New mathematical payload

The packet may declare a finite control system \((\Omega,\mathcal T)\) whose
rows contain typed P7-T01 transitions and one exact identity transition per
configuration. It may define
\[
  K_\Omega(x,T)=|\mathcal T_x|^{-1}
\]
on enabled rows and zero otherwise, and the source-bookkeeping update
\[
  (U_\Omega\mu)(y)=
  \sum_{x\in\Omega}\mu(x)
  \sum_{\substack{T\in\mathcal T_x\\t(T)=y}}K_\Omega(x,T).
\]
The packet must derive row normalization, preservation of total bookkeeping
mass, finite update closure, inherited \(Q\) and defect-class sector support,
and equivariance under compatible source-presentation isomorphisms.

These are exact statements inside one stipulated proposal-only finite
candidate. The coefficients are not physical probabilities, rates,
frequencies, or propensities; the update counter is not physical time; and
sector support is inherited from imposed P7-T01 kinematics rather than a
Noether or physical conservation law.

## Role-Fit Matrix

| Candidate role | Fit | Reason |
|---|---|---|
| `candidate-constructor@0.2.0` | selected | Owns one bounded proposal-only source-law witness or exact scoped obstruction under the ontology-law route. |
| `ontology-formalizer@0.2.0` | not selected | P7-T01 already fixed the typed configuration and transition grammar; this packet adds a candidate transition rule rather than redefining the ontology target. |
| `theoretical-continuation-selector@0.1.0` | not selected | The prior selector already chose this exact branch and candidate identity. |
| Gate Chair roles | blocked | No candidacy promotion, ontology adoption, source-law adoption, benchmark verdict, or protected decision is requested. |

## Parent-child synthesis

The single AgentJob uses two internal child perspectives:

- a physicist-mathematician checks typed rows, normalization, closure,
  equivariance, factorization, and finite controls;
- a physicist-philosopher checks model-to-world language, identity and
  intervention boundaries, and all probability, time, conservation, geometry,
  detector, coupling, and GR overreads.

The parent must resolve any conflict before writing the fused candidate. The
children create no independent AgentJobs or role authority.

## Claim boundary

The candidate remains `draft/control` and `proposal-only`; current ontology
does not derive it. Adoption is
`blocked_adoption_open_continuation`. No target atlas, metric, cone, proper
time, detector response, stress-energy object, matter action, coupling law,
Einstein equation, or desired exact-GR behavior may be used as an input.
Validation and checkpoint success are operational evidence only.

## Next route

The candidate passes 80 of 80 task-local checks, the six-page scratch TeX
build resolves its references without overfull boxes, and the parent fusion
has zero unresolved conflicts. P7-T02 is complete at proposal-only
construction scope.

After the governed checkpoint commits, P7-T03 becomes dependency ready. The
next packet must still be freshly routed, must construct materially new
source-level operational devices and protocols, and may not treat this
candidate as adopted or physically interpreted.
