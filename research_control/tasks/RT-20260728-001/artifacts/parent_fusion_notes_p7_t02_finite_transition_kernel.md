<!-- authority: science_draft -->

# P7-T02 finite transition-kernel parent fusion notes

## Shared consensus

Both child reviews accept one exact candidate-relative construction. A finite
set of P7-T01 configurations, finite enabled rows of P7-T01 transitions, and
exactly one typed identity record per row support a uniform nonnegative
rational kernel. Inside that stipulated finite control, row normalization,
finite update closure, total bookkeeping-mass preservation, transported
source-presentation equivariance, Cartesian product factorization, and one
nonfactorizing joint control are exact.

Both children also agree that current ontology does not derive the finite
control, row membership, identity convention, uniform coefficient rule, or
formal composition index. The result is `draft/control`, `proposal-only`, and
`blocked_adoption_open_continuation`.

## Physicist-mathematician contribution

The mathematical review supplies the exact row and record typing, uniform
kernel and endpoint update, finite rational closure, total and
sector-resolved bookkeeping-mass calculations, row-bijective presentation
equivariance, Cartesian factorization, and a synchronized two-component
nonfactorizing witness. It also preserves the crucial provenance split:
pathwise equality of \(Q\) and \(D\) is inherited from P7-T01, whereas
sector-block update closure is new.

The parent adopted the child's disjoint-component synchronized witness. Each
component projects to an identity and a move, so their Cartesian product has
four records; the declared joint row has only identity and the synchronized
double move. This is an exact nonfactorization result with explicitly
stipulated joint enablement.

## Physicist-philosopher contribution

The semantic review supplies the four-level model-to-world ladder and the
strongest language guards. A typed identity is a totality convention, not
physical persistence or waiting. Uniformity is lawful proposal data, not
uniqueness, indifference, entropy, empirical chance, rate, or frequency. The
composition index is an algebraic count, not time. Nonfactorization is a
candidate-relative row property, not force, coupling, locality, or
entanglement.

The review also forces nullity, non-null identity-only degeneracy,
malformedness, and underdetermination to remain distinct. It preserves the
open interpretation and adoption layers even when the candidate-relative
mathematics is exact.

## Resolved differences

The parent resolved twelve child-level calibration points in one review
round. Uniform weights are retained but declared nonunique proposal inputs.
Every row is a set of extensionally distinct records with exactly one
identity. Equivariance compares a control with its transported control; a
fixed-system commutation statement requires setwise preservation. The
interacting control uses explicit synchronized joint enablement rather than
relying on an arbitrary truncation.

The word “conservation” is restricted carefully. Total bookkeeping mass is an
exact normalization identity. \(Q\) and \(D\) support is inherited from the
P7-T01 chain constraints. Neither result is a Noether theorem or physical
conservation law. Representation grade \(M\), occupation \(N\), energy,
momentum, and stress energy are not asserted to be preserved.

## Unresolved limitations

No canonical or source-derived rule selects the finite control, enabled rows,
uniform weights, cutoff, or composition convention. No uniqueness,
projective-limit, continuum, ergodicity, recurrence, reversibility,
long-index stability, action, generator, principal symbol, characteristic
cone, energy, stress tensor, detector, or coupling theorem is supplied.

The synchronized nonfactorizing row is a positive source-control witness by
stipulation. It does not explain why a physical interaction exists or why
nature would realize that row. The analytic two-state solution solves only
the finite bookkeeping recurrence and is not a particle trajectory or
physical propagation.

## Claim boundary

The fused no-fog result is `constructed_candidate`:
`SourceMatterFiniteTransitionKernelCandidate_v1` exists as one explicit
proposal-only finite transition law with exact candidate-relative
normalization, closure, sector support, equivariance, and controls.

It is not a physical probability, rate, frequency, or propensity; not
physical time, duration, or causal order; not a Noether or physical
conservation law; not detector or empirical semantics; not target geometry,
stress energy, a matter action, or coupling; and not Einstein-equation or
exact-GR recovery. No canonical ontology, source law, matter ontology,
benchmark, proof, publication, push, completed derivation, global no-go, or
future-impossibility authority is created.
