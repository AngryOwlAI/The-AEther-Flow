# Parent Fusion Notes: EqSrc Orientation-Torsor Descent Law

## Scope

The parent fused the physicist–mathematician and physicist–philosopher
perspectives into proposal-only
`EqSrcOrientationTorsorDescentLaw_src^cand,v1`. The packet answers the exact
handoff-0763 formalization burden. It does not repair the frozen scalar
candidate, derive or adopt an unordered bipartition, edit canonical ontology,
establish a physical gauge category, or discharge general EqSrc.

## Mathematical Result

An admitted record supplies a finite set \(X\), an independently sourced
unordered bipartition \(\Pi=\{P,Q\}\) with both blocks odd, and pre-relation
morphism and variation declarations. Its two orderings form a free
\(C_2\)-torsor \(O_U\). On
\[
A_U=\operatorname{span}_{\mathbb F_2}
\{\mathbf1_P,\mathbf1_Q\},
\]
the two orientations select the complementary lines
\[
B_{U,(P,Q)}=\langle\mathbf1_Q\rangle,\qquad
B_{U,(Q,P)}=\langle\mathbf1_P\rangle.
\]
The complement involution exchanges the indicators, lines, and kernel-pair
relations.

The fused candidate retains both fibres. It forms the associated vector space
\[
\bar A_U=(O_U\times A_U)/C_2
\]
and an associated line \(\bar B_U\) and relation \(\bar R_U\). Choosing an
orientation gives a trivialization \(\tau_o:A_U\to\bar A_U\), and
\(\tau_o^*\bar R_U=R_{U,o}\). Changing orientation replaces \(\tau_o\) by
\(\tau_oc_U\). Thus bundle-valued descent is exact, while a single relation on
the original \(A_U\) under identity comparison is impossible because the two
boundary lines are unequal.

## Independent Variation Control

The orientation-dependent translations of exact candidate v1 are not reused
as independent variation evidence. Before any line or relation is defined,
the unordered record determines the orientation-blind constant vector
\(\mathbf1_X=\mathbf1_P+\mathbf1_Q\) and the affine control
\[
T_U(\epsilon)(a)=a+\epsilon\mathbf1_X.
\]
It commutes with complement transport and preserves both relations, although
it may exchange quotient labels. This is a nontrivial source-side algebraic
control, not an established physical variation class. Any richer
\(\Xi_{\mathrm{src}}\) variation family must be independently acquired before
the relations and then separately certified.

## Theorems, Witness, and Qualifications

The candidate proves ordering independence, inverse and cocycle laws,
equivariance, functoriality for independently admitted unordered-partition
bijections, exact fibre pullback, strict scalar non-descent, and the
pointed-torsor repair equivalence.

The no-natural-section theorem is deliberately scoped. It applies to any
admitted source category containing a block-exchange automorphism, including
the symmetric two-point record. It is not a claim that every decorated or
restricted category lacks a natural section. Unequal sizes, parity, or extra
source data may point the torsor; that changes the route and may recover the
frozen candidate-v1 orientation.

The two-point witness exhausts the reduced \(\mathbb F_2^2\) algebra. The two
fibre relations each have two two-element classes. Their meet is equality and
their generated join is universal. The complement-invariant diagonal line
gives a distinct nondegenerate scalar law, not descent of either fibre. Without
source-derived bipartition data, full \(\mathrm{GL}(2,2)\) acts transitively on
all three projective lines, so the lawful branch retains the three-line orbit
or fails closed.

## Authority and Next Route

Current ontology does not derive the unordered bipartition, the
\(\mathbb F_2\) block-span model, both-odd domain, physical morphisms, physical
variations, or downstream observable semantics. Braces and `Xi_src`
declarations are not provenance proofs. The result remains `proposal-only`
source-extension data with `blocked_adoption_open_continuation`.

The next lawful step is one fresh bounded Smuggling Auditor audit of this
exact bundle-law candidate. It must inspect provenance-as-declaration,
identity versus complement descent, associated-state substitution, hidden
sections, parity stability, reduced \(C_2\) versus full
\(\mathrm{GL}(2,2)\), scalarization scope, and variation independence. It may
not repair, adopt, or promote the candidate.
