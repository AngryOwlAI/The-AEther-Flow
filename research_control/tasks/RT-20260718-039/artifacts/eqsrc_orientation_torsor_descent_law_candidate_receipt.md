# EqSrc Orientation-Torsor Descent Law Candidate Receipt

## Result

`RT-20260718-039` formalizes proposal-only
`EqSrcOrientationTorsorDescentLaw_src^cand,v1`. The result is
`candidate_formalized_pending_fresh_smuggling_audit`.

For an admitted finite source record with an independently sourced unordered
bipartition \(\{P,Q\}\), both blocks odd, the two orderings form an unpointed
\(C_2\)-torsor. The proposal retains the two complementary boundary-line
relations and descends them to an associated state object. It does not choose
one orientation or produce one orientation-independent relation on the
original common state space.

## Decisive Mathematical Payload

The packet proves:

- ordering independence, inverse and cocycle laws for complement transport;
- equivariance and functoriality in the admitted unordered-partition
  groupoid;
- exact pullback of the associated relation to either oriented fibre;
- strict scalar non-descent under identity identification on every
  nondegenerate two-block record;
- a scoped no-natural-section theorem when a block-exchange automorphism is
  admitted;
- equivalence between a torsor pointing and selection of one frozen
  candidate-v1 fibre; and
- robustness under an orientation-blind constant-one affine control defined
  before the fibre relations.

The two-point witness has four states, two complementary two-element lines,
and two distinct eight-pair equivalence relations. Their meet is equality and
their generated join is universal. The diagonal line is a distinct
\(C_2\)-invariant scalar candidate. Full \(\mathrm{GL}(2,2)\) acts transitively
on all three projective lines, so a two-line torsor requires independently
sourced bipartition data.

## Claim and Authority Boundary

Current ontology does not derive the unordered bipartition, its provenance,
the block-span state model, the both-odd parity domain, a physical
transformation category, physical variations, or downstream observable
semantics. The associated construction proves algebraic bundle descent, not
physical gauge equivalence or covariance.

The candidate remains `proposal-only`, `source-extension data`, and
`blocked_adoption_open_continuation`. No canonical ontology edit, source-law
adoption, physical-admissibility result, general EqSrc discharge, RetainH,
GenH, M_src, g_eff, matter coupling, Einstein equations, benchmark promotion,
Gate Chair verdict, completed derivation, future source-extension
impossibility, or project-wide rejection conclusion follows. The
Distance-to-GR and metric-use ledgers are unchanged.

## Freeze and Next Route

The exact v3 and exact candidate-v1 construction–audit–stress cycles remain
locally frozen. This distinct associated-object route is `not_frozen` because
it changes the codomain and supplies new descent, non-descent, section,
scalarization, parity, and variation payload.

The next lawful packet is one fresh bounded
`smuggling-auditor@0.2.0` audit of the exact bundle-law candidate. It may not
repair or adopt the candidate.

## Validation

Task-local validator:

```text
.venv/bin/python research_control/tasks/RT-20260718-039/artifacts/validate_eqsrc_orientation_torsor_descent_law_candidate.py --write-report --json
```

Expected status: `PASS`.
