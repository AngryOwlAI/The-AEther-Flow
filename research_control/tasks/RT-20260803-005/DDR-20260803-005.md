---
authority: "control"
decision_id: "DDR-20260803-005"
task_id: "RT-20260803-005"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "project-control-maintainer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260803-005-001"
status: "completed"
supersedes_decision_id: "DDR-20260803-004"
requires_human_gate: false
role_fit_candidates:
  - "project-control-maintainer@0.2.0|selected|The registered high-severity control-contract-drift signal requires bounded control-registry, validator, renderer, and test synchronization."
  - "validator-engineer@0.2.0|not_selected|The defect spans authoritative control taxonomy and registries rather than an isolated validator implementation."
  - "documentation-curator@2.0.0|not_selected|The repair changes machine-facing control semantics and cannot be handled as explanatory prose."
created_at: "2026-08-03T07:38:47Z"
activated_at: "2026-08-03T07:38:47Z"
completed_at: "2026-08-03T08:22:38Z"
validation_status: "PASS"
---

# DDR-20260803-005 — Synchronize the P16-T02 Gate status-layer contract

## Current Objective

Convert `PIS-RT-20260803-003-001` and its issue-inventory sidecar into one
executable project-control contract. The repair must express both halves of
Gate C without collapsing them: the exact finite P7 source package is adopted
by protected constitutive postulate, while target-side universal matter
coupling through a source-derived `g_eff` remains unestablished. The same job
must calibrate the existing negative Gate E verdict versus future positive
closure and synchronize the stale current Gate A source-validation label.

The Project-System Director frame created and activated the selected AgentJob.
Generation 233 then executed that exact bounded maintainer contract once.

## Authority Surfaces Read

The repository and research-control guidance; recursive-relay and
project-system skills; Project-System Director and Project-Control Maintainer
roles; AgentJob, execution-role, and project-improvement-handoff schemas;
`program_state.yaml`; `handoff-0946`; the open improvement signal and sidecar;
the registered P16-T02 consistency audit and its findings, authority map, and
validation receipt; the Gate A, Gate C, and Gate E authority paths named by
that audit; the Distance-to-GR ledger, status aliases, both accepted-status
calibrations, current renderers, validator constants, and focused tests; and
live memory, classifier, resolver, signal, Git, and continuation state were
inspected.

## Role-Fit Matrix

| Role | Fit | Reason |
| --- | --- | --- |
| `project-control-maintainer@0.2.0` | selected | Owns control registries, schemas expressed through control data, validator hooks, and control-marked status surfaces. |
| `validator-engineer@0.2.0` | not selected | Validator code participates, but the authoritative drift begins in shared status taxonomy and control registries. |
| `documentation-curator@2.0.0` | not selected | The defect changes machine-facing control meaning rather than explanatory-only documentation. |

## Selected Role

Use one task-scoped Project-Control Maintainer overlay. It may update only the
allowlisted status-taxonomy, registry, renderer, validator, focused-test, task,
sidecar, and generated-memory surfaces. It must preserve exact source-side
qualification, retain the open `g_eff`-dependent derivational burden, and use
the approved generators for derivatives. It may not edit a canonical Gate
decision or any science source.

## Claim Boundary

The job may claim that current project-control status surfaces agree with
already-authoritative records. It may not claim a new Gate decision, new
source-law or ontology adoption, first-principles matter derivation, target
`T_mu_nu`, coupling through derived `g_eff`, equivalence-principle universality,
Einstein equations, benchmark promotion, external review, publication,
outward action, push, or completed derivation. P16-T02 remains
`REPAIR_REQUIRED` until a separate fresh canonical audit verifies the repair.

## Validation

Completion requires exact hashes for protected Gate and P16-T02 sources; explicit
source-qualified Gate C adoption plus explicit non-derivation guards across
the ledger, aliases, calibrations, renderers, and compact views; the exact Gate
E negative-verdict/future-positive-closure split; current Gate A registry
validation synchronized without rewriting historical receipts; focused tests
and task-local status-contract checks; signal, documentation-impact,
research-control, memory, and diff gates; generated derivatives produced only
by their registered generators; one PASS completion resolving the signal; and
one governed checkpoint. The bounded status payload and focused checks pass,
but the affected-profile repository shard fails the sealed P10-T05
`render_current_frontier.py` current-authority binding. Repairing that binding
requires the historical validator and test paths outside this AgentJob's
allowlist. This job therefore stops blocked with zero checkpoint invocations.
One distinct, fail-closed renderer-binding recovery must preserve this payload
and all historical evidence, restore P10 readiness, and checkpoint before the
separate fresh P16-T02 canonical audit can run.
