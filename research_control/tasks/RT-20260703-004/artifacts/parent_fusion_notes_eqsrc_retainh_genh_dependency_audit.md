<!-- authority: science_draft -->

# P5-T01 Parent Fusion Notes

The child mathematical review and child ontology/status review agree on the
core distinction: the P2 theorem does not require general `EqSrc`, `RetainH`,
or `GenH` when it remains scoped to declared source objects and explicit source
certificate records; broader matter-sector continuation conditionally depends
on those upstream burdens if the route tries to generalize beyond certificate
premises.

The fused audit therefore records two layers for each dependency:

- P2 theorem scope: `not_required_for_current_scope`.
- Broader matter-sector continuation: `conditionally_required`.

No child review found grounds to classify any of `EqSrc`, `RetainH`, or `GenH`
as `already_supplied_by_tracked_source` for the broad upstream role. The
tracked local EqSrc datum is local, while the Distance-to-GR ledger still marks
general EqSrc as missing and marks `RetainH` and `GenH` as missing primitives.

The next route is not an adoption route. It is the v15 P5-T02 dependency
consequence selector.
