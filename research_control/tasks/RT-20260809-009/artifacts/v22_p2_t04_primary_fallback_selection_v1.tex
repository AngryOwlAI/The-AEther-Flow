% authority: science_draft
% claim_status: draft/control
\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,booktabs,array,longtable,enumitem,hyperref}
\hypersetup{hidelinks}
\setlist{nosep}

\title{V22 P2-T04 Primary and Fallback Selection v1\\
\large A source-local rank-two seed, objective fallback, and activation boundary}
\author{Theoretical-Continuation Selector with Refuter and Smuggling-Auditor perspectives}
\date{2026-08-09}

\newcommand{\A}{\mathcal{A}}
\newcommand{\PB}{\mathcal{P}_{\mathrm{B1}}}
\newcommand{\sigm}{\operatorname{logistic}}

\begin{document}
\sloppy
\maketitle

\begin{center}
\fbox{\parbox{0.92\textwidth}{
\textbf{Status and authority.} This task-local draft/control packet selects
one program route and one inactive fallback. It does not activate a scientific
candidate, adopt a source extension, modify canonical ontology, construct an
effective metric, pass Gate B, establish target matter coupling, contact an
external reviewer, or complete a GR derivation. Internal role-separated review
is not external review or independent replication.
}}
\end{center}

\section{Decision and live dependency boundary}

The active milestone is \texttt{effective\_metric\_g\_eff}. P1-T03 supplies
the exact finite P7 package only as protected, adopted-not-derived conditional
source input. P2-T01 supplies a quotient-local information-capacity necessary
condition. P2-T03 fixes three material family classes, one-at-a-time execution,
append-only failure, and a separate transition from selection to activation.

The decision is:
\begin{enumerate}
  \item select B1, the local six-field source-response seed, as the sole active
  \emph{program route};
  \item preregister B2, the exact-P7/common-principal continuum-lift target, as
  an inactive fallback;
  \item leave B3 reserved and unallocated; and
  \item route scientific activation and exact local-state construction to a
  separate P3-T01 AgentJob.
\end{enumerate}
Thus one selection route is active, while the number of scientifically
activated candidates remains zero.

\section{Primary candidate descriptor}

Let the existing primitive smooth four-dimensional source arena be $\A$; it is
not physical spacetime. The proposal-only bundle is
\[
 E_{\mathrm{B1}}=\A\times\mathbb{R}^{6},\qquad
 q=(q^1,\ldots,q^6).
\]
On a source patch $U\subset\A$, the structural equation target is the
first-jet quasilinear system
\[
 K_A{}^{B\mu}(x,q)\,\partial_\mu q^B=F_A(x,q),
 \qquad A,B=1,\ldots,6.
\]
No metric contracts the derivative index. For $\xi\in T_x^*\A$, the
unreduced structural principal target is
\[
 P_{\mathrm{B1}}(x,q,\xi)_A{}^B
   =K_A{}^{B\mu}(x,q)\xi_\mu,
 \qquad
 \PB(x,q,\xi)=\det P_{\mathrm{B1}}(x,q,\xi).
\]
The determinant is homogeneous of degree six in $\xi$. This statement does
not choose its coefficients, prove reduction, hyperbolicity, factorization,
Lorentzian signature, cone stability, time orientation, or universality.

The exact conditional P7 interface is represented only by two proposal-only
dimensionless token probabilities
\[
 O_1=\frac{q^1}{q^6},\qquad O_2=\frac{q^2}{q^6},\qquad
 p_i=\sigm(O_i),
\]
on a patch where $q^6\neq0$. These tokens are not target clocks, rods,
signals, detectors, locations, or realistic continuum matter.

\section{New mathematical payload: a local rank-two certificate}

\paragraph{Proposition (rank-two response certificate).}
Let $V$ be a quotient-local regular patch on which $q^6\neq0$ and
$dO_1\wedge dO_2\neq0$. For
\[
 I_{\mathrm{B1}}=(O_1,O_2),\qquad
 R_{\mathrm{B1}}=(\sigm(O_1),\sigm(O_2)),
\]
the differential of $R_{\mathrm{B1}}$ has rank two on $V$.

\paragraph{Proof.}
The exterior-product hypothesis gives
$\operatorname{rank}dI_{\mathrm{B1}}=2$. The derivative of the logistic map
is $\sigm(z)(1-\sigm(z))$, which is strictly positive for finite $z$ and
interior probabilities. Therefore the Jacobian of the componentwise logistic
postprocessing is an invertible diagonal $2\times2$ matrix. Composition with
$dI_{\mathrm{B1}}$ preserves rank two. $\square$

This clears only the P2-T01 verdict
\texttt{necessary\_condition\_met\_not\_sufficient}. It supplies no physical
scale, global gluing, causal cone, metric, curvature, universal matter response,
or Gate B credit. The excluded singular strata $q^6=0$ and
$dO_1\wedge dO_2=0$ remain explicit falsification domains.

\paragraph{Lemma (selection--activation noncollapse).}
Under the P2-T03 state machine, a transition that records exactly one selected
primary, one inactive fallback, and no candidate execution changes the active
program-route count to one but leaves the scientific-activation count at zero.

\paragraph{Proof.}
The protocol defines \texttt{SELECT\_PRIMARY\_AND\_FALLBACK} and
\texttt{ACTIVATE\_PRIMARY} as distinct transitions. This packet executes only
the former; the latter is routed to P3-T01. $\square$

\section{Fallback candidate and objective activation conditions}

B2 preserves the exact finite P7 state and proposes a future continuum lift
whose declared matter sectors would have to derive a common reduced principal
polynomial. No such continuum state, sector equation, quotient tangent, or
common polynomial exists in this packet. Its P2-T01 verdict is therefore
\texttt{outside\_differential\_scope}.

B2 cannot activate automatically. A later selector may consider the exact
fallback identity only after: (i) an append-only B1 hard-fail or termination
receipt; (ii) zero other active candidates; (iii) a constructed continuum-lift
descriptor with fields, regularity, sector equations, quotient data, and a
source-derived common-principal target; (iv) a fresh adequacy evaluation; and
(v) a no-target-import receipt. Selection now is not evidence of later
viability.

\section{Noncompensatory comparison and minimality}

Scores run from 0 (unavailable) to 4 (strongest among the bounded packets).
There is no scalar total; a high score cannot compensate for target import or
failure of minimum adequacy.

\begin{longtable}{p{0.20\textwidth}p{0.48\textwidth}p{0.22\textwidth}}
\toprule
Packet & Vector (information, no metric debt, universality,
calculability, falsifiability, continuum, frozen difference, assumption cost)
& Disposition\\
\midrule
B1 six-field seed & $(4,4,3,3,4,3,4,2)$ & selected primary route\\
B2 P7 principal lift & $(2,4,3,2,3,1,4,1)$ & inactive fallback\\
B3 discrete continuum & $(0,0,0,0,0,0,0,0)$ & reserved unallocated\\
\bottomrule
\end{longtable}

B1 is minimally sufficient only relative to the admitted packets and this
construction stage. It is the only packet with native continuum locality, an
explicit rank-two source-response certificate, and a directly calculable
principal target while avoiding a metric, coframe, connection, or desired
cone premise. Six scalar components are not themselves evidence; the explicit
quotient statistic and determinant target are the deciding burden-bearing
features. The choice is not based on familiarity or target agreement.

\section{Assumption cost, frozen routes, and hard-fail exposure}

B1 adds a proposal-only rank-six source-scalar bundle, a proposal-only
first-jet coefficient/source map, two proposal-only P7-token response bridges,
and a rank-two patch restriction. B2 retains the protected P7 conditional
input but adds unresolved continuum, common-principal, and convergence burdens.
Neither assumption ledger is an ontology adoption.

The one-amplitude factor-through route remains frozen by its rank-at-most-one
result. The unchanged finite graph-decoder route remains frozen because it
adds neither source information nor controlled continuum structure. Renaming,
additional downstream decoding, a new file, or target fitting cannot reopen
either route.

The B1 selection passes only a pre-result hard-fail screen. Hyperbolicity,
universal cone selection, stability, and controlled global behavior are not
yet evaluable. Any later metric-equivalent import, target-observable fiat, GR
fit, nonuniversal unselected multicone, instability, absent controlled limit,
postulated Einstein structure, or frozen-route repackaging terminates the
candidate append-only.

\section{Expanded Distance-to-GR matrix}

\begin{longtable}{p{0.27\textwidth}p{0.62\textwidth}}
\toprule
Burden & Status after P2-T04\\
\midrule
Source ontology primitives & unchanged; B1 fields and laws are proposal-only new-primitive candidates\\
Source equivalence $\mathrm{EqSrc}$ & provisional source-chart redundancy declared locally; not adopted canonically\\
$\mathrm{RetainH}$ & unchanged; no retention law is derived\\
$\mathrm{GenH}$ & unchanged; no generator law is derived\\
$\mathrm{ObsLoc}_{\rm lc}$ & a two-token source response interface is proposed and locally typed; no physical witness is admitted\\
$\mathrm{Resp}_{\rm lc}$ & local rank-two necessary screen clears conditionally; no universal physical response law is derived\\
$M_{\rm src}$ & unchanged within its historical scoped status; the source patch is not physical spacetime\\
$g_{\rm eff}$ & active burden; one construction route is selected, but no metric is constructed, activated, or adopted\\
Matter coupling & unchanged; P7 remains conditional source input and is not target coupling\\
Einstein equations & blocked; no target action or field equation is supplied\\
Finite-variation robustness & open P3/P4 burden; no stability or gluing result is supplied\\
Benchmark promotion & blocked; no benchmark is executed or used for selection\\
Gate Chair status & unchanged; no protected Gate review is requested or issued\\
Current route freeze or hard-fail & scalar and unchanged graph routes remain frozen; B1 has no triggered precheck fail but later tests remain open\\
\bottomrule
\end{longtable}

The physical Distance-to-GR delta is zero. The new mathematical payload is the
rank-two response certificate and the selection--activation noncollapse lemma.
The new scientific-control payload is a source-merit comparison that selects a
single construction route without supplying the metric law.

\section{Internal review and forbidden overreads}

The Physicist-Mathematician/Refuter perspective checks quotient rank,
principal-target syntax, packet completeness, and one-route accounting. The
Physicist-Philosopher/Smuggling-Auditor perspective checks metric and target
imports, P7 label laundering, frozen-route repackaging, and selection/adoption
collapse. Both are task-local same-model internal review.

This packet authorizes none of: canonical ontology modification, source-law
adoption, scientific candidate activation, effective metric, target matter
coupling, Einstein equations, Gate B, benchmark promotion, external review,
publication, release, push, or completed derivation. P3-T01 is the next bounded
construction and activation packet after this checkpoint.

\end{document}
