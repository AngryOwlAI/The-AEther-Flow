---
authority: science-draft
status: draft/control
task_id: RT-20260718-046
agent_job_id: AJ-RT-20260718-046-001
---

# Parent fusion notes: EqSrc flow-generated graded-orbit root selector

Both required perspectives select one distinct constructive and
non-promotional next packet: an `ontology-law-research-packet` executed by
`ontology-formalizer@0.2.0`.

The proposal-only target is
`EqSrcFlowGeneratedGradedOrbitRootLaw_src^cand,v1`. It moves the burden
upstream of the exact rooted-partition candidate. The prior packet accepted a
finite transition graph, event-to-record projection, and root certificate as
independent candidate data. The new target must not. It asks whether a
proposal-only ordered-monoid action extending the schematic
\(\Phi_\lambda\) can itself induce the transition graph, root, and rank.

For source events in one action orbit, define reachability by
\(x\preceq_\Phi y\) when an allowed nonnegative source-order element carries
\(x\) to \(y\). An admitted branch must prove that this reachability is a
locally finite partial order. Its transition graph is then the cover relation,
not an independently loaded graph. The full source component must have one
unique minimum \(r\), and every saturated chain from \(r\) to an event \(x\)
must have the same finite length \(\rho(x)\).

Only on that branch may rank parity define
\(P=\{x:\rho(x)\equiv0\pmod2\}\) and
\(Q=\{x:\rho(x)\equiv1\pmod2\}\). Both blocks must be nonempty. A
proposal-only pointing may then use
\(B=\operatorname{span}_{\mathbb F_2}\{\mathbf1_Q\}\), with the relation
given by the associated kernel pair. Neither the partition, line, nor
relation is adopted `EqSrc`.

The future formalizer must prove naturality under independently declared
action-preserving source isomorphisms, invariance under allowed ordered-monoid
reparameterizations, and robustness or a precise obstruction under an
independently declared variation family. The action, morphisms, and variations
may not be selected after inspecting the desired partition or relation.

A four-event discrete chain is the positive control. Periodic cycles must fail
antisymmetry; bi-infinite orbits must fail the global-minimum guard; dense
orbits without locally finite covers must fail the rank interface; multiple
minima and non-graded reachability must fail uniqueness or rank
well-definedness. A finite observed segment of a rootless orbit is a mandatory
truncation-negative control: its first sample is not a source-global minimum.

This packet does not follow from current ontology. The canonical foundations
source provides only an intentionally schematic \(\Phi_\lambda\), not an
ordered monoid action, discrete orbit structure, cover relation, unique
minimum, graded rank, or independent morphism and variation categories. The
target is therefore a `proposal-only`
`new_ontology_primitive_candidate`, with adoption blocked and
same-milestone continuation open.

A source-side root- or relation-irrelevance theorem is deferred because no
independently derived physical equivalence or downstream functor exists.
The negative orbit families remain controls rather than a global no-go. The
exact rooted-partition candidate-v1 construction, audit, and stress cycle
remains locally frozen.

No Distance-to-GR or metric-use ledger row changes. No source law, canonical
ontology, physical admissibility, `RetainH`, `GenH`, `M_src`, `g_eff`, matter
coupling, Einstein equations, benchmark status, Gate Chair verdict, or
completed derivation is promoted.
