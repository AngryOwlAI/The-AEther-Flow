---
decision_id: "DDR-20260708-008"
task_id: "RT-20260708-008"
agent_job_id: "AJ-RT-20260708-008-001"
director_version: "director-of-research@0.3.0"
decision_type: "task_overlay"
selected_role_id: "project-control-maintainer"
selected_role_version: "0.2.0"
status: "completed"
requires_human_gate: false
created_at: "2026-07-08T04:36:00Z"
activated_at: "2026-07-08T04:36:00Z"
completed_at: "2026-07-08T04:36:00Z"
validation_status: "PASS"
---

<!-- authority: control -->

# DDR-20260708-008

## Decision

Select one bounded `project-control-maintainer@0.2.0` task overlay for v18
P5-T02: matter-coupling DAG and ledger-delta question setup for
`source_detector_readout_semantics`.

## Basis

`handoff-0700` completed P5-T01 and routed immediate continuation to P5-T02.
The v18 plan defines P5-T02 as a setup-only project-control packet that creates
a protected ledger-delta question and a DAG patch proposal without performing
the update.

The current populated matter-coupling DAG already has a blocked
`mc_detector_semantics_target` node. P5-T01 introduced
`source_detector_readout_semantics` as a proposal-only burden that may later
serve as an explicitly source-side detector/readout replacement target. P5-T02
therefore fits Project-Control Maintainer authority: it prepares the controlled
question and patch shape, while leaving the canonical DAG and
Distance-to-GR ledger unchanged.

## Role-Fit Matrix

| Role | Decision | Reasoning |
| --- | --- | --- |
| `project-control-maintainer@0.2.0` | Selected | P5-T02 is a project-control setup packet for DAG/ledger question surfaces, not science construction. |
| `candidate-constructor@0.2.0` | Deferred | P5-T03 and later packets may set up or construct a source detector/readout candidate. |
| `theoretical-continuation-selector@0.1.0` | Deferred | The latest handoff already selected P5-T02; no new theoretical route choice is needed. |
| `gate-chair@0.1.0` | Not selected | No protected adoption or ledger status update is requested in this packet. |

## Selected Role

The selected role is `project-control-maintainer@0.2.0` with execution role
record `project-control-maintainer@0.2.0--RT-20260708-008`.

This is a task overlay, not a permanent role registration. It is limited to
creating a protected ledger-delta question, a DAG patch proposal, a validator,
the required receipts, registry rows, generated refreshes, and the P5-T03
handoff.

## Boundaries

The packet may create task-local control artifacts that ask whether and how a
later protected task should add `source_detector_readout_semantics` to the DAG
or Distance-to-GR surfaces.

It may not edit `research_control/design/matter_coupling_dependency_dag_v1.md`,
edit `registries/DISTANCE_TO_GR_LEDGER.csv`, adopt `Det_src` or `Readout_src`,
adopt detector semantics, adopt a source law, adopt a coupling law, derive
matter coupling, introduce stress-energy semantics, introduce a matter action,
derive Einstein equations, promote an exact-GR benchmark, issue a Gate Chair
verdict, or claim completed derivation.

## Expected Result

The expected required outputs are:

```text
research_control/tasks/RT-20260708-008/artifacts/source_detector_readout_ledger_delta_question_v1.yaml
research_control/tasks/RT-20260708-008/artifacts/source_detector_readout_dag_patch_proposal_v1.md
```

The selected next route is expected to be P5-T03: source detector/readout
candidate setup.

## Validation

The packet must pass the task-local validator, generated frontier and graph
refresh checks, memory bootstrap, documentation-impact validation,
claim-language validation, research-control validation, full local validation,
and checkpoint transaction validation before commit.
