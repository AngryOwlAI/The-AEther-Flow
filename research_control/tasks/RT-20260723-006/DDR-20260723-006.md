---
authority: "control"
decision_id: "DDR-20260723-006"
task_id: "RT-20260723-006"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "validator-engineer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260723-006-001"
status: "completed"
requires_human_gate: false
created_at: "2026-07-23T12:17:43Z"
activated_at: "2026-07-23T12:17:43Z"
completed_at: "2026-07-23T12:59:07Z"
validation_status: "FAIL"
supersedes_decision_id: "DDR-20260723-005"
role_fit_candidates:
  - "validator-engineer@0.2.0|selected|The blocker is one deterministic integration-contract assertion whose expected status contradicts the registered advisory-warning policy."
  - "project-control-maintainer@0.2.0|not_selected|The canonical policy and runtime semantics are already aligned and require no control-policy edit."
  - "process-integrity-auditor@0.1.0|not_selected|The hash-sealed blocker already proves the mismatch and names the exact safe recovery."
  - "documentation-curator@2.0.0|not_selected|No explanatory source drift requires a documentation rewrite."
---

# DDR-20260723-006 — Reconcile the threshold-next warning contract

## Current Objective

Align the live Director-context regression with the registered ordinary-route
policy: at two consecutive project-system tasks the evaluator emits the
advisory `ordinary_route_guard_threshold_next` warning, while only a `FAIL`
blocks continuation. Preserve the observed route history, threshold-three hard
guard, all-ready-science exception, P12-T05 metric semantics, and every
scientific authority boundary.

## Authority Surfaces Read

The repository control instructions, `improve-project-system`, Project-System
Director and Validator Engineer roles, current AgentJob schema, the
generation-72 immutable route, `handoff-0838`, its completion and hash-sealed
blocker, the registered ordinary-route policy and registry row,
`ordinary_route_guard.py`, `continue_research.py`, and the failing integration
test were inspected. Generation 72 was claimed and consumed exactly once
before this worker step.

## Source-Backed Resolution

The registered policy states that run length two emits an advisory warning and
that the hard routing requirement begins at three. The evaluator implements
that policy by returning `WARN` only when warnings exist and no errors exist.
The continuation boundary blocks only `FAIL`; therefore a policy-valid `WARN`
must remain ready and visible. The stale integration assertion requiring
`PASS` contradicts both canonical policy and runtime behavior.

## Role-Fit Matrix

| Role | Fit | Reason |
| --- | --- | --- |
| `validator-engineer@0.2.0` | selected | Owns deterministic test-contract corrections and focused regression evidence. |
| `project-control-maintainer@0.2.0` | not selected | The registered policy and runtime already agree and require no source-policy change. |
| `process-integrity-auditor@0.1.0` | not selected | The hash-sealed blocker already isolates the exact mismatch. |
| `documentation-curator@2.0.0` | not selected | No explanatory documentation source change is required. |

## Selected Role

Use one task overlay of `validator-engineer@0.2.0`. It may change only the
live Director-context test assertion and focused control evidence. It may not
change the evaluator, continuation runtime, policy source, route history,
threshold, exception semantics, scientific records, or P12-T06.

## Validation

PASS requires the focused Director-context test to verify `WARN`, the exact
`ordinary_route_guard_threshold_next` identifier, empty errors, observed run
length two, and a still-ready continuation boundary. The ordinary-route guard
suite and full 155-test P12 shard must pass, all project-control gates must
pass, and one normal governed checkpoint must commit without legacy
validation.
