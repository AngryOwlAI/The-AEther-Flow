<!-- authority: draft/control -->

# P9-T06 parent fusion: radiative completion and detector nonselection

The mathematical perspective fixes the four-cycle source record and derives
its quotient spectrum \(\{2,2,4\}\). It constructs the proposal-only family

\[
m\ddot q+2m\gamma\dot q+L_Cq=0
\]

and proves that all \(m>0,\gamma\geq0\) members share the same stationary
equation \(L_Cq=0\). The two fixtures
\((m,\gamma)=(1,0)\) and \((4,1/4)\) have distinct exact roots and distinct
responses to the same initial data. The same record admits commuting
rank-two and rank-three projectors and the readout family \(D_d[q]=d^Tq\).

The interpretive perspective fixes the model-to-world boundary. A finite graph
mode is not by itself a physical gravitational-wave polarization; the update
parameter is not a physical clock; a graph eigenvalue is not a physical
wavenumber; and a token readout is not a calibrated detector interaction. The
two-dimensional eigenspace may be noted only as a formal analogy after source
sealing.

The fused no-fog result is
`OBST-P9T06-RADIATIVE-DYNAMICS-DETECTOR-NONSELECTION-001`: the current source
package does not derive a total map from the registered static finite equation
to a physical radiative sector, polarization space, propagation speed,
damping/dispersion law, and detector coupling. The P9-T06 case is
`INCONCLUSIVE` with secondary label `FORMAL_ANALOGY`, not `PASS`.

The source outputs are sealed before the typed target comparison. No target
wave equation, polarization count, speed, damping, dispersion, detector
response, desired agreement, or tolerance selected \(m\), \(\gamma\), a
projector, a readout, or a rerun. Same-context validation is not independent
reproduction.

This is a current-source obstruction with
`blocked_adoption_open_continuation` status. It is not a global no-go theorem;
a materially new source-derived kinetic law, physical clock and length map,
radiative selector, and detector interaction can reopen the burden. No
canonical ontology, physical radiative sector, effective geometry, Einstein
equation, exact-GR recovery, benchmark promotion, proof, publication, push,
or completed-derivation authority follows.
