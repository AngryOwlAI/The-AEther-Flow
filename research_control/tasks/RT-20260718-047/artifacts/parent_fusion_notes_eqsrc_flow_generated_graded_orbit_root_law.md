---
authority: science-draft
status: proposal-only
task_id: RT-20260718-047
candidate: EqSrcFlowGeneratedGradedOrbitRootLaw_src^cand,v1
---

# Parent fusion: flow-generated graded-orbit root law

## Fused result

The parent accepts both child outputs without an unresolved conflict and
formalizes one exact proposal-only source-extension target:
`EqSrcFlowGeneratedGradedOrbitRootLaw_src^cand,v1`.

The dependency order is strict. A packet begins with a source-order monoid, an
identity-and-composition action extending the schematic `Phi_lambda` anchor,
one full source component, and independently declared source morphisms and
variations. Only after action reachability is antisymmetric and locally
finite, has one global minimum, and is graded by common finite saturated-chain
length may the packet define rank parity, the indicator state space, the
one-dimensional boundary, and its kernel-pair relation.

## Mathematical payload

Action identity and composition make reachability a preorder. Antisymmetry
turns it into a partial order; local finiteness derives the transition graph
as the cover relation. The unique minimum and graded-chain condition define a
rank, and rank parity defines the two ordered blocks. Equivariant source and
ordered-monoid isomorphisms preserve reachability, covers, the minimum, rank,
the boundary line, and the relation. Ordered-monoid automorphism
reparameterization leaves the action-map set unchanged. Variations are
accepted only when they independently preserve every admission guard.

The positive control is the four-event action
`Phi_n(e_i)=e_min(i+n,3)`. It produces ranks `0,1,2,3`, parity blocks
`{e0,e2}` and `{e1,e3}`, and an eight-pair relation on four indicator states.
The task-local validator also classifies all unary actions through four
states: the only admitted connected one-generator actions are the labeled
finite chains, with counts 2, 6, and 24 for two, three, and four states.

Periodic rotation fails antisymmetry. The nonnegative shift on the integers
has no global minimum. Nonnegative rational translation has infinite
intervals and no covers. A finite commuting-idempotent action has two minima.
A finite free-monoid action has root-to-top saturated chains of lengths two
and three. A finite observation window of the bi-infinite shift has only an
apparent minimum. These are scoped admission failures, not a global no-go.

## Authority separation

The canonical foundations source contributes only schematic intrinsic ordered
motion. It does not derive the monoid, action law, full component, local
finiteness, unique minimum, gradedness, morphism category, or variation
family. The candidate is therefore a `proposal-only new ontology primitive
candidate`, with `blocked_adoption_open_continuation`.

The formal theorems are conditional mathematical results. They do not
establish physical admissibility, covariance, detector semantics, empirical
recovery, or a source-to-GR bridge. Validator success, registry entries,
generated derivatives, role identity, handoff state, and checkpoint status
remain non-promotional.

## Freeze and next route

The exact `EqSrcOrderedMotionRootedPartitionLaw_src^cand,v1` construction,
audit, and stress cycle remains locally frozen. This packet does not repair or
reload its independently supplied graph, projection, or root. It moves the
burden upstream to the proposed action itself and must receive one fresh
`smuggling-auditor@0.2.0` review before stress or any protected decision.

No Distance-to-GR or metric-use ledger row changes. General `EqSrc` and every
downstream GR burden remain blocked.
