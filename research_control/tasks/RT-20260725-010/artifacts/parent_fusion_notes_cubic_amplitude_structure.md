---
authority: science_draft
status: draft_control
task_id: RT-20260725-010
job_id: AJ-RT-20260725-010-001
plan_task_id: P5-T04
---

# P5-T04 parent fusion notes

## Fused structural result

The two internal perspectives analyze the exact unchanged P5-T03 state family
and flow. The result is a theorem package plus one candidate-specific
obstruction:

- a state restricted to any nonempty open source patch fixes the one global
  amplitude, so different amplitudes on disjoint patches cannot be glued
  inside `Q_amp`;
- forward-parameter composition remains exact, while the cubic generator has
  additive defect `-3 gamma a b(a+b) sin(theta_1)` and is not a superposition
  law;
- membership in the one-mode family is a preserved kinematic constraint, but
  primary, secondary, first-class, and second-class labels are not applicable
  without a Hamiltonian formalism;
- every global `C1` first integral is constant, while `L=a^2/2` is monotone;
- identity-only declared redundancy has zero infinitesimal algebra, while
  amplitude reflection is a discrete non-gauge symmetry;
- admitted tangent variations are nonexpanding; and
- if Borel probabilities are added, pushforward preserves normalization and
  `delta_0` is the unique invariant probability for any positive flow step.

## Exact locality boundary

The patching theorem establishes failure of one declared patch-independence
test for this global-mode family. It does not prove that every source-side
extension is nonlocal, that physical spacetime is nonlocal, or that future
local degrees of freedom cannot be constructed. The precise consequence is
`blocked_adoption_open_continuation`, not route freeze or theory rejection.

## Constraint and conservation boundary

The quotient-valued condition selecting `span{sin(theta_1)}` is a
configuration-family membership condition. It is preserved because the
candidate vector field is tangent. The packet has no phase space, action,
Legendre map, bracket, Hamiltonian, or multiplier, so it reports no physical
constraint count.

The first-integral theorem is global and smooth at the fixed point. The sign
of the amplitude labels orbit sectors but is discontinuous at zero. The
Lyapunov functional is not conserved and is not physical energy, entropy, an
action, a Hamiltonian, or a Noether charge.

## Symmetry and probability boundary

The update generator is not a gauge generator. The declared redundancy is
identity-only, and its infinitesimal algebra is zero. Reflection commutes with
the flow but is a discrete global symmetry, not declared redundancy.

Probability is not part of the candidate. The probability proposition is a
conditional theorem about an extra Borel measure structure; it neither
selects an ensemble nor creates physical stochastic semantics.

## Remaining burden and next route

P5-T04 is complete for the unchanged candidate because every requested
structural branch has an exact theorem, applicability determination, or
scoped obstruction. P5-T05 may next construct source backgrounds, phases,
linearized modes, stability conditions, and symmetry-breaking data. It must
not erase the patching obstruction, treat the one-mode tangent result as
generic robustness, or import a physical background from target GR.

Canonical ontology or source-law adoption, physical locality, physical
constraints, energy, probability, gauge, `M_src`, `g_eff`, matter coupling,
Einstein equations, exact-GR recovery, benchmark promotion, Gate Chair
closure, proof publication, push, completed derivation, global theory
rejection, and future source-extension impossibility remain blocked.
