---
authority: control
status: completed
decision_id: DDR-20260725-010
task_id: RT-20260725-010
agent_job_id: AJ-RT-20260725-010-001
director_version: director-of-research@0.3.0
decision_type: task_overlay
selected_role_id: ontology-formalizer
selected_role_version: 0.2.0
requires_human_gate: false
---

# DDR-20260725-010: execute only v21 P5-T04

## Decision

Activate one `ontology-formalizer@0.2.0` task overlay for v21 work item
P5-T04. The generation-114 checkpoint at
`0ad7210aa50b7ef3262cab425561924efdc9783a` satisfies the dependency preserved
by `handoff-0869`, and immutable relay generation 115 selects the same work
item under `continue-research`.

Use the named `ontology-law-research-packet` route because current ontology
does not derive the structural law that would provide independent patch
composition, Hamiltonian constraints, conserved quantities, gauge generators,
or probability normalization for the P5-T03 candidate. The task analyzes that
candidate unchanged. It does not propose a canonical ontology edit or request
adoption.

## Evidence and route resolution

The exact source boundary comes from the registered P5-T03 cubic-amplitude
candidate, P5-T02 forward-action semantics, P5-T01 compact source-theory
object, the v21 plan, the continuum-first P4-T06 integration, the canonical
ontology manuscripts, the burden map, and the Distance-to-GR ledger.
`research_control/support_formalization/` was inspected as absent and supplies
no premise or authority.

Memory preflight refreshed retrieval-only local state and then passed with
1,014 canonical source objects and 5,016 relationship rows. A parser-unsafe
query containing `P5-T04` failed and influenced no result. A parser-safe
source-dynamics query and direct lookup navigated back to the registered
P5-T03 source, which was inspected directly with its registry row.

## Role-Fit Matrix

| Candidate role | Fit | Reason |
|---|---|---|
| `ontology-formalizer@0.2.0` | selected | P5-T04 requires exact source-only definitions, theorems, applicability classifications, and a scoped obstruction. |
| `candidate-constructor@0.2.0` | not selected | The candidate must remain unchanged; background and perturbation construction belongs to P5-T05. |
| `refuter@0.1.0` | internal adverse-control perspective only | The task must expose failed patching and superposition branches without opening a second AgentJob. |
| `theoretical-continuation-selector@0.1.0` | not selected | The plan, handoff, and immutable route already select P5-T04 and its role. |

## Selected formalization

Define one narrow patch-independence locality test: arbitrary amplitude
restrictions on disjoint nonempty open source patches should glue to a state
in `Q_amp`. Because every nonempty open patch contains a point where
`sin(theta_1)` is nonzero, equality on one patch fixes the single global
amplitude. Different patch amplitudes therefore cannot be glued. This is a
candidate-specific obstruction, not a generic source-locality no-go.

Separate three composition questions. P5-T03 parameter composition remains
exact; the state family is closed under addition; the cubic generator is not
additive, with defect
`-3 gamma a b(a+b) sin(theta_1)`. No independent-subsystem product is supplied.

Use the quotient-valued membership condition
`C_e(q)=pi_e(q)` in
`C-infinity(T^4,R)/span{sin(theta_1)}` to state the preserved kinematic
constraint. Do not assign Dirac constraint classes because the candidate has
no action, phase space, bracket, Hamiltonian, or multiplier.

Prove that every global `C1` first integral is constant. Classify the
identity-only redundancy algebra as zero, retain amplitude reflection only as
a discrete non-gauge symmetry, derive exact tangent nonexpansion, and state
probability results only conditionally after adjoining a Borel measure.

## Parent-child synthesis

The single outer AgentJob uses `parent_child_parallel_synthesis`. The internal
physicist-mathematician perspective derives patching, composition,
first-integral, tangent-variation, and invariant-measure results. The internal
physicist-philosopher perspective audits locality, constraint, conservation,
gauge, probability, adoption, and downstream-GR overreads. The parent resolves
six tensions and retains no unresolved conflict.

## Claim boundary

The result may be a task-local theorem package, a scoped
patch-independence obstruction, and a scoped Distance-to-GR source-ontology
evidence delta. It is not canonical ontology, an adopted or rejected source
law, generic or physical nonlocality, a physical constraint theory, energy,
probability, gauge, source manifold, reconstruction, metric, matter coupling,
Einstein-equation derivation, benchmark promotion, proof publication, push,
completed derivation, global theory rejection, or future source-extension
impossibility.

## Freeze evaluation

The candidate-specific locality branch is not frozen. It is the first
P5-T04 structural audit of this candidate, carries new theorem-level payload,
and leaves the planned P5-T05 background and perturbation construction
available. The obstruction remains preserved and may not be erased by that
continuation.

## Validation

The task-local validator checks candidate identity, patching, generator and
flow composition defects, constraint applicability, smooth first integrals,
redundancy and reflection, tangent nonexpansion, conditional probability,
parent-child synthesis, and claim boundaries. Repository validation,
documentation-impact accounting, claim-language validation, memory
regeneration, TeX scratch compilation, and one governed checkpoint remain
required.

## Completion disposition

The registered source and companion specification pass all 21 task-local
checks. P5-T04 is complete at `draft/control`, `proposal-only`,
`source-extension data` scope and is ready for one governed checkpoint.
P5-T05 remains unexecuted until that checkpoint commits.
