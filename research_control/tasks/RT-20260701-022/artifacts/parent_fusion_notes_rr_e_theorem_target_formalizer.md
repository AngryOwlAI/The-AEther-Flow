<!-- authority: science_draft -->

# Parent Fusion Notes: RR_E Theorem Target Formalizer

Task `RT-20260701-022` fuses the Physicist-Mathematician and
Physicist-Philosopher child outputs into one Ontology Formalizer artifact.

Consensus:

- `RR_E` is retained source syntax inside admissible finite/local source
  records, not detector semantics or empirical calibration.
- Separation preservation means distinct retained `RR_E` records cannot be
  identified unless a named source-side transport or source equivalence
  certificate identifies them.
- Source-side irrelevance would require a future proof of invariance under
  admitted `RR_E` variation or quotienting, using only source-side premises.
- If neither source transport nor source-only invariance is available, the
  fail-closed label is `RR_E_underdetermination_obstruction`.

Unique mathematical contribution:

- `RR_ETheoremTarget_v1` defines the source `RR_E` record domain, separation
  preservation, irrelevance target, allowed assumptions, forbidden imports,
  finite/local examples, and obstruction family.

Unique philosophical/control contribution:

- The artifact separates ontology from mathematical target and empirical
  recovery. It blocks detector semantics, process authority, matter semantics,
  matter coupling, Einstein-equation, benchmark, and completed-derivation
  readings.

No unresolved blocking conflict remains.
