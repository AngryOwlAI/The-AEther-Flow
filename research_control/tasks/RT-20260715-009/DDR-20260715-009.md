---
authority: control
decision_id: "DDR-20260715-009"
task_id: "RT-20260715-009"
created_at: "2026-07-15T22:39:35Z"
director_version: "project-system-director@0.2.0"
decision_type: "task_overlay"
selected_role_id: "memory-system-maintainer"
selected_role_version: "0.2.0"
agent_job_id: "AJ-RT-20260715-009-001"
status: "completed"
completed_at: "2026-07-15T22:56:16Z"
supersedes_decision_id: ""
requires_human_gate: false
validation_status: "PASS"
---

# DDR-20260715-009: Separate memory validation ownership

## Decision

Execute one bounded Memory-System Maintainer AgentJob for v19 `P3-T03`.
The job may create independently selectable `publication_validation` and
`local_retrieval_health` gates, add one report-composition helper, route the
legacy bootstrap compatibility profile through those gates, and add focused
ownership and compatibility tests.

## Role-Fit Matrix

| Role | Decision | Reason |
| --- | --- | --- |
| `memory-system-maintainer@0.2.0` | Selected | P3-T03 changes memory validation ownership and local retrieval policy without changing scientific authority. |
| `validator-engineer@0.2.0` | Not selected | The adapters remain inside the registered memory-system ownership boundary. |
| `project-system-director@0.2.0` | Routing authority | It bounds the planned ownership separation without expanding role authority. |
| Physics roles | Rejected | No derivation ontology proof benchmark or scientific promotion occurs. |

## Evidence and boundary

- P3-T02 completed with PASS at commit
  `9aa9fe31690de44affdf7def16a3cad3bd50d938`.
- Entry Git state was clean and local `main` was seven commits ahead of
  `origin/main`.
- The registered v19 plan names P3-T03, the selected role, the two adapter
  paths, one shared memory-operations path, one focused test path, independent
  gate obligations, and the no-physics boundary.
- The memory preflight refreshed stale local retrieval state and then passed
  core and local retrieval validation.
- The advisory project-improvement resolver had no competing open signal.
- `handoff-0740` remains the ordinary research authority and is not
  superseded by this project-system task.

## Scope-alarm review

The plan names four implementation paths. One additional compatibility wiring
edit in `bootstrap_memory_system.py` is necessary so the legacy wrapper calls
the explicit adapters instead of retaining a second publication owner. This
is the narrow tracked-decision exception contemplated by the P3-T03 planned
write-scope rule. The change adds no dependency or public data schema and does
not widen into Make CI checkpoint skill P3-T04 or legacy retirement work.

## Preserved boundaries

- Preserve `memory_validate_core()` as the side-effect-free tracked core gate.
- Preserve publication failures in the full compatibility profile.
- Keep local retrieval advisory unless an authorized memory-maintenance caller
  explicitly selects required mode.
- Preserve `handoff-0740` and `EqSrc_family_closure_repair_or_stress` as the
  ordinary research authority.
- Do not edit Make CI checkpoint skill role canonical ontology science draft
  or generated derivatives by hand.
- Do not treat validator results tests registries derivatives or checkpoint
  evidence as physics proof.

## Outcome rule

PASS requires independent gate execution with IDs `memory_core`,
`publication_validation`, and `local_retrieval_health`; publication errors
must remain blocking; stale local retrieval must remain advisory by default
and unable to invalidate tracked core; the legacy wrapper must preserve its
overall current-state result; all focused publication and memory fixtures must
pass; and all project-system gates required by current policy must pass.
Otherwise issue `REPAIR_REQUIRED`, `ROLLBACK_REQUIRED`, or `BLOCKED` and do not
checkpoint invalid state.
