\documentclass[11pt]{article}
\usepackage[margin=0.85in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs,array,longtable}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{xcolor}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.55em}
\sloppy

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newcommand{\Fext}{\mathcal F_{\mathrm{ext}}}
\newcommand{\Adm}{\mathrm{Adm}_{\mathrm{ext}}}
\newcommand{\Kext}{K_{p}}
\newcommand{\Gen}{\mathcal L_{p}}

\title{V22 P4--T02 B2: Proposal-Only Measurable-Fiber Occurrence-Law Extension}
\author{RT-20260816-001 / AJ-RT-20260816-001-001}
\date{2026-08-16}

\begin{document}
\maketitle

\begin{center}
\fbox{\parbox{0.94\linewidth}{
\textbf{Decisive status.}  A complete target-free \emph{proposal-only}
measurable-fiber law is constructed under disclosed new roots.  The result is
pending a separately admitted Smuggling Auditor.  It is not derived from the
current ontology, does not select its roots epistemically, and does not assign
realized occurrence, physical probability, empirical frequency, P4
sufficiency, source-law adoption, or canonical-ontology authority.
}}
\end{center}

\section{Fixed boundary}

The canonical predecessor supplies only the current-topology open carrier and
its restriction/gluing theorem.  This packet does not rename an open set as a
proposal.  It adds the following roots before any P4 diagnostic:
\[
 S=\{0,1\},\qquad p\in(0,1),\qquad c(s)=1-s,
\]
together with the declaration that proposal states are locally constant
$S$-valued sections.  These are transparent source-extension inputs.  The
project does not presently derive or adopt them.

All eight inherited local freezes remain active.  D7 is not reevaluated, B2
remains inactive, and P4--T03 remains locked.  No target atlas, target metric,
desired GR cone, detector response, benchmark answer, or frozen-candidate
output enters any definition below.

\section{Typed construction}

Let $\mathsf{OpenSrc}_{4}$ denote the category generated by open inclusions in
second-countable smooth four-manifolds and by source diffeomorphisms.  For an
open object $O$, define
\[
 \Fext(O)=\operatorname{LC}(O,S)\cong S^{\pi_{0}(O)}.
\]
A manifold is locally connected, so the connected components of $O$ are open.
Second countability then makes $\pi_{0}(O)$ at most countable.  Consequently
$S^{\pi_{0}(O)}$, equipped with its countable product sigma-algebra, is a
standard Borel space.  For an inclusion $i:U\hookrightarrow O$, let
$\rho_i(x)=x|_U$; for a source diffeomorphism $f:O\to O'$, use pullback
$f^{*}$.  These maps are coordinate pullbacks and hence measurable.

Set
\[
 \Adm(O)=\Fext(O), \qquad (c_Ox)(u)=1-x(u).
\]
Thus admissibility is explicitly total, nonempty, measurable, restriction
closed, and not selected from a desired output.

For $x\in\Fext(O)$ define the formal Markov kernel
\[
 \Kext^{O}(x)=p\,\delta_x+(1-p)\,\delta_{c_Ox}.
\]
On the empty open, $x=c_Ox$ and the two weights combine to the single unit mass
$\delta_x$.  Let $P_p^O$ be the Markov operator induced by this kernel.  The
associated one-step proposal generator is
\[
 (\Gen^{O}\varphi)(x)
   =(1-p)\bigl(\varphi(c_Ox)-\varphi(x)\bigr),
 \qquad I+\Gen^{O}=P_p^O
\]
as operators on bounded measurable test functions.  ``One step'' is only a
declared proposal index.  It is not physical time.

\begin{theorem}[Conditional proposal-law theorem]
Under the displayed roots and typing:
\begin{enumerate}
\setlength{\itemsep}{0.15em}
\setlength{\parskip}{0pt}
\item $\Fext$ is a measurable contravariant functor and a sheaf on open covers;
\item $\Adm=\Fext$ is a nonempty measurable descent-compatible subfunctor;
\item $c$ is a measurable natural involution;
\item $\Kext$ is normalized, measurable, and natural under restrictions and
source diffeomorphisms;
\item $\Gen$ is a natural proposal generator and $I+\Gen=P_p$;
\item on every nonempty two-state complement orbit,
\[
 d_{\mathrm{TV}}\!\left(K_p^O(x),K_q^O(x)\right)=|p-q|,
\]
while the distance is zero on the empty-open fixed point.
\end{enumerate}
\end{theorem}

\begin{proof}
Restriction and pullback are coordinate projections or permutations on the
countable product $S^{\pi_0(O)}$, so identity, composition, and measurability
are immediate.  Compatible locally constant sections agree on overlaps and
therefore glue uniquely pointwise; local constancy of the glued section follows
from the cover.  Total admissibility inherits these properties.

For every admitted arrow $r$,
$r(c_Ox)=c_{O'}r(x)$.  Pushing the two-atom measure forward therefore gives
\[
 r_*K_p^O(x)=p\delta_{r(x)}+(1-p)\delta_{c_{O'}r(x)}
             =K_p^{O'}(r(x)).
\]
The same identity intertwines the generators.  Normalization is the sum of the
two displayed weights, with coincident atoms combined on the empty open.  On a
nontrivial complement orbit the signed measure difference has atomic weights
$p-q$ and $q-p$, giving total variation $|p-q|$.
\end{proof}

The sheaf claim concerns sections and the section-indexed law with one
disclosed global stay/flip choice.  Local probability marginals do \emph{not}
determine a unique global coupling.

\section{Exact controls and nonselection}

The finite controls are exact rather than evidentially interpretive.
\begin{longtable}{p{0.20\linewidth}p{0.73\linewidth}}
\toprule
Control & Result \\
\midrule
Clopen endpoint & The topology-derived empty/whole endpoints are not the two
token sections; an explicit extension typing is required. \\
Paired roots & $p=1/3$ and $p=2/3$ give distinct natural kernels on the same
fiber, admissibility rule, arrows, and complement. \\
Token swap & Complement transport is equivariant, but for $0<p<1$ both orbit
states have positive weight, so no deterministic token is selected. \\
Finite cover & Compatible local sections glue uniquely; incompatible overlaps
fail closed. \\
Circular root & Any edge from desired P4 success, empirical response, target
geometry, or frozen output to $p$ or the arrow class invalidates the packet. \\
TV variation & $d_{\rm TV}(K_p,K_q)=|p-q|$ is a finite proposal-space bound,
not physical robustness. \\
\bottomrule
\end{longtable}

The paired-root theorem is decisive for epistemic scope: construction proves
existence conditional on $p$, not a source derivation or selection of $p$.
Likewise swap equivariance transports the formal law but never realizes a
token.  A normalized formal kernel is not by itself a physical probability,
frequency law, or occurrence statement.

\section{Provenance and failure branches}

The construction DAG is acyclic:
\[
 \text{smooth source topology}\longrightarrow\operatorname{Open}(-),
 \qquad
 (S,p,c)\longrightarrow(\Fext,\Adm,K_p,\Gen),
\]
followed only by theorem and diagnostic outputs.  There is no reverse edge
from P4 behavior.  Predeclaration proves within-packet nonadaptivity; it does
not prove independent current-source provenance or epistemic justification.

The fifteen fail-closed branches cover: inherited-boundary change; untyped
fibers; open/proposal relabeling; restriction failure; empty or nonfunctorial
admissibility; kernel failure; occurrence overread; goal or target import;
descent failure; output-relative arrows; target-derived robustness;
predeclaration overread; physical-probability overread; adoption or P4--T03
activation; and promotion of a bounded obstruction to a global no-go.  None is
triggered by the fixed disclosed control family.  Triggering any branch removes
the positive result rather than weakening the branch.

\section{Distance to GR and authority}

All fourteen Distance-to-GR burdens have literal status \texttt{no\_delta}:
source ontology primitives, source equivalence, RetainH, GenH,
observer-localization, response, $M_{\rm src}$, $g_{\rm eff}$, matter coupling,
Einstein equations, finite-variation robustness, benchmark promotion, Gate
Chair status, and the current freeze/hard-fail row.  The construction supplies
proposal-only draft/control data, not an adopted source primitive or a physical
bridge.

The only lawful next action is one separately admitted, unexecuted
source-extension Smuggling Auditor packet, with the exact identity bound in the
formal record.  Refuter stress, adoption, D7, B2,
P4--T03, $g_{\rm eff}$, coupling, Einstein-equation work, proof promotion,
publication, push, external action, and every global-impossibility claim remain
blocked.

\end{document}
