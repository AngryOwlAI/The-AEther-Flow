<!-- authority: scientific_draft_control -->

# Parent fusion: proposal-only measurable-fiber occurrence law

## Decisive result

The fused result is
`proposal_only_measurable_fiber_occurrence_law_constructed_pending_smuggling_audit`.
This is a complete conditional construction under disclosed new roots, not a
current-ontology derivation or an adopted occurrence law.

The construction fixes `S={0,1}`, the discrete sigma-algebra, the locally
constant proposal-section sheaf `F_ext(O)=LC(O,S)`, total admissibility
`Adm_ext=F_ext`, pointwise complement, a disclosed global `p in (0,1)`, the
formal kernel

`K_p(x)=p delta_x+(1-p) delta_complement(x)`,

and the induced one-step proposal generator. Every root is declared before P4
diagnostics. The construction imports no target atlas, target metric, desired
GR cone, detector response, benchmark answer, or frozen-candidate output.

## Fused mathematical result

For every source open `O`, local connectedness and second countability make
`pi_0(O)` at most countable. Therefore `F_ext(O)` is the countable product
standard-Borel space `S^pi_0(O)`. Restriction and source-diffeomorphism pullback
are measurable coordinate maps. Compatible locally constant sections glue
uniquely, so `F_ext` is a measurable contravariant sheaf and total
admissibility is a nonempty descent-compatible subfunctor.

Complement commutes with every admitted restriction and pullback. It follows
that `K_p` is normalized, measurable, and natural. On the empty open the unique
section is complement-fixed, so the two displayed atoms combine to a single
unit mass. On every nonempty complement orbit,
`d_TV(K_p(x),K_q(x))=abs(p-q)`; on the empty-open fixed point the distance is
zero, giving the global upper bound `d_TV<=abs(p-q)`.

The descent claim is deliberately narrow. Compatible sections glue, and the
same disclosed global stay/flip law is natural on the glued section. Arbitrary
local probability marginals do not determine a unique cross-component
coupling, and no such coupling-descent theorem is claimed.

## Fused philosophical and authority result

The proposal roots are transparent constructive preload. Their syntactically
acyclic DAG establishes within-packet nonadaptivity only. It does not establish
independent current-source provenance, epistemic selection, current-ontology
derivation, or adoption.

The paired `p=1/3` and `p=2/3` controls keep the fiber, arrows,
admissibility, and complement fixed but produce distinct kernels. The token
swap transports the law equivariantly while selecting no token. Consequently,
normalization and equivariance do not imply realized occurrence, empirical
frequency, deterministic token selection, physical probability, a clock, or a
causal law.

## Child comparison and conflict resolution

Both internal specialists independently selected the same decisive result and
the same Smuggling Auditor role. Their only conflict was identity spelling:
the mathematical child first proposed the shorter descriptive alias
`PKT-V22-P4T02-B2-PROPOSAL-ONLY-MEASURABLE-FIBER-OCCURRENCE-LAW-SMUGGLING-AUDIT-V1`.
The parent canonicalized the more explicit packet identity
`PKT-V22-P4T02-B2-PROPOSAL-ONLY-MEASURABLE-FIBER-OCCURRENCE-LAW-SOURCE-EXTENSION-SMUGGLING-AUDIT-V1`.
Both children converged in round 1. There is one packet, no scientific conflict,
and no unresolved conflict.

The parent preserved both precision contributions: the empty-open fixed-point
exception to the TV equality, and the distinction between section-indexed law
descent and arbitrary marginal-coupling descent.

## Obligations, failures, and controls

All 16 proof obligations are discharged under the explicit roots. All 15
fail-closed branches are evaluated and none is triggered by the fixed control
family. Six exact controls pass: Open/clopen endpoint separation, paired kernel
roots, token swap, finite-cover gluing, circular-root rejection, and bounded
total-variation variation. The executable finite model passes 19 exact checks.

## Distance to GR and freezes

All eight inherited candidate-local freezes remain active and untouched. All
14 Distance-to-GR rows retain literal `no_delta` status. In particular, the
proposal generator is not `GenH`, finite proposal-space total variation earns
no physical robustness credit, and no `g_eff`, matter coupling, Einstein
equation, or benchmark evidence is produced.

D7 was not reevaluated, B2 remains inactive, and P4-T03 remains locked. No
source law or ontology is adopted. No Gate Chair, proof, promotion,
publication, push, external-action, global-no-go, or completed-derivation
authority is created.

## Selected successor

Exactly one separately admitted successor is selected but not executed:

- packet: `PKT-V22-P4T02-B2-PROPOSAL-ONLY-MEASURABLE-FIBER-OCCURRENCE-LAW-SOURCE-EXTENSION-SMUGGLING-AUDIT-V1`
- type: `source_extension_smuggling_audit`
- route: `ontology-law-research-packet`
- role: `smuggling-auditor@0.2.0`
- status: `selected_not_executed`

The audit must test whether the disclosed token, complement, global parameter,
global flip coupling, total admissibility, arrow class, and total-variation
structure hide a goal property or target-shaped choice. It may not adopt the
candidate or infer occurrence or physical probability.
