<!-- authority: science_draft -->

# Parent Fusion Notes: Source Detector/Readout Candidate

## Consensus

Both child perspectives support a positive P5-T04 construction rather than an
obstruction. The mathematical reason is finite: the P5-T03 setup provides a
bounded source scope, readout symbol, detector source record symbol, and
compatibility target. A finite partial source-side readout map can therefore
be written without using target metric, proper time, empirical detector
protocol, stress-energy semantics, matter action, benchmark behavior, or
process authority.

## Fused Output

The fused artifact is
`research_control/tasks/RT-20260708-010/artifacts/source_detector_readout_candidate_v1.tex`.
It constructs exactly one draft/control candidate:
`SourceReadoutCandidate_EStar_v1`.

The candidate supplies:

- `source_domain`: `SMScope(E_*)`.
- `readout_interface`: `Readout_src(E_*): Readable_src(E_*) -> Tok_R(E_*)`.
- `detector_source_record`: `Det_src(E_*)`.
- `certificate_bundle`: `Cert_R(E_*)`.
- `finite_local_witness`: `W_R(E_*)`.
- `compatible_with_K_Estar`: true, only in the limited draft/control
  projection sense.

## Boundary

The candidate is not detector semantics, not readout semantics adoption, not
matter coupling, not stress-energy semantics, not a matter action, not
Einstein equations, not benchmark promotion, and not completed derivation.

## Next Route

The logical next bounded route is v18 P5-T05: run a smuggling audit against
`SourceReadoutCandidate_EStar_v1`.
