<!-- authority: science_draft -->

# P7-T01 source-matter ontology receipt

## Result

`AJ-RT-20260727-007-001` constructed
`SourceMatterIncidenceCandidate_v1` as a task-local `draft/control`,
`proposal-only` candidate. The candidate has primitive source occupation and
defect data `(m,z)`, derives occupation, charge, charge chains, and
representation grades, and defines the transition-invariant sector
`(Q,[z])`.

The finite witness has two vertices, one transport link, one closed loop,
`chi(rho)=1`, a nonzero defect class, and exact zero continuity and defect
residuals. Separate records cover the valid null configuration, a valid
nontrivial neutral configuration, a malformed transition with its exact
residual, and a typed but underdetermined detector request.

## Mathematical result

`P7T01-THM-SOURCE-RELABELING-SECTOR-NATURALITY-001` proves, within the
stipulated candidate, that admissible configurations and transitions
transport under compatible source-chain, coefficient-group, internal-group,
and representation-label isomorphisms. Total source charge transforms by
`alpha`, the defect class by induced homology transport, and representation
content as a grade. A nonzero continuity residual remains nonzero under the
isomorphism.

The separate charge-and-defect proposition is conditional on the proposed
transition grammar. It is not an independently derived dynamics, a Noether
theorem, or a physical conservation law.

## Review and validation

Two same-context internal child perspectives reviewed the packet. The parent
resolved all required mathematical and philosophical corrections in one
fusion pass, with zero unresolved conflicts. This is structured internal
review only, not human expert review or independent replication.

The deterministic task-local validator passes 68 of 68 checks. A three-pass
scratch TeX build produces a 10-page PDF with resolved references. The
validator and build are operational receipts; neither creates scientific,
ontology, proof, or promotion authority.

## Claim boundary

The packet supplies an actual mathematical source-matter ontology target at
proposal-only level. It does not adopt canonical ontology or a source law,
establish physical matter, charge, motion, detector semantics, a physical
conservation law, an action, stress energy, coupling, effective geometry,
Einstein equations, benchmark recovery, or a completed derivation. All
legacy matter records retain their inherited status. P6 Gate B remains zero
of eight and its local effective-geometry freeze is unchanged.

The resulting status is
`blocked_adoption_open_continuation`. P7-T02 may be considered only after the
governed P7-T01 checkpoint commits.
