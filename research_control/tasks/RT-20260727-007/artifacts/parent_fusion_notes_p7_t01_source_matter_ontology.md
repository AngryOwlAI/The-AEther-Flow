<!-- authority: science_draft -->

# P7-T01 parent fusion notes

These notes record the same-context internal parent-child synthesis for
`AJ-RT-20260727-007-001`. They are not human review, independent replication,
proof authority, ontology adoption, or publication authority.

## Inputs

- `child_phys_math_p7_t01_source_matter_ontology.yaml`
  (`fb104c08a874bb69d389a3d38039fd32a6b4e070fb38e22b792bd49366f31f06`)
  supplied the augmented-complex typing, occupation-to-charge construction,
  finite calculations, sector/grade separation, and exact naturality maps.
- `child_phys_phil_p7_t01_source_matter_ontology.yaml`
  (`7cb88dfebc13c6a264ea37fe766342dd356b5714caa8b00ecf6ee68e6a533b4c`)
  supplied the model-to-world and identity ladders, symmetry and observable
  cautions, branch semantics, legacy-status controls, and adoption boundary.

## Fused construction

The fused candidate uses a finite augmented source-incidence complex
`C_2 -> C_1 -> C_0 -> Z`, coefficient groups `A_int` and `B_def`, an internal
group with declared representations, and a compatible charge-label map
`chi`. Its primitive configuration is `(m,z)`. Occupation `nu`, local charge,
the charge zero-chain `q`, and representation grade `M` are derived from `m`;
the defect chain `z` is independently required to be closed.

The transition-invariant sector is `(Q,[z])`. The representation grade `M` is
kept separate because the P7-T01 transition grammar does not require `Pi` to
preserve it. The source-invariant record `(N,Q,[z],M)` is algebraic
bookkeeping only, not an empirical or operational observable.

The naturality theorem now uses typed source-chain maps, coefficientwise maps
`F_i tensor alpha` and `F_i tensor beta`, an internal-group isomorphism
`phi`, and a compatible representation-label bijection `gamma`. Charge
transforms equivariantly. A raw charge is not called invariant unless the
admitted automorphism fixes it or a later packet explicitly uses its orbit.

## Resolved review findings

All five mathematical required corrections and all ten philosophical fusion
corrections were incorporated. In particular:

- charge is derived through `m` and `chi`, not independently attached;
- `epsilon partial_1=0` is an explicit augmented-incidence axiom;
- the zero invariant sector is not identified uniquely with the null
  configuration;
- valid null, valid nontrivial neutral, malformed, and underdetermined cases
  remain distinct;
- the charge result is disclosed as a conditional chain-level invariant
  following from the proposed transition grammar;
- candidate presentation identity, candidate isomorphism, sector
  coincidence, physical identity, and protected ontology adoption are not
  conflated;
- every legacy object keeps its inherited status, and P6 Gate B remains zero
  of eight with its local freeze unchanged.

There was no material disagreement between the child conclusions. One parent
resolution pass was sufficient because the proposed corrections were
complementary and compatible.

## Preserved limitations

The exact theorem and finite witness are true only within the stipulated
proposal-only candidate. The packet supplies no physical matter reference,
detector semantics, chosen dynamics, action, stress energy, universal
coupling, effective geometry, Einstein equations, or benchmark recovery.
Canonical ontology and source-law adoption remain
`blocked_adoption_open_continuation` and human-gated.
