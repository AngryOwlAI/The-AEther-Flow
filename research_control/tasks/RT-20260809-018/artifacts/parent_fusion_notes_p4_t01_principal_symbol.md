---
authority: draft_control_artifact
object_id: MD-V22-P4-T01-PARENT-FUSION-PRINCIPAL-SYMBOL-V1
task_id: RT-20260809-018
job_id: AJ-RT-20260809-018-001
status: draft/control
---

# P4-T01 parent fusion: reduced source principal symbol

The fused result executes only P4-T01 for the sealed P3-T02 fixed-law
candidate. P3-T04 already established that the fixed-D tangent operator is
`L_D u=(X_1u1,...,X_6u6)` and that the algebraic-constraint,
differential-constraint, internal field-gauge, and algebraic-redundancy spaces
are all zero. The candidate reduced source field and equation fibers are
therefore both six-dimensional. They are not yet physically interpreted
perturbation or matter spaces.

The lawful reduced symbol is `diag(k(X_1),...,k(X_6))`. In the declared source
chart its determinant is
`(w+x)(w+y)(w+z)(w+x+y)(w+y+2z)(w+2x+z)`. This source polynomial is nonzero,
homogeneous of degree six, and square-free. All six global linear factors are
pairwise distinct and have multiplicity one. The source-coordinate density
weight is zero. Under invertible field and equation frame changes, the scalar
representative acquires the relative multiplier `det(S)/det(R)`; intrinsically
it is a determinant-line section, and its zero set is unchanged.

The branch ledger preserves every degeneracy. All 15 pairs meet and all 20
triples have rank three. The only four-branch subset with a nonzero common
covector is `{2,3,4,6}`, represented by `k=(1,0,-1,-1)`, where the symbol has
rank two and corank four. No fivefold nonzero intersection exists. Fixed
spatial directions can also produce repeated roots in `w`; these are crossings
of distinct global factors, not repeated global irreducible factors.

Passive source-chart changes preserve every pairing `k(X_A)` exactly. The
primary exact-rational implementation and a separate rational Leibniz
determinant implementation agree at all preregistered cases. The independent
script imports no primary-model code. This is internal computational
reproduction, not external hyperbolic-PDE review or independent scientific
replication.

All five parent conflicts are resolved. The decisive result is a constructed,
branch-complete source-principal candidate pending the separate P4-T02
hyperbolicity, universality, robustness, and hard-fail screen. No branch is
suppressed and no quadratic metric factor is inserted. The six source
hyperplanes are not called one physical causal cone; `d tau` is not called a
physical clock; no Lorentzian signature, scale, effective metric, matter
coupling, Einstein equation, Gate verdict, benchmark result, or completed
derivation is created.
