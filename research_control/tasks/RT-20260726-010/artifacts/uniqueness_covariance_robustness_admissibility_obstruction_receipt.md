<!-- authority: control -->

# P6-T06 uniqueness, robustness, and physical-admissibility receipt

Status: `draft/control`; `blocked_adoption_open_continuation`.

The bounded P6-T06 Refuter packet records six new mathematical payloads:

1. The exact same-source family
   `q_(a,epsilon)=diag(-1,exp(2a),exp(-2a),epsilon^2)` is Lorentzian for
   positive `epsilon`.
2. For the fixed source covector `xi=e^0+e^1`, the inverse quadratic value
   `-1+exp(-2a)` changes from spacelike to null to timelike across `a=0`,
   while transverse comparisons also vary.
3. Calling the determinant-one transformations
   `B_a=diag(1,exp(a),exp(-a),1)` gauge does not supply the missing
   operational-covector transport or representative-independence theorem.
4. As `epsilon` tends to zero, inverse conditioning grows as
   `epsilon^(-2)` and the limiting form degenerates; a signed continuation
   crosses to split signature.
5. `2I` fails Lorentz preservation, and exact `I/-I` controls show that
   inverse and triple-overlap cocycle laws remain independent obligations.
6. The physical-admissibility matrix fails closed on characteristic-cone
   identification, clocks, rods, coarse-grained geometry stability, and
   universal matter coupling. Bimetric, multicone, and nonuniversal branches
   remain unexcluded comparison branches, not adopted physical sectors.

The inherited P6-T05 source-automorphism theorem remains fixed: the only
natural symmetric bilinear form on the admitted maximally symmetric datum is
zero.

The exact obstruction is
`OBST-P6T06-GATE-B-UNIQUENESS-ROBUSTNESS-ADMISSIBILITY-001`, classified as
`scoped_obstruction`. Current ontology does not derive the required cone
discriminator, operational transport, uniform robustness, geometry-level
coarse-graining map, characteristic identification, clock and rod
calibration, or universal-coupling theorem.

The exact unchanged route is locally frozen, but this is not a global no-go
theorem. Materially richer source-side continuation remains open. Gate B is
not passed. Historical scoped `g_eff`, canonical ontology, matter coupling,
Einstein equations, benchmark status, proof authority, publication, and push
state are unchanged. After one governed checkpoint, P6-T07 may select the
honest status and materially distinct next route.
