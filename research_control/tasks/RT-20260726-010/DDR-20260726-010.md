---
authority: control
status: completed
decision_id: DDR-20260726-010
task_id: RT-20260726-010
agent_job_id: AJ-RT-20260726-010-001
director_version: director-of-research@0.3.0
decision_type: task_overlay
selected_role_id: refuter
selected_role_version: 0.2.0
supersedes_decision_id: DDR-20260726-009
requires_human_gate: false
role_fit_candidates:
  - refuter@0.2.0
  - ontology-formalizer@0.2.0
  - smuggling-auditor@0.2.0
---

# DDR-20260726-010: execute only v21 P6-T06

## Current objective

Execute immutable generation 130 and only P6-T06. Stress the cumulative
P6-T03 through P6-T05 result for uniqueness up to declared gauge, covariance,
finite-variation robustness, and physical admissibility. Return a theorem or
precise scoped obstruction without presuming that any comparison form or
conditional coframe construction is a reconstructed physical metric.

## Authority surfaces read

The Director read `SOUL.md`, both applicable `AGENTS.md` files, the recursive
relay and `continue-research` skills, the Director and Refuter roles, AgentJob
and mathematical-decisiveness schemas, the v21 P6-T06 plan, the
Distance-to-GR burden map and ledgers, obstruction and family-freeze policies,
`handoff-0883`, the complete P6-T03 through P6-T05 theorem packets, canonical
target-side geometry, and the historical scoped `g_eff` candidate and Gate
Chair record. Memory preflight refreshed retrieval-only state and passed with
1,037 source objects and 5,215 relationship rows. Direct scientific source
hashes were held fixed.

Checkpoint commit `a03c6d84f3a315eaba213b0c7ea6806a5957d62d` satisfies the
cumulative P6-T05 dependency and recovery condition recorded by
handoff-0883. Immutable generation 130, route SHA-256
`17dffa12de3c6a3111ae3196dbc653f568be1a56f4ce76dc8d90327818bf0b37`,
and handoff-0883 uniquely select P6-T06.

## Role-Fit Matrix

| Candidate role | Fit | Reason |
|---|---|---|
| `refuter@0.2.0` | selected | P6-T06 explicitly requires countermodels and stress tests for uniqueness, covariance, variation robustness, and physical-admissibility overreads. |
| `ontology-formalizer@0.2.0` | supporting input | P6-T05 already typed the transition, inverse, cocycle, determinant, and naturality burdens consumed here. |
| `smuggling-auditor@0.2.0` | internal boundary only | Target-cone, clock, rod, matter, and benchmark imports must be blocked, but the routed output is a mathematical Refuter result. |

## Selected role and decomposition

Activate one `refuter@0.2.0` task overlay and one outer AgentJob. The internal
physicist-mathematician perspective constructs an exact same-source
two-parameter Lorentzian family, calculates cone variation, inverse
conditioning, degeneracy crossing, and transition-law countermodels. The
internal physicist-philosopher perspective audits declared gauge, operational
covectors, characteristic cones, clocks, rods, universal coupling,
coarse-graining, and the distinction between comparison geometry and physical
geometry. The parent resolves the tensions without child AgentJobs or
subagents.

## Decisive result

On a four-dimensional source tangent space with oriented ray `[e_0]_+`, an
independent positive density, and scalar phases constant on the comparison
datum, define

`q_(a,epsilon)=diag(-1,exp(2a),exp(-2a),epsilon^2)`,

where `a` is real and `epsilon` is positive. Every member is nondegenerate and
Lorentzian, has determinant `-epsilon^2`, and is compatible with the same
admitted source data. Yet

`d q_(a,epsilon)/d a =
diag(0,2 exp(2a),-2 exp(-2a),0)`

is nonzero at fixed source data. For the fixed source covector
`xi=e^0+e^1`,

`q_(a,epsilon)^(-1)(xi,xi)=-1+exp(-2a)`,

which is null at `a=0`, timelike for `a>0`, and spacelike for `a<0`. Thus the
same named source covector changes causal classification across the family.
Calling determinant-one source automorphisms “gauge” only moves the burden:
an orbit cannot assign a definite operational cone unless the source derives
how operational covectors transform and proves the quotient observable
well-defined.

As `epsilon` tends to zero, an inverse component grows as
`epsilon^(-2)`, and the limit is degenerate. Extending the last diagonal entry
through zero gives split signature for negative values. Current source data
derive neither a positive lower bound nor an exclusion of the crossing. The
P6-T05 determinant-one source-automorphism theorem remains stronger: on the
admitted maximally symmetric datum, naturality forces every invariant
symmetric bilinear form to vanish.

Conditional transition data also fail closed. A transition `2I` violates
Lorentz preservation. Three pairwise Lorentz-valued transitions `I`, `I`, and
`-I` can violate the cocycle. These controls do not claim that such
transitions are derived; they show that local signature alone does not
establish a global covariant field.

The physical-admissibility audit records exact non-implications. The P6-T02
characteristic set `Ann(V)` is a hyperplane, not a Lorentzian quadratic null
cone. The ray `[V]_+` is compatible with many cones. P6-T04 leaves clock
normalization free under positive rescaling. Transverse rod comparisons vary
with `a`. No source-side theorem excludes multicone, bimetric, or
nonuniversal comparison branches. No coarse-graining map for geometry and no
output comparison metric transfer source-side stability to metric stability.

The precise Refuter disposition is
`OBST-P6T06-GATE-B-UNIQUENESS-ROBUSTNESS-ADMISSIBILITY-001`, classified as
`scoped_obstruction`. The exact cumulative route from the unchanged one-ray,
unnormalized-density, dimensionless-phase package to a unique, robust,
operationally admissible unscoped physical geometry is locally frozen.

## Claim boundary and continuation

Current ontology does not derive a source discriminator that selects one cone
orbit, an operational covector transport law, uniform nondegeneracy and
conditioning bounds, a geometry-level coarse-graining map, characteristic
cone identification, absolute clock calibration, rod calibration, or
universal coupling. Adoption remains
`blocked_adoption_open_continuation`: a materially richer source-side
extension or a conditional comparison-geometry program remains logically
open.

P6-T06 does not establish a physical metric, causal cone, proper time, rods,
coordinates, matter semantics, universal coupling, or an unscoped `g_eff`.
It does not import target geometry, tune `G` or `c`, couple matter, derive
Einstein equations, promote the exact-GR benchmark, authorize proof or
publication, push, globally reject the theory, or close future source
extensions.

## Next bounded route

After one governed checkpoint, P6-T07 may use one fresh bounded
`theoretical-continuation-selector@0.1.0` packet to name the honest status of
the remaining metric branch and select a materially distinct next route. It
may not treat this scoped obstruction as a global no-go or promote any
comparison form to physical geometry.
