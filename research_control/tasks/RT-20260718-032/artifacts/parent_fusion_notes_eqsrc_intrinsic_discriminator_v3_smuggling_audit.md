# Parent Fusion Notes: EqSrc Intrinsic Discriminator v3 Smuggling Audit

## Scope

These notes fuse the independent Physicist-Mathematician and
Physicist-Philosopher reviews of exact proposal-only
`EqSrcIntrinsicDiscriminatorAdmissibilityLaw_src^cand,v3`. The audit has no
repair, Refuter-stress, adoption, canonical-ontology, or promotion authority.

## Consensus

The v3 repair is formally valid and source-pure as written within its declared
finite algebraic scope. Its cross-complex relation predicates are typed. For an
arbitrary function, preservation is exactly quotient-set factorization and
reflection is exactly injectivity of the induced set map. For a linear map,
preservation becomes \(G(B_E)\subseteq B_{E'}\), the quotient map is linear,
and reflection becomes \(G^{-1}(B_{E'})=B_E\). For a chain map this is
equivalent to injectivity of \(H_1(F)\).

Both children reproduced the four-state noninvertible reflecting witness. The
16 ordered pairs split into eight related and eight unrelated pairs with zero
preservation or reflection mismatches. Thus the v2 whole-chain-invertibility
obstruction is repaired at proposal scope.

No target atlas, metric, benchmark result, process record, registry, role,
validator, handoff, checkpoint, or generated derivative enters the candidate's
defining premises.

## Categorical Interpretation Guard

The arbitrary-map theorem lives in `Set`. It must not be overread as asserting
that every induced quotient map is linear. With \(A=K=\mathbb F_2\), \(B=0\),
the function \(G(z)=z+1\) preserves and reflects equality and factors through
the quotient but is not linear. V3 says “map” in the general theorem and marks
the linear specialization separately, so the guard is nonblocking.

## New Mathematical Payload

The audit adds a cross-complex finite census. Let the source have four states
and two quotient classes of size two, and the destination eight states and
four quotient classes of size two. Among all \(8^4=4096\) functions:

- 256 preserve the equivalence relation;
- 192 preserve and reflect it;
- preservation versus quotient factorization has zero mismatches; and
- reflection versus induced-map injectivity has zero mismatches.

The declared relation also has the orbit identity
\[
z\mathrel{\mathrm{EqCand}_E}z'
\iff z'-z\in B_E
\iff \exists \tau_c\in\mathrm{AdmVar}(E):\tau_c(z)=z'.
\]
Hence the relation classes are exactly the orbits of the declared translation
action.

## Physical and Ontological Boundary

The current ontology does not derive the chain-package family, differential,
homology discriminator, physical morphism category, or admissible variations.
The same differential defines both \(B_E\) and the admitted translations, so
their agreement is transparent but construction-relative. Formal chain-map
naturality is not physical covariance, reversibility, or admissibility.

The exact classification is `new_ontology_primitive_candidate`,
`proposal-only`, and `blocked_adoption_open_continuation`. General EqSrc,
RetainH, GenH, M_src, effective metric, matter coupling, Einstein equations,
benchmark status, and both authoritative ledgers remain unchanged.

## Freeze and Next Route

The intrinsic-discriminator route is not frozen because v3 is a materially
repaired object and this audit adds a categorical guard, an orbit lemma, and a
new cross-complex census. Repeating the same v3 audit without new mathematical
payload would require freeze review. The earlier quotient-selector route
remains locally frozen.

The next lawful action is one bounded `refuter@0.2.0`
`ontology-law-research-packet` against exact v3. It may stress nontrivial
source-only instantiability, category discipline, primitive independence,
degenerate models, malformed maps, and finite-variation robustness. It may
not repair, adopt, or promote the candidate.
