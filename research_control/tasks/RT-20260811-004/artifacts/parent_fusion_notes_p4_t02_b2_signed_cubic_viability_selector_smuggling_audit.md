<!-- authority: science-draft -->

# Parent fusion: signed cubic viability-selector smuggling audit

## Fused result

Both inherited child perspectives independently return
`source_pure_as_written`, narrowly. The fixed
`SignedCubicViabilitySelector_v1` contains no literal target-atlas, target-metric,
desired-GR-cone, empirical-response, physical-semantic, frozen-route,
benchmark-answer, generated-derivative, registry, role, validator, or
checkpoint premise. That local verdict applies only to the fully disclosed
proposal-only existence-witness syntax.

The verdict supplies no current-ontology derivation or epistemic selection.
`sigma` is the sole component-identity selector. It is fixed before diagnostics
and is source-typed, but it is also the transparent constructive preload that
makes exactly one sign component admissible. `gamma`, `Delta`, and `a_star`
supply underived dynamics and certificate structure. Reflection exchanges the
two token-indexed candidates and selects neither. Acyclic predeclaration proves
formal noncircularity, not why either token or the source law should hold.

The candidate therefore remains proposal-only source-extension data classified
as a new ontology primitive candidate. Adoption is
`blocked_adoption_open_continuation`. No physical causality, time orientation,
empirical response, probability occurrence, sector universality, conformal
geometry, scale, or effective metric follows.

## Mathematical synthesis

The fused continuous modulus is

\[
H=\gamma\Delta a_\star^2.
\]

It is invariant and complete under simultaneous positive amplitude/time
regraduation. Two positive triples lie in the same orbit exactly when they
have the same `H`, and each orbit has the unique normal form `(H,1,1)`. In the
chart `x=a/a_star`, `tau=t/Delta`, the flow and certificate are

\[
x(\tau)=\frac{x}{\sqrt{1+2H\tau x^2}},\qquad
b_\sigma=\frac{\sigma x}{\sqrt{1+x^2}},\qquad
\lambda=(1+2H)^{-1/2}.
\]

For every positive `gamma` and `Delta`, the finite transitive closure from a
nonzero amplitude `a` is exactly the same-sign interval
`0 < |y| <= |a|`. Hence positive parameter magnitudes alter rates and
conditioning but not reachability or component identity.

For every admitted `n`-step path the greatest state-independent factor is

\[
\Lambda_n=(1+2nH)^{-1/2}.
\]

This factor is at least `lambda^n`, is strictly larger for `n>1`, and tends to
zero. Thus every finite path remains in `U_sigma`, while the trajectory and
barrier approach the excluded boundary zero. The exact finite theorem does not
provide a positive asymptotic margin or closed limiting viability.

For every fixed positive-time map, the unique invariant Borel probability on
the full amplitude line is `delta_0`. It assigns zero mass to `U_sigma`, so no
stationary probability is supported on the selected component. Component
selection is not point, event, or probabilistic occurrence.

The parent exact model adds a sign-role control: `gamma=0` preserves both
components and selects neither, while negative `gamma` loses global forward
existence for sufficiently large amplitude. Positive `gamma` is therefore
explicit dynamical preparation, not component selection or hidden target
orientation.

## Conflict review

There are no blocking conflicts. Two precision differences were resolved
without scientific disagreement:

- The parent's initial dimensionless time chart and the mathematician child's
  `tau=t/Delta` chart are equivalent. Fusion uses the latter because it exposes
  the complete `H`-orbit classification.
- RT003's iterated `lambda^n` estimate remains valid. Fusion preserves it and
  adds the sharper `Lambda_n` theorem.

The philosopher child's nonselection and occurrence distinctions are fully
compatible with the mathematician child's exact results. No child claims
current-ontology derivation, physical gauge, spontaneous symmetry breaking,
empirical semantics, adoption, P4-T02 sufficiency, or Distance-to-GR progress.

## Preserved boundary and next packet

All four inherited freezes remain active:

- `NDCL-V22-P4T02-B2-SHARED-TAU-SELECTOR`
- `NDCL-V22-P4T02-B2-REPAIRED-QUOTIENT-DESCENT-ROBUSTNESS`
- `NDCL-V22-P4T02-B2-COMMON-CHARACTER-HOLONOMY-PROTECTION`
- `NDCL-V22-P4T02-B2-ORIENTED-MATROID-BRIDGE-SELECTION-ROBUSTNESS`

Every one of the fourteen Distance-to-GR burdens has `no_delta`. D7 is not
reevaluated, B2 remains inactive and undisposed, and P4-T03 remains locked.

The fused audit selects, but does not execute,
`PKT-V22-P4T02-B2-SIGNED-CUBIC-VIABILITY-SELECTOR-SOURCE-EXTENSION-REFUTER-STRESS-V1`,
type `source_extension_refuter_stress`, under `refuter@0.2.0`. The future
packet is the first stress of this distinct source law and must not replay any
of the four frozen routes.

No canonical ontology edit, source-law adoption, physical or empirical
interpretation, `g_eff`, coupling, Einstein equation, benchmark promotion,
Gate action, proof promotion, publication, push, external action, global
no-go, or completed derivation is authorized.
